[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "npm install && npm run build && npm run db:migrate"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
