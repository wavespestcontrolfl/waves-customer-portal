node_modules/
.env
*.log
client/dist/
.DS_Store
*.swp
*.swo
coverage/
.nyc_output/
.tmp/
.claude/*.lock
.claude/db-schema.txt
.claude/worktrees/

# Reconcile-script outputs — contain row-level subscriber PII. Never commit.
tmp-beehiiv-reconcile-*.json

# Calibration script output — may contain redacted-but-still-sensitive snippets.
reports/

# Local live-edit stubs (real payloads) must never ship — served verbatim from the public web root
client/public/__stub/

# Task-local test credentials and browser artifacts
.local/
.ui-verify/

# Local synthetic UI verification fixtures and screenshots
/client/.qa/
/.qa/
