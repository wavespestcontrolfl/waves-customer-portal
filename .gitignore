node_modules/
.env
*.log
client/dist/
.DS_Store
*.swp
*.swo
coverage/
.nyc_output/
.claude/*.lock
.claude/db-schema.txt

# Reconcile-script outputs — contain row-level subscriber PII. Never commit.
tmp-beehiiv-reconcile-*.json

# Playwright MCP screenshot output — design-study artifacts live in ~/Downloads, not the repo
.playwright-mcp/
