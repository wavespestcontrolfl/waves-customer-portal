[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "npm run build"

[deploy]
startCommand = "node server/scripts/run-seo-pipeline-worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
