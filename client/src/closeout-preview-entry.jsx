// Standalone dev entry for the Phase 3 fast tech closeout preview.
import { createRoot } from 'react-dom/client';
import CloseoutPreview from './pages/CloseoutPreview';

createRoot(document.getElementById('root')).render(<CloseoutPreview />);
