<!--
  DEPRECATED. Info.plist is now generated from ../project.yml by XcodeGen.
  Edit the `info.properties` block in project.yml and run ./bootstrap.sh
  (or `xcodegen generate`) to regenerate Resources/Info.plist.

  Kept as a historical reference only.
-->
