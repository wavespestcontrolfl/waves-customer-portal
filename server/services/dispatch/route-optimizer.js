const { retired } = require('./retired');

module.exports = {
  optimizeDay: retired('optimizeDay'),
  optimizeTechRoute: retired('optimizeTechRoute'),
  absorbCancellation: retired('absorbCancellation'),
};
