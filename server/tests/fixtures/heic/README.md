# HEIC converter fixture

`synthetic-96x64.heic` contains only generated pixels. It was created on
macOS from a deterministic 96x64 RGBA gradient rendered by Sharp, then encoded
with `sips -s format heic -s formatOptions 80`. It contains no customer data or
third-party image content.
