# Waves UI consistency audit — 2026-09-07

**Scope.** Every React surface in `client/src` (admin portal, tech portal, customer portal + public token pages), read against the pinned worktree `~/wt-ui-audit` at `d77f6ac58` (origin/main, 2026-09-07 05:00 ET). Brief: Adam, 2026-09-07 08:01Z ("go through our React templates … UI consistency, bugs, mobile/desktop renders, fonts, style, colors").

**Method.** Phase 1 discovery ran 31 read-only agents in two workflow runs — 18 surface readers (one per page family, full-file reads with line citations) and 13 cross-cutting scanners (tokens/style, glass census, duplication, dead code, a11y, responsive, overlays, forms, tables/lists, states, perf/arch, navigation map, dev harness). Their 638 raw findings were merged and de-duplicated (same file, line ±3, same category) to 613, then every P0, every P1 and every P2 flagged `safe_to_autofix` (250 findings in 136 files) went through a refute-by-default verification pass (21 skeptic agents, each re-reading the cited code). A rendered census of 45 admin routes at desktop 1440 and mobile 390 (`.tmp/ui-audit/render-census.json`, 90 screenshots) backs the typography and responsive numbers with what actually paints. Customer surfaces were rendered through the fixture harnesses (`/preview-*.html`).

**Headline numbers (post-merge, pre-verification).**

| Severity | Count | Flagged safe to autofix |
|---|---|---|
| P0 | 1 | 1 |
| P1 | 63 | 31 |
| P2 | 348 | 186 |
| P3 | 201 | 132 |

Verified counts and the confirmed bug/responsive/a11y lists are in sections D–F. Section J lists what this lane fixed; K ranks the rest.

---

## A. Application UI map

### Entry points and shells

| Entry | File | Notes |
|---|---|---|
| App | `client/index.html` → `src/main.jsx` → `src/App.jsx` | 68 `lazyWithRetry` routes; `PageErrorBoundary` wraps every route and the admin layout; `/*` catch-all is the customer `PortalPage` (it also swallows unknown `/admin/*` and `/tech/*`). |
| Fixture harnesses | 9 `client/preview-*.html` → `src/dev-preview/*-main.jsx` | Render the REAL portal, estimate, secure-appointment, service-report, project-report, irrigation, tokens and completion pages with stubbed fetch; no server needed. Admin and tech pages have no harness and no dev auth bypass. |
| Tech capture preview | `client/tech-capture-preview.html` | Standalone mock, not the tech shell. |

| Shell | File | Owns |
|---|---|---|
| Admin | `components/AdminLayoutV2.jsx` (inline styles over `styles/theme-square.css` vars) | 220px sidebar, mobile 52px top bar (zIndex 90) + 56px bottom tab bar (95) + drawer (100); `.admin-main` is the only scroll container (`--admin-vh`); `useAdminViewport` writes `--admin-vh` / `--vv-offset-top` / `--keyboard-inset`. No `<main>` landmark, no skip link, no content max-width (each page sets its own). |
| Tech | `components/TechLayout.jsx` (inline, dark D + Nunito Sans/Montserrat) | Sticky header, `<main>` padded 80px + safe-area, fixed bottom nav (Route / Estimate[admin] / Protocols, 44px). No `components/ui` adoption by rule. |
| Customer | `components/brand/WavesShell.jsx` (18 pages) and `pages/PortalPage.jsx`'s own app shell (sticky glass bar, 760px main, desktop section nav, mobile BottomNav + MoreSheet; no footer by owner D5) | Glass is mounted by `useGlassSurface(true)` (`glass/glass-engine.js`); `glass/glass-theme.css` then overrides authored radii/type with `!important`. |

### Route families

| Portal | Routes | Navigation source of truth |
|---|---|---|
| Admin | 41 page routes + 27 compatibility redirects under `/admin` | `config/adminNavigation.js` — 34 registry entries, 8 desktop sections (Overview, Service operations, Customers & Sales, Communications, Billing & Finance, People, Marketing, System), 5 mobile tabs (dashboard, schedule, customers, communications, more); `isPathAdminOnly` default-deny allowlist for technicians; `adminNavigation.test.js` pins every registry path to a mounted route. Second and third hand-maintained lists exist: `config/mobileSettingsSections.js` and `SettingsPage` `SETTINGS_TAB_GROUPS`. Off-registry pages: `/admin/turf-height` (no inbound link anywhere), `/admin/_design-system`, `/admin/customers/duplicates` (server-built links only). |
| Tech | `/tech`, `/tech/protocols`, `/tech/lawn-diagnostic`, `/tech/social-post`, `/tech/estimate` (redirects OUT of the PWA to `/admin/pipeline?tab=new`) | Bottom nav only; lawn-diagnostic and social-post reachable from home quick actions only, no back affordance. |
| Customer | Portal tabs in `?tab=` (dashboard, plan, visits, billing, refer + More: documents, property, learn; cancelled accounts clamp to plan/visits/billing/documents) + 26 public token routes (`/estimate/:token`, `/pay`, `/receipt`, `/statement`, `/contract`, `/prep`, `/price-change`, `/outline`, `/card`, `/report/*`, `/lawn-report`, `/pest-report`, `/track`, `/reschedule`, `/reservice`, `/appointment`, `/secure`, `/rate`, `/book`, `/newsletter`…) | Token deep links; `/review/:token` redirects to `/rate` (ReviewPage.jsx is unrouted). |

### Page families (templates observed)

| Template | Where it appears | Implementations |
|---|---|---|
| Hub with section tabs | Every consolidated admin page (Pricing, Contracts, Assessments, Service Library, Knowledge, Agents, Communications, Blog, Reviews, Equipment, Settings…) | `AdminCommandHeader` (sticky md+, section tile grid, `onSecondaryNav` for children). EstimatesPageV2 rolls its own `PipelineCommandHeader`; DashboardPageV2 its own greeting header; KnowledgeHub stacks a second header from each child. |
| List | Customers directory, Schedule list, Invoices, Payers, Estimates, Projects, Reviews, SEO/Ads/Inventory tables, Triage/Call log | Five row idioms: CSS-grid rows (CustomersPageV2), `ui/Table` (10 files), raw `<table>` (152 in 47 files), Card rows (EstimatesPageV2, Projects), divided lists (Comms). Row click means "open sheet" in Customers/Schedule, "inline expand" in Invoices/Banking/Inventory, nothing in Payers/Estimates. |
| Detail | Customer 360 (8,474-line right slide-over, 7 tabs), JobDrawer/TechDrawer (`ui/Sheet`), PayerDetailSheet, PhotoAssessmentDetailSheet, MobileAppointmentDetailSheet, MobileCustomerDetailSheet | Same task, different kinds: appointment detail is a 430px aside on desktop, a full-screen sheet on phone, and a `ui/Sheet` on the dispatch board. |
| Dashboard | `/admin/dashboard` (5 sections, KPI strips, recharts), Banking, Tax, Billing recovery, Agents overview, Tool health, tech home, portal Home | One real dashboard archetype (DashboardPageV2 KpiTile/ChartCard/Verdict/FormulaNote); 13 other KPI-tile implementations elsewhere. |
| Form | CreateAppointmentModal (3,474 lines), EstimateToolViewV2 (8,352 lines), CreateProjectModal (tech), MobileNewCustomerSheet, Settings cards, Invoice builder | See B → Forms. |
| Wizard | `/book` (4 steps), CancelFlow, RatePage, OneTapPurchaseOverlay, Checkout → Payment → tender chain (parent state in DispatchPageV2), lawn-diagnostic / social-post capture→review | No shared stepper. |
| Split / master-detail | ServiceLibrary 3-pane, Projects, Decisions, Data hygiene, EmailTemplates, PriceMatch, Autonomous content review | Each hand-rolls the collapse rule (900 / 1024 / 1280 breakpoints). |
| Document | Estimate, report (V1 + six V2 folders), pay, receipt, statement, contract, prep guide, price-change notice, outline | Customer glass; `DocumentActionBar` on top, one card column (760 or 640). |
| Overlays | ~95 hand-rolled roots vs 20 files on `ui/Dialog`, 6 on `ui/Sheet`, 7 on `CustomerDialogHost` | See B → Modals. |

### Style systems in the tree (and the islands)

| System | Where | Signature |
|---|---|---|
| Tier 1 admin | `components/ui` (13 primitives) + Tailwind (`tailwind.config.js`: zinc ramp, `ink.*`, `surface.*`, `alert-fg`, type scale 11–28, `border-hairline`, radius xs3/sm4/md6/lg8, weights 400/500) | Dashboard, Dispatch/Schedule V2, Customers V2 + 360, Pipeline/Estimates V2, Communications V2, Payers, Contracts, Assessments, Agents hub (4 of 9 tabs), Recruiting, Newsletter tabs, Photo assessments… |
| Tier 2 admin | Inline styles + a page-local `const D` | 64 files, **17 distinct palette signatures**: 30 zinc-mono light (`bg #F4F4F5 teal #18181B green #15803D amber #A16207 red #991B1B`), 8 slate light (`#F1F5F9 / #0A7EC2 / #16A34A / #F0A500 / #C0392B`), 6 zinc-mono-without-teal, 4 tech dark, 3 admin-auth dark, 11 one-offs. The documented dark `D` in the design skill survives only in tech, admin auth pages and `PendingActionsCard`. |
| Customer glass | `glass/glass-theme.css` (scoped `html[data-glass-theme]`) + `glass/glass-engine.js` + `data-glass` markup; tokens from `theme-brand.js`, `theme-customer.js`, `theme-doc.js`, `components/estimate/tokens.js`, `styles/brand-tokens.css` | 24 routes / 60 files carry `data-glass`; PortalPage alone 231 attributes. |
| Tech | Inline styles, dark D re-declared in ~14 files (green drifts `#22c55e` vs `#10b981`), Nunito Sans body, Montserrat headings, 'DM Sans' referenced but never loaded | No shared primitive layer. |
| Islands | `AutonomousContentReviewPage` + `autonomous-content/*` (forest-green/lime/cream brand, rounded-2xl) inside `/admin`; `EstimateViewPage?website=1` (`website-estimate.css`, Inter, `#1b2c5b/#ffd700`, gated); `CardPage` (own navy dark glass); `PricingLogicPanel`, `PushSettings`, `MileageTab`, `JobFormSection` (un-folded slate/teal legacy palette); `GlobalCommandPalette` (own light D + per-context accents + 16px radius + 9999 pills); `TurfHeightReviewPage` (third palette `M`); `CustomerHealthTabs` (third system inside Customers V2); `ProjectsPage` (Tier 1 imports + 157 inline style blocks in one file). | |

### Shared libraries and where they are bypassed

| Library | Adoption | Bypass |
|---|---|---|
| `components/ui` | Button 69 files/459 uses; Badge 55/229; Card 51; Input 17; Select 21; Textarea 18; Dialog 20; Sheet 6; Table 10; Tabs 4; Switch 11; Checkbox 3; Radio 3; `cn` 84 | 829 raw `<input>` in 137 files; 152 raw `<table>`; ~95 hand-rolled overlays; 26 local `*Button` defs; 28 local Badge/Pill/Chip/Tag defs; 19 local tab strips |
| `components/brand` | WavesShell 18 pages, SerifHeading 20, BrandCard 22 uses/4 pages, BrandButton 3 pages, CustomerDialogHost helpers 9 files | BrandInput 0 consumers; the `brand/index.js` barrel has 0 importers; PortalPage re-declares 5 button styles + 6 card styles and never imports BrandButton/BrandCard; 129 raw `data-glass-accent` buttons in 35 files |
| `components/estimate/cardStyles.js` (`estimateCard` / `estimateInnerBox`) | 13–14 files | Six report-V2 folders each carry their own r16 `card` object |
| Icons | `Icon.jsx` 20 consumers (customer) vs `lucide-react` 115 (admin 105) | ReportViewPage imports both; tech uses neither (emoji spans) |
| Fetch | `utils/admin-fetch.js` (401 redirect + 429 retry, 35 importers) vs `lib/adminFetch.js` (raw Response, no 401 handling, sends `Bearer null`, 22 importers) | **81 inline `adminFetch` copies**; 135 files read `waves_admin_token` directly; three files call `.then(r => r.json())` with no `ok` check |
| Hooks | `useModalFocus` (17 files), `useLockBodyScroll` (5), `useIsMobile` (reactive), `useAdminViewport` (admin only), `useFeatureFlag` | 12 files snapshot `const isMobile = window.innerWidth < N` at module load (never updates); ~95 overlays skip `useModalFocus` |
| Formatters | `lib/money.js` (9 importers, customer only), `lib/timezone.js`, `lib/invoiceDates.js` | 36 money, 47 date, 9 hh:mm, 5 phone formatter definitions; 14 inline `Intl` currency sites; 360 renders DATE columns with browser-local `toLocaleDateString` |
| CSS | `index.css` (@imports fonts/design-tokens/buttons/tokens/theme-square), `brand-tokens.css` (main.jsx), `glass-theme.css` (engine), `website-estimate.css`, `glass-components.css` | `design-tokens.css` has 17 vars with zero consumers and 2 shadowed by `brand-tokens.css` (`--radius-md` 6→8, `--radius-xl` 12→8); `theme-brand.js` has 24 dead exports |

### Largest files (maintenance hotspots)

| File | Lines | useState | Note |
|---|---|---|---|
| `pages/admin/SchedulePage.jsx` | 21,350 | 228 | retained V1 shared-export module (not dead) |
| `pages/PortalPage.jsx` | 16,168 | 238 | 56 top-level components |
| `pages/ReportViewPage.jsx` | 9,636 | 13 | ~3,000-line embedded `<style>` block |
| `components/admin/Customer360ProfileV2.jsx` | 8,474 | 145 | 8 hand-rolled overlays |
| `pages/admin/SEOPage.jsx` | 8,402 | 148 | 27 raw tables |
| `pages/admin/EstimateToolViewV2.jsx` | 8,352 | 38 | local InputV2/SelectV2 |
| `pages/EstimateViewPage.jsx` | 8,138 | 73 | runtime glass walker |
| `pages/admin/AdminInvoicesPage.jsx` | 7,323 | 114 | 6 portal modals, 32 raw `$x.toFixed(2)` |

---

## B. Design-system inventory (what ships today, with the inconsistency evidence)

### Typography

**Families.** Admin renders 100% Roboto: `index.css:187-206` forces `Roboto !important` on `html.admin-app *`, including `code`, `pre` and `[class*=font-mono]`, so the 133 `'JetBrains Mono'` literals, 230 `MONO` constants, 8 `ROBOTO` constant copies and 30 files with literal Roboto stacks are inert in admin; `tailwind.config.js` still declares Inter as `font-sans` (10 files use it; also inert). Three admin pages inject Google Fonts `<link>`s for DM Sans / Montserrat / Poppins that nothing renders (`PPCDashboardPage.jsx:559`, `SEODashboardPage.jsx:760`, `EstimatePage.jsx:811` — the last in a dead branch). Tech: Nunito Sans body + Montserrat headings, 'DM Sans' set in three surfaces but never loaded. Customer: the glass sheet's system stack; `public/fonts/fonts.css` still ships Instrument Serif and Nunito faces with zero references.

**Sizes.** The design skill's hard line is 14px minimum readable text; `docs/design/admin-ui-consistency-contract.md` sets body 13 / label 12 / caption 11. The code follows neither consistently:

| Portal | 9px | 10px | 11px | 12px | 13px | Total sub-14 literals |
|---|---|---|---|---|---|---|
| Admin | 53 | 313 | 919 | 1,335 | 938 | 3,577 (top: SEOPage 315, SchedulePage 298, InventoryPage 178, TaxPage 171) |
| Tech | – | 10 | 63 | 84 | 80 | 237 |
| Customer | 9 | 34 | 72 | 50 | 52 | 220 (mostly print documents + un-gated components) |

Rendered census (45 admin routes × 2 viewports): 1,851 elements paint at 12px, 627 at 11px, 237 at 10px, 20 at 9px. Inventory renders 646 sub-14 text nodes on one screen; pricing-logic 330; dashboard 315. Tier 1 primitives themselves encode 11–13px (`ui/Button` sm `text-11`, md `text-12`; `ui/Table` TH `text-11`; `ui/Input` `md:text-13`). Four Tailwind size classes that do not exist emit no CSS and silently inherit: `text-10` (86 sites), `text-15` (22), `text-20` (13), `text-24` (5). **Owner ruling needed (see K): which floor governs Tier 1 — the contract's 11/12/13 or the 14px hard line.**

**Weights.** Contract: Tier 1 = 400/500 only. Literals in the tree: 700 × 1,548, 600 × 277, 800 × 36, 850 × 9, 900 × 4, plus fractional 620/640/650/750. Rendered: 807 elements at 700 and 450 at 600 across the admin census; `/admin/agents` alone paints 71 at 700, `/admin/pricing-logic` 43. Customer weights >700 outside the brand gate: `App.jsx` (850 ×7), `StationMapCard`, `NewsletterSignup`, `InstallPrompt` (800), `theme-brand.js BUTTON_BASE` 800.

**Scale sources (four).** Tailwind `fontSize` keys 11/12/13/14/16/18/22/28; `brand-tokens.css` `--text-xs…xl` (0 consumers); `theme-doc.js FS`; glass sheet h1 `clamp(32px,4vw,40px)` / h2 26 / h3 20 / eyebrow 14 (`!important`, overrides every authored heading on 18 shell pages).

### Color

| Portal | Hex literals | Top values |
|---|---|---|
| Admin | 2,203 | `#FFFFFF` 243, `#18181B` 210, `#E4E4E7` 158, `#71717A` 110, `#F4F4F5` 99, `#991B1B` 71 (SchedulePage 180, ProjectsPage 138) |
| Tech | 279 | `#ef4444` 31, `#f59e0b` 20, `#1e293b` 20, `#0ea5e9` 20 |
| Customer | 1,400 | `#E7E2D7` 155, `#04395E` 113, `#D8D0C0` 64 (PortalPage 284, ReportViewPage 102) |

Brand hex written outside theme files: `#04395E` in 34 files, `#0A7EC2` in 30, `#E7E2D7` in 17, `#C8312F` in 14, `#F0A500` in 15. Token sources are seven-way split (`tailwind.config.js`, `styles/design-tokens.css` [mostly dead], `styles/brand-tokens.css`, `styles/theme-square.css` [all 13 vars live], `theme-brand.js` [24 dead exports], `theme-customer.js` + `components/estimate/tokens.js`, `theme-doc.js`, `glass/glass-theme.css`). Muted text: `#71717A` on white ≈ 4.7:1 (passes), but `#A1A1AA` (28 sites) ≈ 2.5:1 and `#94A3B8` (46 sites) ≈ 2.9:1 fail AA; 642 sites pair `D.muted` with 11–12px text. Semantic colour outside the sanctioned exceptions: dashboard chart palette (documented), Customers V2 health/tier/stage (documented), dispatch time-grid `#3B82F6/#1E40AF` blocks (undocumented), `#DCFCE7/#166534` prepaid chips in 4 schedule files (undocumented), Tier 2 green/amber/red chips and status text everywhere.

### Spacing and page width

Shell padding: admin 28/24 desktop, 16 mobile; no shell-owned content cap. Pages cap themselves eight different ways: `max-w-[1300px]` class (8 pages), inline `maxWidth:1300` (13), 1500 (Contracts, AgentEstimate), 1320, 1200, 1100, 1000, `max-w-6xl`, `max-w-7xl`, and 10 route pages uncapped (Dashboard, Customers, Pipeline, Reviews, PPC, SEO, Blog, Referrals, Settings, Newsletter). Double padding layers: `EstimateToolViewV2` adds `px-4 md:px-7` inside the shell's 16/28; `CommercialProposalPage` `px-4`; five Tier 2 agents-hub tabs add 24px; `LawnProtocolCommandCenterPage` `px-4` when embedded; `AdminPriceChangePage` `p-6` inside the hub. Customer columns: 760 (documents/portal), 640 (flows), 480 (`/book`), 420 (`/rate`), 560 (pay-page error states only).

### Radius

Literal sites: admin 1,657 (0×25, 2×27, 3×27, 4×151, 5×14, 6×388, 8×574, 10×157, 12×170, 16×12, 20×16, 999×46, 9999×15), tech 173, customer 975 (8×329, 10×153, 12×77, 14×12, 16×33, 999×86, 50%×61). Six token definitions disagree: Tailwind xs3/sm4/md6/lg8; `design-tokens.css` sm4/md6/lg8/xl12; `brand-tokens.css` sm6/md8/lg8/xl8 (loaded last, so `--radius-md`=8 and `--radius-xl`=8 win → `BrandCard` renders r8 while `theme-doc RADIUS.card`=12 and `DocumentActionBar` is r16); `theme-doc RADIUS` tag6/input8/button10/card12/modal16; `theme-brand RADIUS` md6…3xl24/button12; admin contract controls 4 / cards 6 / hero 8. Rendered admin census: 4px 1,022 · 6px 363 · 3px 266 · 12px 215 · 8px 188 · 50% 81 · 9999 39 · 10px 39. Overlay panels alone use 4, 6, 10, 16 (admin), 8, 16, 24 (customer), 12–18 (tech).

### Shadows and blur

309 `box-shadow` usages with **146 distinct values**; the only tokens (`--shadow-soft`, `SHADOW.card`, `PORTAL_SHELL.shadowSoft`, `theme-brand SHADOWS`) have 3–4 consumers each and `theme-brand SHADOWS` has zero external importers. 86 `backdrop-filter` sites: `glass-theme.css` 25, `glass-components.css` 18, `PortalPage` 15, `CardPage` 4, plus one-offs in NotificationBell, InstallPrompt, PublicFunnelTracking, GlobalCommandPalette (admin), ReviewVelocityEngine (admin) and two Tailwind `backdrop-blur` in admin bars. Seventeen distinct blur/saturate pairs (canonical 32px/185% desktop, 20px mobile; soft 18/165; chip 18/170; accent 18/175; modal 36/190; scrim 8) — see G.

### Buttons

| Implementation | Consumers | Notes |
|---|---|---|
| `components/ui/Button` | 69 files / 459 uses | sizes sm 128 : md 4 (sm = `h-11 sm:h-7 text-11`); variants secondary 126 / ghost 54 / primary 18 / danger 4; no `loading` prop (118 inline `saving ? 'Saving…'` ternaries); unknown variant/size silently renders unstyled |
| `components/brand/BrandButton` | 3 pages (PayPageV2, StatementPayPage, ContractSignPage) | 48px, weight 600, auto-tags `data-glass-accent` / `chip`; `glass-theme.css:462` forces chip min-height 40 over its inline 48 |
| `components/Button.jsx` + `styles/buttons.css` (legacy `.btn`) | PublicBookingPage, RatePage, ReviewPage (unrouted), StickyBottomCTA (dead) | `nav`/`utility` variants and `[data-surface=admin]` block (DM Sans + teal) have 0 consumers |
| `theme-doc docButton()` | 6 files | customer document CTAs |
| Local | 13 button style objects, 26 `*Button` component defs, PortalPage 5 button styles, EstimateViewPage 3 CTA styles, 129 raw `data-glass-accent` elements in 35 files, tech `ActionBtn`/`LinkBtn` | |

### Inputs and forms

`ui/Input` `h-11 text-16` on mobile → `md:h-9 md:text-13`; `Select` matches with an SVG caret; `Checkbox`/`Radio` 16px + 44px `u-touch-hit`; `Switch` is `role=switch`. `index.css:45-62` forces 16px on every input under `pointer: coarse` and 44px min-height on admin inputs. Adoption: 17 files use `Input`, 24 `Select`; **829 raw `<input>` in 137 files**; local input class constants in EstimateToolViewV2 (`INPUT_CLS` + `InputV2/SelectV2`), CustomersPageV2, NewsletterTabs, CustomerPropertiesPanelV2. Labels: 256 placeholder-only inputs (SchedulePage 39, InventoryPage 31), 672 `<label>` without `htmlFor` (some wrap the control and are fine; InventoryPage 31/0, CreateAppointmentModal quick-add, FieldLeadModal are verified sibling-label misses). Phone fields: 11 `type=tel`, 4 phone inputs with no `type=tel`. Money inputs: 47 `type=number` without `step`, 10 with `inputMode=decimal`. Error surfacing: **294 `window.alert()` and 142 `window.confirm()`** (SchedulePage 69, EstimatePage 40, TaxPage 20, EstimateToolViewV2 15 validation alerts); the good patterns exist (LoginPage `role=alert` + `aria-describedby`; PublicBookingPage phone `aria-invalid`; PayPageV2 `role=alert`). `BrandInput` has an `error` prop that only recolours the border and 0 consumers.

### Cards

`ui/Card` 33–51 files (CardFooter 0 consumers); `BrandCard` 7 files; `estimateCard()` 13 files; PortalPage 13 local card objects (10 byte-identical `#E7E2D7`/r8); six report-V2 folders each with an r16 `card` object and a 14/700/.08em `eyebrow` (vs `DOC_EYEBROW` 14/600/.06em); 8 more local card/panel styles (WeeklyWateringPlanCard, ProjectReportViewPage, LawnAssessmentPanel, PricingRealityCheckPage, EquipmentCalibrationPanel, AutoDispatchPage, WdoSignaturePad, AdminInvoicesPage). The glass sheet forces r12 on every `[data-glass]` regardless of the authored radius, so most inline card radii on customer pages are dead code.

### Navigation and tabs

One registry (`adminNavigation.js`) drives desktop sidebar, mobile tabs, More page and the role guard — good. Below it: `ui/Tabs` has 4 consumers (click-only tablist, no arrow keys, no `aria-controls`); 19 files map their own tab arrays with 0 `role=tab`; the marketing pages alone use **nine** leaf-tab styles (Blog/Social/Newsletter/Reviews share one byte-identical 36px uppercase row; SEOWorkspaceNav 12px pill strip; GBP 14px/700 grey pill; PPC slate segmented control; SEODashboard centered segmented; ContentRegistry 12px/700 buttons; Recruiting `Button` + `role=tab`; autonomous-content PillTab); the four ops pages (Inventory, Equipment, TimeTracking, Referrals) copy a fourth inline pill row instead of `AdminCommandHeader secondarySections`. Tab state is URL-backed in about half the pages (Blog, Newsletter, Knowledge, Equipment, Compliance, ServiceLibrary, hubs) and `useState`-only in the rest (SEOPage 16 leaves, SocialMedia 10, Reviews, Ads, GBP 9, TimeTracking, PricingStrategy) — refresh loses the tab and bell links cannot deep-link. Horizontal strips: three mechanisms (`HorizontalScroll.jsx`, `.u-scroll-strip`, `.tab-pill-scroll`).

### Tables and lists

`ui/Table` in 10 files; raw `<table>` 152 elements in 47 admin files (SEOPage 27, InventoryPage 14, TimeTracking 9, EquipmentMaintenance 7, WinLossSlicesCard 7), 0 in tech, 2 on customer surfaces. **`aria-sort` appears nowhere in client/src**; the two sortable headers are `<th onClick>` (ScheduleListView ×6) and a Name-only button (CustomersPageV2). Eight pagination implementations plus four load-more variants, all different labels/sizes; four lists hard-cap with no paging (CallLog 200 rows/365 days, Estimates `limit=all`, Tax mileage 200, Inventory products). Unwrapped tables that scroll the page sideways on phones: TimeTracking ×4, Banking, GBPManagement, ToolHealth, WinLossSlicesCard ×7, SourcePerformanceCard ×2, EmailAutomationsPanelV2; Compliance/Discounts `sTableWrap` uses `overflow: hidden` (clips). Nine per-file `thStyle/tdStyle` copies (th at 10, 11 or 12px, uppercase or not). No table has a stacked/card mobile treatment; number alignment is left in Inventory/TimeTracking/Tax, right in Banking/SEO, centred in CustomersPageV2.

### Modals, sheets and drawers

| Kind | Canonical | Actual population |
|---|---|---|
| Center dialog | `ui/Dialog` (role/aria-modal/labelledby, `useModalFocus`, full-screen below `sm`, safe-area, `z-[120]` hardcoded, body `overflow:hidden` lock) | 20 files; ~60 hand-rolled centre modals (Customer 360 ×5, AdminInvoicesPage ×6, EmailAutomationsPanelV2 ×5, EstimatesPageV2 ×5, NewsletterTabs ×3, AnnualPrepayLauncher ×3, tech ×6, EstimateViewPage ×5, PortalPage ×10 [these ten are correct: role + `useModalFocus` + `useLockBodyScroll`]) |
| Side sheet | `ui/Sheet` (`layer` prop, 100dvh, insets) | 6 files (JobDrawer, TechDrawer, PayerDetailSheet, PhotoAssessmentDetailSheet, InsertLinkSheet…); hand-rolled: ScheduleCustomerSidebar 430px, TreatmentPlanPanel 760px, CustomerEstimatesPanel 480px, Customer 360 900px, CustomerSmsPanel 420px, VisitProtocol 600px, ReviewVelocityEngine drawer |
| Bottom sheet | none | Dispatch More sheet, QuickActionMenu, EstimatesPageV2 pickers ×3, MarkPrepaidModal, RainOutSheet, ServiceOutlineComposerModal, tech ×5, TechRecapCapture + SchedulePage duplicate chip sheet |
| Full-screen phone sheet | none | 12 `components/schedule/Mobile*Sheet.jsx` files (only one has `role=dialog`, none `aria-modal`, none focus trap, z ladder 100/105/110/115, header 56 vs 64px, four close idioms), MobileNewCustomerSheet (correct), CreateAppointmentModal (correct) |
| Customer confirm | `CustomerDialogHost` (alertdialog, `useLockBodyScroll`) | 7 consumers |

47 raw overlays in 25 files have no dialog role, no focus trap, no Escape; 25 distinct z-index values (50 → 99999) and 18 backdrop recipes are in use; scroll lock is `useLockBodyScroll` (iOS-safe) in 4 places, `body.style.overflow` (ignored by iOS Safari for touch) in the two `ui` primitives and 6 more, and nothing in every other overlay. Same task, different kind: money actions are a `ui/Dialog` (CancelPlan), a hand-rolled centre/full-screen modal (360 Refund/Prepay), a bottom sheet on mobile (MarkPrepaid) and a full-screen sheet (PrepaySwitch).

### Badges and status

`ui/Badge` 55–67 files (tones neutral/alert/strong; 9 call sites pass a `muted` tone that does not exist and render unstyled); 28 local Badge/Pill/Chip/Tag definitions, four byte-identical Tier 2 `Chip` copies (DataHygiene, ContentRegistry, PriceMatch, AgentDecisions) plus two tone-object variants (AgentOps, AgentShadowDrafts); report `StatusPill` duplicated in lawnV2 and treeShrubV2; EstimatesPageV2 `StatusPillV3` behind a per-user flag adds a third filter vocabulary. Colour-only status with no text/aria: NotificationBell unread dot, ActionInbox critical-vs-warning dot, CustomersPageV2 alert-stage dot.

### Loading, empty, error, toast

No Spinner / Skeleton / EmptyState / ErrorState / Toast primitive exists in `components/ui`. Loading copy: `Loading…` 90 sites + `Loading...` 43; admin list pages replace the whole table with a one-line text node (layout jump) — only AgentEstimatePage and IntelligenceBarShell use skeleton bars; customer token pages use six hand-rolled static skeleton components. Toasts: 15 fixed-position copies (top-right green 13px, bottom-right zinc, bottom-right green + shadow, top-center) in 37 files rendering `position:fixed`, only one with `role=status`; every one sits at `bottom:20` with no safe-area. Error handling: 116 silent `catch` sites in `pages/admin`; verified "error renders as empty" cases in DiscountsTabs stats, RevenuePage (dead), LawnAssessmentPanel customers, CallLogTabV2 search, EquipmentMaintenance lists, LeadsTabs ROI, CompliancePage lookup, SEODashboardPage ("No SEO data yet" on a 500), AdsPage/PPC ("Connect your Google Ads account" on any failure); three writes fail invisibly (InlineTechPicker, EquipmentMaintenance record + mileage). Native dialogs: 294 `alert()`, 142 `confirm()` (customer pages: 0).

### Z-index

45 distinct values; primitives sit at `z-[120]` (Dialog/Sheet) while pages override with 1000/1100/1120 (Customer 360), 1000 (EstimatesPageV2), 9998/9999 (palette, NotificationBell, InlineTechPicker), `99999 !important` (EstimatePage, EstimateToolViewV2). The one P0 of this audit is a direct consequence (CancelPlanDialog at 120 under the 360 at 1000).

---

## C. Duplication report

Each row names the copies found, what should become canonical, and what the migration needs. "Owner" marks rows that need a ruling before code moves; "Delete" marks whole-file deletions that need explicit authorization (rule 5).

| Family | Copies today | Canonical | Migration |
|---|---|---|---|
| Admin fetch | `utils/admin-fetch.js` (35 importers) + `lib/adminFetch.js` (22, raw Response, no 401/429, `Bearer null`) + **81 inline `adminFetch` copies** + `apiGet/apiPost` (GeofenceArrivalPrompt), `techFetch` (TechIntelligenceBar), 3 files that never check `r.ok` (AdsPage, PPCDashboardPage, KnowledgePage) | `utils/admin-fetch.js`; add a raw-Response option so `lib/adminFetch.js` can retire | Page-by-page; delete each inline copy in the same PR; add a domain-rules grep that forbids new inline definitions |
| Buttons | `ui/Button`, `BrandButton`, legacy `components/Button.jsx` + `buttons.css`, `docButton`, 13 local style objects, 26 local components, PortalPage ×5, EstimateViewPage ×3, tech `ActionBtn`/`LinkBtn` | Admin: `ui/Button` (+ a `loading` prop to replace 118 ternaries). Customer: `BrandButton` primary/secondary + `docButton` for document CTAs. | Migrate PublicBookingPage/RatePage off `.btn`, then delete `components/Button.jsx` + `buttons.css` (Delete). Drop the 0-consumer `nav`/`utility`/`[data-surface=admin]` branches now. **Owner:** BrandButton secondary = 40px choice chip or 48px action? |
| Badges / chips | `ui/Badge` + 28 local defs; 4 byte-identical Tier 2 `Chip` (DataHygiene, ContentRegistry, PriceMatch, AgentDecisions) + 2 tone-object variants; report `StatusPill` ×2; `StatusPillV3` (flagged) | `ui/Badge` for Tier 1; one shared Tier 2 `Chip` export until those pages migrate; one `components/report` StatusPill | Fix the 9 `tone="muted"` call sites (renders unstyled today). **Owner:** which customer pills survive the 2026-09-04 "no status chips" ruling (weather %, score pill, TrendChip, TypedFindings value chips). |
| Overlays | `ui/Dialog` (20 files), `ui/Sheet` (6), `CustomerDialogHost` (7) vs ~95 hand-rolled roots; 12 `Mobile*Sheet` full-screen shells; 4 dialog implementations inside PortalPage/AutopayCard | Centre = `ui/Dialog` (add `layer` prop, switch to `useLockBodyScroll`); side = `ui/Sheet` (add `onClose` to `SheetHeader`); phone full-screen = one `MobileSheetFrame` owning role/aria/`useModalFocus`/56px+inset header/z ladder; customer confirm = `CustomerDialogHost` | Start with the Tier 1 files that already import `ui` but hand-roll (Customer 360 ×5, EmailAutomationsPanelV2 ×5, EstimatesPageV2 ×5, NewsletterTabs ×3); bottom sheets never carry inputs (MarkPrepaid, PrepaySwitch, RainOut move → Dialog) |
| Cards | `ui/Card` (33), `BrandCard` (7), `estimateCard()` (13), PortalPage ×13 (10 identical), report V2 ×6 (r16), 8 other local card styles | `ui/Card` (admin), `estimateCard()`/`BrandCard` (customer) after reconciling the `--radius-xl` conflict | Fold PortalPage's 13 into `PORTAL_CARD_STYLE`; hoist the six V2 report `card`/`eyebrow` blocks into one `components/report/reportStyles.js` |
| Formatters | 36 money, 47 date, 9 hh:mm, 5 phone definitions; 14 inline `Intl` currency sites; `lib/money.js` has 9 importers (customer only); AdminInvoicesPage `$${x.toFixed(2)}` ×32 with no thousands grouping; Banking/Tax/Payers disagree on null (`—` vs `$0.00`) | `lib/money.js` (+ whole-dollar option), `lib/timezone.js` `formatETDateOnly` for every DATE column (+ new `formatHHMM`), new `lib/phone.js formatPhoneDisplay` | **Owner:** null semantics for the shared admin money formatter (`—` or `$0.00`; Banking tests may pin `$0.00`) |
| Toast / notice | 15 fixed toast copies, 47 files with local toast state, 1 with `role=status` | `components/ui/Toast.jsx` + `ToastProvider/useToast` mounted once in `AdminLayoutV2` (bottom-right, zinc-900, 14px, `role=status aria-live=polite`, safe-area, 3.5s) | Replace `alert()` for outcomes first (294 sites), then the 15 toasts |
| Loading / empty / error | `charts.jsx EmptyState` (dashboard), `autonomous-content Empty`, `AdsPage EmptyState`, 6 customer skeleton components, `PublicLoadError` (11 pages) + 6 hand-rolled retry cards | `components/ui/State.jsx` — `LoadingState` (min-height to stop layout collapse), `EmptyState` (icon + copy + next action), `ErrorState({message,onRetry})`; customer keeps `PublicLoadError` and one shared `SkeletonCard` | Rule: primary-panel read failure renders `ErrorState`; only optional widgets may `.catch(() => {})` |
| Tab strips | `ui/Tabs` (4), `AdminCommandHeader` sections (52 consumers), 19 local tab arrays with 0 `role=tab`, 9 marketing leaf styles, 4 ops leaf pill rows, 3 horizontal-strip mechanisms | Level 1 = `AdminCommandHeader` sections; level 2 = `AdminCommandHeader secondarySections` (hubs already do this) or `ui/Tabs` (add arrow keys + `aria-controls`) | URL-back every tab (`?tab=`), which also fixes the bell/deep-link gaps on SEO/Social/Reviews |
| Tables | `ui/Table` (10) vs 152 raw tables in 47 files; 9 per-file `thStyle/tdStyle`; 8 pagination + 4 load-more variants | `ui/Table` grows `SortableTH` (button + `aria-sort`), `Pagination`/`LoadMore`, opt-in `minWidth` | Two mechanical PRs first: wrap every unwrapped `<table>` in an overflow-x scroller; extract one `adminLegacy.js` (`D`, `thStyle`, `tdStyle`, `tdNums`) for Tier 2 pages until each migrates |
| Tier 2 palette | 64 `const D` copies, 17 signatures; the only shared export is `pages/admin/email/emailStyles.js D` | One `client/src/theme-admin.js` (promote the 30-file zinc-mono set) | **Owner:** fold the 8 slate/teal files (PricingLogicPanel, PushSettings, MileageTab, JobFormSection, GlobalCommandPalette, SchedulePage V1 modals, SEODashboard, PPC) to zinc, or keep as a documented exception? |
| Customer tokens | `theme-brand.js` (42 importers, 24 dead exports), `theme-customer.js` (34), `theme-doc.js` (20), `estimate/tokens.js` (1), `brand-tokens.css` (7 dead vars), `design-tokens.css` (17 dead vars, 2 shadowed) | `theme-customer.js` + `theme-doc.js` for JS; `brand-tokens.css` for CSS vars (after the `--radius-*` values are reconciled with `theme-doc RADIUS`); delete the dead `design-tokens.css` vars | Prune `theme-brand.js` to live exports |
| Glass recipes | ~20 inline frosted surfaces, 6 scrim recipes (4 blur strengths), `.gc-*` re-declarations, BiometricGate's copied scene (missing one orb), CardPage's own glass, 4 admin blur sites | The `glass-theme.css` tiers (see G) | See G sequence |
| KPI / stat tiles | 13 implementations (DashboardPage KpiTile + KpiSparklineTile, StatCardV2, StatButton, EmailMailbox stat bar, SEO/Ads/Knowledge KpiCard, PPC KPI, ContentRegistry Kpi, autonomous-content Kpi, NewsletterPage StatTile, Reviews StatCard, agents `Metric` ×4, Tax/Compliance/Referrals StatCard) | Dashboard `KpiTile` for metric tiles with tone; a plain `ui/Card` stat block elsewhere | Not urgent; do as pages migrate |
| Markdown renderers | IntelligenceBarShell (Tailwind), GlobalCommandPalette (inline, 700 weights, teal bullets), TechIntelligenceBar | One `renderMarkdown` module | Blocks on the IB chrome ruling below |
| Intelligence Bar chrome | `GlobalCommandPalette` (⌘K, Tier 2 inline with own D + accents), `IntelligenceBarShell` (Tailwind, dispatch only), AgentEstimatePage direct hook use, `TechIntelligenceBar`; recents stored under two different keys | **Owner:** should `IntelligenceBarShell` become the single admin IB chrome (the palette does not use it) and which follow-up pattern survives? | |
| Customer search dropdown | CommunicationsPageV2 (div rows, no debounce), CallLogTabV2 (button rows, no debounce), EmailComposer (debounced, inline), CallIntelligencePanel (seq-guarded) | One `CustomerSearchCombobox` in `components/admin` | |
| `timeAgo` | 5 live copies (CommunicationsPageV2, CallLogTabV2, TriageInboxTabV2, EmailMailbox, SmsInboxWidget) | `lib/timezone.js` | Also fixes the mixed browser-local vs ET rendering on the comms surface |
| Health rings / radars | HealthCircle (360), HealthDot (directory), GradeCircle (CustomerHealthTabs); RadarChart ×2 | One ring in `components/admin/customer360` | 360 radar currently plots hardcoded values while the real sub-scores are in the payload (P1) |
| Same-name files | `components/NotificationBell.jsx` (live) vs `components/admin/NotificationBell.jsx` (0 imports); `PushSettingsV2` vs `PushSettings` (dead import only); `ReferralsPageV2` vs `ReferralsPage` (0 imports); `StickyActionBar` (live) vs `customer/StickyBottomCTA` (0 imports); `TaxPage` MileageTab vs `components/equipment/MileageTab` (dead) | The V2 / live file in each pair | Delete list below |
| Dead code | 37 files unreachable from every entry (~12,800 lines): `pages/admin/pipeline/*` (9 files, 2,660L, unrouted since 2026-05-25), `CustomerHealthPage` 1,797L, `ReferralsPage` 1,012L, `RevenuePage` 914L, `MileageTab` 493L, `CommercialProposalModal` 381L, `JobFormSection` 340L, `admin/NotificationBell` 396L, `ReportShowcaseCard` 273L, `JobCostCard` 249L, `ExpenseCapture` 237L, `AnnualPrepayLauncher` 198L, `SmsInboxWidget` 146L, `StickyBottomCTA` 141L, `CalendarDatePicker` 133L, `WeatherBar` 62L, `ReviewPage` 290L, `GlassNewsletterCard` 46L, `TurnstileWidget` + `lib/turnstile.js` + `useTurnstile.js`, `lib/customerPortalChrome.js`, `lib/pricingEngineClient.js`, `lib/pricingFormatters.js`, `lib/spokeSites.js`, `pages/admin/LeadsPage.jsx` (6L), `SampleBadge`, `TechEstimatorPage` (14L); plus 2 files kept alive only by an unused import in the retained `CommunicationsPage.jsx` (`CallRecordingsPanel` 1,248L, `PushSettings` 415L); 4 test files exercise unreachable code. Four recent sweeps (mobile Safari 08-24, mobile defects 08-24, sentence-case 08-31, glass batch C 09-05) spent edits on these dead files. | — | **Delete (owner authorization):** one cleanup-only PR, plus a client test asserting every file under `pages/` and `components/` is reachable from an entry so it cannot recur. Never the four retained V1 modules (trim their 6 unconsumed SchedulePage exports and the CustomersPage/CommunicationsPage/EstimatePage dead named exports in owner-scoped PRs). |

---

## G. Waves glass UI specification

There is **one** glass system of record — `client/src/glass/glass-theme.css` tiers mounted by `useGlassSurface` — adopted by 24 customer routes and 60 files through `data-glass` markup. The fragmentation is at the edges: the estimate DOM walker (`EstimateGlassTheme.jsx`, re-runs `getComputedStyle` over `#root *` on every mutation), `.gc-*` classes that re-declare chip/accent material, ~20 inline frosted surfaces, six scrim recipes, CardPage's private dark glass, BiometricGate's copied scene, and four admin blur sites. This section is the standard those edges collapse into. Attribute names stay as they are so no consumer moves.

### Surfaces

| Tier | Attribute | Material | Radius | Min size | Used for |
|---|---|---|---|---|---|
| Scene | `useGlassSurface(true)` | html background = 5 radial + 1 linear gradient, 5 orbs `filter: blur(70px)`, dot grid, fixed grain layer, pointer parallax | — | — | Every customer route; export `GLASS_SCENE_BACKGROUND` / `GLASS_SCENE_ORBS` from `glass-engine.js` so BiometricGate stops hand-copying them |
| Nav / shell | `data-glass="nav"` (new; today `""`, `"soft"`, `.gc-mbb` and inline recipes) | modal material at blur 22 / sat 180, `#fff .96` fallback, safe-area padding | 12 (999 with `data-glass-pill`) | — | Sticky header, fixed bottom tab bar, mobile book bar, InstallPrompt floating action |
| Card | `data-glass="card"` (or bare `data-glass`) | gradient `#fff .30→.08` over `rgba(244,239,224,.36)` warm tint; border `#fff .62`; blur 32 / sat 185 (20 below 640px); shadow `0 28px 90px navy .18 + 0 5px 18px navy .07` + 4 inset highlights; hover specular `::before`, sheen `::after` | 12 | — | Page sections; `BrandCard` is its component |
| Inner / raised | `data-glass="soft"` | gradient `.38→.15` over the warm tint; blur 18 / sat 165 (14 mobile); shadow `0 8px 26px navy .08` | 12 (10 when nested) | — | Tiles inside a card. **Rule:** when nested inside a `[data-glass]` parent, tint + border only — no second `backdrop-filter` (stacked blur is the single heaviest cost on ReportViewPage: 28 unconditional surfaces + 5 orbs) |
| Chip | `data-glass="chip"` + `data-glass-pill` | gradient `.34→.10` over `#fff .22`; blur 18 / sat 170 | 12 / 999 | 40 (44 pill) | Choice chips (frequency, filters); `BrandButton secondary` emits it; fold `.gc-freq-btn`, `.gc-tech-chip`, `.gc-scarcity` into it |
| Action | `data-glass-accent` (+ pill) | gold gradient `rgba(255,222,120,.6)→rgba(244,176,20,.45)` over `rgba(240,165,0,.38)`; border `rgba(255,238,180,.92)`; blur 18 / sat 175; ink `#04395E` | 10 (999 pill) | 44 | Every gold CTA; `BrandButton primary` emits it; fold `.gc-section-cta`, `.gc-mbb-btn`, `.gc-freq-btn[data-active]` |
| Modal | `data-glass="modal"` | gradient `.55→.28` over `#fff .50`; border `#fff .75`; blur 36 / sat 190; shadow `0 40px 120px navy .35` | 24 | — | Dialogs and sheets (CustomerDialogHost, AutopayCard, NotificationBell already comply) |
| Popover | `data-glass="modal" data-glass-popover` (new alias) | modal material, no lift | 12 | — | Property listbox, notification dropdown, account menu (today three ad-hoc radii 8/16/24) |
| Scrim | `data-glass-scrim` | radial `#fff .08` over `rgba(4,57,94,.38)`; **blur 8px made `!important`** and added to the fallback blocks | — | — | Every overlay backdrop; delete the six inline scrims (`slate .42/.48 + blur 5`, `slate .48 + blur 6`, `black .5`, `black .55 + blur 4`) |
| Input | `data-glass="input"` (new; promote the `.waves-ask-form input` recipe) | `#fff .8`, border navy `.16`, 48px, 16px text, italic 14px placeholder | 10 | 48 | `BrandInput` emits it |
| Clear | `data-glass-clear` | transparent | — | — | unchanged |

### Typography under glass (unchanged, now written down)

h1 `clamp(32px, 4vw, 40px)`, h2 26, h3 20, eyebrow 14 / 600 / .06em uppercase (`data-gt="eyebrow"`), body 16 minimum, labels 14 minimum, system font stack, ink `#04395E` (never remapped), muted `#475569` inside the glass scope. The sheet applies these with `!important`, so authoring a different heading size or card radius inline is dead code; write to the tokens instead.

### Interaction states

Hover: card specular only (no lift on mobile ≤640, already suppressed). Focus: `.waves-focus-ring` / `:focus-visible` 2px ring in navy — never `outline: none` without it (`glass-theme.css:502` is the one rule that drops the ring; 15 inline `outline:'none'` keys elsewhere). Active: accent darkens one step; chips flip to accent when selected. Disabled: 50% opacity, no blur change. Loading: `SkeletonCard` keeps the card's height (CLS fix pattern from EstimateViewPage).

### Where glass is NOT used

- **Admin and tech never carry `backdrop-filter`.** The two admin mobile bars (`StickyActionBar`, Customer 360 action bar) go solid; the two admin scrims (palette, ReviewVelocityEngine) align to the `ui` overlay (`bg-zinc-900/30`, no blur).
- Data-dense tables and forms inside modals sit on the opaque inner surface, not on nested glass.
- Print (`@media print`), `prefers-reduced-transparency`, `forced-colors` and `@supports not (backdrop-filter)` already provide solid fallbacks for card/soft/chip/accent — extend them to scrim, nav and input so no hand-rolled surface is left without one.
- Documents rendered for PDF (`?mode=pdf`, `EstimateProposalDocument`, `ServiceReportDocument`) stay non-glass by design.

### Enforcement

Extend `scripts/check-portal-brand.js` to `.css` under `client/src/glass` and `components/estimate/glass`, and to `components/NotificationBell.jsx`, `components/InstallPrompt.jsx`, `components/VanScene.jsx`, `components/analytics/`; forbid inline `backdropFilter` in customer files (the tiers own the material) and any `backdrop-filter` / `backdrop-blur` under `pages/admin`, `components/admin`, `pages/tech`, `components/tech`. Sequence: small safe folds (scene export, scrim authority, popover alias, input tier) → fold `.gc-*` into tiers → nested-blur cap (screenshot-verified on ReportViewPage) → retire the estimate walker → owner ruling on CardPage's dark glass.

---

## H. Canonical component architecture

The recommendation is to grow the components that already win adoption rather than introduce a new layer. "Retire" items are deletions that need authorization.

| Role | Canonical | Today | Do next | Retire |
|---|---|---|---|---|
| Admin shell | `AdminLayoutV2` | no `<main>`, no skip link, no content cap | add `<main id="admin-main">` + skip link; own a `max-w-[1300px]` content wrapper (with a documented `wide` opt-out for Dispatch board, Contracts, Assessments) | `components/admin/NotificationBell.jsx` (dead duplicate) |
| Admin page header | `AdminCommandHeader` (sections, secondarySections, actions) | 52 consumers; Pipeline, Dashboard, PPC, PricingRealityCheck, PricingStrategy roll their own | make `PipelineCommandHeader` a thin wrapper; move ops leaf pill rows into `secondarySections` | — |
| Tabs | `ui/Tabs` | click-only, 4 consumers | arrow keys, `aria-controls`, URL-sync helper | 19 local tab arrays |
| Buttons | `ui/Button` (admin), `BrandButton` / `docButton` (customer) | see B | `loading` prop; `size` floor on coarse pointer already handled by index.css | `components/Button.jsx` + `styles/buttons.css` after 2 pages migrate |
| Inputs | `ui/Input`/`Select`/`Textarea`/`Checkbox`/`Radio`/`Switch` (admin), `BrandInput` (customer) | 17/24 files; 829 raw inputs | add a `Field` wrapper (label + help + error + `aria-describedby`/`aria-invalid`) so labels stop being placeholder-only | EstimateToolViewV2 `InputV2/SelectV2`, per-page `INPUT_CLS` |
| Cards | `ui/Card` (admin), `estimateCard()`/`BrandCard` (customer) | | reconcile `--radius-xl`; export one `reportStyles.js` | PortalPage's 13 local card objects, six V2 `card` objects |
| Badge | `ui/Badge` (add `muted` tone or fix the 9 callers) | | one Tier 2 `Chip` export | 4 identical Chip copies |
| Dialog / Sheet | `ui/Dialog` (+`layer`, `useLockBodyScroll`), `ui/Sheet` (+`SheetHeader onClose`), new `MobileSheetFrame`, `CustomerDialogHost` | | migrate Tier 1 hand-rolled overlays first (Customer 360, EmailAutomations, Estimates, Newsletter) | AnnualPrepayLauncher (dead) |
| Table | `ui/Table` + `SortableTH` + `Pagination`/`LoadMore` + `minWidth` | 10 files | mechanical overflow-wrapper PR; `adminLegacy.js` for Tier 2 | 9 per-file th/td style copies |
| States | new `ui/State.jsx` (`LoadingState`, `EmptyState`, `ErrorState`) + `ui/Toast.jsx` | none | replace `alert()` outcomes and the 15 toasts | `charts.jsx EmptyState`, `autonomous-content Empty`, `AdsPage EmptyState` |
| Fetch | `utils/admin-fetch.js` (+ raw option) | 3 wrappers + 81 copies | domain-rules grep | `lib/adminFetch.js` |
| Polling | new `usePolling(fn, ms)` (clears on unmount, skips hidden tabs) | 17 network pollers, 4 visibility-gated, 1 leak (SEOPage scan) | move the bell, SMS widget, time tracking, tool health, tech notifications, portal tracker, recap card onto it | — |
| Formatters | `lib/money.js`, `lib/timezone.js` (+`formatHHMM`), new `lib/phone.js` | 97 local definitions | | — |
| Viewport | `useIsMobile` (reactive) + `useAdminViewport` | 12 module-level snapshots | replace the snapshots (P1, autofixable) | — |
| Icons | `lucide-react` (admin), `Icon.jsx` (customer) | tech uses emoji spans | give tech `Icon.jsx`; stop customer files importing lucide directly (4 files) | — |
| Glass | `glass-theme.css` tiers + `glass-engine.js` exports | see G | see G | `EstimateGlassTheme.jsx` walker (after tagging), CardPage `GLASS_MATERIAL` (owner) |
| Tokens | `tailwind.config.js` + `theme-square.css` (admin V2); new `theme-admin.js` (Tier 2 legacy); `theme-customer.js` + `theme-doc.js` + `brand-tokens.css` (customer) | 7-way split | prune dead vars/exports; add `text-10/15/20/24` to the domain-rules grep so undefined classes cannot return | `styles/design-tokens.css` dead vars, 24 `theme-brand.js` exports |
| Navigation | `config/adminNavigation.js` | 3 lists | `replace` on the `/admin` index redirect; wildcard children under `/admin` and `/tech` so unknown staff URLs never fall into the customer portal; fix the one server link to an unmounted route; extend the mounted-route test to server-generated links | `mobileSettingsSections.js` dead entries |
| A11y mechanisms | `ui/Dialog|Sheet` or `useModalFocus` + role/aria; `<button>`/`<a>` or role+tabIndex+key handler; `u-focus-ring` / `waves-focus-ring` | 47 raw overlays, 102 clickable divs, 15 inline `outline:'none'` | enable `eslint-plugin-jsx-a11y` (click-events-have-key-events, no-static-element-interactions, label-has-associated-control, control-has-associated-label) as CI warnings | — |

---

## I. Screens and pages that need redesign (not just fixes)

Ranked by daily use and by how many independent readers landed on the same screen. Each entry says why a local fix will not do and what the redesign is.

1. **Customer 360 (`components/admin/Customer360ProfileV2.jsx`, 8,474 lines).** Six readers converged here. Two navigation systems live inside one panel (a 7-tab strip plus a 6-chip timeline filter and a decorative 3-step stepper on Contracts); on phones a top bar and a bottom bar both carry Text/Call. Five sub-modals are hand-rolled fixed overlays with no dialog role or focus trap, and Escape or a backdrop click inside any of them closes the whole profile (C360-06 / C360-02). The Health Radar plots hardcoded values while the real sub-scores sit in the payload; "Add Note" (header pill and ⋯ menu) lands on the SMS composer; the Comms tab duplicates a weaker composer than `CustomerSmsPanel` and never marks texts read; date columns render a day early for ET viewers; 36 raw inputs, 14 raw selects and 7 identical black pill buttons bypass the primitives; the Map view (Tier 2 dark) and Health view (third palette) render inside the Tier 1 page. **Redesign:** one navigation model (tabs only, timeline filter as a segmented control inside its tab), every sub-modal on `ui/Dialog` with `layer` (this lane's P0 PR proves the path), financial actions grouped under Billing, the SMS panel reused, real health data or no radar, and the profile moved onto `ui/Sheet` so it inherits focus/Escape semantics instead of a window listener.

2. **Pipeline → Estimator (`EstimatesPageV2.jsx` + `LeadsTabs.jsx` + `EstimateToolViewV2.jsx`).** The Leads tab is inline-styled with its own `C` palette inside the Tier 1 Pipeline page, so one page shows two visual systems (12px-radius cards / 9999 pills next to the 4px/6px contract). The mobile estimates list drops Follow Up, Mark lost, Schedule, proposal and preview actions that desktop rows carry (PE-01). "Convert to Customer" asks the operator to paste a customer UUID (PE-02) while mark-accepted and schedule-appointment already convert leads automatically. Lead prefill passes a street-only address and never carries `customer_id`, so information is lost between lead → estimate → customer. The estimator on a phone (the tech path) never collapses its eight `grid-cols-3` field groups. The entire `pages/admin/pipeline/*` Unified Pipeline directory (2,660 lines) has been unrouted since 2026-05-25. **Redesign:** one row-action model shared by desktop and mobile, lead → estimate handoff that carries the lead id and full address, remove or rebuild "Convert to Customer" (owner ruling), Leads tab migrated to Tier 1, estimator form grids responsive, and a ruling on the Unified Pipeline (delete or return).

3. **Communications hub (`CommunicationsPageV2.jsx` + Email tab).** The Email tab (`EmailPage` + `email/*`) is the last inline-styled Tier 2 island rendered inside a Tier 1 page, with its own `AdminCommandHeader`, stat bar, filter chips and modal. On phones the SMS composer card sits above the thread list (600px to scroll past), the Calls stat filters are hidden with no substitute, and conversation/log/search rows are click-only divs (102 such rows across the surface). Four different customer-search dropdowns, five `timeAgo` copies, and ET vs browser-local timestamps on the same page. **Redesign:** docked reply composer under the conversation (the `CustomerSmsPanel` pattern), Email tab to Tier 1, one search combobox, one time helper, keyboard-reachable rows.

4. **Schedule / Dispatch mobile sheets (`components/schedule/Mobile*Sheet.jsx`, 12 files).** Ten full-screen sheets hand-roll their shell with five header variants, four close idioms, a 100/105/110/115 z ladder, no `role=dialog`, no focus trap, no scroll lock; "Back" from Checkout lands on the list instead of the appointment. The cash numpad is decorative (the amount never reaches the server). **Redesign:** one `MobileSheetFrame` owning role/aria/focus/z and a 56px + safe-area header; Checkout → Payment → tender as a real stack with Back; owner rulings on the numpad and the Square-placeholder rows.

5. **Dispatch board (`DispatchBoardLayout.jsx`).** Sized `h-[calc(100vh-64px)]` inside a shell with no 64px bar, so the board is always clipped and sits under the mobile tab bar; below `xl` drag-to-reassign has no drop target because the roster pane is `display:none`; Board is the default tab at every width though its header comment declares mobile out of scope. **Redesign:** `flex-1 min-h-0` against `--admin-vh`, a phone board that defaults to Schedule or offers a real reassign affordance.

6. **Admin ops Tier 2 pages (Inventory 5,845 lines, TimeTracking 5,402, Tax 5,245, SEO 8,402, Settings, Equipment, Compliance, Referrals).** Each carries its own `D` copy, `sCard/sBtn/sBadge/sInput/thS/tdS`, a hand-copied 36px uppercase leaf-tab row, a module-level `isMobile` snapshot (6 files), 10px table headers and hundreds of 11–13px runs, `alert()` outcomes and fixed toasts with no safe-area. **Redesign:** these are migration candidates, one page per PR, onto `AdminCommandHeader secondarySections` + `ui/Table` + `ui/State` + `useToast`; until then the shared `adminLegacy.js` styles stop the drift.

7. **Marketing hub (SEO, Blog, Social, Newsletter, Reviews, Ads/PPC, Knowledge, GBP).** Nine leaf-tab styles; sub-tab state lost on refresh in SEO (16 leaves), Social (10), Reviews, Ads, GBP (9); `AutonomousContentReviewPage` ships a forest-green/lime/cream brand hero and iPhone mockup inside `/admin`; SEO and PPC inject `<style>` `!important` overrides at 640px while the header switches at 768; PPC and SEODashboard duplicate views that also exist in Ads and SEO tabs; two pages inject Google Fonts nothing uses. **Redesign:** URL-backed tabs everywhere, one leaf-tab control, the autopilot hero on zinc, and an IA ruling on the duplicated dashboards.

8. **Tech portal home (`TechHomePage.jsx` + `VisitBriefPanel.jsx`).** The Next Stop action row packs up to six ~34px buttons with 12px labels in one non-wrapping row; in-brief actions are ~32px with an icon-only flag; five bottom sheets, the geofence prompt and the rain-out toast ignore safe areas; `100vh` inside a padded main adds blank scroll; no offline state anywhere; two incompatible API-base conventions. **Redesign:** one primary next action per stop (glanceability), 44px targets, a shared tech bottom-sheet with safe-area, a `Icon` set instead of emoji spans, and a documented offline stance.

9. **Customer portal app shell (`PortalPage.jsx`, 16,168 lines).** Every tab re-declares its own card/button/label styles (13 card objects, 5 button styles, 52 boxed section labels), ten hand-rolled dialog shells, two switch primitives, two icon-tile primitives, and inline radii/type that the glass sheet overrides anyway. The Visits tab prints invented per-visit treatment descriptions (CPA-001). **Redesign:** fold the local styles into `BrandCard`/`BrandButton`/`ShellIconTile`/`GoldSwitch`, one dialog shell, and real visit copy from the schedule payload (or none).

10. **Customer report V2 folders (`components/report/*V2`).** Six folders each ship a local r16 `card`, a 14/700 `eyebrow`, and copy-pasted `StatusPill`/`ScoreRing`/`TrendChip` (lawn and tree are near-identical files); the hero and re-entry card still render status pills after the 2026-09-04 no-pills ruling; lawn and tree dashboards nest inside the visit-summary card and drop to a ~160px text measure at 320px. **Redesign:** one `reportStyles.js`, one shared pill/ring module, edge-to-edge V2 sections, and the owner's per-pill ruling.

11. **Intelligence Bar chrome.** Four implementations (⌘K palette, `IntelligenceBarShell`, AgentEstimatePage's direct hook use, tech bar) with three markdown renderers and two recents stores; the primary palette is Tier 2 inline with its own palette and accent colours inside the monochrome admin, and no IB surface has a live region. **Redesign:** `IntelligenceBarShell` as the single admin chrome (owner ruling), one renderer, one recents key, `role=status` on loading and answers.

12. **Admin shell (`AdminLayoutV2.jsx`).** Structurally sound, but it owns no content cap (pages cap themselves eight ways), has no `<main>` landmark or skip link, its mobile drawer has no focus trap, the shared bell panel hardcodes 56px offsets, and unknown `/admin/*` URLs fall through to the customer portal. **Redesign (small):** shell-owned `max-w` with a `wide` opt-out, landmark + skip link, drawer on `useModalFocus`, wildcard routes.

---

## D. Functional bug report (verified)

Every entry survived the refute-by-default pass. "Fix" is the verifier's sketch where one exists.

### F0053 · P1 · DATE columns (service/scheduled/payment dates) render one day early for ET viewers

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:150` (reader customer-360:C360-03)
- **Reproduction:** Open any customer with a visit dated today in an ET browser; the Services tab shows yesterday's date.
- **Expected:** A visit stored as 2026-09-05 shows 'Sep 5, 2026' (canonical helper: formatETDateOnly in client/src/lib/timezone.js).
- **Actual:** pg serialises DATE columns as '2026-09-05T00:00:00.000Z' on the TZ=UTC Railway box; the regex branch misses the 'T', and new Date(...).toLocaleDateString() in America/New_York yields 'Sep 4, 2026'. Affects Upcoming Appointments, Recent Services, Service History, Scheduled Services, Recent Transactions, Payment History and the timeline (via timeAgo). The 360 has zero imports from lib/timezone.js (rule 15 canonical path).
- **Likely cause:** Local fmtDate written for 'YYYY-MM-DD' strings; the payload actually carries midnight-UTC ISO.
- **Fix:** In Customer360ProfileV2.jsx import `formatETDateOnly` from "../../lib/timezone" and use it (instead of fmtDate) at the DATE-typed call sites: :2185, :6522 (service_date), :6495, :6881 (scheduled_date), :5053, :6654, :7046 (payment_date), :7858 (application_date). Leave fmtDate for timestamp columns (created_at, signedAt, etc.).
- **Test:** Unit (TZ=America/New_York): formatETDateOnly('2026-09-05T00:00:00.000Z') === 'Sep 5, 2026'; RTL: render the Services tab with a service_record whose service_date is '2026-09-05T00:00:00.000Z' and assert 'Sep 5, 2026' is shown.
- **Verifier:** confirmed — Customer360ProfileV2.jsx:150-162 `fmtDate` only treats a bare `^\d{4}-\d{2}-\d{2}$` as date-only; anything else goes through `new Date(d).toLocaleDateString("en-US", {...})` with no timeZone. The GET /:id payload (server/routes/admin-customers.js:3078) returns raw `service_records.*` (:3140-3145), `payments.*` (:3147) and scheduled_services rows (customerScheduledHistoryQuery :152-159; upcoming :3

### F0063 · P1 · Clicking any sub-modal backdrop bubbles to the overlay and closes the whole profile (mid-refund unmount)

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:5907` (reader customer-360:C360-02)
- **Reproduction:** Open a customer → Edit → click the dim area outside the form. Profile closes and edits are lost.
- **Expected:** Clicking outside a sub-modal dismisses only that modal; while a refund/charge is in flight nothing closes.
- **Actual:** React synthetic events bubble through portals to the overlay's onClick={onClose}, so a backdrop click on Edit customer / Record prepay / Send prepay invoice / Cancel signup / Refund closes the entire 360. During a running refund the modal's own guard (!running) keeps it open, but the overlay still closes and unmounts the modal mid-request — the exact outcome the Escape-swallow at 5060-5069 was written to prevent.
- **Likely cause:** Sub-modals were added inside the overlay's React subtree without e.stopPropagation() on their backdrop handlers.
- **Fix:** Customer360ProfileV2.jsx:5907 — replace `onClick={onClose}` on the root overlay with `onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}` so only a click on the overlay's own dim area closes the profile (covers all six sub-modals in one line).
- **Test:** RTL: open the profile, click Edit, fireEvent.click on the edit modal's backdrop — assert the profile onClose was not called and setEditOpen(false) ran; also click the profile overlay itself → onClose called.
- **Verifier:** confirmed — A @babel/parser walk of the file shows `<AnnualPrepayModal>` :8193, `<AnnualPrepayInvoiceModal>` :8204, `<CancelSignupModal>` :8214, `<CancelPlanDialog>` :8221, `<RefundPaymentModal>` :8228 and the inline edit modal `<div>` :8236 each have exactly ONE JSX ancestor: the root overlay div at :5902-5907 (`className="admin-shell-v2 fixed inset-0 bg-black/70 z-[1000] flex justify-end font-sans" onClick=

### F0074 · P1 · CancelPlanDialog (ui/Dialog z-120) is portaled beneath the Customer 360 overlay (z-1000)

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:8221` (reader overlays:OV-01)
- **Reproduction:** Open /admin/customers -> a customer (Customer 360 panel) -> Actions -> Cancel plan. Observe whether the dialog is visible/clickable above the dark scrim.
- **Expected:** Cancel plan dialog paints above the Customer 360 panel and its bg-black/70 scrim, like the sibling Refund / Cancel-signup / Edit-customer modals that were raised to z-[1100]/z-[1120].
- **Actual:** Both roots are direct children of document.body with explicit z-index; z-[120] loses to z-[1000], so the dialog sits under the opaque scrim and panel. ui/Dialog has no layer prop (Sheet does) and CancelPlanDialog passes no className/style override. Static analysis only - not runtime verified.
- **Likely cause:** Dialog.jsx z ladder comment (lines 70-74) only accounts for shell chrome up to 110; Customer 360 predates it at z-[1000] and its child modals were hand-bumped instead of the primitive gaining a layer prop.
- **Fix:** client/src/components/admin/CancelPlanDialog.jsx:203 — `<Dialog open onClose={() => !running && onClose()} size="md" style={{ zIndex: 1100 }}>` with a one-line comment: hosted inside the Customer 360 overlay (z-[1000]); matches the sibling C360 modals at z-[1100].
- **Test:** client/src/components/admin/CancelPlanDialog.test.jsx: render the dialog (existing fetch mocks), then `expect(document.querySelector('[role="dialog"]').style.zIndex).toBe('1100')`; optionally Dialog.test.jsx asserting `<Dialog open style={{zIndex: 1100}}>` lands zIndex on the portal root.
- **Verifier:** confirmed — Proven by CSS stacking, no runtime needed. client/src/components/admin/Customer360ProfileV2.jsx:5901-5906 `return createPortal(<div … className="admin-shell-v2 fixed inset-0 bg-black/70 z-[1000] flex justify-end font-sans" onClick={onClose}>` … :8473 `, document.body);`. CancelPlanDialog is rendered inside that tree at :8220-8225 `{cancelPlanOpen && (<CancelPlanDialog customer={c} onClose={() => s

### F0195 · P1 · MobileServiceEditModal rewrites the visit's service_type on every save, appending '— Billed Monthly' to any type without a cadence keyword

- **Location:** `client/src/components/schedule/MobileServiceEditModal.jsx:101` (reader schedule-mobile-sheets:SMS-01)
- **Reproduction:** Dispatch mobile -> appointment -> Review & checkout -> tap the base service line -> change only Price -> Save -> reload: service_type now ends with '— Billed Monthly' (or has its cadence prefix moved after an em dash).
- **Expected:** Editing price, staff, duration or notes leaves service_type untouched unless the tech explicitly changed the cadence; a service with no cadence keyword ('Lawn Care - Track B', 'Rodent Bait Station') is never relabeled.
- **Actual:** detectTier defaults to 'monthly' and handleSave always rebuilds the string, so a price-only save on 'Lawn Care - Track B' persists 'Lawn Care - Track B — Billed Monthly' (em dash + cadence label); 'Quarterly Pest Control' becomes 'Pest Control — Quarterly'. Downstream readers (serviceColor, isLawn regex, labels, cadence inference) see a rewritten string.
- **Likely cause:** Square-style 'choose one tier' list assumed every service type carries a cadence; fallback tier is treated as a selection.
- **Fix:** MobileServiceEditModal.jsx: `const initialTier = useRef(detectTier(service?.serviceType));` then in handleSave `const tierChanged = tier !== initialTier.current;` and build the body with `...(tierChanged && tierOpt ? { serviceType: `${baseName} — ${tierOpt.label}` } : {})` — omit `serviceType` entirely when the tech did not pick a different tier so the server keeps the stored value.
- **Test:** RTL: render with service.serviceType='Lawn Care - Track B', edit Price, Save → the PUT body has no `serviceType` key; pick 'Quarterly' then Save → body.serviceType === 'Lawn Care - Track B — Quarterly'.
- **Verifier:** confirmed — MobileServiceEditModal.jsx:41-47 `function detectTier(serviceType) { if (!serviceType) return 'monthly'; for (const opt of TIER_OPTIONS) { if (opt.pattern.test(serviceType)) return opt.key; } return 'monthly'; }` — the fallback is indistinguishable from a selection; 96-101 `const tierOpt = TIER_OPTIONS.find((o) => o.key === tier); const nextServiceType = tierOpt ? `${baseName} — ${tierOpt.label}` 

### F0288 · P1 · Visits tab prints invented per-visit treatment descriptions

- **Location:** `client/src/pages/PortalPage.jsx:3960` (reader customer-portal-app-a:CPA-001)
- **Reproduction:** Log in as any customer with ≥1 upcoming visit → Visits → Upcoming: read the 'Visit #N — …' line on the featured/compact card.
- **Expected:** Upcoming-visit cards only show what the schedule payload says will be done (or nothing), per the truth rule the same tab states at 4881-4883 ('never invent a next-treatment month or mosquito restart').
- **Actual:** Every upcoming card shows 'Visit #N — <hardcoded sentence>' chosen by visitNumber modulo a static list (3930-3958), e.g. a lawn visit #3 always reads 'Summer fertilizer + chinch bug preventive' regardless of season, plan, or the work order — a fabricated promise to the customer.
- **Likely cause:** Legacy placeholder copy from before the schedule payload carried real service data; never removed when the truth-copy pass hit the empty state.
- **Fix:** Drop SCHEDULE_TREATMENT_DESCRIPTIONS/getScheduleVisitDescription and render a server-supplied description field only when present (or the service type alone). Owner to confirm whether the visit counter '#N' should stay.
- **Test:** Render ScheduleTab with a getSchedule payload row (serviceType 'Lawn Care', no description) and assert `queryByText(/fertilizer\|chinch\|pre-emergent\|Visit #/)` is null.
- **Verifier:** confirmed — client/src/pages/PortalPage.jsx:3930-3958 is a static table (`'Lawn': ['Spring green-up fertilizer + pre-emergent weed control', ..., 'Summer fertilizer + chinch bug preventive', ...]`) and 3960-3966 picks by index: `return descs[(num - 1) % descs.length];`. PortalPage.jsx:4562-4564 applies it unconditionally, overwriting any payload field: `const visitNum = s.visitNumber \|\| (idx + 1); const des
- **Needs owner decision**

### F0348 · P1 · 'Charge card on file' from Invoices collects an off-session charge with one tap, no amount and no confirmation

- **Location:** `client/src/pages/admin/AdminInvoicesPage.jsx:1871` (reader billing-admin:BA-03)
- **Reproduction:** Admin (role admin) → Invoices → expand an open invoice for a customer with a saved card → Charge card on file → tap Charge: the card is charged immediately.
- **Expected:** Money movement follows the surface's own pattern: quote the exact amount (incl. funding-aware surcharge / credit) → confirm → charge with `expectedTotal`, as CreateProjectModal.jsx:1702-1722 already does against the same two endpoints.
- **Actual:** The sheet lists saved cards with a bare 'Charge' pill; nothing on screen states the amount that will be collected, there is no confirm step, and the request omits `expectedTotal`, so the server's amount guard (admin-invoices.js:1570 `const { paymentMethodId, expectedTotal } = req.body`) is not exercised from this path. The Record-payment, Apply-credit and Send flows on the same page all go through a modal with the amount shown.
- **Likely cause:** MobileCardOnFileSheet was built for the tech checkout sheet and reused on desktop via `desktopVisible` without the quote/confirm layer the tech project modal later gained.
- **Fix:** In MobileCardOnFileSheet (or a wrapper used by the invoice page), call POST /admin/invoices/:id/charge-card-quote for the selected card, show base / card fee / total, require a confirm, and pass `expectedTotal: quote.total` to charge-card. Keep the existing orphan/ambiguous 'Do not retry' handling.
- **Test:** MobileCardOnFileSheet test: Charge stays disabled until the charge-card-quote resolves and confirm is accepted; the charge-card request body contains expectedTotal equal to quote.total.
- **Verifier:** confirmed — AdminInvoicesPage.jsx:1871-1878 `{isAdminUser && canCollect && cardOnFile && !inv.payer_id && (<button onClick={() => setCardOnFileInvoice(inv)} …>Charge card on file</button>)}` -> :2241-2250 `<MobileCardOnFileSheet desktopVisible invoiceId={…} customerId={…} customerName={…} onClose onChargeSuccess>`. MobileCardOnFileSheet.jsx:72-79 POSTs `/admin/invoices/${invoiceId}/charge-card` with `body: JS
- **Needs owner decision**

### F0467 · P1 · Mobile estimates list has no Follow Up, Mark lost, Schedule or proposal actions that desktop rows carry

- **Location:** `client/src/pages/admin/EstimatesPageV2.jsx:3365` (reader pipeline-estimator:PE-01)
- **Reproduction:** Open /admin/pipeline?tab=estimates at <768px width, tap ⋯ on a sent/viewed estimate: no Mark lost / Follow up / Schedule entries.
- **Expected:** The mobile list (the view Adam gets on his phone under 768px, per useIsMobile(768) at 3961) exposes the same status transitions as desktop: Follow Up (POST /follow-up, increments followUpCount), Mark lost (DeclineModalV2 with normalized disposition), Schedule (CreateAppointmentModal with defaultEstimateId), Commercial proposal, Preview customer view, 1x-option/Invoice toggles.
- **Actual:** EstimatesMobileListView never imports FollowUpModalV2, DeclineModalV2 or CreateAppointmentModal (grep of lines 3490-3958 finds no setFollowUpTarget/setDeclineTarget/scheduleEstimate). On a phone an estimate cannot be marked lost, followed up via the tracked endpoint, scheduled from the row, or previewed; the operator must find a desktop.
- **Likely cause:** Mobile list was built as a 'touch-optimized counterpart' (comment at 3494) and never caught up as desktop row actions grew.
- **Fix:** Add followUpTarget/declineTarget/scheduleEstimate state to EstimatesMobileListView, render FollowUpModalV2 / DeclineModalV2 / CreateAppointmentModal alongside the existing ExtendEstimateModalV2, and add the 'Follow up', 'Mark lost', 'Schedule', 'Commercial proposal', 'Preview customer view' items (same status guards as desktop) to the mobile RowActionsMenu. Better: derive both rows' item lists from one buildRowActions(estimate, handlers) helper so the two views cannot drift again.
- **Test:** RTL: render EstimatesMobileListView with a status:'viewed' estimate, open the ⋯ menu, assert 'Mark lost' and 'Follow up' items exist and open DeclineModalV2 / FollowUpModalV2.
- **Verifier:** confirmed — Desktop pipeline view owns the transitions: state at 1700-1707 (`followUpTarget`, `declineTarget`, `scheduleEstimate`), modals at 2016 `<FollowUpModalV2`, 2026 `<DeclineModalV2`, 2068 `<CreateAppointmentModal`, row actions 2503 `onClick={() => setFollowUpTarget(e)}` 'Follow Up', 2519/2554 `setScheduleEstimate(e)`, 2719 ``navigate(`/admin/estimates/${e.id}/proposal`)``, 2735 'Preview customer view'
- **Needs owner decision**

### F0532 · P1 · RecapCapture renders white text on a white/light card (D.white copied from the dark tech palette)

- **Location:** `client/src/pages/admin/SchedulePage.jsx:10213` (reader tokens-style:TS-01)
- **Reproduction:** Admin → Schedule → open a recap-eligible pest visit closeout (recapEligible) → 'Recap clips' → tap '+ Capture clip' → the bottom sheet title is blank; after a clip is added the role label row is blank.
- **Expected:** Clip role label and the 'What were you doing?' sheet title are readable on the light D palette (D.heading / D.text).
- **Actual:** #FFFFFF text on #FFFFFF (sheet) and on #F1F5F9 (clip row) — invisible. Same pattern at L19949 (`<span style={{ color: D.white }}>` for the re-entry stepper value inside CompletionPanel) and the block also authors Montserrat + fractional sizes 12.5/11.5/13.5 inside the Roboto-forced admin shell.
- **Likely cause:** RecapCapture was ported from the dark tech D palette (pages/TechCapturePreview.jsx uses the identical strings with D.white on #0f1923); SchedulePage's D was later flipped to a light palette without re-checking `color: D.white` call sites.
- **Fix:** SchedulePage.jsx:10194 `color: D.white` → `color: D.heading`; :10213 `color: D.white` → `color: D.heading` (optionally drop the inert `fontFamily: "'Montserrat', sans-serif"`); :19949 `<span style={{ color: D.white }}>` → `<span style={{ color: D.heading, fontWeight: 500 }}>`. Leave the other D.white uses (:4778, :4916, :5030, :5556, :18669 sit on dark/active backgrounds; :8303-8748 use it as a background).
- **Test:** RTL: render RecapCapture with one clip item and a pending file, assert the 'What were you doing?' element and the role label do not have inline color #FFFFFF (or that computed color differs from the parent background); regression grep: no `color: D.white` in SchedulePage.jsx lines 10127-10290.
- **Verifier:** confirmed — client/src/pages/admin/SchedulePage.jsx:110-127 defines the light palette: `bg: "#F1F5F9", card: "#FFFFFF", … white: "#FFFFFF", heading: "#0F172A"`. RecapCapture (:10127): :10191 row `background: D.bg` → :10194 `<div style={{ fontSize: 12.5, fontWeight: 500, color: D.white, textTransform: "capitalize" }}>{m.role}</div>` (white on #F1F5F9); :10211 sheet `background: D.card` → :10213 `<div style={{ 

### F0535 · P1 · SettingsPage toggle is a plain div: no role=switch, aria-checked, tabIndex, or keyboard handler (sibling PestPressureSettingsPage does it right; ui/Switch exists)

- **Location:** `client/src/pages/admin/SettingsPage.jsx:110` (reader a11y:A11Y-03)
- **Reproduction:** Open /admin/settings, Tab through the page — no toggle receives focus.
- **Expected:** The Settings toggles (every admin setting row) are keyboard-operable and announce as switches, matching PestPressureSettingsPage.jsx:110-118 or components/ui/Switch.jsx:10-15
- **Actual:** Settings toggles cannot be focused or flipped from the keyboard and read as generic text to a screen reader; also a 24px-tall target with no coarse-pointer hit area
- **Likely cause:** Toggle component copied before the role=switch pattern was added to PestPressureSettingsPage
- **Fix:** Same edit as F0534 (add role="switch", aria-checked, aria-label, tabIndex=0, Enter/Space onKeyDown, focus-visible ring to the div at SettingsPage.jsx:110). Optional hit-area bump: `padding: 10px 0` on the row's control wrapper or `className="u-touch-hit"` if that utility applies to non-button elements. One PR closes both uids.
- **Test:** Same test as F0534; additionally assert getAllByRole('switch').length === 18 when rendering the Visit Timeline + Service Coverage cards, or simply that every rendered Toggle is reachable via userEvent.tab().
- **Verifier:** confirmed — Duplicate of F0534 — same file, same line, same defect. SettingsPage.jsx:110-120 is `<div onClick={() => onChange(!checked)} …>` with no role/aria-checked/tabIndex/onKeyDown (file-wide grep for those tokens is empty). The cited comparators exist exactly as claimed: PestPressureSettingsPage.jsx:110-118 (`role="switch" aria-checked={checked} tabIndex={0} … onKeyDown` Enter/Space) and client/src/comp

### F0544 · P1 · Tax A/R 'Send Reminder' posts to a non-existent endpoint — every click fails

- **Location:** `client/src/pages/admin/TaxPage.jsx:3776` (reader billing-admin:BA-01)
- **Reproduction:** Admin → Taxes → Exports & A/R → A/R → any overdue row with a phone → Send Reminder → confirm → 'Failed to send: HTTP 404'.
- **Expected:** The A/R row's reminder action either reaches a mounted SMS route or defers to the invoice follow-up sequence (POST /admin/invoices/:id/followup/send-now) which already owns stopOnPayment, quiet hours and audit.
- **Actual:** server/index.js mounts only '/api/admin/sms-templates'; grep of server/ finds no '/sms/send' handler (admin-communications exposes POST /sms under its own mount; admin-billing-health mounted at /api/admin has no sms route). The request 404s and the operator sees `alert("Failed to send: ...")` on every attempt; the confirm dialog promises a text that never goes out.
- **Likely cause:** Button was written against an SMS route that was later consolidated into admin-communications; the Tax page was never re-pointed.
- **Fix:** Remove the dead button or replace it with a link to the invoice row (`/admin/invoices?invoice=<id>`) where the follow-up panel's 'Send Next Now' already sends the reminder through the sequence. Do not wire it to a raw SMS send — reminders belong to the follow-up lane (owner decision).
- **Test:** Owner ruling needed: remove the A/R 'Send Reminder' control, or replace it with a Link to `/admin/invoices?invoice=<id>` (follow-up panel owns quiet hours/stop-on-payment/audit). Never re-point it at a raw SMS send (rule 12). After the ruling: RTL asserts the A/R row renders no control that posts to /admin/sms/send; optionally a server contract test that every `adminFetch("/admin/…")` literal in client/src resolves to a mounted route.
- **Verifier:** confirmed — client/src/pages/admin/TaxPage.jsx:3776-3783 `await adminFetch("/admin/sms/send", { method: "POST", body: JSON.stringify({ to: inv.phone, message: \`Hi ${inv.customerName}, this is Waves Pest Control. You have an outstanding balance …\` }) }); alert("Reminder sent!");` after a `window.confirm` at 3767-3771. adminFetch (43-45) resolves to `${API_BASE}${path}` with API_BASE `/api` (line 21) → `/api/
- **Needs owner decision**

### F0547 · P1 · A/R 'Total Outstanding' renders white text on a white card when total ≤ $500

- **Location:** `client/src/pages/admin/TaxPage.jsx:3855` (reader billing-admin:BA-02)
- **Reproduction:** Admin → Taxes → A/R with total outstanding ≤ $500 → first stat card shows no number.
- **Expected:** The headline receivable is always legible; only the > $500 case is emphasized red.
- **Actual:** Below the threshold the value is painted #FFFFFF on a #FFFFFF card (StatCard background D.card), so the number is invisible. Leftover from the dark-palette era where D.white on a dark card made sense.
- **Likely cause:** V2 token fold changed the card background to white but left the D.white value color.
- **Fix:** TaxPage.jsx:3855 → `color={s.total > 500 ? D.red : D.heading}` (or `color={s.total > 500 ? D.red : undefined}` so the StatCard default applies).
- **Test:** RTL: render AccountsReceivableTab with /admin/tax/accounts-receivable mocked to summary.total = 100; find the 'Total Outstanding' value node and assert its inline color !== '#FFFFFF' / rgb(255, 255, 255); with total = 600 assert it is D.red.
- **Verifier:** confirmed — client/src/pages/admin/TaxPage.jsx:3853-3858 `<StatCard label="Total Outstanding" value={fmtM(s.total)} color={s.total > 500 ? D.red : D.white} sub={…} />`. Palette at 23-40: `card: "#FFFFFF"` (line 25) and `white: "#FFFFFF"` (line 35). StatCard (87-133) paints `background: D.card` (line 92) and the value at 118-127 `style={{ fontFamily: MONO, fontSize: 22, fontWeight: 700, color: color \|\| D.hea

### F0008 · P2 · /admin index redirect pushes a history entry → Back is trapped after every login / PWA launch

- **Location:** `client/src/App.jsx:563` (reader admin-shell-nav:ASN-01)
- **Reproduction:** Log in at /admin/login (or open the installed PWA), land on /admin/dashboard, press browser Back — you are returned to the dashboard.
- **Expected:** The index redirect replaces the /admin entry (react-router v6 <Navigate> pushes unless replace is set), so Back from /admin/dashboard leaves the admin or returns to the previous page.
- **Actual:** AdminLoginPage navigates to '/admin' (replace) and admin-manifest.json start_url is '/admin/', so the first history entry is /admin; the index route then PUSHES /admin/dashboard. Pressing Back lands on /admin, which pushes /admin/dashboard again — a back-button loop. Technicians take a further /admin/schedule → /admin/dispatch hop on top.
- **Likely cause:** Missing `replace` on the index <Navigate>; every other admin redirect in the block passes replace.
- **Fix:** client/src/App.jsx:563 — replace `<Route index element={<Navigate to="dashboard" />} />` with `<Route index element={<Navigate to="dashboard" replace />} />`.
- **Test:** client/src/components/admin/AdminAliasRoutes.test.jsx already has a Probe with Back/Forward buttons and a MemoryRouter harness: render with initialEntries ['/x', '/admin'] and the index Navigate, expect location /admin/dashboard, click Back, expect /x (not /admin/dashboard).
- **Verifier:** confirmed — App.jsx:563 `<Route index element={<Navigate to="dashboard" />} />` has no replace while every sibling redirect does (:581 revenue, :584 ads, :663/:664, :668 all pass `replace`). BrowserRouter (App.jsx:501) is a non-data router, and react-router 6.30.4 (client/package.json:47; node_modules/react-router/package.json) implements Navigate as `React.useEffect(() => navigate(JSON.parse(jsonPath), { rep

### F0009 · P2 · /admin index redirect pushes history — Back button loops on /admin

- **Location:** `client/src/App.jsx:563` (reader navigation-map:NAV-01)
- **Reproduction:** Open any page, navigate to /admin (the login success target and the desktop MorePage redirect both use it), press Back.
- **Expected:** Landing on /admin replaces the history entry with /admin/dashboard so Back returns to wherever the user came from.
- **Actual:** React Router's <Navigate> defaults to push; /admin stays in history, so Back from the dashboard lands on /admin which immediately pushes dashboard again — the user cannot leave via Back.
- **Likely cause:** Missing `replace` prop on the index Navigate.
- **Fix:** client/src/App.jsx:563 — `<Route index element={<Navigate to="dashboard" replace />} />` (same edit as F0008; apply once).
- **Test:** Same test as F0008 (AdminAliasRoutes.test.jsx harness): MemoryRouter initialEntries ['/x','/admin'], Back returns to /x.
- **Verifier:** confirmed — Duplicate of F0008 — same line, same defect. App.jsx:563 omits `replace`; react-router 6.30.4 Navigate → navigate(path, { replace }) (node_modules/react-router/dist/index.js:1147-1151) and `(!!options.replace ? navigator.replace : navigator.push)` (:241) pushes when replace is undefined. Both entry points named in the finding are verified: MorePage.jsx:44 `<Navigate to="/admin" replace />` and Adm

### F0020 · P2 · Admin shell has no <main> landmark and no skip link — the only portal without one (WavesShell and PortalPage both have them)

- **Location:** `client/src/components/AdminLayoutV2.jsx:626` (reader a11y:A11Y-04)
- **Reproduction:** Load any /admin page with a screen reader landmark list: no main region; press Tab from page load — first stop is the sidebar.
- **Expected:** Admin content container is <main id="admin-main" tabIndex={-1}> preceded by a `.waves-skip-link` anchor, consistent with the customer shells
- **Actual:** Sidebar (aside + two labelled navs) precedes content with no landmark to jump to; keyboard users tab through every sidebar section (nav :439 Admin sections, :663 Primary) on every page
- **Likely cause:** AdminLayoutV2 predates the skip-link pattern added to WavesShell (chrome audit 2026-09-03)
- **Fix:** Apply F0019's sketch once (main landmark + admin-toned `.admin-skip-link` + demote the two nested <main>s). Do NOT add `className="waves-skip-link"` inside AdminLayoutV2.
- **Test:** Same RTL assertions as F0019: getByRole('main') resolves to #admin-main; getByText('Skip to content') is the first focusable and has href '#admin-main'; additionally assert the link does not carry class 'waves-skip-link'.
- **Verifier:** confirmed — Duplicate of F0019 on the defect: AdminLayoutV2.jsx:626-649 is a `<div className="admin-main">`, no `<main`/skip link in the file (grep zero hits), while WavesShell.jsx:59/:103, PortalPage.jsx:15627/:16079 and TechLayout.jsx:272 all carry a main landmark — 'the only portal without one' holds. The finding's proposed fix is wrong on one point and must not be applied as written: it reuses `.waves-ski

### F0055 · P2 · 'Charge now' is offered to every lane; the server refuses non-monthly customers after the confirm dialog

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:3372` (reader customer-360:C360-13)
- **Reproduction:** Open a per-visit customer → Billing → Charge now → confirm → error.
- **Expected:** Per the contract ('Disabled actions with a reason'), the button is hidden/disabled with the lane reason unless billingMode is monthly_membership — c.billingMode is already in the same payload and shown in the panel directly above.
- **Actual:** Per-visit / prepay / one-time customers get a 'Charge now ($X)' button and a money confirm, then a 400.
- **Likely cause:** canCharge = isAdmin only.
- **Fix:** canCharge={isAdmin && c.billingMode === 'monthly_membership'} (or disabled + title with the lane reason).
- **Test:** Server: add `billingLane: resolveBillingLane(c)` (mode + source) to the GET /:id payload; client: `canCharge={isAdmin && data.billingLane?.mode === 'monthly_membership'}` (or disabled + title with the lane reason). RTL: billing_mode null + tier Silver + rate 89 → button present; billing_mode 'per_visit' → absent.
- **Verifier:** confirmed — Customer360ProfileV2.jsx:6957-6962 mounts `<AdminAutopayPanelV2 … canCharge={isAdmin} />`; :3372-3378 renders 'Charge now ($X)' whenever canCharge; chargeNow :3301-3316 runs `window.confirm(...)` then POSTs `{}` (never an amount). server/routes/admin-billing-health.js:104-112: `if (amount == null) { const lane = resolveBillingLane(customer); if (lane.mode !== 'monthly_membership') return res.statu

### F0056 · P2 · Prepay/refund/cancel-signup modals portal without .admin-shell-v2, so their raw w-full inputs are content-box and overflow

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:4067` (reader customer-360:C360-09)
- **Reproduction:** Billing → 'Send prepay invoice' → inspect any input width vs its grid column.
- **Expected:** Inputs in the prepay modals fit their grid cells like the rest of the admin.
- **Actual:** Only the three profile roots restate .admin-shell-v2 (#3462); the four sub-modals portal separately to body, so each 100%-wide input adds 20px padding + 2px border beyond its column.
- **Likely cause:** #3462 added the class to the root overlays only.
- **Fix:** Add `admin-shell-v2` as the first class on the four portal root divs (:4069, :4494, :4744, :5007), matching the profile root at :5906.
- **Test:** RTL: open AnnualPrepayModal and assert the portal root (document.body > div.fixed) has class 'admin-shell-v2'; ui-verify screenshot of Send prepay invoice at 390px.
- **Verifier:** confirmed — createPortal roots at Customer360ProfileV2.jsx:4067-4071 (AnnualPrepayModal), :4492-4496 (AnnualPrepayInvoiceModal), :4742-4746 (CancelSignupModal), :5005-5009 (RefundPaymentModal) are `fixed inset-0 bg-black/70 z-[11x0] …` with no `admin-shell-v2`, portaled to document.body (:4229, :4694, :4869, :5128); the profile root restates the class at :5902-5906 with the comment 'restate .admin-shell-v2 he

### F0062 · P2 · Health Radar plots fabricated values while the real sub-scores are already in the payload

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:5820` (reader customer-360:C360-04)
- **Reproduction:** Open any customer Overview; compare radar axes to /admin/health/scores/:id sub-scores (CustomerHealthTabs CustomerDetail).
- **Expected:** The radar reflects the customer's stored sub-scores (payment_score, service_score, engagement_score, satisfaction_score, loyalty_score, growth_score).
- **Actual:** risk_factors is not a column, so the chart always draws overall±offset pseudo-axes (or hardcoded 80/60/70/75/90/65); every customer's radar is the same shape shifted by one number. Presented under a 'Health Radar' title as if it were analytics.
- **Likely cause:** Placeholder from the V1 port never wired to the health-score row.
- **Fix:** Map hs.payment_score/service_score/engagement_score/satisfaction_score/loyalty_score/growth_score into radarData and hide the chart when the row is absent; or drop the widget (Health view already has a real RadarChart).
- **Test:** If remapped: RTL with healthScore {payment_score: 20, service_score: 90} asserting the Payment point radius is 20%; if dropped: snapshot of the Overview without the Health Radar block.
- **Verifier:** downgraded — Real: Customer360ProfileV2.jsx:5820-5850 builds `radarData = hs.risk_factors ? [{Payment:80},{Engagement:60},{Service:70},{Satisfaction:75},{Tenure:90},{Revenue:65}] : [score+10, score, score+5, score-5, tenure/3.65, lifetimeRevenue/50]`, with `hs = data.healthScore \|\| {}` (:5598) and `score = hs.overall_score ?? hs.health_score ?? hs.score` (:5599); rendered under `<SectionTitle>Health Radar</S
- **Needs owner decision**

### F0065 · P2 · 'Add Note' (header pill + ⋯ menu) is a dead action that lands on the customer SMS composer

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:6119` (reader customer-360:C360-05)
- **Reproduction:** Desktop: click 'Add Note' → Comms tab; there is no note field.
- **Expected:** 'Add Note' opens a note composer that POSTs /admin/customers/:id/interactions.
- **Actual:** It only switches tabs. The nearest input on that tab is the SMS reply box; an operator typing an internal note and pressing Enter texts the customer (rule 12 hazard).
- **Likely cause:** V1 parity port kept the button but the note composer never existed in V2.
- **Fix:** Either wire a small note form (subject/body → POST /:id/interactions, then reloadCustomer) or remove both 'Add Note' entries.
- **Test:** RTL after the ruling: either clicking 'Add Note' shows a textbox labelled 'Note' whose submit POSTs /admin/customers/:id/interactions, or no element with text 'Add Note'/'Add note' renders.
- **Verifier:** downgraded — Dead action confirmed: Customer360ProfileV2.jsx:6119-6124 header 'Add Note' `onClick={() => setActiveTab("comms")}` and :8162-8170 menu item `onClick={() => { setActiveTab("comms"); setMenuOpen(false); }}` do nothing else; `grep -rn '/interactions' client/src` finds no caller, so server/routes/admin-customers.js:4468-4478 `router.post('/:id/interactions', requireAdmin, …)` is orphaned; Notes & Int
- **Needs owner decision**

### F0070 · P2 · Comms thread opens scrolled to the oldest message inside a 400px box

- **Location:** `client/src/components/admin/Customer360ProfileV2.jsx:7153` (reader customer-360:C360-20)
- **Reproduction:** Customer with >6 texts → Comms tab.
- **Expected:** Newest messages visible on open (bottom-anchored like the shared panel).
- **Actual:** The operator lands on the first-ever text and must scroll a 400px pane to see the latest reply.
- **Likely cause:** Chronological reverse without bottom anchoring.
- **Fix:** Add `const commsThreadRef = useRef(null)` in Customer360ProfileV2, put `ref={commsThreadRef}` on the :7153 container, and `useEffect(() => { const el = commsThreadRef.current; if (el && activeTab === 'comms' && commsLoaded) el.scrollTop = el.scrollHeight; }, [comms, commsLoaded, activeTab]);`. Note: becomes moot if F0071's panel adoption is chosen — the inline thread would be deleted.
- **Test:** RTL: Object.defineProperty(HTMLElement.prototype,'scrollHeight',{configurable:true,get(){return 900}}); mock /comms with 8 sms rows; open the Comms tab; assert the thread container's scrollTop === 900 after load.
- **Verifier:** confirmed — client/src/components/admin/Customer360ProfileV2.jsx:7153 `<div className="flex-1 overflow-y-auto flex flex-col gap-1.5 mb-3 max-h-[400px]">` carries no ref, and :7164 `{[...comms].reverse().map((m, i) => {` renders oldest-first (server/routes/admin-customers.js:2701 `router.get('/:id/comms'` orders `messages.created_at` desc). `grep -n "scrollTop\\|scrollIntoView\\|scrollHeight"` on the file retu

### F0076 · P2 · Decline / Follow-up / Extend dialogs swallow the server's error message ('Failed: HTTP 409')

- **Location:** `client/src/components/admin/EstimateModalsV2.jsx:28` (reader pipeline-estimator:PE-06)
- **Reproduction:** Mark as Lost on an estimate that is mid-clarify hold → alert shows 'Failed: HTTP 409'.
- **Expected:** The operator sees the server's reason (the shared client/src/utils/admin-fetch.js:86-105 already extracts body.error, code and details).
- **Actual:** Every non-2xx from Mark as Lost, Follow Up and Extend surfaces as 'Failed: HTTP 400/409' with no explanation, even though the file's own comment (line 111) acknowledges the server 400s a blank Other note.
- **Likely cause:** Copy-pasted minimal fetch wrapper predating utils/admin-fetch.
- **Fix:** In client/src/components/admin/EstimateModalsV2.jsx delete lines 26-38 (`const API_BASE …` and the local `function adminFetch`) and add `import { adminFetch } from "../../utils/admin-fetch";` after the existing imports. No call-site change: all three calls already pass `(path, { method, body })`. (Side effect, consistent with the canonical path: 401 now redirects to /admin/login and 429 retries once.)
- **Test:** EstimateModalsV2.test.jsx: mock global.fetch to resolve `new Response(JSON.stringify({ error: 'held for a re-price' }), { status: 409 })`, spy window.alert, render DeclineModalV2 with a reason selected, click Save, assert alert was called with a string containing 'held for a re-price' (today it receives 'Failed: HTTP 409').
- **Verifier:** confirmed — client/src/components/admin/EstimateModalsV2.jsx:28-38 defines a local `adminFetch` whose only error path is `if (!r.ok) throw new Error(`HTTP ${r.status}`)` (line 36); consumers alert `err.message` at :58 ('Follow-up failed: '), :124 ('Failed: '), :272 ('Extend failed: '). The server bodies it discards carry the reason: server/routes/admin-estimates.js:4959 `res.status(409).json({ error: 'This es

### F0085 · P2 · Confirm-write card renders in the TRUE dark navy D palette inside the white desktop palette (variant='dark' mismatch)

- **Location:** `client/src/components/admin/GlobalCommandPalette.jsx:998` (reader agents-ib:AIB-03)
- **Reproduction:** ⌘K on desktop, ask for any write tool (e.g. 'reschedule …'); the proposal card is dark navy.
- **Expected:** The operator confirmation card (the write trust boundary) looks the same everywhere: zinc 'light' variant as on the mobile sheet (1436), IntelligenceBarShell (481) and AgentEstimatePage (1087).
- **Actual:** On desktop ⌘K a #1e293b navy card with #e2e8f0 text, amber border and a #10b981 green Confirm button appears inside a white modal — the only place in the admin that shows the dark-mode D palette.
- **Likely cause:** The palette's local D was recoloured to light without updating the PendingActionsCard variant it passes.
- **Fix:** GlobalCommandPalette.jsx:998 → `<PendingActionsCard actions={pendingActions} variant="light" onResolved={onActionResolved} />`. (Follow-up per rule 19, separate: `variant="dark"` then has zero callers.)
- **Test:** GlobalCommandPalette.test.jsx (desktop, useIsMobile → false): mock /admin/intelligence-bar/query to return `{ response: 'ok', pendingActions: [{ id: 'p1', tool: 'x', summary: 's', expiresAt: … }] }`, submit, assert the card container has class containing 'bg-white' and no inline `background: #1e293b`.
- **Verifier:** confirmed — GlobalCommandPalette.jsx:49 `card: "#FFFFFF"` and :752 `background: D.card` make the desktop modal white, yet :998 renders `<PendingActionsCard actions={pendingActions} variant="dark" …>`. client/src/components/admin/PendingActionsCard.jsx:18-22 `const D = { bg: "#0f1923", card: "#1e293b", border: "#334155", … green: "#10b981", … text: "#e2e8f0" }`, :238-241 dark card `background: D.card, border: 

### F0095 · P2 · Mobile New Customer sheet: all 9 fields placeholder-only with no accessible name, plus fake dropdown carets on plain inputs

- **Location:** `client/src/components/admin/MobileNewCustomerSheet.jsx:239` (reader forms:FORMS-02)
- **Reproduction:** Open /admin/customers on an iPhone → New customer → fill First name; the field's only label is gone.
- **Expected:** Admin consistency contract + the V2 pattern elsewhere (CustomersPageV2.jsx:692 `<label htmlFor={`${formId}-phone`} className={LABEL_CLS}>Phone *</label>`): visible label or at minimum aria-label per field; a caret only on a real <select>.
- **Actual:** Nine inputs (239-286, 343-408) rely solely on placeholder text, which disappears on entry; the '+1 ▾' (255-276), 'Country ▾' (299-322) and 'State ▾' (381-399) wrappers look like dropdowns but are decorative — the country cannot be changed and State is a free-text 2-char input. Required (first name + phone, line 84-87) is never marked.
- **Likely cause:** Built to match a screenshot (header comment IMG_3735/3736) rather than the form contract.
- **Fix:** Add `aria-label` matching each placeholder to the inputs at :239, :247, :266, :278, :323 (passes through to AddressAutocomplete's input via `...inputProps`), :343, :362, :371, :381, :400 and `aria-label="Property label"` to the <select> at :351; add `aria-required="true"` (and `required`) to the First name (:239) and Phone (:266) inputs. Leave the caret/select question to F0094's ruling.
- **Test:** RTL: render MobileNewCustomerSheet open; `getByLabelText('First name')`, `getByLabelText('Phone number')`, `getByLabelText('ZIP')` resolve; `getByLabelText('First name')` has `aria-required="true"`.
- **Verifier:** downgraded — Facts verified: MobileNewCustomerSheet.jsx inputs :239-246 (`placeholder="First name"`), :247-254, :266-276 (`placeholder="Phone number"`), :278-286, :323-341 (AddressAutocomplete `placeholder="Address line 1"`), :343-350, :362-369, :371-378, :381-393 (State), :400-408 (ZIP) rely on placeholder only, and the <select> at :351 is unlabeled too; `canSave` :83-86 requires `form.firstName.trim() && for

### F0127 · P2 · RevenuePanelV2 and CSRPanelV2 never check res.ok — auth/server failures render as the empty state; sibling panels handle errors three different ways

- **Location:** `client/src/components/dispatch/RevenuePanelV2.jsx:27` (reader dispatch-board:DB-18)
- **Reproduction:** Expire the admin token and open ?tab=revenue — 'No jobs found for <date>' with no error.
- **Expected:** All four reference panels share one loading / error-with-retry / empty pattern; a 401/500 is shown as a failure, never as 'No jobs found'.
- **Actual:** Job Scores and CSR Booking swallow non-OK responses (a 401 body `{error}` becomes `[]`), so an expired session or server error reads as an empty day; Insights has a full error card, Tech Match an inline alert, Job Scores/CSR none.
- **Likely cause:** 1:1 port of V1 panels that also swallowed errors.
- **Fix:** RevenuePanelV2.jsx:27-31 and CSRPanelV2.jsx:33-43: add `if (!res.ok) throw new Error(`HTTP ${res.status}`)` after fetch, an `error` state set in catch, and render the InsightsPanelV2.jsx:79-82 block (role=alert message + secondary Retry calling load/loadSlots) instead of the empty-state text when error is set.
- **Test:** RTL: mock fetch -> { ok: false, status: 500, json: async () => ({ error: 'boom' }) } for RevenuePanelV2; expect getByRole('alert') and getByRole('button', { name: /retry/i }); expect queryByText(/No jobs found/) toBeNull.
- **Verifier:** confirmed — RevenuePanelV2.jsx:27-31 `const res = await fetch('/api/dispatch/jobs?...', { headers: authHeader() }); const data = await res.json(); const sorted = (Array.isArray(data) ? data : []).sort(...)` then `} catch { setJobs([]); }` — no res.ok; :140-142 `{!jobs.length && (<div ...>No jobs found for {date}</div>)}`. CSRPanelV2.jsx:33-43 same shape, `setSlots(data.slots \|\| [])`. The server returns non-

### F0166 · P2 · Schedule-appointment submit sets `saving` only after an awaited re-quote fetch, leaving a double-submit window for auto-priced mosquito lines

- **Location:** `client/src/components/schedule/CreateAppointmentModal.jsx:1773` (reader states:STATES-09)
- **Reproduction:** Add a one-time mosquito line without a price, tap Schedule twice quickly on a slow network.
- **Expected:** Re-entrancy guard at the top of handleSubmit (`if (saving) return; setSaving(true)`), like submitQuickAdd (:1331).
- **Actual:** While the re-quote request is in flight `saving` is still false, so the Schedule button stays enabled and a second tap runs a second handleSubmit; both then POST the appointment groups.
- **Likely cause:** The re-quote step was inserted before the existing setSaving(true) call.
- **Fix:** Add `const submittingRef = useRef(false);` next to :914; at the top of handleSubmit (after the :1751 validity return) `if (submittingRef.current) return; submittingRef.current = true; setSaving(true);` and wrap the body so every early `return` and the end path run `submittingRef.current = false; setSaving(false);` (a try/finally around the whole body is simplest — keep the existing :1999 setSaving(false) or fold it into the finally). Delete the now-duplicate :1789 `setSaving(true);`.
- **Test:** CreateAppointmentModal.test: one-time mosquito line with no price, mock the mosquito-onetime-quote fetch to a pending promise, click Schedule twice, resolve, assert exactly one POST /admin/schedule.
- **Verifier:** confirmed — CreateAppointmentModal.jsx:1750-1751 `const handleSubmit = async () => { if (!selectedCustomer \|\| services.length === 0 \|\| bookingPropertyState === 'loading') return;` — no `saving` check. :1772-1773 `if (services.some((s) => isOneTimeMosquitoLine(s) && !lineHasEnteredPrice(s))) { try { const fresh = await adminFetch(`/admin/schedule/mosquito-onetime-quote?…`);` runs BEFORE :1789 `setSaving(tr

### F0171 · P2 · Mobile dispatch tech assignment fails silently — PUT error goes to console only

- **Location:** `client/src/components/schedule/InlineTechPicker.jsx:18` (reader states:STATES-02)
- **Reproduction:** Dispatch day view on mobile width, tap the tech initial on a stop, pick a tech while the API returns 500.
- **Expected:** A failed assignment tells the operator (inline alert text or toast) and leaves the picker open with the previous value.
- **Actual:** The picker just un-busies; no message, no state change. The picker is live in DispatchPageV2 via MobileDispatchList (:271, mounted at DispatchPageV2 :1385 and :1748), so a scheduling write can fail with zero feedback. grep for error\|alert in the file finds no UI path.
- **Likely cause:** Component predates the seriesMove apiErrorMessage helper and never had an error state.
- **Fix:** Same edit as F0172 — apply once.
- **Test:** See F0172.
- **Verifier:** downgraded — Code is as quoted: InlineTechPicker.jsx:27-33 `if (!res.ok) throw new Error(`HTTP ${res.status}`); onAssigned?.(techId); } catch (e) { console.error('Assign failed:', e); } setBusy(false); onClose();` — no error state, no UI path (file grep: no `error`/`alert` rendering). Sole consumer MobileDispatchList.jsx:271 `<InlineTechPicker … onAssigned={() => { onRefresh?.(); }} onClose={() => setShowTechP

### F0172 · P2 · InlineTechPicker swallows a failed assignment (console.error) and closes as if it succeeded

- **Location:** `client/src/components/schedule/InlineTechPicker.jsx:29` (reader schedule-mobile-sheets:SMS-06)
- **Expected:** A failed PUT /admin/schedule/:id/assign keeps the picker open (or surfaces an inline error) so the dispatcher knows the tech was not assigned.
- **Actual:** On any non-2xx the picker logs to console and closes; the row still shows the old tech initial and nothing tells the user. This is the only tech-assignment path on the mobile dispatch list.
- **Fix:** In InlineTechPicker.jsx: `import { apiErrorMessage } from './seriesMove';` add `const [error, setError] = useState('');`; in assign: `setError('')` at start, move `onClose()` inside the try right after `onAssigned?.(techId)`, and in catch `setError(apiErrorMessage(e, "Couldn't assign — try again"))`; keep `setBusy(false)` after the try/catch. Render `{error && <div role="alert" className="px-3 py-2 text-13 text-alert-fg">{error}</div>}` as the last child of the picker div (genuine alert, so alert-fg is allowed).
- **Test:** InlineTechPicker.test: mock fetch → { ok: false, status: 500 }; click a tech; expect the alert text, expect onAssigned and onClose NOT called, picker still in the document; then mock ok → onAssigned and onClose called.
- **Verifier:** confirmed — InlineTechPicker.jsx:18-34: `const assign = async (techId) => { setBusy(true); try { … if (!res.ok) throw new Error(`HTTP ${res.status}`); onAssigned?.(techId); } catch (e) { console.error('Assign failed:', e); } setBusy(false); onClose(); };` — a non-2xx PUT /admin/schedule/:id/assign logs and then closes exactly like success; nothing renders an error (the component has no error state). Only moun

### F0186 · P2 · Checkout line items round money to whole dollars (toFixed(0)) while Total and extras show cents — a $112.50 service reads '$113' above a '$112.50' total

- **Location:** `client/src/components/schedule/MobileCheckoutSheet.jsx:409` (reader schedule-mobile-sheets:SMS-04)
- **Expected:** Every money figure on the checkout review shows cents (owner directive in lib/money.js: 'every price shows the full amount with cents — never a bare $149'); line items sum visibly to the Total.
- **Actual:** Base service and add-on rows use toFixed(0), so interpolated-bracket prices with cents are rounded (112.5 -> '$113') and the visible rows do not add up to the Total shown two rows below. Same toFixed(0) in MobileServicePickerSheet:36 and MobileServiceLibrary:415/802.
- **Fix:** MobileCheckoutSheet.jsx:409 `${baseServicePrice.toFixed(0)}` → `${baseServicePrice.toFixed(2)}`; :430 `${Number(addon.estimatedPrice \|\| 0).toFixed(0)}` → `${Number(addon.estimatedPrice \|\| 0).toFixed(2)}`.
- **Test:** MobileCheckoutSheet.test: service.estimatedPrice = 112.5 → expect getByText('$112.50') on the base row and the Charge button name 'Charge $112.50'; with a 12.5 add-on expect '$12.50'.
- **Verifier:** confirmed — MobileCheckoutSheet.jsx:409 `${baseServicePrice.toFixed(0)}` and :430 `${Number(addon.estimatedPrice \|\| 0).toFixed(0)}` versus :455 `{isDiscount ? '−' : ''}${Math.abs(Number(e.amount)).toFixed(2)}` and :484 `${total.toFixed(2)}`. The math keeps cents — :96-98 `const baseServicePrice = splitAppointmentAddons ? Math.max(0, Math.round((price - appointmentAddonTotal) * 100) / 100) : price;` and :147

### F0221 · P2 · TechIntelligenceBar: 24–36px tap targets on a field phone surface, 13px answers, and 'Error:' responses styled like normal answers

- **Location:** `client/src/components/tech/TechIntelligenceBar.jsx:239` (reader agents-ib:AIB-23)
- **Reproduction:** On /tech with the API down, ask anything — the failure is grey body text.
- **Expected:** Tech portal is field-first: ≥44px targets, ≥14px copy, and an error visibly distinct from an answer (matching the Shell's alert treatment).
- **Actual:** A network/auth failure reads as a normal grey answer starting with 'Error:'; the Go/✕/attach buttons are hard to hit with gloves.
- **Likely cause:** Oldest of the IB implementations; never revisited after the admin shell work.
- **Fix:** TechIntelligenceBar.jsx: `const isError = typeof response === 'string' && response.startsWith('Error:');` then at 238-239 `<div role={isError ? 'alert' : undefined} style={{ fontSize: 14, lineHeight: 1.6, color: isError ? D.amber : D.text, … }}>`; add `minHeight: 44` to the Go buttons (181-184, 246-249) and quick-action buttons (214-219), `width: 44, height: 44` on attach (163-168), and `minHeight: 44, minWidth: 44, aria-label="Clear response"` on ✕ (187-190).
- **Test:** RTL: mock fetch to reject → getByRole('alert') contains the message and is not D.text colored; getByLabelText('Clear response') exists.
- **Verifier:** confirmed — TechIntelligenceBar.jsx:126 `setResponse(`Error: ${err.message}`);` and 238-239 `<div style={{ padding: '0 12px 12px', maxHeight: 350, overflowY: 'auto' }}><div style={{ fontSize: 13, lineHeight: 1.6, color: D.text, … }}>{renderMarkdown(response)}` — no isError branch, whereas IntelligenceBarShell.jsx:226 `const isError = response && response.startsWith("Error:")` and :474 `isError ? "text-alert-f

### F0227 · P2 · Badge silently renders unstyled for unknown tones; 14 live call sites use non-existent 'muted'/'danger' tones

- **Location:** `client/src/components/ui/Badge.jsx:8` (reader ui-primitives:UIP-03)
- **Reproduction:** Open /admin/newsletter — the 'active subscribers' / 'sent campaigns' badges have no chip background; open Email Templates health issues — the event_type badge is not red.
- **Expected:** Only neutral/strong/alert exist. An unknown tone should either resolve to a documented fallback or fail loudly in dev; a 'danger' badge on the email-template issues table should read as an alert.
- **Actual:** TONES['muted'] / TONES['danger'] are undefined, cn() drops them, so the badge renders with no background and inherited colour — an invisible box with uppercase 11px text. Static sites: NewsletterTabs.jsx:2425, 2774, 3107, 3115; NewsletterPage.jsx:166, 172, 493; EmailAutomationsPanelV2.jsx:944; CustomerEstimatesPanel.jsx:282; EmailTemplatesPanelV2.jsx:383 (danger). Dynamic: EmailAutomationsPanelV2.jsx:711, NewsletterPage.jsx:176. The 'issue' badge on the templates health table therefore never reads red. Button has the same silent-undefined pattern for variant/size (no live misuse found).
- **Likely cause:** No prop validation; authors assumed a `muted` tone existed.
- **Fix:** Badge.jsx: `const TONE_ALIASES = { danger: 'alert', muted: 'neutral' }; const key = TONE_ALIASES[tone] \|\| tone; const toneClass = TONES[key] \|\| TONES.neutral; if (import.meta.env.DEV && !TONES[key]) console.warn(`Badge: unknown tone "${tone}"`);` and use `toneClass` / `DOT_TONES[key]`; then sweep the 13 call sites to `neutral` / `alert` so the aliases carry no permanent traffic. Same `\|\| SIZES.md` / `\|\| VARIANTS.primary` fallback + DEV warn in Button.jsx.
- **Test:** RTL: render <Badge tone="muted"> and <Badge tone="danger">; assert a `bg-` class is present and that danger carries `bg-alert-bg text-alert-fg`.
- **Verifier:** confirmed — client/src/components/ui/Badge.jsx:8-12 `TONES = { neutral, strong, alert }` only, :21 `className={cn(BASE, TONES[tone], className)}` — an unknown tone is `undefined`, cn drops it, so the chip renders with no background and inherited colour (BASE :4-6 keeps the 11px uppercase text). Live misuse is 13 sites, not 14 (title) / 12 (body): static `tone="muted"` at pages/admin/NewsletterTabs.jsx:2425, 2

### F0314 · P2 · A failed property switch gives the customer no feedback

- **Location:** `client/src/pages/PortalPage.jsx:15556` (reader customer-portal-app-b:F02)
- **Reproduction:** Multi-property account, throttle network to offline, open account menu, tap another property.
- **Expected:** A rejected /auth/select-property (network drop, 5xx, 401) should tell the customer the switch did not happen and leave a retry.
- **Actual:** switchProperty stores the raw err.message in AuthContext.error, but PortalPage never reads `error` (15320 destructures only customer/logout/properties/propertiesError/refreshProperties/switchProperty/refreshCustomer). The 'Switching' label (15828) simply disappears and the menu stays on the old property with no message.
- **Fix:** PortalPage.jsx after the `if (switched) { ... }` block at 15558-15570 add `else { showCustomerAlert('We could not switch to that property just now. Please try again.'); }` (showCustomerAlert is already imported at line 13).
- **Test:** Mock api.selectAuthProperty to reject; open the account menu, click another property, assert showCustomerAlert was called with the copy and no 'Switching' text remains.
- **Verifier:** confirmed — client/src/pages/PortalPage.jsx:15556-15558 `const switched = await switchProperty(propertyId); setSwitchingPropertyId(null); if (switched) { ... }` has no else branch, while the sibling failure two lines up (15551-15554) does surface: `showCustomerAlert('Your latest property edits could not be saved, ...')`. client/src/hooks/useAuth.jsx:371-374 `} catch (err) { setError(err.message); return false

### F0341 · P2 · Invoice list refetches on every keystroke with no debounce and no latest-request guard; failed loads read as 'No invoices match'

- **Location:** `client/src/pages/admin/AdminInvoicesPage.jsx:975` (reader billing-admin:BA-05)
- **Reproduction:** Throttle network, type quickly in the invoice search, watch rows flicker to an earlier query's results; or block /admin/invoices and observe 'No invoices match'.
- **Expected:** Search is debounced (the same file debounces customer search at 5019 by 300 ms), only the newest response paints (BankingPage reqIdRef pattern, lines 191-215), there is a loading state, and a failed fetch shows an error with retry rather than an empty state.
- **Actual:** Typing 'smith' fires five list requests; a slower earlier response can overwrite the newer rows and `total`. No `loading` flag exists for the list, so the first paint and any network/401 failure both render 'No invoices match'.
- **Likely cause:** List effect keyed directly on `query`; error path was written as a silent reset.
- **Fix:** Add a 300 ms debounced copy of `query` (useState + useEffect/setTimeout) and put it in `load`'s deps; `const reqIdRef = useRef(0)` with `const reqId = ++reqIdRef.current` at the top of load and `if (reqId !== reqIdRef.current) return;` after the await; add `listLoading` / `listError` state; render 'Loading…' and "Couldn't load invoices — Retry" (onClick={() => load()}) branches ahead of the empty state at :1573.
- **Test:** Mock adminFetch to resolve two list calls out of order and assert the later query's rows/total win; reject the fetch and assert the error copy renders instead of 'No invoices match'.
- **Verifier:** confirmed — AdminInvoicesPage.jsx:976-1011 `load` has deps `[PAGE_SIZE, customerFilterId, datePeriod, filter, query, sort]` and :1013-1015 `useEffect(() => { load(); }, [load, refreshKey]);`, so every keystroke through :1432 `onChange={(e) => setQuery(e.target.value)}` rebuilds `load` and refires the list request; no reqId/AbortController guard (0 hits in the file) and no list loading flag (only :952 `loading

### F0342 · P2 · Reverse prepaid invoice has no pending state — double-click fires two money-moving POSTs

- **Location:** `client/src/pages/admin/AdminInvoicesPage.jsx:1161` (reader states:STATES-01)
- **Reproduction:** Invoices page, a prepaid invoice row, click Reverse prepaid, confirm, click again before the toast lands.
- **Expected:** Same guard as the sibling actions: setRowActionBusy(id) before the POST, cleared in finally, and the button disabled/dimmed while rowActionBusy === inv.id (handleVoid :1126-1139, button :1965-1972).
- **Actual:** No busy flag is set and the button is never disabled; a second click while the first POST is in flight sends a second reverse-prepaid request for the same invoice.
- **Likely cause:** Handler added after the rowActionBusy pattern and never wired to it.
- **Fix:** Wrap :1167-1179 in `setRowActionBusy(id); try { … } catch … finally { setRowActionBusy(null); }`; at :1975 add `disabled={rowActionBusy === inv.id}` and `style={{ ...sBtn("transparent", D.red, isMobile), opacity: rowActionBusy === inv.id ? 0.5 : 1 }}` to match Void/Unvoid.
- **Test:** Render a prepaid row with a never-resolving adminFetch mock, click Reverse prepaid twice (confirm mocked true), assert exactly one POST and the button disabled.
- **Verifier:** downgraded — Client side is as claimed: AdminInvoicesPage.jsx:1161-1180 `handleReversePrepaid` never calls setRowActionBusy, and the button at :1975-1981 has no `disabled`/opacity, unlike handleVoid :1126-1139 and its button :1965-1972 — a double-click does send two POSTs. But the money side is guarded on the server: admin-invoices.js:2354-2364 locks the row `trx('invoices').where({ id }).forUpdate().first()` 

### F0343 · P2 · Client-side overdue check uses browser-local time and breaks on the ISO date shape the same file already normalises

- **Location:** `client/src/pages/admin/AdminInvoicesPage.jsx:1365` (reader billing-admin:BA-19)
- **Reproduction:** Open an unpaid invoice due yesterday before the overdue cron runs: list shows 'Sent'.
- **Expected:** One overdue rule (ET calendar day, both 'YYYY-MM-DD' and midnight-UTC ISO inputs) shared with the pay page, which already imports `isInvoiceDueDateOverdue` (PayPageV2.jsx:2608).
- **Actual:** `invoices.due_date` is a pg DATE column (migration 20260401000082_invoices.js:17 `t.date('due_date')`); no `setTypeParser`/`dateStrings` override exists in server/, so the list serialises it as an ISO instant, and `"2026-09-07T00:00:00.000Z" + "T23:59:59"` is an Invalid Date — the client branch never fires and the row only reads Overdue once the cron flips status. When the value is date-only the comparison is in the operator's browser zone rather than ET.
- **Likely cause:** Local helper predates lib/invoiceDates.
- **Fix:** AdminInvoicesPage.jsx:80: `import { invoiceDateOnly, formatInvoiceDate, isInvoiceDueDateOverdue } from "../../lib/invoiceDates";` and replace :1365-1369 with `if (isInvoiceDueDateOverdue(inv.due_date)) return { key: "overdue", label: "Overdue", color: D.red };`.
- **Test:** Unit test getDisplayStatus: { status: 'sent', due_date: '2026-01-01T00:00:00.000Z' } -> key 'overdue'; due_date = today's ET date -> not overdue.
- **Verifier:** confirmed — AdminInvoicesPage.jsx:1365-1369 `const due = new Date(inv.due_date + "T23:59:59"); if (Date.now() > due.getTime()) return { key: "overdue", … }`. Migration 20260401000082_invoices.js:17 `t.date('due_date')`; InvoiceService.list selects `"invoices.due_date"` raw (invoice.js:3686) and the route returns rows untouched (admin-invoices.js:559-570 `res.json({ invoices, total, page: pageNum })`); no `set

### F0390 · P2 · Opening a conversation lands on the OLDEST message; no scroll-to-latest

- **Location:** `client/src/pages/admin/CommunicationsPageV2.jsx:591` (reader communications:COMM-03)
- **Reproduction:** Desktop: open any thread with >10 messages in the SMS tab.
- **Expected:** A chat view opens scrolled to the newest message (CustomerSmsPanel does `requestAnimationFrame(scrollToBottom)` after load, lines 227-229).
- **Actual:** activeThread.messages is sorted newest-first (2045) and reversed to oldest-first for render; the 500px desktop scroll pane starts at scrollTop 0, so a long thread (the log loads 500 messages/page) opens on the oldest bubble and the operator scrolls down every time. On mobile there is no max-height, so the newest bubble is at the bottom of the page under the composer.
- **Likely cause:** V1 parity; no ref/effect on the scroll container.
- **Fix:** In ConversationViewV2 (:528): `const scrollRef = useRef(null); useEffect(() => { const el = scrollRef.current; if (el) el.scrollTop = el.scrollHeight; }, [thread.contactPhone, thread.ourNumber]);` and `ref={scrollRef}` on the :591 div. Key on thread identity, NOT messages.length — 'Load older SMS history' (:2878) prepends and would otherwise jump to the bottom; if live-appended messages should follow, add CustomerSmsPanel's near-bottom check (:186-229). On mobile the pane is not scrollable so the effect is a harmless no-op.
- **Test:** RTL: render ConversationViewV2 with 30 messages, Object.defineProperty(scrollHeight) on the pane, assert scrollTop === scrollHeight after mount and unchanged after a prepend re-render.
- **Verifier:** confirmed — CommunicationsPageV2.jsx:591 `<div className="flex-1 md:max-h-[500px] md:overflow-y-auto flex flex-col gap-2">` carries no ref; grep scrollTop\|scrollHeight\|flex-col-reverse in the file returns nothing (the only scroll calls are :2161 scrollIntoView on the compose card and :3084 `.admin-main` scrollTo). :2045 `t.messages.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));` (newest-firs

### F0393 · P2 · Reading a thread in the inbox does not refresh the global Messages badge (C360 panel does)

- **Location:** `client/src/pages/admin/CommunicationsPageV2.jsx:941` (reader communications:COMM-05)
- **Reproduction:** With an unread thread, open it in the SMS tab; watch the sidebar Messages badge stay for up to 30s.
- **Expected:** Unread indicators agree across surfaces: after the read POST succeeds the shell badge updates immediately, as CustomerSmsPanel does (`notifyUnreadChanged()` at line 195 after its own /messages/read call).
- **Actual:** markMessagesRead never calls notifyUnreadChanged(); the AdminLayoutV2 badge keeps the stale count until the next 30s poll (useUnreadConversations POLL_MS = 30000) even though the thread row's dot cleared instantly.
- **Likely cause:** The event hook was added with the C360 panel and not wired into the older inbox path.
- **Fix:** CommunicationsPageV2.jsx: add `import { notifyUnreadChanged } from "../../hooks/useUnreadConversations";` and inside markMessagesRead's try, directly after the awaited POST (:949), add `notifyUnreadChanged();`.
- **Test:** Extend an inbox RTL test: mock adminFetch, click a thread with an unread inbound, await the read POST, assert a window 'waves:sms-unread-changed' event was dispatched (pattern already in CustomerSmsPanel.test.jsx).
- **Verifier:** confirmed — CommunicationsPageV2.jsx:941-953: `await adminFetch("/admin/communications/messages/read", { method: "POST", body: JSON.stringify({ messageIds: unreadIds, conversationIds, readBefore }) });` is followed only by `} catch { loadData(smsSearch.trim()); }` — nothing runs on success. grep notifyUnreadChanged\|useUnreadConversations\|UNREAD_CHANGED\|dispatchEvent in the file: no matches. Repo-wide the o

### F0394 · P2 · Upload / AI-draft / call failures use window.alert while the same composer reports other errors inline

- **Location:** `client/src/pages/admin/CommunicationsPageV2.jsx:1384` (reader communications:COMM-26)
- **Reproduction:** Attach 11 images, or click AI Draft with an empty To.
- **Expected:** One feedback channel per composer: the existing sendResult line (with aria-live) for every outcome.
- **Actual:** Send, link-insert and rewrite errors go to the inline sendResult line, but attachment errors, the attachment cap, the AI-draft precondition and AI-draft failure open blocking native alerts; CallBridgeLink (26/29), CallRoutingSettingsV2 (90/105), useEmailInbox (103/250/300) and useEmailEditor (65) do the same. Native alerts block the whole tab and are unstyled on iOS PWA.
- **Likely cause:** Later features grafted with quick alert() calls.
- **Fix:** Replace the five composer alerts with the inline line: :1331 → `setSendResult({ ok: false, text: "Max 10 attachments per message" }); return;`; :1353-1361 → `setSendResult({ ok: false, text: <same budget string> }); return;`; :1384 → `setSendResult({ ok: false, text: \`Upload failed: ${e.message}\` });`; :1402 → `setSendResult({ ok: false, text: "Enter a To number first" }); return;`; :1422 → `setSendResult({ ok: false, text: "AI draft failed: " + e.message });`. Add `role="status"` to the :2803 div. Leave CallBridgeLink / CallRoutingSettingsV2 / email hooks to a separate app-wide pattern ruling.
- **Test:** RTL: vi.spyOn(window, 'alert'); trigger AI Draft with an empty To and an 11th attachment; assert alert was not called and the sendResult status line shows the copy.
- **Verifier:** confirmed — CommunicationsPageV2.jsx alert() sites: :1331 `alert("Max 10 attachments per message");`, :1353-1361 the attachment-budget alert, :1384 `alert(\`Upload failed: ${e.message}\`);`, :1402 `alert("Enter a To number first");`, :1422 `alert("AI draft failed: " + e.message);` — versus :1317 `setSendResult({ ok: false, text: \`Failed: ${e.message}\` });` for send failures, rendered at :2802-2810. One corr

### F0406 · P2 · Compliance dashboard shows 'Loading dashboard…' forever when the API fails

- **Location:** `client/src/pages/admin/CompliancePage.jsx:132` (reader admin-ops-pages:OPS-005)
- **Reproduction:** Block /api/admin/compliance-v2/dashboard in DevTools and open /admin/compliance — the page never leaves 'Loading dashboard…'.
- **Expected:** Contract required states: error with retry when retry is safe; never a perpetual loading indicator.
- **Actual:** useFetch (85-100) does `.then((r) => r.json()).then(setData).catch(console.error)` — a 403/500 body still parses to an object without `warningCount`, and a network error leaves data null with loading false; both branches render the loading line indefinitely. Same pattern in LicensesTab (`if (loading)` then reads data?.technicians) and the other tabs that use useFetch.
- **Likely cause:** useFetch never modelled failure.
- **Fix:** useFetch (:84-98): add `const [error, setError] = useState(null);` and in reload `setError(null); fetch(url, { headers: headers(token) }).then(async (r) => { if (!r.ok) throw new Error(\`HTTP ${r.status}\`); return r.json(); }).then(setData).catch((e) => setError(e.message \|\| "Request failed")).finally(() => setLoading(false));` returning `{ data, loading, error, reload }`. DashboardTab (:134-140) and LicensesTab (:747): render `if (error) return <div style={{ color: D.red, padding: 20 }}>Couldn't load — {error} <button type="button" onClick={reload}>Retry</button></div>;` before the loading branch, and keep the loading line only while `loading`.
- **Test:** CompliancePage.test.jsx: mock fetch to reject (and to resolve { ok:false, status:500 }) → expect the error copy and a Retry control; clicking Retry issues a second request.
- **Verifier:** confirmed — CompliancePage.jsx:84-98 useFetch: `fetch(url, { headers: headers(token) }).then((r) => r.json()).then(setData).catch(console.error).finally(() => setLoading(false));` — never checks `r.ok`, keeps no error state, returns `{ data, loading, reload }`. :134-140 `if (loading \|\| !data) return (<div style={{ color: D.muted, padding: 20 }}>Loading dashboard…</div>);`. A network error or non-JSON body r

### F0407 · P2 · ContentCalendar legend colors Social the same as Blog while items use a different color; TYPE_ICONS are empty strings

- **Location:** `client/src/pages/admin/ContentCalendar.jsx:273` (reader admin-marketing-pages:AM-19)
- **Reproduction:** Open the calendar with both blog and social items scheduled; the legend shows two identical dark dots.
- **Expected:** Legend swatches match the item colors; no empty decorative spans.
- **Actual:** Blog and Social legend dots are both #18181B while social items are #71717A, so the legend misidentifies social posts; every item row and the day-detail list render an empty 16px span where an icon used to be.
- **Likely cause:** Token fold collapsed teal and purple to the same hex; TYPE_COLORS was patched but the legend was not; emoji stripped.
- **Fix:** Legend :275-281: use `TYPE_COLORS.blog`, `TYPE_COLORS.social`, `TYPE_COLORS.rss` for the three dots (or map over TYPE_COLORS entries); delete :42 TYPE_ICONS; :399 → literal `•`; delete the :500-502 empty span (or render `•` in `TYPE_COLORS[item.type]` there). Behaviour-preserving: the grid already always shows the bullet.
- **Test:** RTL: legend swatch inline colors equal TYPE_COLORS.blog/social/rss; the day-detail list renders no empty 16px span.
- **Verifier:** confirmed — ContentCalendar.jsx:14 `teal: "#18181B"`, :18 `purple: "#18181B"`; legend :275-281 `<span style={{ color: D.teal }}>●</span>Blog` / `<span style={{ color: D.purple }}>●</span>Social` / `<span style={{ color: D.green }}>●</span>RSS Auto` — Blog and Social dots are both zinc-900. Items are driven by :41 `const TYPE_COLORS = { blog: "#18181B", social: "#71717A", rss: "#15803D" };` (:392-393 grid, :51

### F0410 · P2 · CustomerEstimatesPanel passes tone="muted" which Badge does not define — Draft/Expired/Commercial badges render unstyled

- **Location:** `client/src/pages/admin/CustomerEstimatesPanel.jsx:27` (reader customer-360:C360-22)
- **Reproduction:** Estimates → click a customer with a draft or expired estimate.
- **Expected:** Neutral (or a real muted) tone.
- **Actual:** No background/text classes are applied, so those badges appear as bare uppercase 11px text with padding.
- **Likely cause:** Tone name invented locally.
- **Fix:** CustomerEstimatesPanel.jsx:28 `draft: "neutral"`, :34 `expired: "neutral"`, :282 `tone="neutral"`, :402 `\|\| "neutral"`.
- **Test:** RTL: render the panel with a draft estimate and a commercial customer; the Draft and Commercial badges carry classes bg-zinc-100 and text-zinc-700.
- **Verifier:** confirmed — CustomerEstimatesPanel.jsx:27-35 `const STATUS_TONES = { draft: "muted", scheduled: "neutral", ..., expired: "muted" };`, :282 `<Badge tone="muted">Commercial</Badge>`, :402 `const tone = STATUS_TONES[e.status] \|\| "muted";` → :420 `<Badge tone={tone}>{label}</Badge>`. components/ui/Badge.jsx:8-12 `const TONES = { neutral: 'bg-zinc-100 text-zinc-700', strong: 'bg-zinc-900 text-white', alert: 'bg-

### F0431 · P2 · Desktop stats row renders '12services 3done 9left $617revenue' — spaces lost in a formatter pass

- **Location:** `client/src/pages/admin/DispatchPageV2.jsx:1219` (reader dispatch-board:DB-03)
- **Reproduction:** Open /admin/dispatch?tab=schedule on desktop in Day view — the centered stats pill reads e.g. '4services 1done 3left'.
- **Expected:** '12 services · 3 done · 9 left · $617 revenue' (the pre-refactor markup was `{totalCount}</span> services`).
- **Actual:** JSX trims the newline between `</span>` and the word, so the number and its label render glued together for services/done/left (1219-1241) and revenue (1255-1261); commit 7d667f08a (2026-05-14) reformatted the single-line spans and dropped the literal space. The revenue span also picks up a stray leading `{" "}` after its left border (1256).
- **Likely cause:** Prettier-style reflow of `<span>…</span> services` into multi-line JSX without preserving the whitespace as `{' '}`.
- **Fix:** DispatchPageV2.jsx: insert `{" "}` between `</span>` and the label at :1222-1223 (services), :1228-1229 (done), :1239-1240 (left), :1259-1260 (revenue); delete the stray `{" "}` at :1256. Alternatively give each stat span `className="inline-flex gap-1"`.
- **Test:** RTL: render DispatchPageV2 (board tab, desktop matchMedia) with a 2-service day payload and assert `screen.getByText((_, el) => el.textContent === '2 services')` resolves; also assert '$… revenue' has a space.
- **Verifier:** confirmed — client/src/pages/admin/DispatchPageV2.jsx:1219-1224 `<span>\n<span className="u-nums font-medium text-zinc-900">\n{totalCount}\n</span>\nservices\n</span>` — the label sits on its own line after `</span>`; JSX removes whitespace runs that contain a newline between an element and text, so no space is emitted (same shape for done :1225-1230, left :1231-1241, revenue :1255-1261 which also has a stray

### F0432 · P2 · List mode keeps the Prev/Next/Today date nav and month header live even though ScheduleListView ignores the page date

- **Location:** `client/src/pages/admin/DispatchPageV2.jsx:1476` (reader dispatch-board:DB-16)
- **Reproduction:** Desktop Schedule tab → List → click Next twice: header advances two months, table unchanged.
- **Expected:** In List mode the date nav is hidden (or drives the list's From/To range), and the header reflects the list's filter range.
- **Actual:** Prev/Next shift `date` by a month, the header shows 'September 2026', a 'Today' button appears — none of which changes the table, which keeps its own From/To filters. Each click also triggers a full page 'Loading schedule…' (DB-05).
- **Likely cause:** List mode was added to ViewModeSelectorV2 after the shared nav row was built for calendar modes.
- **Fix:** Option A (smallest): wrap :1267-1297 in `{viewMode !== "list" && (…)}`. Option B: add a `date` prop to ScheduleListView that seeds filterFrom and make shiftDate page the range by 30 days in list mode. Owner picks.
- **Test:** RTL: select List mode; assert either the Prev/Next buttons are absent (A) or clicking Next updates the From date input (B).
- **Verifier:** confirmed — client/src/pages/admin/DispatchPageV2.jsx:1476-1486 `{viewMode === "list" && (<ScheduleListView technicians=… onEdit=… onRefresh={() => fetchSchedule(date)} refreshKey=… lastSave=… />)}` passes no date; client/src/components/schedule/ScheduleListView.jsx:39 `export default function ScheduleListView({ technicians = [], onEdit, onRefresh, refreshKey = 0, lastSave = null })`, :81-82 `const [filterFro
- **Needs owner decision**

### F0441 · P2 · Equipment maintenance record and mileage log saves fail silently (console.error only, no error UI in the file)

- **Location:** `client/src/pages/admin/EquipmentMaintenancePage.jsx:1477` (reader states:STATES-03)
- **Reproduction:** Equipment > Maintenance > add record with the API failing; nothing happens on Save.
- **Expected:** A failed save leaves the modal open with a visible error and the operator's data intact.
- **Actual:** The modal stays open with no message; grep -E 'setError\|error &&' over the file returns nothing, so there is no error surface anywhere on the page beyond the success toast (showToast :303).
- **Likely cause:** Page was built before an error-state convention; only the happy path was wired.
- **Fix:** In both modal components add `const [error, setError] = useState("")`; in each `submit` set `setError("")` before the try and `setError(\`Save failed: ${e.message}\`)` in the catch (keep console.error); render `{error && <div style={{ color: D.danger ?? "#B91C1C", fontSize: 13, marginBottom: 8 }}>{error}</div>}` directly above the Save button rows (~:1758 and ~:1963).
- **Test:** RTL: mock fetch to resolve `{ ok: false, status: 500 }`, fill taskName, click Save, assert /Save failed/ is in the document and onDone was not called; then resolve ok and assert onDone fires.
- **Verifier:** downgraded — client/src/pages/admin/EquipmentMaintenancePage.jsx:29-40 `function af(...)` → `.then((r) => { if (!r.ok) throw new Error(\`HTTP ${r.status}\`); return r.json(); })`; :1440-1479 record modal `submit` → `try { await af(…, { method: "POST", … }); onDone(); } catch (e) { console.error(e); } setSaving(false);`; :1799-1822 mileage `submit` identical. Neither modal receives `showToast`, and the file's o

### F0453 · P2 · Review & send action grid orphans the fourth button (Review and send) on its own row

- **Location:** `client/src/pages/admin/EstimateToolViewV2.jsx:7045` (reader pipeline-estimator:PE-07)
- **Reproduction:** Create Estimate → select a service → Generate Estimate; observe the button row.
- **Expected:** After Generate the four actions (Regenerate / Save draft / Preview / Review and send) sit in one row on desktop and two rows of two on a phone; the terminal action reads as the next step.
- **Actual:** With an estimate and no editMode the grid is `grid-cols-3` for four buttons, so 'Review and send' drops alone onto a second row at every width; on a phone three h-12 buttons share ~311px. In editMode the same four buttons correctly get `grid-cols-2 sm:grid-cols-4`, which shows the non-edit branch was not updated when Preview was added.
- **Likely cause:** Preview button added later; only the editMode branch's column count was adjusted.
- **Fix:** EstimateToolViewV2.jsx:7048-7052 → `estimate ? "grid-cols-2 sm:grid-cols-4" : "grid-cols-2"` (drop the editMode ternary). The optional 'flip primary to Review and send' idea is a design call — leave it out.
- **Test:** RTL: render with a generated estimate and no editMode; assert the action grid element has class sm:grid-cols-4 and four buttons.
- **Verifier:** confirmed — EstimateToolViewV2.jsx:7045-7053 `className={cn("grid gap-3", estimate ? editMode ? "grid-cols-2 sm:grid-cols-4" : "grid-cols-3" : "grid-cols-2")}`. Children: Generate/Regenerate (7055, unconditional), Save draft (7068-7079, `{estimate && …}`), Preview (7080-7092, `{estimate && …}`), Review and send (7093-7102, unconditional, outside any guard). So with `estimate` set and `editMode` unset there ar

### F0474 · P2 · Inventory Products tab: load() has no try/catch — a failed fetch leaves 'Loading products...' forever

- **Location:** `client/src/pages/admin/InventoryPage.jsx:1993` (reader admin-ops-pages:OPS-006)
- **Reproduction:** Return 500 from /api/admin/inventory and open /admin/inventory.
- **Expected:** Error state with the message and a retry; loading cleared in finally.
- **Actual:** adminFetch (line 37-48) throws on any non-2xx; the products request has no catch, so setLoading(false) is skipped and the default tab of a 5.8k-line page shows only 'Loading products...' with no toast. The vendors call was hardened for the tech-role 403 (comment at 2004-2006) but the primary request was not.
- **Fix:** InventoryPage.jsx:2001-2012 — wrap the body: `let pData, vData; try { [pData, vData] = await Promise.all([...same two calls...]); setProducts(pData.products \|\| []); setCategories(pData.categories \|\| []); setTotalProducts(pData.total \|\| 0); setVendors(vData.vendors \|\| []); setLoadError(null); } catch (e) { setLoadError(e.message); showToast(`Failed to load products: ${e.message}`); } finally { setLoading(false); }`; add `const [loadError, setLoadError] = useState(null);` next to `loading`, and before the main return render `if (loadError) return (<div style={{ color: D.red, padding: 40, textAlign: "center" }}>Failed to load products: {loadError} <button type="button" style={sBtn(D.teal, D.white)} onClick={() => { setLoading(true); load(); }}>Retry</button></div>);`.
- **Test:** RTL: mock fetch to return 500 for `/admin/inventory?…`; assert 'Loading products...' disappears, the error text and a Retry button render, and clicking Retry re-issues the request.
- **Verifier:** confirmed — InventoryPage.jsx:37-48 `adminFetch` does `if (!r.ok) throw new Error(`HTTP ${r.status}`)`. ProductsTab's `load` (1993-2013) awaits `Promise.all([adminFetch(`/admin/inventory?…`), adminFetch("/admin/inventory/vendors").catch(() => ({ vendors: [] }))])` with no try/catch; `setLoading(false)` at 2012 runs only on success, and `useEffect(() => { load(); }, [load]);` (2015-2017) swallows nothing, so a

### F0479 · P2 · Native alert()/confirm() dialogs for outcomes — including success — across ops pages

- **Location:** `client/src/pages/admin/LawnAssessmentPanel.jsx:269` (reader admin-ops-pages:OPS-017)
- **Expected:** Overlays use Dialog/Sheet; outcomes surface inline or via the page toast (ProjectsPage already replaced confirm()/prompt() with a Dialog-backed ask(), lines 42-109).
- **Actual:** Blocking browser alerts remain in LawnAssessmentPanel (235, 269, 271, 338, 365, 381 — two of them on SUCCESS, so every field confirm interrupts the tech), EquipmentCalibrationPanel:220/250, CredentialsPage:380/397 (+ window.confirm 388), PushSettings/PushSettingsV2:167/182, PricingLogicPanel:1161/1185/1333, SettingsPage (5), ServiceLibraryPage (4), TimeTrackingPage (4), DocumentRequestsPage:152, DocumentTemplatesPage (1). On the iOS PWA these render as unstyled system sheets.
- **Fix:** Success alerts → the page's existing showToast/inline status; failures → inline error text (Tier 2) or the ui Dialog / an `Alert` (Tier 1); destructive confirms → ui Dialog (Tier 1) or ProjectsPage's ask() pattern (inline copy for Tier 2).
- **Test:** Per page: add a `notice` state rendered inline under the active step; RTL asserts that after a successful confirm no `window.alert` is called (spy) and the inline 'Assessment confirmed' text renders.
- **Verifier:** confirmed — LawnAssessmentPanel.jsx has six native alerts: :235 `alert("Analysis failed: " + e.message);`, :269 `alert("Assessment confirmed.");`, :271 `alert("Confirm failed: " + e.message);`, :338 `alert("Failed to load turf profile: " + e.message);`, :365 `alert("Turf profile saved");`, :381 `alert("Save failed: " + e.message);` — two on success, and the file has no toast or status mechanism at all (grep f

### F0482 · P2 · Lead → Create Estimate drops city/zip: prefill passes raw lead.address instead of formatLeadAddress(lead)

- **Location:** `client/src/pages/admin/LeadsTabs.jsx:276` (reader pipeline-estimator:PE-12)
- **Reproduction:** Create a lead via the call-extraction path with separate city/zip columns → Create Estimate → address field lacks city.
- **Expected:** The estimator's Service address (EstimateToolViewV2 4551-4559, fed into Property Lookup) receives the composed 'street, City, FL zip' string the lead card itself displays.
- **Actual:** Leads written by intake paths that store a street-only address arrive in the estimator as e.g. '123 Palmetto Rd' with city/zip stripped, so Property Lookup geocodes without locality context and the operator re-types information the lead already holds. lead.customer_id is likewise never forwarded (compare createEstimateUrl in the dead OpportunityActions.jsx:19-30, which did pass customerId).
- **Likely cause:** leadEstimateParams predates formatLeadAddress.
- **Fix:** LeadsTabs.jsx:276 — replace `if (lead.address) params.set("address", lead.address);` with `const address = formatLeadAddress(lead); if (address) params.set("address", address);` (formatLeadAddress is defined at :21 in the same file and already guards against double-appending an included city/zip).
- **Test:** Export `leadEstimateParams` for test (or test through the row action) and assert `leadEstimateParams({ id: 'l1', address: '123 Palmetto Rd', city: 'Palmetto', zip: '34221' }).get('address') === '123 Palmetto Rd, Palmetto, 34221'` and that an already-composed address is passed through unchanged.
- **Verifier:** confirmed — LeadsTabs.jsx:276 `if (lead.address) params.set("address", lead.address);` forwards the raw column, while the same file's comment at 15-18 says 'leads.address may hold either a street-only line or a fully composed "street, City, FL zip" string depending on which intake path wrote the row' and the lead card itself displays `formatLeadAddress(lead)` at :1529. Downstream: EstimatesPageV2.jsx:3972 `ad

### F0488 · P2 · 'Convert to Customer' asks the operator to paste a raw customer UUID

- **Location:** `client/src/pages/admin/LeadsTabs.jsx:4331` (reader pipeline-estimator:PE-02)
- **Reproduction:** Pipeline → Leads → expand a lead → More actions → Convert to Customer.
- **Expected:** Converting a lead links it to a customer by picking one (search by name/phone/email, defaulting to the contact-match the New Lead flow already computes via /admin/leads/contact-matches) or by creating the customer from the lead's own fields — the way POST /admin/leads/:id/schedule-appointment already auto-creates one (admin-leads.js:1334).
- **Actual:** The modal (opened from the lead card at 2149-2158 'Convert to Customer') requires an existing customer UUID typed by hand; the server (admin-leads.js:1183-1186) 400s without it. There is no picker and no create path, so the button is effectively unusable without opening another tab, finding the customer and copying its id from the URL.
- **Likely cause:** Legacy convert modal written before customer search/contact-match endpoints existed; never revisited.
- **Fix:** Replace the UUID field with a customer picker (reuse the estimator's customer search endpoint used at EstimateToolViewV2 4497-4531, seeded from /admin/leads/contact-matches for this lead's phone/email) and offer 'Create customer from this lead' when no match; or hide 'Convert to Customer' in favor of the estimate-accept and schedule-appointment paths that already convert. Owner decision on which path is canonical.
- **Test:** After the ruling: RTL on the convert modal asserting a customer search input (role combobox) replaces the UUID field and that submit posts the selected id; or that the 'Convert to Customer' button is absent.
- **Verifier:** downgraded — The defect is real: LeadsTabs.jsx:4331-4337 `<Input label="Customer ID (required)" value={formData.customer_id} … placeholder="Existing customer UUID" />`, submit at :1157-1162 `if (!formData.customer_id) { alert("Customer ID is required to convert a lead."); …}`, opened from the card's 'Convert to Customer' `<Btn>` at :2149-2158; server admin-leads.js:1182-1186 returns 400 `customer_id is require
- **Needs owner decision**

### F0502 · P2 · Payers, statements and AR dialogs collapse fetch failures into their empty states

- **Location:** `client/src/pages/admin/PayersPage.jsx:80` (reader billing-admin:BA-11)
- **Reproduction:** Block /admin/payers → 'No payers yet'.
- **Expected:** Contract 'Required states': error with retry, and never present missing metrics as zero/empty.
- **Actual:** A rate-limited or failed request tells the operator there are no payers, no statements, or no outstanding balance — the reassuring answer — with no retry.
- **Likely cause:** Tier-1 migration copied the fetch shape without an error branch.
- **Fix:** PayersPage.jsx load(): add `const [loadError, setLoadError] = useState("")`; before `setPayers` do `if (!r.ok) throw new Error(data?.error \|\| \`HTTP ${r.status}\`)`; in catch `setPayers([]); setLoadError(e.message \|\| "Could not load payers.")`; reset it at the top of load(); in the table (:214) render `loadError ? <TR><TD colSpan={6} className="text-center py-6 text-alert-fg">{loadError} <Button size="sm" variant="ghost" onClick={load}>Retry</Button></TD></TR> : payers.length === 0 ? …`. Same shape for loadStatements/loadAr in PayerDetailSheet.jsx:88-108 (render the error line above the Statements/AR panels with a Retry calling refresh) and for PayerArAgingDialog.jsx:38-49 (error line + Retry in DialogBody instead of the empty copy). Genuine load failure is a sanctioned alert-fg use.
- **Test:** RTL for each component: mock adminFetch to resolve `{ ok:false, status:500, json: async () => ({ error: 'boom' }) }` and separately to reject; assert the error text (not 'No payers yet' / 'No statements yet' / 'No outstanding balance') renders and that clicking Retry re-invokes adminFetch.
- **Verifier:** confirmed — client/src/lib/adminFetch.js:13-27 returns the raw fetch Response (no ok check, no throw). PayersPage.jsx:80-93: `const r = await adminFetch(…); const data = await r.json(); setPayers(Array.isArray(data?.payers) ? data.payers : []); } catch { setPayers([]); }` — a 403/500 `{error}` body has no `payers` so it becomes `[]`, and :214-218 renders 'No payers yet. Create one to bill a third party.' (the

### F0508 · P2 · Pricing fetch helpers return error bodies as data (no r.ok check) — margins/logic tables go blank silently

- **Location:** `client/src/pages/admin/PricingLogicPage.jsx:64` (reader admin-ops-pages:OPS-018)
- **Expected:** Non-2xx responses reject so callers show the server's error (every other page's adminFetch does this).
- **Actual:** MarginCalculator (103-121) does `setMargins(data)` with a 403/500 body like `{error:"…"}`; `margins.services` is undefined so the table just disappears with 'Calculate' re-enabled and no message. PricingLogicPanel.jsx:30-38 has the identical helper feeding its save/load paths (a technician's 403 on save reads as success unless the panel checks `d.error`).
- **Fix:** In PricingLogicPanel.jsx:30-38 and PricingLogicPage.jsx:64-72 change the helper to `.then(async (r) => { const body = await r.json().catch(() => ({})); if (!r.ok) throw new Error(body.error \|\| \`HTTP ${r.status}\`); return body; })` (EquipmentCalibrationPanel.jsx:22-33 idiom). Wrap the PricingLogicPanel saves (:1313, :1325, :1768, :1932) in try/catch that surfaces `e.message` via the existing toast (:620 pattern `setToast({ kind: "error", msg })`), skips `onUpdate`, and reverts the optimistic `setTracks` at :1766; in MarginCalculator :117-119 set `setMargins({ error: e.message })` instead of null so the existing :490 branch shows it.
- **Test:** RTL: mock PUT /admin/pricing-config/:key → 400 `{ error: 'bad value' }`; edit a value in PricingLogicPanel and assert the error toast renders and onUpdate is not called; mock POST margin-check → 500 `{ error }` and assert the message renders in MarginCalculator.
- **Verifier:** confirmed — Helper claim holds: client/src/pages/admin/PricingLogicPage.jsx:64-72 `const af = (p, o = {}) => fetch(…).then((r) => r.json());` and client/src/components/admin/PricingLogicPanel.jsx:30-38 identical — neither checks r.ok. BUT the cited MarginCalculator symptom is wrong: PricingLogicPage.jsx:490-494 `{margins?.error && (<div style={{ color: D.red, fontSize: 12, padding: 10 }}>{margins.error}</div>

### F0543 · P2 · Tax 'Add Expense → Save' has no in-flight guard — a double click records the expense twice

- **Location:** `client/src/pages/admin/TaxPage.jsx:1006` (reader billing-admin:BA-15)
- **Reproduction:** Taxes → Expenses → + Add Expense → fill → double-click Save → two identical rows.
- **Expected:** Money-recording writes are single-flight (AdminInvoicesPage uses `rowActionBusy`/`saving`; PayerDetailSheet uses `busy`).
- **Actual:** Two rapid taps (common on iOS) issue two POSTs before the form closes, creating duplicate expense rows that then feed the P&L and Schedule C export.
- **Likely cause:** Missing saving state.
- **Fix:** TaxPage.jsx ExpensesTab: `const [savingExpense, setSavingExpense] = useState(false);` at the top; in handleAdd add `if (savingExpense) return; setSavingExpense(true);` before the fetch and `finally { setSavingExpense(false); }`; on the Save button (1199) add `disabled={savingExpense}` and `opacity: savingExpense ? 0.6 : 1`, label `{savingExpense ? "Saving…" : "Save"}`.
- **Test:** RTL: mock fetch with a deferred promise; fill description/amount/date; click Save twice before resolving; assert fetch called once with POST /admin/tax/expenses and the button is disabled while pending.
- **Verifier:** confirmed — client/src/pages/admin/TaxPage.jsx:1006-1024 `const handleAdd = async () => { if (!form.description \|\| !form.amount \|\| !form.expenseDate) return; try { await adminFetch("/admin/tax/expenses", { method: "POST", … }); setShowAdd(false); … load(); } catch (e) { alert("Failed: " + e.message); } };` — no in-flight flag; the Save button at 1199-1213 `<button onClick={handleAdd} style={{…}}>Save</but

### F0549 · P2 · Tax overview renders nothing while loading and stays blank forever if /admin/tax/dashboard fails; 15 swallowed catches on the page

- **Location:** `client/src/pages/admin/TaxPage.jsx:4807` (reader billing-admin:BA-16)
- **Reproduction:** Block /admin/tax/dashboard and open /admin/tax.
- **Expected:** Loading without layout collapse; error with retry.
- **Actual:** Below the header the Overview tab is empty during the fetch and permanently empty on any failure; the Expenses/Rates/Taxability tabs likewise show an empty list with no message on failure.
- **Likely cause:** Early page; no-op catches.
- **Fix:** TaxPage.jsx TaxPage(): add `const [dashboardStatus, setDashboardStatus] = useState("loading")` and `const [dashboardError, setDashboardError] = useState(null)`; wrap the three overview fetches in a `loadOverview` useCallback that sets 'ready' / 'error' (message from adminFetch); render `{activeTab === "overview" && dashboardStatus === "loading" && <div style={{ padding: 40, textAlign: "center", color: D.muted }}>Loading tax overview…</div>}` and an error block with a Retry button (same shape as AccountsReceivableTab's loadError block at ~3793) ahead of the `{d && …}` block. Leave the per-tab catches to a follow-up unless the same one-line error state is added to each tab's load.
- **Test:** RTL: mock /admin/tax/dashboard to reject; render TaxPage; assert an error message + Retry button on the Overview tab; resolve on retry and assert 'Tax Collected YTD' renders. Also assert 'Loading tax overview…' appears before the promise resolves.
- **Verifier:** confirmed — client/src/pages/admin/TaxPage.jsx:4808-4813 `adminFetch("/admin/tax/dashboard").then(setDashboard).catch(() => {}); adminFetch("/admin/tax/pnl?period=mtd").then(setQuickPnl).catch(() => {}); adminFetch("/admin/tax/accounts-receivable").then((d) => setArSummary(d?.summary)).catch(() => {});` then 4881 `{d && activeTab === "overview" && (` — grep for `!d &&\|!dashboard\|dashboardError\|dashboardSta

### F0550 · P2 · Staff (TimeTracking) tabs live only in local state — no deep link, no back/forward, no usage beacon

- **Location:** `client/src/pages/admin/TimeTrackingPage.jsx:175` (reader admin-ops-pages:OPS-008)
- **Reproduction:** Open /admin/timetracking, click Time → Approvals, press browser refresh — Dashboard renders.
- **Expected:** Sibling hubs (EquipmentPage 211-258, CompliancePage, InventoryPage, ServiceLibraryPage) read/write ?tab= and call useRenderedTabBeacon so refresh, back/forward, bookmarks and the Portal Usage report work; contract per-page checklist item 1.
- **Actual:** The file has zero useSearchParams/useRenderedTabBeacon references. Refreshing /admin/timetracking while on Approvals returns to Dashboard; nothing can link to ?tab=approvals (e.g. from an alert); the usage report records only the route. Same gap in PricingStrategyPage (tab useState at line 89, though it is hub-embedded).
- **Fix:** TimeTrackingPage.jsx: import useSearchParams + useRenderedTabBeacon; seed `useState(() => normalizeTab(searchParams.get("tab")))` where normalizeTab returns the value if it is in some group's tabs else 'dashboard'; add `selectLeaf(next)` that sets state and writes `?tab=` (delete the param for 'dashboard') with `{ replace: true }`; sync effect on [searchParams]; `useRenderedTabBeacon("/admin/timetracking", tab, [searchParams])`; add 'timetracking' to SELF_REPORTING_PAGES in lib/adminUsage.js:67 and its membership test. Guard the active-re-click like Equipment. (Separately, ApprovalsTab ignoring `weekStart` from the same cron link is outside this finding.)
- **Test:** RTL with MemoryRouter initialEntries ['/admin/timetracking?tab=approvals']: assert ApprovalsTab content renders; click 'Team' and assert the URL search becomes ?tab=team; invalid ?tab=typo renders Dashboard and the beacon receives 'dashboard'. Extend the adminUsage lib membership test for 'timetracking'.
- **Verifier:** confirmed — client/src/pages/admin/TimeTrackingPage.jsx:174-178 `export default function TimeTrackingPage() { const [tab, setTab] = useState("dashboard"); const activeGroup = TIMETRACKING_TAB_GROUPS.find(…)`; grep for `useSearchParams\|useRenderedTabBeacon\|searchParams\|useLocation\|useNavigate` in the file is empty. Route is `<Route path="timetracking" …>` (App.jsx:651). The gap already bites a real produce

### F0558 · P2 · Turf height 'Confirm reading' silently does nothing when the PATCH fails

- **Location:** `client/src/pages/admin/TurfHeightReviewPage.jsx:55` (reader admin-ops-pages:OPS-007)
- **Expected:** A failed action surfaces an error message; the button either recovers with feedback or the row shows why it stayed.
- **Actual:** On a 4xx/5xx the row simply stays and the button re-enables with no message — the operator cannot tell whether the click registered. A thrown network error also propagates unhandled (no catch). The `error` state that exists for load is never set here.
- **Fix:** Replace :62 with `if (!r.ok) { const b = await r.json().catch(() => ({})); setError(b.error \|\| `Could not confirm reading (HTTP ${r.status})`); return; } setError(null); setItems((prev) => prev.filter((it) => it.id !== id));` and add `catch { setError("Could not confirm reading — check your connection and try again."); }` before the `finally`. The existing `{error && <div style={{ color: M.red }}>{error}</div>}` at :80 renders it; add `role="alert"` to that div.
- **Test:** RTL: mock fetch so GET returns one item and PATCH returns {ok:false,status:500}; click "Confirm reading"; assert an alert-role message appears and the item row remains; second case: PATCH rejects → same message, no unhandled rejection.
- **Verifier:** confirmed — TurfHeightReviewPage.jsx:55-66: `async function resolve(id) { setResolving(id); try { const r = await adminFetch(`/admin/turf-height/${id}/resolve`, { method: "PATCH", … }); if (r.ok) setItems((prev) => prev.filter((it) => it.id !== id)); } finally { setResolving(null); } }` — no else branch and no catch. The `error` state declared at :41 is only written by `load` (:48-49). A 4xx/5xx therefore re-

### F0570 · P2 · Negative adjusted-EBITDA renders as "$-1,234" instead of "−$1,234"

- **Location:** `client/src/pages/admin/dashboard/EbitdaBridgeCard.jsx:46` (reader dashboard:DASH-02)
- **Reproduction:** Enter operating costs larger than month-to-date contribution (Settings → Financials → Operating Costs) or view early in a month; the 'Adjusted EBITDA' result row shows `$-N`.
- **Expected:** A negative `result` row (adjusted EBITDA or Contribution below zero — explicitly anticipated by `r.amount < 0 ? { bar: "#C8312F", text: "text-alert-fg" }` at line 26-27) prints with the same true-minus convention as the rest of the card: `−$1,234`.
- **Actual:** `fmtMoney(-1234)` returns `$-1,234` (dollar sign before the hyphen-minus) for `start`/`subtotal`/`result` rows — the one row that carries the alert tone is the one with the malformed number. MrrBridgeCard.jsx:57 already handles this correctly (`amount < 0 ? `−${fmtMoney(Math.abs(amount))}` : fmtMoney(amount)`).
- **Likely cause:** fmtMoney never handles sign; the minus branch was only written for `kind === 'minus'` rows.
- **Fix:** EbitdaBridgeCard.jsx:46 → `{r.amount < 0 ? `−${fmtMoney(Math.abs(r.amount))}` : fmtMoney(r.amount)}` (a zero-amount minus row then renders `$0` instead of `−$0`, which is correct). Do not change fmtMoney's contract.
- **Test:** RTL render `<EbitdaBridgeCard bridge={{ revenue: 1000, rows: [{key:'revenue',label:'Revenue',kind:'start',amount:1000},{key:'cogs',label:'COGS',kind:'minus',amount:-1500},{key:'ebitda',label:'Adjusted EBITDA',kind:'result',amount:-1234}] }} />`; expect text to contain `−$1,234` and `−$1,500` and never `$-`.
- **Verifier:** confirmed — EbitdaBridgeCard.jsx:46 `{r.kind === "minus" ? `−${fmtMoney(Math.abs(r.amount))}` : fmtMoney(r.amount)}`; charts.jsx:66-71 `export function fmtMoney(n, opts = {}) { const v = Number(n \|\| 0); return '$' + v.toLocaleString(…) }` has no sign handling, so `fmtMoney(-1234)` → `$-1,234`. The server emits negative non-minus rows: ebitda-bridge.js:111 `gross_profit … kind: 'subtotal'`, :116-118 `contrib

### F0578 · P2 · Email composer sends unescaped text as HTML — '<' in a message breaks the email

- **Location:** `client/src/pages/admin/email/useEmailEditor.js:59` (reader communications:COMM-08)
- **Reproduction:** Reply to any email with the text `a < b` and view the received message.
- **Expected:** Plain text typed in the composer/reply box arrives verbatim; angle brackets and ampersands are escaped before the newline->br conversion.
- **Actual:** The client only swaps newlines for <br>; server sendMessage (gmail-client.js:265-293) wraps the body as text/html with no escaping, so 'price <100 and >50' loses the bracketed text in the recipient's mail client and any pasted markup is rendered as HTML.
- **Likely cause:** Composer treats the textarea as HTML source.
- **Fix:** In useEmailEditor.js add `const escapeHtml = (s) => String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");` (or export `encodeEmailBody`) and change :59 to `body: escapeHtml(payload.body).replace(/\n/g, "<br>")`.
- **Test:** Unit test of the encoder: `'price <100 & >50\nthanks'` → `'price &lt;100 &amp; &gt;50<br>thanks'`; hook test: handleReply with that text posts the encoded body to /api/admin/email/send.
- **Verifier:** confirmed — useEmailEditor.js:55-61 posts `body: payload.body.replace(/\n/g, "<br>")` — the only transformation; admin-email.js:322-327 `router.post('/send' … const { to, subject, body, threadId, inReplyTo } = req.body; … gmailClient.sendMessage(to, subject \|\| '(no subject)', body, threadId, inReplyTo)` passes it through untouched; gmail-client.js:279 sets `'Content-Type: text/html; charset=utf-8'` and :287

### F0591 · P2 · Next Stop action row packs up to six ~34px-tall, 12px-label buttons in one non-wrapping row; the next action is not visually singular

- **Location:** `client/src/pages/tech/TechHomePage.jsx:1689` (reader tech-portal:TECH-03)
- **Reproduction:** Open /tech with a pending stop; mark En Route; observe four buttons + On Site in one row at 375px width.
- **Expected:** Field priorities: tap targets >= 44px, one obvious next action ('En Route' → 'On Site'), secondary actions demoted.
- **Actual:** Lines 725-757 render Navigate, Protocol, Quick Move, En Route (primary), plus On Site (primary, when en_route) and Sync Stop (primary, when out of sync) in a single `display:flex, gap:8` row with no wrap. On a 375px phone inside the 480px column each button is ~50px wide and ~34px tall (8px padding + 14px icon line) with a 12px label; 'Quick Move' and 'En Route' truncate/wrap awkwardly. Two or three teal primaries can be on screen at once, so the 'one primary action per view' idea is lost exactly where glanceability matters most.
- **Likely cause:** Buttons were appended one at a time (Sync Stop, On Site) to a row designed for three.
- **Fix:** Give ActionBtn minHeight: 44 and fontSize 14, add flexWrap: 'wrap' with a minWidth per button, and render the single status-advancing CTA (En Route → On Site → Sync Stop) full-width above a secondary row (Navigate / Protocol / Quick Move) so only one primary exists at a time.
- **Test:** If ruled in: render the Next Stop card at status 'en_route' and assert exactly one ActionBtn has the primary background.
- **Verifier:** downgraded — Facts hold: TechHomePage.jsx:725-757 renders Navigate, Protocol, Quick Move, En Route (primary) plus On Site (primary, `ON_SITE_ELIGIBLE = new Set(['en_route'])` :196) and Sync Stop (primary, `stopOutOfSync` :403) inside `<div style={{ display: 'flex', gap: 8 }}>` with no wrap; `EN_ROUTE_ELIGIBLE` (:195) excludes 'en_route', so En Route stays rendered (disabled, teal at 0.6) beside the On Site pri
- **Needs owner decision**

### F0597 · P2 · In-brief service action buttons (Report / Photos / Zone / 🚩) are ~32px tall with 12px labels; the flag button is icon-only

- **Location:** `client/src/pages/tech/VisitBriefPanel.jsx:467` (reader tech-portal:TECH-04)
- **Reproduction:** Expand any stop in Today's Services and measure the Actions row.
- **Expected:** The per-visit actions that create the customer-facing report and attach photos should be >= 44px tall with >= 14px labels, and every action should carry a visible label.
- **Actual:** 8px vertical padding + a 12px line yields ~32px buttons; '🚩' has no visible text so a tech cannot tell what it does without tapping. These are the report-creation and photo entry points for every stop.
- **Likely cause:** Moved verbatim from the old ServiceRow (per the comment at 463-465) without revisiting sizing.
- **Fix:** minHeight: 44, fontSize: 14, and label the flag button ('🚩 Opportunity'); allow flexWrap (already set).
- **Test:** If ruled in: VisitBriefPanel.test.jsx asserts the flag button has non-empty visible text and each action button's inline minHeight is 44.
- **Verifier:** downgraded — Measurements hold: VisitBriefPanel.jsx:467-471 `const btn = { padding: '8px 10px', borderRadius: 6, fontSize: 12, fontWeight: 600, border: … }` (≈32px tall at 12px), and :495 `<button onClick={() => onLead(service)} aria-label="Flag opportunity" style={{ ...btn, color: DARK.amber }}>🚩</button>` has no visible label. But the comment at 463-465 says these were 'moved verbatim from the old ServiceRow
- **Needs owner decision**

### F0606 · P2 · `text-10` / `text-15` / `text-20` / `text-24` are not Tailwind tokens — the classes emit no CSS and silently inherit the parent size

- **Location:** `client/tailwind.config.js:67` (reader tables-lists:TL-01)
- **Reproduction:** Open /admin/estimates → any estimate → Audit; inspect the grid header row: computed font-size is inherited (not 10px) and no `.text-10` rule exists in the built stylesheet.
- **Expected:** Every `text-N` size class used in admin resolves to a theme token from the contract scale (22/18/14/13/11); table header labels render at 11px.
- **Actual:** 86 `text-10`, 22 `text-15`, 13 `text-20`, 5 `text-24` sites reference sizes absent from `theme.extend.fontSize` (no plugins, presets, safelist, or `.text-10` utility in any src CSS), so the rule is never generated. The EstimatesPageV2 audit grid header (1189), the PipelineAnalytics grid header (672), the ScheduleListView filter labels (383), DocumentRequestsPage delivery chips (316-326) and CallLogTabV2 `md:text-10` (1452) all render at whatever the parent size is (often 13-14px in a container meant for a small uppercase label, or 12px text meant to be 15px in ScheduleCustomerSidebar 329-497).
- **Likely cause:** Authors assumed the numeric fontSize scale was continuous; the theme only defines eight steps and nothing lints for unknown utilities.
- **Fix:** Map each undefined class to the nearest contract token: `text-10`→`text-11`, `text-15`→`text-14` (or `text-16` where it labels a mobile primary), `text-20`→`text-22`, `text-24`→`text-22`/`text-28`. Add an ESLint/`check:domain-rules` grep that fails on `\btext-(9\|10\|15\|17\|20\|24)\b`. Do NOT add a `10` token — 10px is below the admin floor.
- **Test:** Static test: collect every `\btext-(\d+)\b` in client/src and assert each N is a key of tailwind.config.js theme.extend.fontSize; add the same grep to check:domain-rules.
- **Verifier:** downgraded — Bug is real: client/tailwind.config.js:67-76 `fontSize` extends only '11','12','13','14','16','18','22','28', `plugins: []` (:98), no safelist/presets, postcss.config.js plain tailwindcss 3.4 (no numeric default scale), and no `.text-10\|15\|20\|24` rule in any client/src .css. Usage counts match exactly: text-10 ×86, text-15 ×22, text-20 ×13, text-24 ×5 (e.g. EstimatesPageV2.jsx:1189 `bg-zinc-50 
- **Needs owner decision**

### F0613 · P2 · SMS-guard admin alert links to unmounted /admin/sms-templates (lands on customer login)

- **Location:** `server/services/twilio.js:153` (reader navigation-map:NAV-02)
- **Reproduction:** Trigger an SMS guard block, open the bell, click the alert.
- **Expected:** The alert link opens the SMS templates workspace (Communications → Templates, i.e. /admin/communications#tab=templates).
- **Actual:** No /admin/sms-templates route exists in App.jsx (no child route, no redirect). The URL falls through to the `/*` catch-all → ProtectedRoute → customer /login?next=/admin/sms-templates. NotificationBell navigates with window.location.href (NotificationBell.jsx:462), so the admin is hard-reloaded into the customer login screen.
- **Likely cause:** Link string never updated when the standalone templates page was folded into Communications.
- **Fix:** server/services/twilio.js:153 `link: "/admin/sms-templates",` → `link: "/admin/communications#tab=templates",`.
- **Test:** Extend server/tests/twilio-internal-alert-redirect.test.js: notifySmsGuardBlocked calls triggerNotification with `link: '/admin/communications#tab=templates'`; longer term mirror client/src/config/adminNavigation.test.js:108 ('only links to mounted admin routes') over the link literals in server/services/notification-triggers.js and twilio.js.
- **Verifier:** downgraded — server/services/twilio.js:153 `link: "/admin/sms-templates"` (inside notifySmsGuardBlocked → triggerNotification('internal_admin_alert')). grep for 'sms-templates' in client/src and server finds only API paths (`adminFetch('/admin/sms-templates…')` in CommunicationsPage/CommunicationsTabsV2/NotificationEventsTabV2) — App.jsx has no `sms-templates` child under `<Route path="/admin">` (552-676) and 


---

## E. Responsive report (verified)

Concrete mobile/tablet/desktop defects that survived verification, plus the rendered-census facts in section B (no horizontal overflow and no page errors on any of the 45 admin routes at 1440/390).

| # | Sev | Location | Finding | Fix | Auto |
|---|---|---|---|---|---|
| F0156 | P1 | `client/src/components/report/lawnV2/LawnReportV2Section.jsx:33` | Lawn and Tree & Shrub V2 dashboards are nested inside the visit-summary card with an extra 16px inset — ~160px text measure at 320px | Mount LawnReportV2Section/TreeShrubReportV2Section as siblings of the visit-summary section (or drop the wrapping section when reportV2 is present) and remove the `maxWidth: 720, padding: '8px 16px 0'` inset from both Se | no |
| F0309 | P1 | `client/src/pages/PortalPage.jsx:14050` | Mobile full-screen Request overlay header sits under the iOS status bar / notch | PortalPage.jsx:14055 `padding: compact ? '12px 14px' : '14px 18px'` -> `padding: compact ? 'calc(12px + env(safe-area-inset-top, 0px)) 14px 12px' : '14px 18px'`. Optionally the tier modal at 9851: `maxHeight: compact ? ` | yes |
| F0408 | P1 | `client/src/pages/admin/ContentCalendar.jsx:388` | ContentCalendar month grid uses 9px item labels and has no phone layout | Add an isMobile branch that renders the selected week/day as a list (existing 'Selected day detail' block at 423-527 is close), keep the grid for md+, and raise item/legend text to >=12px (13/14 for the day list). | owner |
| F0034 | P2 | `client/src/components/NotificationBell.jsx:360` | Admin mobile notifications panel hardcodes top/bottom 56px — mismatches the shell's safe-area bars on notched iPhones | client/src/components/NotificationBell.jsx:361 `top: isDark ? 'calc(52px + env(safe-area-inset-top, 0px))' : 'calc(env(safe-area-inset-top, 0px) + 8px)',` and :364 `bottom: isDark ? 'calc(56px + env(safe-area-inset-botto | yes |
| F0042 | P2 | `client/src/components/ServiceRecapModal.jsx:385` | Tech-portal bottom sheets have no safe-area-inset-bottom padding; bottom controls sit under the iOS home indicator | Per panel, fold the inset into the existing bottom padding: ServiceRecapModal.jsx:423 `padding: '14px 18px calc(18px + env(safe-area-inset-bottom, 0px))'`; TechServicePhotosModal.jsx:154 and TechTreatmentZoneModal.jsx:65 | yes |
| F0073 | P2 | `client/src/components/admin/Customer360ProfileV2.jsx:8059` | Mobile: Text/Call duplicated in both fixed bars and the 768px breakpoint disagrees between them | Mechanical half: Customer360ProfileV2.jsx:5917 `@media (max-width: 768px)` → `@media (max-width: 767.98px)` and rewrite the :8059-8062 comment (768 = desktop, matching Tailwind `md` and useIsMobile). Ruling half: remove  | owner |
| F0089 | P2 | `client/src/components/admin/IntelligenceBarShell.jsx:154` | IntelligenceBarShell controls are 20–36px tall on mobile (contract: 44px touch controls) | IntelligenceBarShell.jsx: :154 `h-6` → `h-11 sm:h-6`; :263 `h-8 sm:h-6` → `h-11 sm:h-6`; :280 `h-9 w-9` → `h-11 w-11 sm:h-9 sm:w-9`; :303 `h-6` → `h-11 sm:h-6`; :354 and :362 `h-6` → `h-11 sm:h-6`; :434 `h-5` → `h-11 sm: | yes |
| F0097 | P2 | `client/src/components/admin/MobileServiceLibrary.jsx:865` | Small touch targets on mobile-only admin sheets and controls (36px filter/tab buttons, 32px icon button, 24px toggle) below the 44px contrac | client/src/components/admin/MobileServiceLibrary.jsx:865 -> `style={{ height: 44, fontSize: 14, fontWeight: 500 }}`. Siblings: NewsletterPage.jsx:1040 `h-8 w-8` -> `h-11 w-11 sm:h-8 sm:w-8`; DataHygienePage.jsx:270/273 ` | yes |
| F0107 | P2 | `client/src/components/analytics/PublicFunnelTracking.jsx:66` | Cookie notice: 13px text, fixed bottom bar without safe-area-inset-bottom, and the file is outside the brand gate's scan dirs | PublicFunnelTracking.jsx:91 fontSize 13 -> 14; :77 padding -> `padding: '12px 18px'` plus `paddingBottom: 'calc(12px + env(safe-area-inset-bottom, 0px))'`; :108 button padding '9px 18px' -> minHeight 44; scripts/check-po | yes |
| F0126 | P2 | `client/src/components/dispatch/DispatchBoardLayout.jsx:49` | Dispatch board is sized 100vh-64px inside a shell with no 64px bar, so part of the board is always clipped (desktop and mobile) | Make DispatchBoardLayout `flex-1 min-h-0` and let AdminDispatchPage size the content column to `calc(var(--admin-vh,100vh) - <shell top/bottom padding>)` (desktop 48px; mobile the 52/56 bars + safe areas), or compute the | no |
| F0176 | P2 | `client/src/components/schedule/MarkPrepaidModal.jsx:205` | MarkPrepaidModal bottom-anchors on phones with only 16px overlay padding and no env(safe-area-inset-bottom); its Save button sits under the  | MarkPrepaidModal.jsx:206 → `style={{ background: 'rgba(15,23,42,0.55)', padding: 16, paddingBottom: 'max(16px, env(safe-area-inset-bottom, 0px))' }}` (Dialog's idiom). Combine with the F0177 panel bound in one edit. | yes |
| F0219 | P2 | `client/src/components/tech/GeofenceArrivalPrompt.jsx:250` | Geofence/visit notification stack renders under the iPhone notch (fixed top:12, no safe-area inset) | GeofenceArrivalPrompt.jsx:250 `top: 12` → `top: 'calc(12px + env(safe-area-inset-top, 0px))'`; :254 `maxHeight: 'calc(100vh - 24px)'` → `maxHeight: 'calc(100dvh - 24px - env(safe-area-inset-top, 0px))'`. | yes |
| F0223 | P2 | `client/src/components/tech/TechServicePhotosModal.jsx:150` | Tech bottom sheets (photos, treatment zone, recap, capture, follow-up) have no safe-area-inset-bottom — last action row sits under the home  | On each sheet panel add `paddingBottom: 'calc(16px + env(safe-area-inset-bottom, 0px))'` (TechServicePhotosModal.jsx:154; TechTreatmentZoneModal.jsx:654; ServiceRecapModal.jsx:387 — its header/body use inner padding, so  | yes |
| F0331 | P2 | `client/src/pages/ServiceOutlinePage.jsx:12` | 100vh panels inside WavesShell on ServiceOutline, NewsletterArchive and CardPage push the universal footer below the fold and break the iOS  | ServiceOutlinePage.jsx:12 and :26: replace `min-h-screen` with `flex-1`. NewsletterArchivePage.jsx:115, :122, :131: `minHeight: '100vh'` -> `flex: 1`. CardPage.jsx:210: `minHeight: '100vh'` -> `flex: 1` (WavesShell's <ma | yes |
| F0358 | P2 | `client/src/pages/admin/AgentDecisionsPage.jsx:582` | AgentDecisionsPage inner panels hard-code two columns while the outer grid branches on isMobile | AgentDecisionsPage.jsx:503, :582, :659 → `gridTemplateColumns: isMobile ? "1fr" : "1fr 1fr"`; :604 → `gridTemplateColumns: isMobile ? "1fr" : "minmax(180px, 260px) 1fr"`. `isMobile` is already in scope in the component ( | yes |
| F0384 | P2 | `client/src/pages/admin/CallLogTabV2.jsx:919` | Call filters live only in the desktop-only stat bar — Missed/Answered unreachable on phones | In CallLogTabV2.jsx directly after the :919-955 bar add a `md:hidden` block: `<label htmlFor="call-filter" className="sr-only">Filter calls</label><Select id="call-filter" value={callFilter} onChange={(e) => setCallFilte | yes |
| F0405 | P2 | `client/src/pages/admin/CompliancePage.jsx:77` | `sTableWrap` uses `overflow: hidden` — Compliance and Discounts tables clip their right-hand columns on narrow screens instead of scrolling | CompliancePage.jsx:81 and DiscountsTabs.jsx:50 `overflow: "hidden"` → `overflow: "auto"` (the 12px radius still clips the scroll container; loading/error/empty spreads are unaffected); wrap the DiscountsTabs.jsx:1079 tab | yes |
| F0442 | P2 | `client/src/pages/admin/EquipmentPage.jsx:160` | Module-level `isMobile` snapshot never updates — wrong layout after rotate/resize in 6 ops files | In EquipmentPage.jsx delete :160 and add `const isMobile = useIsMobile(640);` at the top of EquipmentTab (:409), TankMixTab (:814) and JobCostTab (:1045); repeat per reading component in EquipmentMaintenancePage.jsx (768 | yes |
| F0452 | P2 | `client/src/pages/admin/EstimateToolViewV2.jsx:5281` | Estimate tool V2 form rows are hard 3-column grids with no breakpoint (three selects at ~105px each on a phone) | Replace `grid grid-cols-3 gap-3` with `grid grid-cols-1 sm:grid-cols-3 gap-3` at EstimateToolViewV2.jsx:5281, 5316, 5975, 6059, 6143, 6664, 6700, 6830 and PayersPage.jsx:308. | yes |
| F0456 | P2 | `client/src/pages/admin/EstimatesPageV2.jsx:513` | Row actions bottom sheet ignores the iOS home-indicator inset (last item — Delete draft — sits under it) | EstimatesPageV2.jsx:525-528 add `style={{ paddingBottom: "env(safe-area-inset-bottom, 0)" }}` to the panel div (`relative bg-white border-hairline …`), matching MobileChipSheet at 2950. | yes |
| F0542 | P2 | `client/src/pages/admin/TaxPage.jsx:171` | TaxPage `isMobile` is a module-level constant evaluated once — layout never responds to resize/rotation | TaxPage.jsx: delete line 171; `import useIsMobile from "../../hooks/useIsMobile";` and call `const isMobile = useIsMobile(640);` inside each function component that reads it (StatCard at 87, and the tab components owning | yes |
| F0552 | P2 | `client/src/pages/admin/TimeTrackingPage.jsx:3059` | Four TimeTracking tables render with no horizontal-scroll wrapper — the page scrolls sideways on phones | TimeTrackingPage.jsx: wrap each of the six tables (1031, 1908, 2000, 2072, 2282, 3059) in `<div style={{ overflowX: "auto" }}>…</div>` — the exact inline idiom the file already uses at 886/1352/5224 and that index.css:24 | yes |
| F0555 | P2 | `client/src/pages/admin/ToolHealthPage.jsx:675` | ToolHealthPage error rows use a fixed 80/170/1fr grid and the tools table has no overflow container — unreadable on phones | ToolHealthPage.jsx: wrap the table (473) in `<div style={{ overflowX: "auto", marginTop: 14 }}>` (move marginTop off the table so index.css:246's scroll-shadow selector matches). For RecentErrorRow: `import useIsMobile f | yes |
| F0556 | P2 | `client/src/pages/admin/TriageInboxTabV2.jsx:502` | Triage tab adds its own page padding and 32px chip controls on mobile | TriageInboxTabV2.jsx:502 → `className="flex flex-col gap-4 pb-10"`; :504 → `className="flex gap-2 flex-wrap"`; at :509, :519, :536, :554 replace `h-8` with `min-h-[44px] md:min-h-0 md:h-8`; add `aria-pressed={isTriage}`  | yes |
| F0557 | P2 | `client/src/pages/admin/TriageInboxTabV2.jsx:656` | Triage card action row squeezes the caller name to nothing on phones | TriageInboxTabV2.jsx:623 → `className="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"`; :656 → `className="flex items-center gap-2 flex-wrap sm:shrink-0"` (same wrap pattern as CallLogTabV2.jsx:1466 ` | yes |
| F0589 | P2 | `client/src/pages/tech/TechHomePage.jsx:1547` | Bottom sheets (Quick Move, Service Photos, Treatment Zone, Service Recap) have no safe-area bottom padding and size with vh | Add to each sheet body: TechHomePage.jsx:1551 `paddingBottom: 'calc(20px + env(safe-area-inset-bottom, 0px))'`; TechServicePhotosModal.jsx:154 and TechTreatmentZoneModal.jsx:654 `paddingBottom: 'calc(16px + env(safe-area | yes |
| F0592 | P2 | `client/src/pages/tech/TechLawnDiagnosticPage.jsx:259` | Lawn Diagnostic and Social Post pages re-apply minHeight:100vh + 16px padding inside TechLayout's <main>, doubling gutters and forcing extra | In both pages replace the wrapper with `<div style={{ maxWidth: 480, margin: '0 auto', color: D.text, fontFamily: BODY }}>` and swap the bare `<h1>` for the TechProtocolsPage.jsx:135-149 header row (`<button onClick={()  | yes |
| F0101 | P3 | `client/src/components/admin/PendingActionsCard.jsx:317` | Confirm/Cancel on the write-confirmation card are ~32px on the mobile palette sheet and in the Shell (touchFriendly only passed by AgentEsti | client/src/components/admin/IntelligenceBarShell.jsx:479-481 add `touchFriendly` to the <PendingActionsCard> (mirrors AgentEstimatePage.jsx:1088), or change PendingActionsCard.jsx:317/:330 to unconditional `min-h-11 sm:m | yes |
| F0133 | P3 | `client/src/components/estimate/AppShowcaseCard.jsx:134` | App card forces the two store badges side by side and a 240px copy column — overflows the card at 320px | Keep nowrap ≥360px and allow wrap below via a small media query (or `flexWrap: 'wrap'` with `justifyContent: 'center'` and a `@media (min-width: 360px)` nowrap), and drop `.gc-av-right` min-width to 0 with `flex-basis: 2 | no |
| F0271 | P3 | `client/src/pages/EstimateViewPage.jsx:2640` | Modal cards use maxHeight 90vh with no dynamic-viewport or safe-area handling | EstimateViewPage.jsx: `const MODAL_CARD = { background: COLORS.white, borderRadius: 16, maxWidth: 440, width: '100%', padding: 24, boxShadow: '0 18px 50px rgba(0,0,0,0.25)', maxHeight: 'min(90vh, 90dvh)', overflow: 'auto | yes |
| F0379 | P3 | `client/src/pages/admin/BlogPage.jsx:112` | Module-level `isMobile` constants (Blog, Reviews, ReviewVelocityEngine) never update; RVE imports useIsMobile but never calls it; Social/KB  | In BlogPage.jsx, ReviewsPage.jsx and ReviewVelocityEngine.jsx delete the module-level `const isMobile = …` and add `const isMobile = useIsMobile(640);` at the top of each component body that references it (Blog: Card + t | yes |
| F0561 | P3 | `client/src/pages/admin/WinLossSlicesCard.jsx:32` | 148 raw <table> in admin vs 10 ui/Table consumers; Tier-1 files drop the overflow-x wrapper ui/Table provides | Swap the eight Tier-1 files to <Table>/<THead>/<TR>/<TH>/<TD>; Tier-2 raw tables (36 files) migrate with their pages. | owner |


---

## F. Accessibility report (verified, ranked)

Ranked by severity. The census counted 0 icon-only buttons without a name on 44 of 45 admin routes (Customer 360 has 1) and 1 dialog without a role (Customer 360).

| # | Sev | Location | Finding | Fix | Auto |
|---|---|---|---|---|---|
| F0060 | P1 | `client/src/components/admin/Customer360ProfileV2.jsx:5482` | Customer 360 window-level Escape closes the whole profile while its hand-rolled child modals (Edit customer, Refund, Prepay) are open; the m | Rebuild RecordPrepayModal, SendPrepayInvoiceModal, CancelSignupModal, RefundPaymentModal and the inline Edit-customer modal on ui/Dialog (size md, with layer once OV-01 lands); drop the profile-level window listener in f | owner |
| F0401 | P1 | `client/src/pages/admin/CommunicationsPageV2.jsx:2935` | Conversation, log and search-result rows are click-only <div>s — not keyboard/screen-reader reachable | Make each row a `<button type="button" className="w-full text-left ...">` (or `role="button" tabIndex={0}` + Enter/Space handler where a nested button is needed — move the profile link out of the row or render it as a si | no |
| F0500 | P1 | `client/src/pages/admin/PayerDetailSheet.jsx:121` | PayerDetailSheet and PhotoAssessmentDetailSheet render no close control; on phone the Sheet backdrop is unreachable so there is no way to di | PayerDetailSheet.jsx:123 — inside `<div className="flex items-start justify-between gap-3">` append after the Outstanding block: `<Button variant="ghost" size="sm" onClick={onClose} aria-label="Close">Close</Button>` (Bu | yes |
| F0534 | P1 | `client/src/pages/admin/SettingsPage.jsx:110` | Settings toggles are clickable divs — not keyboard or screen-reader operable | In SettingsPage.jsx Toggle (line 110) add to the outer control div: `role="switch" aria-checked={checked} aria-label={label} tabIndex={0} onKeyDown={(e) => { if (e.key === " " \|\| e.key === "Enter") { e.preventDefault() | yes |
| F0014 | P2 | `client/src/components/AdminLayoutV2.jsx:297` | Mobile nav drawer has no focus management: no trap, no Escape, page + tab bar stay tabbable behind the backdrop | client/src/components/AdminLayoutV2.jsx — add `import useModalFocus from "../hooks/useModalFocus";`; after line 102 add `const drawerRef = useModalFocus(isMobile && sidebarOpen, () => setSidebarOpen(false));`; on the `<a | yes |
| F0019 | P2 | `client/src/components/AdminLayoutV2.jsx:626` | Shell has no <main> landmark and no skip link — 30+ sidebar links precede page content on every route | 1) AdminLayoutV2.jsx:626 `<div` → `<main id="admin-main" tabIndex={-1}` (keep style, className="admin-main", ref={mainRef}); closing tag :659 `</div>` → `</main>`. 2) Insert as the first child of the `.admin-shell-v2` di | yes |
| F0040 | P2 | `client/src/components/NotificationBell.jsx:577` | Desktop bell drawer group headers paint a navy V1 stripe with ~3.6:1 grey text inside the white admin drawer | client/src/components/NotificationBell.jsx:578 `background: isDark ? colors.bg : 'rgba(255,255,255,0.75)',` — #64748B on #FFFFFF is 4.76:1 and clears AA without touching size or weight; optionally `borderBottom: isDark ? | yes |
| F0049 | P2 | `client/src/components/admin/CallRoutingSettingsV2.jsx:174` | Unlabelled switches: template active toggle and four call-routing toggles have no accessible name | CallRoutingSettingsV2.jsx: add `aria-label="Backstop unanswered calls"` (:174), `aria-label="AI answers first (override)"` (:182), `aria-label="Nightly answers-first schedule"` (:190), `aria-label="Spanish menu (press 2) | yes |
| F0086 | P2 | `client/src/components/admin/GlobalCommandPalette.jsx:1072` | Palette footer text is 10px in the border colour (#E2E8F0 on white) — effectively invisible | GlobalCommandPalette.jsx:1072 → `<span style={{ fontSize: 11, color: D.muted }}>`; :1093-1097 → `style={{ fontSize: 11, color: D.muted, fontFamily: "JetBrains Mono, monospace" }}`. | yes |
| F0090 | P2 | `client/src/components/admin/IntelligenceBarShell.jsx:244` | IB prompt inputs have no accessible name (placeholder only) | Add `aria-label="Ask Waves AI"` to IntelligenceBarShell.jsx:244 input and `aria-label="Follow-up question"` to :499; `aria-label="Ask Waves AI"` to GlobalCommandPalette.jsx:771 and :1264 inputs; `aria-label="Ask Waves AI | yes |
| F0091 | P2 | `client/src/components/admin/IntelligenceBarShell.jsx:449` | No live region anywhere in the IB: loading, answers and 'Error:' responses are silent to screen readers | IntelligenceBarShell.jsx:450 → `<div className="px-4 pb-4" role="status" aria-live="polite" aria-label="Thinking">`; :466 add `aria-live="polite"`; :471 add `role={isError ? "alert" : undefined}`. Same three edits on Glo | yes |
| F0094 | P2 | `client/src/components/admin/MobileNewCustomerSheet.jsx:230` | MobileNewCustomerSheet ships an 'Import from contacts' stub, fake dropdown chevrons and label-less inputs | Remove the stub and chevrons; add aria-label (or visible labels) to each input. | owner |
| F0139 | P2 | `client/src/components/estimate/TerminalStateCard.jsx:20` | Terminal-card 'Call' CTA is bright blue with white 15px text — fails AA contrast and sits outside the navy/gold system | TerminalStateCard.jsx: render the call CTA as `<a href={…} data-glass-accent="" style={callCtaStyle}>` and change callCtaStyle to `{ display: 'inline-flex', alignItems: 'center', minHeight: 44, marginTop: 16, padding: '0 | yes |
| F0160 | P2 | `client/src/components/report/termiteV2/TermiteReportV2.jsx:251` | Interactive targets below 44px: termite station toggle and warranty link, pest/mosquito rating buttons, flow-page retry buttons, RatePage sk | Add `minHeight: 44` (plus `display: 'inline-flex', alignItems: 'center'` and horizontal padding where the control is a bare text button) to: TermiteReportV2.jsx:255 and :361; PestReportV2.jsx:301; MosquitoReportV2.jsx:23 | yes |
| F0168 | P2 | `client/src/components/schedule/CreateAppointmentModal.jsx:2325` | Customer, service and discount search results in CreateAppointmentModal are clickable <div>s — not focusable, no role, unreachable by keyboa | At :2325, :3016 and :2953 change `<div` → `<button type="button"` (closing tag too), add `u-focus-ring` to className, and extend the existing style object with `width: '100%', textAlign: 'left', font: 'inherit', backgrou | yes |
| F0175 | P2 | `client/src/components/schedule/MarkPrepaidModal.jsx:202` | Schedule/Dispatch V2 overlay family is hand-rolled without focus trap, Escape, focus return or scroll lock; most lack role=dialog | Mechanical pass: `const panelRef = useModalFocus(true, onClose)` + ref/tabIndex/role/aria-modal/aria-labelledby on each panel (guarding onClose with the existing busy flags). Then migrate the center ones (Reschedule, Mar | owner |
| F0185 | P2 | `client/src/components/schedule/MobileCheckoutSheet.jsx:290` | 12 hand-rolled full-screen mobile sheets in components/schedule bypass ui/Dialog: no role=dialog, no focus trap, no Escape, drifting z-index | Create one shared MobileFullScreenSheet (or use Dialog size=lg) that owns role=dialog/aria-modal, useModalFocus(open,onClose), body lock, the 56px safe-area header and a single z-index ladder; convert the 12 files to it. | owner |
| F0197 | P2 | `client/src/components/schedule/MobileServiceEditModal.jsx:382` | Interactive inputs remove the outline with no replacement focus style (inline outline:'none' overrides u-focus-ring; outline-none with no ri | MobileServiceEditModal.jsx:382-390 delete `outline: 'none'` from the inline style (keep `u-focus-ring`). MobileCheckTenderSheet.jsx:94, MarkPrepaidModal.jsx:338, RescheduleConfirmModal.jsx:246: replace `outline-none` wit | yes |
| F0203 | P2 | `client/src/components/schedule/ScheduleListView.jsx:499` | Sortable column headers are plain `<th onClick>` — not keyboard reachable, and zero `aria-sort` anywhere in the client | ScheduleListView.jsx:499-506, for each sortable header: `<th className={thClass} aria-sort={sortCol === 'customerName' ? (sortDir === 'asc' ? 'ascending' : 'descending') : 'none'}><button type="button" onClick={() => tog | yes |
| F0208 | P2 | `client/src/components/schedule/TimeGridDay.jsx:387` | Time grids are pointer-only: appointment blocks are announced as buttons but Enter/Space does nothing, and no keyboard sensor or focusable s | Add an onKeyDown on AppointmentBlock/RailItem (Enter/Space → onEdit, Shift+Enter → onToggleSelect), register `KeyboardSensor` with `sortableKeyboardCoordinates` in both grids' `useSensors`, and give slot cells a focusabl | owner |
| F0238 | P2 | `client/src/components/ui/Tabs.jsx:29` | ui/Tabs is not an accessible tablist: no arrow-key navigation, no tab↔panel ARIA linkage, panel not focusable | Tabs.jsx: `const base = useId()` in `Tabs`, put `base` in the context; `Tab` adds `id={`${base}-tab-${value}`}`, `aria-controls={`${base}-panel-${value}`}`, `tabIndex={active ? 0 : -1}`; `TabList` gets an onKeyDown that, | yes |
| F0260 | P2 | `client/src/pages/AppointmentPage.jsx:412` | AppointmentPage has no h1 in any state; TrackPage EnRoute and Cancelled cards also skip the h1 the sibling states use | Promote the first heading of each state card to `<h1>` (glass sizes it; keep the inline size for non-glass) — AppointmentPage upcoming title, MessageCard title, load-error title; TrackPage EnRouteCard (wrap the ETA line  | owner |
| F0262 | P2 | `client/src/pages/CardPage.jsx:348` | Digital business card controls are below the 44px touch floor (40px share/save, 37px socials, ~36px Text/Call pills); BrandFooter socials ar | CardPage.jsx style block (:262): add `.wcard-tap { position: relative; } .wcard-tap::before { content: ''; position: absolute; inset: -4px; }` (40→48, 37→45, 36→44 hit areas, glyphs unchanged); BrandFooter.jsx:287-292: k | yes |
| F0270 | P2 | `client/src/pages/EstimateViewPage.jsx:2424` | EstimateViewPage modals: Escape only works while focus is inside, and none lock background scroll | EstimateViewPage.jsx: `import useLockBodyScroll from '../hooks/useLockBodyScroll';` and add `useLockBodyScroll(true);` beside the dialogRef line in DepositModal (:2424), CardHoldModal (:2684), RecurringCardModal (:2802), | yes |
| F0276 | P2 | `client/src/pages/PayPageV2.jsx:1584` | Surcharge quote on the pay page appears with no live region and the CTA label changes silently | In PayPageV2.jsx around 1584: render a persistent `<div role="status" aria-live="polite" aria-atomic="true" id="pay-review-totals">` unconditionally and place the existing conditional totals box inside it (empty when no  | yes |
| F0283 | P2 | `client/src/pages/PortalPage.jsx:3029` | Controls under the 44px floor remain after the batch-B sweep | PortalPage.jsx:3029 `height: 38` -> `height: 44`; 7258/7264 `width: 38, height: 38` -> `44`; 7213 `width: 32, height: 32` -> `44` (keep right: 8); replace the two inline close buttons at 6615-6619 and 6660-6664 with `<Sh | yes |
| F0304 | P2 | `client/src/pages/PortalPage.jsx:11015` | Year-at-a-glance calendar details are hover/focus-only and the targets are 26px — unreachable on touch | PortalPage.jsx:11015 add `onClick={() => setHoveredCalendarItem(prev => (prev === tooltipKey ? null : tooltipKey))}` and change `height: 26` to `minHeight: 44` on the button style; keep the 15px dot. | yes |
| F0319 | P2 | `client/src/pages/PublicBookingPage.jsx:1067` | Public booking address + unit inputs have no accessible label | PublicBookingPage.jsx:1067 add `aria-label="Street address"` to `<AddressAutocomplete`; 1106 add `aria-label="Apartment or unit (optional)"` to the line2 `<input`. | yes |
| F0335 | P2 | `client/src/pages/TrackPage.jsx:138` | TrackPage en-route status text uses marketing blue/green at 14px — fails AA contrast on the glass card | TrackPage.jsx:578: `color: TRACK_SURFACE.text` (the ink already used at :167) instead of `status.color`; leave `accent={status.color}` on the Card stripe. | yes |
| F0340 | P2 | `client/src/pages/admin/AdminInvoicesPage.jsx:610` | Invoices toast prefixes every message — including failures — with a green 'OK', has no aria-live, and vanishes in 3.5 s | Change to `showToast(msg, tone = 'ok')` storing `{ msg, tone }`; render the prefix ('OK' vs 'Error'), border colour (D.green vs D.red) from tone; add `role="status" aria-live="polite"`; raise fontSize to 13; hold tone 'e | yes |
| F0345 | P2 | `client/src/pages/admin/AdminInvoicesPage.jsx:1638` | Invoice row is a <button> that contains an <input type=checkbox> — nested interactive controls | Replace the row <button> with a `<div style={{ display: "flex", alignItems: "center", gap: 12, padding: rowPad, background: … }}>` holding the checkbox (add `aria-label={\`Select invoice ${inv.invoice_number}\`}`, drop t | yes |
| F0352 | P2 | `client/src/pages/admin/AdminInvoicesPage.jsx:6049` | Autocomplete results (customers, services, discounts) are mouse-only <div> rows; 29 of 32 form labels lack htmlFor | Give the three dropdown containers `role="listbox"` and render each row as `<button type="button" role="option" …>` (keep the onMouseDown preventDefault for the service/discount pickers so blur doesn't close them) and ad | yes |
| F0469 | P2 | `client/src/pages/admin/GBPManagement.jsx:124` | Inactive pill tabs in GBP and SEO Dashboard use #A1A1AA on #F4F4F5 (about 2.3:1 contrast) | GBPManagement.jsx:130 `color: isActive ? "#FFFFFF" : "#A1A1AA"` → `"#52525B"`; :144 gbpPillBadge inactive color `"#A1A1AA"` → `"#52525B"`; SEODashboardPage.jsx:796 `WAVES_COLORS.textMuted` → `WAVES_COLORS.textSecondary`. | yes |
| F0472 | P2 | `client/src/pages/admin/InventoryPage.jsx:407` | Inventory stat cards are clickable <div>s with no keyboard access or role | InventoryPage.jsx:407 — replace the `<div key={s.label} onClick=… style={{ …sCard, flex: "1 1 120px", minWidth: 120, marginBottom: 0, textAlign: "center", cursor: "pointer" }}>` with `<button type="button" key={s.label}  | yes |
| F0492 | P2 | `client/src/pages/admin/NewsletterPage.jsx:1036` | Icon-only Newsletter search button has no accessible name (the one confirmed exception in an otherwise well-labelled codebase) | NewsletterPage.jsx:1036-1042 — `<button type="button" onClick={doSearch} aria-label="Search" className="h-11 w-11 sm:h-8 sm:w-8 inline-flex items-center justify-center border-hairline border-zinc-300 rounded-sm hover:bg- | yes |
| F0498 | P2 | `client/src/pages/admin/PPCDashboardPage.jsx:583` | PPC Dashboard renders its own 28px h1 beneath the PPC command header (two h1s, off-contract title) | In PPCDashboardPage.jsx:578-608 remove the `<h1>Waves PPC command center</h1>` (the AdminCommandHeader already titles the view) and keep the flex row with the activeCampaigns badge and the 13px subtitle line; minimum fal | yes |
| F0554 | P2 | `client/src/pages/admin/ToolHealthPage.jsx:437` | ToolHealthPage accordion headers and error rows are clickable <div>s — no keyboard access, no aria-expanded | ToolHealthPage.jsx:436 → `<button type="button" aria-expanded={isOpen} className="u-focus-ring" onClick={…} style={{ all: "unset", boxSizing: "border-box", width: "100%", display: "flex", alignItems: "center", gap: 12, c | yes |
| F0103 | P3 | `client/src/components/admin/PricingLogicPanel.jsx:1131` | Inline `outline: 'none'` on inputs/textareas with no :focus-visible or focus-state replacement (15 verified sites) even though u-focus-ring  | Delete the `outline: 'none'` / `outline-none` entry at each admin/tech site listed and add className `u-focus-ring` (tokens.css:16-23 restores a :focus-visible ring); PricingLogicPanel :178/:1131/:1250/:1674, GlobalComma | yes |
| F0122 | P3 | `client/src/components/dashboard/charts.jsx:337` | Recharts charts have no accessible name or text alternative (only CaptureGauge does) | charts.jsx:337, :407, :873 wrapper div add `role='img'` and a computed aria-label (e.g. `Revenue trend: ${fmtMoney(currentTotal)} month to date vs ${fmtMoney(priorTotal)} prior`); KpiRing :261 `aria-hidden` when `display | yes |
| F0447 | P3 | `client/src/pages/admin/EstimateToolViewV2.jsx:1062` | Tier/frequency rows are click-only divs — not keyboard operable | In TierRowV2 (1051-1092) render `clickable ? <button type="button" aria-pressed={selected} className={cn("w-full text-left u-focus-ring", …same grid classes…)} onClick={onSelect}> : <div className=…>` — no other file cha | yes |


---

## J. Fixes implemented (this lane)

| PR | Scope | Findings closed | Tests | Status |
|---|---|---|---|---|
| #4085 `fix/c360-cancel-plan-dialog-layer` | `ui/Dialog` gains a `layer` prop (mirrors `ui/Sheet`) and stops click propagation at its root; `CancelPlanDialog` forwards `layer`; Customer 360 passes 1120. The audit's one P0 — the Cancel plan dialog opened invisibly beneath the profile overlay with `GATE_CANCEL_FLOW_V2` on in prod. | F0050 / F0074 (C360-01, OV-01), pre-push Codex P1 (click bubbling) | Dialog layer + click boundary; CancelPlanDialog forwards layer and stays usable inside a parent with `onClick`; two page tests re-pinned to the inline contract. Rendered before/after on the local QA stack (desktop 1440 + mobile 390) attached to the PR. | **Merged** 2026-09-07 10:29Z (8d7fa426b); not yet deployed — Railway's main deploy fails on migration `20260907000021` (another lane) |
| #4090 `fix/c360-submodal-integrity` | Customer 360: the four hand-rolled sub-modals move onto `useModalFocus` with dialog semantics; the profile's window Escape defers while any sub-modal is open; backdrop clicks stop at the sub-modal; portaled sub-modals restate `admin-shell-v2`; DATE columns render through `formatETDateOnly`; Comms thread opens at the latest message. | F0053, F0056, F0059, F0060, F0063, F0070 | `Customer360ProfileV2.submodals.test.jsx` (Escape, backdrop, DATE under ET/UTC/Pacific); refund Escape contract test updated. | Open, Codex review in progress |
| PR B `fix/schedule-mobile-audit-batch` | Schedule / dispatch: `MobileServiceEditModal` no longer rewrites `service_type` on every save; `ServiceOutlineComposerModal` above the mobile bars; `InlineTechPicker` surfaces a failed assignment; `CreateAppointmentModal` submit guard; `MarkPrepaidModal` safe-area + scroll; checkout cents; `ScheduleListView` sortable headers as buttons with `aria-sort`; dispatch stats-row spacing; estimates row-sheet inset; `text-15/20/24` → defined scale. | F0195, F0106, F0171, F0172, F0166, F0176, F0177, F0186, F0203, F0431, F0456, F0183, F0200 | `MobileServiceEditModal.test.jsx`, `InlineTechPicker.test.jsx`, `ScheduleListView.sort-a11y.test.jsx`, checkout cents case. | Open, Codex review in progress |

Visual changes: none intended beyond the dialog now being visible and the DATE day; behaviour changes are listed above. Tests run: the Customer 360 suites (56 tests) and the full client vitest run (2,748 pass; the 4 storage-mock failures fail identically on the untouched base under Node 26).

---

## K. Remaining work, ranked

### K.1 Verified, autofixable, queued as PR-sized units (155 findings)

Ordered by user impact × frequency; each row is one PR with its own tests.

1. **Schedule & dispatch mobile fixes** — `MobileServiceEditModal` rewrites `service_type` on every save (F0195, P1: appends "— Billed Monthly" to any type without a cadence keyword); `ServiceOutlineComposerModal` at `z-[70]` under the mobile bars (F0106, P1); `InlineTechPicker` swallows a failed assignment and closes as if it succeeded (F0171/F0172); `CreateAppointmentModal` sets `saving` only after an awaited re-quote (F0166); `MarkPrepaidModal` safe-area + max-height (F0176/F0177); checkout line items rounded to whole dollars (F0186); sortable headers as buttons with `aria-sort` (F0203); DispatchPageV2 stats row loses its spaces (F0431); `EstimatesPageV2` row-actions sheet ignores the home-indicator inset (F0456).
2. **Money-adjacent hardening in Invoices / Tax / Banking** — reverse-prepaid has no busy guard (F0342, two money-moving POSTs on a double click); Tax "Add expense" double-submit (F0543); A/R "Total Outstanding" white on white when ≤ $500 (F0547, P1); overdue check in browser-local time (F0343); invoice list refetch per keystroke with no latest-request guard (F0341); the invoices toast prefixes failures with a green "OK" and has no `aria-live` (F0340); negative EBITDA rendered "$-1,234" (F0570); Tax overview blank forever on failure (F0549); pricing / revenue / CSR panels treat error bodies as data (F0508, F0127); `RecapCapture` white on white (F0532, P1).
3. **Responsive & safe-area sweep** — 12 module-level `isMobile` snapshots → `useIsMobile` (F0442, F0542); `sTableWrap` `overflow: hidden` → auto (F0405); unwrapped tables in TimeTracking ×4, ToolHealth, EmailAutomations (F0552, F0555, F0438); portal Request overlay header under the notch (F0309, P1); Geofence prompt, tech bottom sheets, recap sheet z-order (F0219, F0042, F0223, F0589, F0594); admin NotificationBell panel offsets (F0034); cookie bar (F0107/F0108); estimator 3-column form rows (F0452); Triage action row (F0557); 100vh inside padded shells (F0331, F0592).
4. **Accessibility quick wins** — Settings toggles → `role="switch"` + keyboard (F0534/F0535, P1); PayerDetailSheet / PhotoAssessmentDetailSheet close control (F0500, P1); `AdminLayoutV2` `<main>` + skip link + drawer focus trap (F0019/F0020/F0014); CallRoutingSettings switch names (F0049); IB input name + live region (F0090/F0091); PublicBooking address labels (F0319); MobileNewCustomerSheet labels (F0095); Newsletter icon button (F0492); clickable divs → buttons in Inventory / ToolHealth / CreateAppointment search (F0472, F0554, F0168); nested interactive in the invoice row (F0345); `ui/Tabs` arrow keys + `aria-controls` (F0238); pay-page surcharge live region (F0276); estimate modals Escape + scroll lock (F0270, F0273).
5. **Navigation & token hygiene** — `/admin` index redirect without `replace` traps Back (F0008/F0009); `/admin/*` wildcard so unknown staff URLs stop landing on the customer login (F0013); SMS-guard alert links to unmounted `/admin/sms-templates` (F0613); TimeTracking tabs into the URL (F0550); `Badge` unknown tones (F0227/F0410 — 14 call sites render unstyled); undefined `text-10/15/20/24` classes (F0200/F0366/F0402/F0450); admin raw `<a href>` full reloads (F0565); Email search debounce (F0579); email composer sends unescaped `<` as HTML (F0578).
6. **States & silent failures** — Equipment maintenance / mileage saves (F0441), Compliance "Loading forever" (F0406), Inventory products load (F0474), Payers empty-on-error (F0502), Turf height confirm (F0558), portal property switch (F0314), Decline/Follow-up/Extend dialogs masking server errors (F0076).
7. **Typography below the caption floor (9–10px)** — the unambiguous sites only: Banking/Tax/Inventory 9–10px headers (F0373), PPC capacity cards (F0357), GBP badges (F0470), Social chart labels (F0540), palette 10px footer (F0086), ReviewVelocity 10–11px (F0517), grid chips 8–9px (F0212). The 11–13px population waits on ruling K.2-1.

### K.2 Needs an owner ruling before code moves (45 findings)

The most consequential first; the full table follows.

1. **Which text floor governs Tier 1 admin: the consistency contract's 13/12/11 scale or the 14px hard line** (F0228 and ~3,500 sites). Every reader hit this. Until ruled, sweeps stop at 10px.
2. **Customer 360 IA**: keep or delete the Health Radar (real sub-scores exist, F0062); "Add Note" — build the note composer or remove the two entries (F0065); reuse `CustomerSmsPanel` in the Comms tab (F0071); collapse the seven identical header pills (F0064); the duplicated mobile bars (F0073).
3. **Tax A/R "Send Reminder"** posts to a route that does not exist (F0544, P1): remove it, or route through the invoice follow-up sequence (which owns stop-on-payment and quiet hours).
4. **"Charge card on file" from Invoices** charges off-session with one tap, no amount, no confirmation (F0348, P1): add a quote → confirm step, or keep one-tap for field speed.
5. **Pipeline**: keep or delete the unrouted Unified Pipeline directory (2,660 lines); does "Convert to Customer" survive; which lead-close path is canonical; give the mobile estimates list the desktop actions (F0467, P1).
6. **Customer pills** after the 2026-09-04 "no status chips" ruling: hero/re-entry status pills (F0326), six V2 report folders (F0153), weather/score/TrendChip (CRF-19).
7. **Dead code deletion** (37 unreachable files, ~12,800 lines + 2 dead-import panels) and the reachability test that stops it recurring.
8. **Palette folds**: PricingLogicPanel / PushSettings / MileageTab / JobFormSection / GlobalCommandPalette slate-teal → zinc (F0102, F0080); AutonomousContentReview forest-green hero (AM-01); dispatch grid `#3B82F6` blocks; dashboard `.dashboard-blackout` override.
9. **Intelligence Bar chrome**: one implementation (F0079/F0080) and which follow-up pattern survives.
10. **ServiceOutlinePage** as a Tailwind page on a customer glass surface (F0332, P1): sanctioned third style or theme-doc migration.
11. **ContentCalendar** phone layout (F0408, P1) and the marketing sub-tab / duplicated-dashboard IA.
12. **Invented visit descriptions** in the portal Visits tab (F0288, P1): real copy from the schedule payload, or none.
13. **NewsletterPage** mixed Tier 1 + inline leaf row (F0493, P1).
14. **BrandButton secondary** = 40px choice chip or 48px action; the shared money formatter's null semantics; CardPage dark glass; the mobile cash numpad (decorative today).

Full table of the 45:

| # | Sev | Location | Decision needed | Verifier note |
|---|---|---|---|---|
| F0057 | P1 | `client/src/components/admin/Customer360ProfileV2.jsx:4068` | 47 hand-rolled fixed overlays in 25 files have no dialog role, aria-modal, focus trap, or Escape close despite Dialog/Sh | Customer360ProfileV2.jsx: `role="dialog"` 0 hits, `aria-modal` 0, `useModalFocus` 0, `fixed inset-0` 8, createPortal ×7 (:4067, :4492, :4742, :5005, :5519, :5538, :5901); the only Escape listeners are :4945 (capture-phas |
| F0060 | P1 | `client/src/components/admin/Customer360ProfileV2.jsx:5482` | Customer 360 window-level Escape closes the whole profile while its hand-rolled child modals (Edit customer, Refund, Pre | Same defect as F0059 plus the file-scoped half of F0057 — treat as a duplicate of those two. Evidence: Customer360ProfileV2.jsx:5480-5486 unconditional window Escape → onClose; :4938-4952 refund-only capture swallow; the |
| F0228 | P1 | `client/src/components/ui/Button.jsx:10` | Tier 1 primitives encode 11–13px text, contradicting the 14px 'hard line' the skill and DECISIONS call unconditional | Primitives verified: client/src/components/ui/Button.jsx:10-11 `sm: '... text-11 ...'`, `md: '... text-12 ...'`; Badge.jsx:5 `text-11`; Tabs.jsx:40 `text-12`; Table.jsx:47 `text-11` (TH); Input.jsx:11-12 `md:text-12` / ` |
| F0288 | P1 | `client/src/pages/PortalPage.jsx:3960` | Visits tab prints invented per-visit treatment descriptions | client/src/pages/PortalPage.jsx:3930-3958 is a static table (`'Lawn': ['Spring green-up fertilizer + pre-emergent weed control', ..., 'Summer fertilizer + chinch bug preventive', ...]`) and 3960-3966 picks by index: `ret |
| F0332 | P1 | `client/src/pages/ServiceOutlinePage.jsx:186` | ServiceOutlinePage is an admin-token Tailwind page on a customer glass surface: 12px text, a status chip, 6px/3px radii | ServiceOutlinePage.jsx:186 `rounded-xs border border-waves-success-border bg-waves-success-bg px-3 py-1 text-xs font-semibold uppercase` chip; :246 `text-xs uppercase tracking-wide text-zinc-500`; :248 `text-xs text-zinc |
| F0348 | P1 | `client/src/pages/admin/AdminInvoicesPage.jsx:1871` | 'Charge card on file' from Invoices collects an off-session charge with one tap, no amount and no confirmation | AdminInvoicesPage.jsx:1871-1878 `{isAdminUser && canCollect && cardOnFile && !inv.payer_id && (<button onClick={() => setCardOnFileInvoice(inv)} …>Charge card on file</button>)}` -> :2241-2250 `<MobileCardOnFileSheet des |
| F0408 | P1 | `client/src/pages/admin/ContentCalendar.jsx:388` | ContentCalendar month grid uses 9px item labels and has no phone layout | ContentCalendar.jsx:384-401 scheduled item `style={{ fontSize: 9, padding: "2px 4px", ... whiteSpace: "nowrap" }}` rendering `{item.title?.substring(0, 25)}`; :404-411 `fontSize: 9` for `+{dayItems.length - 3} more`; :31 |
| F0467 | P1 | `client/src/pages/admin/EstimatesPageV2.jsx:3365` | Mobile estimates list has no Follow Up, Mark lost, Schedule or proposal actions that desktop rows carry | Desktop pipeline view owns the transitions: state at 1700-1707 (`followUpTarget`, `declineTarget`, `scheduleEstimate`), modals at 2016 `<FollowUpModalV2`, 2026 `<DeclineModalV2`, 2068 `<CreateAppointmentModal`, row actio |
| F0493 | P1 | `client/src/pages/admin/NewsletterPage.jsx:1715` | NewsletterPage (Tier-1) mixes an inline-styled sub-tab row and inline fontSize into a components/ui page | NewsletterPage.jsx imports the Tier-1 system at :24 (`import { Badge, Button, Card, CardBody } from "../../components/ui";`) yet renders the leaf row at :1715-1737 as `<button … style={{ display: "inline-flex", …, height |
| F0544 | P1 | `client/src/pages/admin/TaxPage.jsx:3776` | Tax A/R 'Send Reminder' posts to a non-existent endpoint — every click fails | client/src/pages/admin/TaxPage.jsx:3776-3783 `await adminFetch("/admin/sms/send", { method: "POST", body: JSON.stringify({ to: inv.phone, message: \`Hi ${inv.customerName}, this is Waves Pest Control. You have an outstan |
| F0062 | P2 | `client/src/components/admin/Customer360ProfileV2.jsx:5820` | Health Radar plots fabricated values while the real sub-scores are already in the payload | Real: Customer360ProfileV2.jsx:5820-5850 builds `radarData = hs.risk_factors ? [{Payment:80},{Engagement:60},{Service:70},{Satisfaction:75},{Tenure:90},{Revenue:65}] : [score+10, score, score+5, score-5, tenure/3.65, lif |
| F0064 | P2 | `client/src/components/admin/Customer360ProfileV2.jsx:6072` | Desktop header stacks seven identical black pill buttons (hand-rolled, all primary) | Customer360ProfileV2.jsx:6077-6132: seven elements share the identical class string `inline-flex items-center h-8 px-3.5 text-11 uppercase tracking-label font-medium rounded-sm bg-zinc-900 text-white no-underline hover:b |
| F0065 | P2 | `client/src/components/admin/Customer360ProfileV2.jsx:6119` | 'Add Note' (header pill + ⋯ menu) is a dead action that lands on the customer SMS composer | Dead action confirmed: Customer360ProfileV2.jsx:6119-6124 header 'Add Note' `onClick={() => setActiveTab("comms")}` and :8162-8170 menu item `onClick={() => { setActiveTab("comms"); setMenuOpen(false); }}` do nothing els |
| F0071 | P2 | `client/src/components/admin/Customer360ProfileV2.jsx:7255` | 360 Comms tab never marks texts read and duplicates a weaker composer instead of CustomerSmsPanel | Every code fact checks out: client/src/components/admin/Customer360ProfileV2.jsx:49-97 imports contain no CustomerSmsPanel/useCustomerSms; `/admin/communications/messages/read` + `notifyUnreadChanged` are called only fro |
| F0073 | P2 | `client/src/components/admin/Customer360ProfileV2.jsx:8059` | Mobile: Text/Call duplicated in both fixed bars and the 768px breakpoint disagrees between them | Both claims hold. Breakpoint disagreement: client/src/components/admin/Customer360ProfileV2.jsx:5917 `@media (max-width: 768px) {` (inclusive) shows `.c360-mobile-actionbar`, `.c360-header-mobile`, `.c360-mobile-footer-s |
| F0079 | P2 | `client/src/components/admin/GlobalCommandPalette.jsx:33` | Four independent IB implementations: the primary ⌘K palette does not use the 'shared' IntelligenceBarShell/useIntelligen | The duplication facts hold: GlobalCommandPalette.jsx:33 `const RECENTS_KEY = "admin_ib_recents"` with its own loadRecents/saveRecent (:176-196, no context in the key) vs client/src/utils/ibStorage.js:10 `return `ib.${kin |
| F0080 | P2 | `client/src/components/admin/GlobalCommandPalette.jsx:47` | ⌘K desktop palette is Tier-2 inline styling with a page-local D palette, per-context accent colours, 16px radius, pill c | Evidence holds: GlobalCommandPalette.jsx:47-58 page-local slate `D` (`bg: "#F1F5F9"`, `border: "#E2E8F0"`, `teal: "#0A7EC2"`, `purple: "#7C3AED"` …); :122-138 `CONTEXT_COLORS` maps routes to teal/purple/green/amber; the  |
| F0094 | P2 | `client/src/components/admin/MobileNewCustomerSheet.jsx:230` | MobileNewCustomerSheet ships an 'Import from contacts' stub, fake dropdown chevrons and label-less inputs | client/src/components/admin/MobileNewCustomerSheet.jsx:227-235 `{/* Import from contacts (stub — needs native bridge) */} <button type="button" onClick={() => alert("Import from contacts — coming soon")} className="w-ful |
| F0153 | P2 | `client/src/components/report/lawnV2/LawnReportV2.jsx:188` | All six V2 report folders still ship status pills (StatusPill 'Watch/Attention', ACTIVE/COMPLETE badges, score pill, due | The pills exist as cited: LawnReportV2.jsx:188-197 `StatusPill … padding: small ? '3px 9px' : '6px 12px', borderRadius: 999` used at :506, :576, :696, :920 and TreeShrubReportV2.jsx:333, :402, :441, :481; TermiteReportV2 |
| F0175 | P2 | `client/src/components/schedule/MarkPrepaidModal.jsx:202` | Schedule/Dispatch V2 overlay family is hand-rolled without focus trap, Escape, focus return or scroll lock; most lack ro | Facts hold. MarkPrepaidModal.jsx:202-211 `return createPortal(<div onClick={dismiss} className="fixed inset-0 z-[1200] flex items-end md:items-center justify-center" …><div onClick={(e) => e.stopPropagation()} className= |
| F0181 | P2 | `client/src/components/schedule/MobileAppointmentDetailSheet.jsx:417` | No shared sheet implementation: 10 full-screen sheets and 8 overlay modals each hand-roll their shell; none use ui/Sheet | Facts hold. MobileAppointmentDetailSheet.jsx:415-417 `return createPortal(<div className="fixed inset-0 z-[100] bg-white overflow-y-auto" …` with no role/aria-modal/keydown/focus hook (scan: Escape=0 useModalFocus=0 role |
| F0183 | P2 | `client/src/components/schedule/MobileAppointmentDetailSheet.jsx:826` | Undefined Tailwind classes text-15 and text-20 are used for headings and controls; they resolve to nothing so the elemen | client/tailwind.config.js:67-76 `fontSize: { '11': …, '12': …, '13': …, '14': …, '16': …, '18': …, '22': …, '28': … }` — no 15, no 20; :98 `plugins: []`, no safelist; grep of every *.css/*.html under client (excluding no |
| F0185 | P2 | `client/src/components/schedule/MobileCheckoutSheet.jsx:290` | 12 hand-rolled full-screen mobile sheets in components/schedule bypass ui/Dialog: no role=dialog, no focus trap, no Esca | Same finding as F0181 (and F0175 for the centre modals); facts verified independently. MobileCheckoutSheet.jsx:290 `<div className={`fixed inset-0 z-[105] bg-white overflow-y-auto ${desktopVisible ? '' : 'md:hidden'}`}>` |
| F0190 | P2 | `client/src/components/schedule/MobileDispatchList.jsx:211` | 'Paid' status chip is hand-rolled in five schedule files at 10-11px with different heights; ui/Badge exists and 10px tex | Duplication + sub-floor sizes hold. MobileDispatchList.jsx:211-224 `<span className="inline-flex items-center rounded-full uppercase tracking-label font-medium" style={{ height: 18, padding: '0 8px', background: '#DCFCE7 |
| F0208 | P2 | `client/src/components/schedule/TimeGridDay.jsx:387` | Time grids are pointer-only: appointment blocks are announced as buttons but Enter/Space does nothing, and no keyboard s | Pointer-only is proven: TimeGridDay.jsx:387-398 `<div ref={setNodeRef} {...attributes} {...listeners} onClick={(e) => { if (isDragging) return; … onEdit?.(service); }}` and TimeGridDays.jsx:235-243 identical; sensors are |
| F0225 | P2 | `client/src/components/tech/WdoIntelligenceBar.jsx:271` | WdoIntelligenceBar (also mounted in admin ProjectsPage) uses 800/900 weights and 10–11px copy | Typography verified in client/src/components/tech/WdoIntelligenceBar.jsx: :271 `fontSize: 12, fontWeight: 900` (header); :289-290 `fontSize: 11, fontWeight: 900` (Analyze); :315-316, :349-350, :368-369, :416, :467 `fontS |
| F0260 | P2 | `client/src/pages/AppointmentPage.jsx:412` | AppointmentPage has no h1 in any state; TrackPage EnRoute and Cancelled cards also skip the h1 the sibling states use | client/src/pages/AppointmentPage.jsx has zero `<h1` (grep -c = 0); every title is `<div data-gt="h3x" …>` — :129 (MessageCard), :366 (load error), :412 (main 'Your {serviceLabel} is …'), :536, :550, :560 — and the shell  |
| F0326 | P2 | `client/src/pages/ReportViewPage.jsx:2530` | Service report hero and re-entry card still render status pills ('Ready now', 'Service complete', 'Follow-up recommended | Both pills exist as cited: client/src/pages/ReportViewPage.jsx:2530-2534 `<div className={`status-badge status-${smartStatus.statusTone \|\| (readinessBadge?.ready ? 'ready' : 'pending')}`}>{smartStatus.status \|\| readi |
| F0420 | P2 | `client/src/pages/admin/DashboardPageV2.jsx:577` | Type scale drifts from the contract: sub-11px chips and ticks, 28/32px 700-weight page title, `font-bold` KPI values on  | client/src/pages/admin/DashboardPageV2.jsx:577 `<h1 className="text-28 font-normal tracking-h1 mt-1 max-md:mt-2">` and :578-580 `<span className="md:hidden" style={{ fontSize: 32, fontWeight: 700, lineHeight: 1.1 }}>`; c |
| F0432 | P2 | `client/src/pages/admin/DispatchPageV2.jsx:1476` | List mode keeps the Prev/Next/Today date nav and month header live even though ScheduleListView ignores the page date | client/src/pages/admin/DispatchPageV2.jsx:1476-1486 `{viewMode === "list" && (<ScheduleListView technicians=… onEdit=… onRefresh={() => fetchSchedule(date)} refreshKey=… lastSave=… />)}` passes no date; client/src/compon |
| F0473 | P2 | `client/src/pages/admin/InventoryPage.jsx:438` | 9–10px labels/badges throughout Tier 2 ops pages (below even the contract's 11px caption floor) | Verified at every cited site: InventoryPage.jsx:114 `fontSize: 10,` (sBadge), :132 `fontSize: 10,` (thS), :438 / :1152 / :4764 / :4784 / :4804 `fontSize: 9,` (KPI captions 'Templates', 'COGS', 'Missing', stat labels); La |
| F0488 | P2 | `client/src/pages/admin/LeadsTabs.jsx:4331` | 'Convert to Customer' asks the operator to paste a raw customer UUID | The defect is real: LeadsTabs.jsx:4331-4337 `<Input label="Customer ID (required)" value={formData.customer_id} … placeholder="Existing customer UUID" />`, submit at :1157-1162 `if (!formData.customer_id) { alert("Custom |
| F0511 | P2 | `client/src/pages/admin/ProjectsPage.jsx:3776` | ProjectsPage mixes Tier 1 primitives + Tailwind with 157 inline style blocks and hand-rolled buttons/inputs | The mix is real inside single components, not just the file: client/src/pages/admin/ProjectsPage.jsx:5 imports `Badge, Button, Dialog, DialogBody, DialogFooter, Select`; useConfirmDialog (:46-109) renders the ui `<Dialog |
| F0551 | P2 | `client/src/pages/admin/TimeTrackingPage.jsx:258` | Nine hand-copied fixed toasts, none safe-area aware, inconsistent corner/colour, one prints a literal 'OK' | All nine verified. Bottom-right green-bordered card copies: TimeTrackingPage.jsx:258-282 (`position: "fixed", bottom: 20, right: 20, background: D.card, border: \`1px solid ${D.green}\` … fontSize: 12, zIndex: 300` and l |
| F0581 | P2 | `client/src/pages/admin/pipeline/UnifiedPipelineView.jsx:124` | Entire client pipeline/ directory (Unified Pipeline + second stage model) is unreachable since May but still maintained | Unreachable is confirmed: App.jsx:368 `const AdminPipelinePage = lazyWithRetry(() => import('./pages/admin/EstimatesPageV2'));` mounted at :568 `<Route path="pipeline" …>`; a grep for UnifiedPipelineView/OpportunityActio |
| F0591 | P2 | `client/src/pages/tech/TechHomePage.jsx:1689` | Next Stop action row packs up to six ~34px-tall, 12px-label buttons in one non-wrapping row; the next action is not visu | Facts hold: TechHomePage.jsx:725-757 renders Navigate, Protocol, Quick Move, En Route (primary) plus On Site (primary, `ON_SITE_ELIGIBLE = new Set(['en_route'])` :196) and Sync Stop (primary, `stopOutOfSync` :403) inside |
| F0597 | P2 | `client/src/pages/tech/VisitBriefPanel.jsx:467` | In-brief service action buttons (Report / Photos / Zone / 🚩) are ~32px tall with 12px labels; the flag button is icon-on | Measurements hold: VisitBriefPanel.jsx:467-471 `const btn = { padding: '8px 10px', borderRadius: 6, fontSize: 12, fontWeight: 600, border: … }` (≈32px tall at 12px), and :495 `<button onClick={() => onLead(service)} aria |
| F0605 | P2 | `client/src/utils/admin-fetch.js:17` | Admin fetch is fragmented: 2 shared wrappers with different contracts plus 81 inline adminFetch copies | Counts reproduce: 80 files define their own `function adminFetch`/`const adminFetch` outside the two helpers, 49 import utils/admin-fetch, 27 import lib/adminFetch, 135 non-test files read `waves_admin_token`. Contracts  |
| F0606 | P2 | `client/tailwind.config.js:67` | `text-10` / `text-15` / `text-20` / `text-24` are not Tailwind tokens — the classes emit no CSS and silently inherit the | Bug is real: client/tailwind.config.js:67-76 `fontSize` extends only '11','12','13','14','16','18','22','28', `plugins: []` (:98), no safelist/presets, postcss.config.js plain tailwindcss 3.4 (no numeric default scale),  |
| F0155 | P3 | `client/src/components/report/lawnV2/LawnReportV2.jsx:282` | Off-scale font sizes (14.5 / 15 / 15.5 / 16.5 / 17px) throughout the V2 report bodies and one flow page | The sizes are real — LawnReportV2.jsx:282 `fontSize: 14.5`, :330 `fontSize: 16.5`, :333 `fontSize: 14.5`; TermiteReportV2.jsx:331 `fontSize: 17`; CockroachReportV2.jsx:80 `fontSize: 15`; ReportViewPage.jsx:7300 `.sr-cell |
| F0275 | P3 | `client/src/pages/PayPageV2.jsx:652` | Pay page still renders a 'No fees' status pill inside the Zelle panel despite the no-chips ruling | The span exists exactly as cited (client/src/pages/PayPageV2.jsx:652-665: `textTransform: 'uppercase' ... borderRadius: 999 ... No fees`). But the ruling it invokes does not cover it. docs/design/DECISIONS.md:2043 (4): " |
| F0278 | P3 | `client/src/pages/PortalPage.jsx:1` | Giant single-file pages: PortalPage 16,168 lines / 238 useState, Customer360ProfileV2 8,474 lines / 145 useState | Size facts verified: `wc -l` = 16168 (PortalPage.jsx), 9636 (ReportViewPage.jsx), 8474 (Customer360ProfileV2.jsx); grep counts 238 `useState(`, 50 `useEffect(`, 55 top-level `function [A-Z]` components in PortalPage; C36 |
| F0371 | P3 | `client/src/pages/admin/AutonomousContentReviewPage.jsx:296` | Autopilot tab renders a TruGreen-style forest-green/lime brand hero, cream page background and iPhone mockup inside /adm | The rendered facts are accurate — client/src/pages/admin/AutonomousContentReviewPage.jsx:296 `bg-[#FAF7EF]` (non-embedded only), :298 `rounded-2xl bg-[#143D2A]`, :299 `bg-[#43B02A]/25 blur-3xl`, PhoneFrame, autonomous-co |
| F0475 | P3 | `client/src/pages/admin/InventoryPage.jsx:2535` | Money and count columns in Tier 2 tables are left-aligned with no tabular figures | Partly refuted. InventoryPage.jsx:2535-2551 — both cited cells set `fontFamily: "'JetBrains Mono', monospace"`; monospace figures are tabular by construction, so 'no tabular figures' does not hold for the cited cells. Ad |
| F0561 | P3 | `client/src/pages/admin/WinLossSlicesCard.jsx:32` | 148 raw <table> in admin vs 10 ui/Table consumers; Tier-1 files drop the overflow-x wrapper ui/Table provides | Duplication half holds: ui/Table.jsx:4-9 wraps `<table>` in `<div className="w-full overflow-x-auto">`; WinLossSlicesCard.jsx hand-rolls seven `<table className="w-full text-13">` (:32, :154, :191, :216, :255, :276, :328 |

### K.3 Verified but larger than a local fix (11 findings)

| # | Sev | Cat | Location | Work |
|---|---|---|---|---|
| F0401 | P1 | a11y | `client/src/pages/admin/CommunicationsPageV2.jsx:2935` | Conversation, log and search-result rows are click-only <div>s — not keyboard/screen-reader reachable |
| F0156 | P1 | responsive | `client/src/components/report/lawnV2/LawnReportV2Section.jsx:33` | Lawn and Tree & Shrub V2 dashboards are nested inside the visit-summary card with an extra 16px inset — ~160px text measure at 320 |
| F0162 | P2 | duplication | `client/src/components/schedule/BillingLaneCard.jsx:27` | Money formatting is re-implemented 14 times across the sheet family with divergent output; the shared lib/money.js fmtMoney is unu |
| F0322 | P2 | duplication | `client/src/pages/ReceiptPage.jsx:242` | Pay, Receipt, Contract and Outline pages hand-roll their outage/retry UI instead of PublicLoadError (sub-44px buttons, missing ret |
| F0481 | P2 | duplication | `client/src/pages/admin/LawnProtocolCommandCenterPage.jsx:414` | Treatment Plans (LawnProtocolCommandCenter) adds nested page padding when embedded and hand-rolls buttons/pills instead of ui prim |
| F0126 | P2 | responsive | `client/src/components/dispatch/DispatchBoardLayout.jsx:49` | Dispatch board is sized 100vh-64px inside a shell with no 64px bar, so part of the board is always clipped (desktop and mobile) |
| F0055 | P2 | states | `client/src/components/admin/Customer360ProfileV2.jsx:3372` | 'Charge now' is offered to every lane; the server refuses non-monthly customers after the confirm dialog |
| F0031 | P2 | tokens | `client/src/components/NewsletterSignup.jsx:125` | check-portal-brand never scans components/ root, so NewsletterSignup/GlassNewsletterCard/PublicLoadError ship 12-13px text, 800/85 |
| F0128 | P2 | tokens | `client/src/components/dispatch/TechCard.jsx:19` | Customer brand colors and weight 700 leak into admin dispatch chrome |
| F0003 | P2 | typography | `client/src/App.jsx:46` | Customer weights above 700 and 620/640 one-offs live outside the portal-brand gate's scan roots |
| F0479 | P2 | ux | `client/src/pages/admin/LawnAssessmentPanel.jsx:269` | Native alert()/confirm() dialogs for outcomes — including success — across ops pages |

### K.4 Programmes (multi-PR, from sections C, G, H, I)

1. Sub-modal / sheet consolidation: every Tier 1 hand-rolled overlay onto `ui/Dialog` (with `layer`) or `ui/Sheet`; one `MobileSheetFrame` for the 12 schedule sheets; `ui/Dialog` scroll lock → `useLockBodyScroll`.
2. `ui/State` + `ui/Toast` and the retirement of 294 `alert()` outcomes and 15 toast copies.
3. Admin fetch consolidation (81 inline copies → `utils/admin-fetch`) with a domain-rules grep.
4. `ui/Table` growth (`SortableTH`, `Pagination`, `minWidth`) and the two mechanical table PRs (wrappers; `adminLegacy.js`).
5. Tier 2 palette convergence (`theme-admin.js`) and per-page Tier 1 migrations in the order of section I.
6. Glass edge collapse in the sequence of section G.
7. Customer portal local-style folding into the brand primitives; report V2 `reportStyles.js`.
8. Enforcement: `check-portal-brand` scope widening, `eslint-plugin-jsx-a11y` as CI warnings, undefined-class grep, import-graph reachability test.

### K.5 Not verified in this pass

364 P2/P3 findings that were neither P1 nor flagged autofixable were not sent through the refute pass (all remain in `phase1-merged.json` with their evidence lines). They are consistency, duplication, typography and dead-code observations; treat each as "reader-reported" until a fix PR re-reads its lines.

---

## Appendix: verification log

## Refuted

| uid | reader id | file | claim | why refuted |
|---|---|---|---|---|
| F0145 | glass-census:GL-05 | `client/src/components/estimate/glass/glass-components.css:115` | Glass component sheet ships 11px/12px type on customer estimate chips (gate cannot see .css) | None of the three rules renders under 14px. (1) `.gc-slot-priority` (glass-components.css:108-123, 11px) and `.gc-slot-rain` (:126-139, 12px) have no consumer: `grep -rn "gc-slot-" client/src \| grep -v .css:` returns nothing (JSX, JS and the glass engine included) — they are dead CSS, not customer  |
| F0230 | overlays:OV-03 | `client/src/components/ui/Dialog.jsx:41` | ui/Dialog and ui/Sheet lock scroll with body overflow:hidden, which the repo's own hook documents as ignored b | The cited code is real — client/src/components/ui/Dialog.jsx:39-44 and Sheet.jsx:11-16 set `document.body.style.overflow = 'hidden'`, and hooks/useLockBodyScroll.js:8-9 says iOS ignores that — but the claimed defect cannot occur where these primitives live. Every consumer renders inside AdminLayoutV |
| F0363 | agents-ib:AIB-12 | `client/src/pages/admin/AgentOpsPage.jsx:46` | Five hub tabs are Tier-2 light-D with blue/green/amber/red status chips and 700 weights, beside four Tier-1 ta | The finding restates a documented repo decision as a bug. docs/design/DECISIONS.md:627-629 (2026-06-12, agents-hub-consolidation): '`AgentsHubPage`, Tier 1 V2 shell … Both pages stay in place as files (Tier 2 styles intact) and gain an `embedded` prop'. The later tabs record the same choice in their |
| F0372 | billing-admin:BA-18 | `client/src/pages/admin/BankingPage.jsx:44` | Banking and Tax force a page-specific JetBrains Mono stack on every amount, against the Roboto-only contract | The claimed outcome ('switch to a monospace family… browser falls back to Menlo/Courier') cannot happen: client/src/index.css:188-193 `html.admin-app, html.admin-app *, .admin-shell-v2, .admin-shell-v2 * { font-family: "Roboto", system-ui, sans-serif !important; }` and :194-207 apply the same `!impo |

## Unverifiable

| uid | file | claim | reason |
|---|---|---|---|

## Downgraded

| uid | file | claim | from → to | why |
|---|---|---|---|---|
| F0013 | `client/src/App.jsx:675` | No catch-all inside /admin — any unmatched admin URL drops staff onto the customer portal login | P2 → P3 | Code facts hold: the /admin layout route (App.jsx:562-676) has no `path="*"` child, so /admin/<unknown> matches the `/*` route (:677-686) → ProtectedRoute → glass 'Loading your portal' (:443-491) → `< |
| F0023 | `client/src/components/BiometricGate.jsx:21` | BiometricGate hand-copies the scene gradient and orb list (and already drifted: 4 orbs vs the engine's 5) | P2 → P3 | Drift is real: glass-engine.js:56-62 renders five orbs including `['44%', '40%', '220px', 'rgba(120,200,255,.28)']` (:61) and glass-engine.test.js:37 pins `toHaveLength(5)`; BiometricGate.jsx:31-36 GL |
| F0030 | `client/src/components/InstallPrompt.jsx:76` | InstallPrompt hand-rolls the soft/chip material on a stale premise (login page IS under the glass theme now) | P2 → P3 | Premise verified stale: InstallPrompt.jsx:29 `if (window.location.pathname !== '/login') return;`, :40 `if (pathname !== '/login') setShow(false);`, :59 `if (!show \|\| !mobileEligible \|\| pathname ! |
| F0032 | `client/src/components/NotificationBell.jsx:116` | Background pollers keep firing in hidden tabs: only 4 of ~14 network pollers check document.visibilityState | P2 → P3 | The ungated ticks are real: client/src/components/NotificationBell.jsx:116-120 `useEffect(() => { fetchCount(); const iv = setInterval(fetchCount, 30000); return () => clearInterval(iv); }, []);` (the |
| F0039 | `client/src/components/NotificationBell.jsx:576` | Customer notification sheet: hand-rolled frosted group header at 11px inside the modal tier | P2 → P3 | The line is real and is a hard-line violation: client/src/components/NotificationBell.jsx:576-580 `padding: '8px 20px', fontSize: 11, fontWeight: 700, color: colors.muted, textTransform: 'uppercase',  |
| F0042 | `client/src/components/ServiceRecapModal.jsx:385` | Tech-portal bottom sheets have no safe-area-inset-bottom padding; bottom controls sit under the iOS home indic | P1 → P2 | Real at all five sheets and `grep -c safe-area` is 0 in each: client/src/components/ServiceRecapModal.jsx:374-390 `position: 'fixed', inset: 0, zIndex: 1000, … alignItems: 'flex-end'` with the panel ` |
| F0050 | `client/src/components/admin/CancelPlanDialog.jsx:222` | Cancel plan dialog renders underneath the 360 overlay (Dialog z-120 vs profile z-1000) | P0 → P1 | The stacking failure is mechanically certain: client/src/components/admin/CancelPlanDialog.jsx:203 `<Dialog open onClose={() => !running && onClose()} size="md">` passes no className/style, so the roo |
| F0051 | `client/src/components/admin/Customer360ProfileV2.jsx:104` | Inline adminFetch duplicate (no 401/429 handling) plus raw fetch in billing panels; autopay-state errors are s | P2 → P3 | Facts hold: Customer360ProfileV2.jsx:104-131 defines a local adminFetch with no 401 redirect / 429 path; :3204-3211 (BillingLanePanelV2 PUT) and :3287-3294 (autopay-state: `.then((r) => (r.ok ? r.json |
| F0062 | `client/src/components/admin/Customer360ProfileV2.jsx:5820` | Health Radar plots fabricated values while the real sub-scores are already in the payload | P1 → P2 | Real: Customer360ProfileV2.jsx:5820-5850 builds `radarData = hs.risk_factors ? [{Payment:80},{Engagement:60},{Service:70},{Satisfaction:75},{Tenure:90},{Revenue:65}] : [score+10, score, score+5, score |
| F0065 | `client/src/components/admin/Customer360ProfileV2.jsx:6119` | 'Add Note' (header pill + ⋯ menu) is a dead action that lands on the customer SMS composer | P1 → P2 | Dead action confirmed: Customer360ProfileV2.jsx:6119-6124 header 'Add Note' `onClick={() => setActiveTab("comms")}` and :8162-8170 menu item `onClick={() => { setActiveTab("comms"); setMenuOpen(false) |
| F0069 | `client/src/components/admin/Customer360ProfileV2.jsx:7042` | Billing Payment History renders ' …' for non-card (Zelle/cash) payments; Overview handles the same rows correc | P2 → P3 | The bare-ellipsis defect is real: client/src/components/admin/Customer360ProfileV2.jsx:7025 `payments.slice(0, 10).map((p, i) => {` → :7042-7044 `<span className="text-ink-secondary">{p.card_brand} …{ |
| F0071 | `client/src/components/admin/Customer360ProfileV2.jsx:7255` | 360 Comms tab never marks texts read and duplicates a weaker composer instead of CustomerSmsPanel | P1 → P2 | Every code fact checks out: client/src/components/admin/Customer360ProfileV2.jsx:49-97 imports contain no CustomerSmsPanel/useCustomerSms; `/admin/communications/messages/read` + `notifyUnreadChanged` |
| F0079 | `client/src/components/admin/GlobalCommandPalette.jsx:33` | Four independent IB implementations: the primary ⌘K palette does not use the 'shared' IntelligenceBarShell/use | P1 → P2 | The duplication facts hold: GlobalCommandPalette.jsx:33 `const RECENTS_KEY = "admin_ib_recents"` with its own loadRecents/saveRecent (:176-196, no context in the key) vs client/src/utils/ibStorage.js: |
| F0080 | `client/src/components/admin/GlobalCommandPalette.jsx:47` | ⌘K desktop palette is Tier-2 inline styling with a page-local D palette, per-context accent colours, 16px radi | P1 → P2 | Evidence holds: GlobalCommandPalette.jsx:47-58 page-local slate `D` (`bg: "#F1F5F9"`, `border: "#E2E8F0"`, `teal: "#0A7EC2"`, `purple: "#7C3AED"` …); :122-138 `CONTEXT_COLORS` maps routes to teal/purp |
| F0091 | `client/src/components/admin/IntelligenceBarShell.jsx:449` | No live region anywhere in the IB: loading, answers and 'Error:' responses are silent to screen readers | P1 → P2 | Fact verified: no live region exists. IntelligenceBarShell.jsx:449-461 skeleton `<div className="px-4 pb-4">…animate-pulse…` and :465-478 response `<div className="px-4 pb-4 pt-1 …"> … isError ? "text |
| F0095 | `client/src/components/admin/MobileNewCustomerSheet.jsx:239` | Mobile New Customer sheet: all 9 fields placeholder-only with no accessible name, plus fake dropdown carets on | P1 → P2 | Facts verified: MobileNewCustomerSheet.jsx inputs :239-246 (`placeholder="First name"`), :247-254, :266-276 (`placeholder="Phone number"`), :278-286, :323-341 (AddressAutocomplete `placeholder="Addres |
| F0101 | `client/src/components/admin/PendingActionsCard.jsx:317` | Confirm/Cancel on the write-confirmation card are ~32px on the mobile palette sheet and in the Shell (touchFri | P2 → P3 | Half refuted. PendingActionsCard.jsx:129 `touchFriendly = false`, :317/:330 `px-4 py-1.5 text-[14px] ... ${touchFriendly ? 'min-h-11' : ''}` — real. But the mobile Cmd-K sheet already gets 44px withou |
| F0102 | `client/src/components/admin/PricingLogicPanel.jsx:14` | PricingLogicPanel still ships the un-folded slate/teal/purple palette inside the monochrome Pricing hub and Es | P1 → P2 | Facts hold: PricingLogicPanel.jsx:14-27 `const D = { bg: '#F1F5F9', border: '#E2E8F0', teal: '#0A7EC2', ... purple: '#7C3AED' }` and :59-67 the seven-colour CATEGORY_COLORS map; its host PricingLogicP |
| F0103 | `client/src/components/admin/PricingLogicPanel.jsx:1131` | Inline `outline: 'none'` on inputs/textareas with no :focus-visible or focus-state replacement (15 verified si | P2 → P3 | Admin/tech sites are real, glass sites are not. Real: PricingLogicPanel.jsx:178, :1131, :1250, :1674 `outline: 'none'` (no focus handler/class), GlobalCommandPalette.jsx:1280, pages/AdminForgotPasswor |
| F0122 | `client/src/components/dashboard/charts.jsx:337` | Recharts charts have no accessible name or text alternative (only CaptureGauge does) | P2 → P3 | Facts hold: charts.jsx:337-338 RevenueTrendArea `<div style={{ height }}><ResponsiveContainer`, :407-408 ReviewTrendChart and :873-874 MrrTrendChart identical, no role/aria-label; KpiRing :261 `<svg . |
| F0126 | `client/src/components/dispatch/DispatchBoardLayout.jsx:49` | Dispatch board is sized 100vh-64px inside a shell with no 64px bar, so part of the board is always clipped (de | P1 → P2 | Sizing chain is as claimed: DispatchBoardLayout.jsx:49 `h-[calc(100vh-64px)] ... overflow-hidden`; AdminDispatchPage.jsx:139 `min-h-[calc(100vh-64px)]` (AgentsHubPage.jsx:165 the same); Board is the d |
| F0133 | `client/src/components/estimate/AppShowcaseCard.jsx:134` | App card forces the two store badges side by side and a 240px copy column — overflows the card at 320px | P2 → P3 | The inputs are as cited: AppShowcaseCard.jsx:27 `width="132" height="40" … style={{ display: 'block', height: 40, width: 'auto' }}`, :39 `width="135"`, :134 `flexWrap: 'nowrap'`, glass-components.css: |
| F0153 | `client/src/components/report/lawnV2/LawnReportV2.jsx:188` | All six V2 report folders still ship status pills (StatusPill 'Watch/Attention', ACTIVE/COMPLETE badges, score | P1 → P2 | The pills exist as cited: LawnReportV2.jsx:188-197 `StatusPill … padding: small ? '3px 9px' : '6px 12px', borderRadius: 999` used at :506, :576, :696, :920 and TreeShrubReportV2.jsx:333, :402, :441, : |
| F0155 | `client/src/components/report/lawnV2/LawnReportV2.jsx:282` | Off-scale font sizes (14.5 / 15 / 15.5 / 16.5 / 17px) throughout the V2 report bodies and one flow page | P2 → P3 | The sizes are real — LawnReportV2.jsx:282 `fontSize: 14.5`, :330 `fontSize: 16.5`, :333 `fontSize: 14.5`; TermiteReportV2.jsx:331 `fontSize: 17`; CockroachReportV2.jsx:80 `fontSize: 15`; ReportViewPag |
| F0170 | `client/src/components/schedule/CreateAppointmentModal.jsx:2358` | Phone fields without type="tel" — full keyboard instead of phone pad on iOS | P2 → P3 | The four inputs are verified bare: CreateAppointmentModal.jsx:2358 `<input value={quickAdd.phone} onChange={e => setQuickAddField('phone', e.target.value)} style={inputStyle} />`; CustomersPageV2.jsx: |
| F0171 | `client/src/components/schedule/InlineTechPicker.jsx:18` | Mobile dispatch tech assignment fails silently — PUT error goes to console only | P1 → P2 | Code is as quoted: InlineTechPicker.jsx:27-33 `if (!res.ok) throw new Error(`HTTP ${res.status}`); onAssigned?.(techId); } catch (e) { console.error('Assign failed:', e); } setBusy(false); onClose();` |
| F0175 | `client/src/components/schedule/MarkPrepaidModal.jsx:202` | Schedule/Dispatch V2 overlay family is hand-rolled without focus trap, Escape, focus return or scroll lock; mo | P1 → P2 | Facts hold. MarkPrepaidModal.jsx:202-211 `return createPortal(<div onClick={dismiss} className="fixed inset-0 z-[1200] flex items-end md:items-center justify-center" …><div onClick={(e) => e.stopPropa |
| F0181 | `client/src/components/schedule/MobileAppointmentDetailSheet.jsx:417` | No shared sheet implementation: 10 full-screen sheets and 8 overlay modals each hand-roll their shell; none us | P1 → P2 | Facts hold. MobileAppointmentDetailSheet.jsx:415-417 `return createPortal(<div className="fixed inset-0 z-[100] bg-white overflow-y-auto" …` with no role/aria-modal/keydown/focus hook (scan: Escape=0  |
| F0185 | `client/src/components/schedule/MobileCheckoutSheet.jsx:290` | 12 hand-rolled full-screen mobile sheets in components/schedule bypass ui/Dialog: no role=dialog, no focus tra | P1 → P2 | Same finding as F0181 (and F0175 for the centre modals); facts verified independently. MobileCheckoutSheet.jsx:290 `<div className={`fixed inset-0 z-[105] bg-white overflow-y-auto ${desktopVisible ? ' |
| F0193 | `client/src/components/schedule/MobilePaymentSheet.jsx:85` | Tap to Pay button stays permanently disabled after handoff; a cancelled or declined Terminal session has no re | P2 → P3 | The stuck button is real: MobilePaymentSheet.jsx:85-98 `setCharging(true); … await launchTapToPay(invoiceId); // Control is now handed off … the parent will close it` and only the catch runs `setCharg |
| F0203 | `client/src/components/schedule/ScheduleListView.jsx:499` | Sortable column headers are plain `<th onClick>` — not keyboard reachable, and zero `aria-sort` anywhere in th | P1 → P2 | Code facts hold: ScheduleListView.jsx:499-506 six `<th className={thClass} onClick={() => toggleSort('…')}>` (Customer, Service, Date, Tech, Status, Price — the only `<th … onClick` sites in client/sr |
| F0208 | `client/src/components/schedule/TimeGridDay.jsx:387` | Time grids are pointer-only: appointment blocks are announced as buttons but Enter/Space does nothing, and no  | P1 → P2 | Pointer-only is proven: TimeGridDay.jsx:387-398 `<div ref={setNodeRef} {...attributes} {...listeners} onClick={(e) => { if (isDragging) return; … onEdit?.(service); }}` and TimeGridDays.jsx:235-243 id |
| F0216 | `client/src/components/tech/FieldLeadModal.jsx:54` | Tech FieldLeadModal panel is unbounded — select + textarea form with no max-height/scroll | P2 → P3 | The code fact is true: FieldLeadModal.jsx:51-57 panel style is `background: '#fff', borderRadius: 16, width: '90%', maxWidth: 420, padding: 24, boxShadow: …` with no maxHeight/overflowY, whereas TechP |
| F0219 | `client/src/components/tech/GeofenceArrivalPrompt.jsx:250` | Geofence/visit notification stack renders under the iPhone notch (fixed top:12, no safe-area inset) | P1 → P2 | GeofenceArrivalPrompt.jsx:249-255 `position: 'fixed', top: 12, left: 12, right: 12, zIndex: 10_000, … maxHeight: 'calc(100vh - 24px)', overflowY: 'auto'` and grep for safe-area\|dvh in the file = 0. T |
| F0271 | `client/src/pages/EstimateViewPage.jsx:2640` | Modal cards use maxHeight 90vh with no dynamic-viewport or safe-area handling | P2 → P3 | The duplication is real — the same card literal `maxWidth: 440, … maxHeight: '90vh', overflow: 'auto'` appears at client/src/pages/EstimateViewPage.jsx:2640, :2761, :2928, :3047, :3278 inside identica |
| F0275 | `client/src/pages/PayPageV2.jsx:652` | Pay page still renders a 'No fees' status pill inside the Zelle panel despite the no-chips ruling | P2 → P3 | The span exists exactly as cited (client/src/pages/PayPageV2.jsx:652-665: `textTransform: 'uppercase' ... borderRadius: 999 ... No fees`). But the ruling it invokes does not cover it. docs/design/DECI |
| F0278 | `client/src/pages/PortalPage.jsx:1` | Giant single-file pages: PortalPage 16,168 lines / 238 useState, Customer360ProfileV2 8,474 lines / 145 useSta | P1 → P3 | Size facts verified: `wc -l` = 16168 (PortalPage.jsx), 9636 (ReportViewPage.jsx), 8474 (Customer360ProfileV2.jsx); grep counts 238 `useState(`, 50 `useEffect(`, 55 top-level `function [A-Z]` component |
| F0286 | `client/src/pages/PortalPage.jsx:3510` | PortalPage defines 13 local `card` objects; 10 are byte-identical and contradict PORTAL_CARD_STYLE (radius 8 v | P2 → P3 | Duplication is real: the 10 objects at client/src/pages/PortalPage.jsx:3510,4489,5844,7623,8473,8892,10580,12096,12767,14985 hash identically (`background: B.white, border: '1px solid #E7E2D7', border |
| F0319 | `client/src/pages/PublicBookingPage.jsx:1067` | Public booking address + unit inputs have no accessible label | P1 → P2 | client/src/pages/PublicBookingPage.jsx:1067-1097 renders `<AddressAutocomplete autoFocus value={address.line1} ... placeholder="Start typing your address" className="waves-focus-ring" style={inputStyl |
| F0326 | `client/src/pages/ReportViewPage.jsx:2530` | Service report hero and re-entry card still render status pills ('Ready now', 'Service complete', 'Follow-up r | P1 → P2 | Both pills exist as cited: client/src/pages/ReportViewPage.jsx:2530-2534 `<div className={`status-badge status-${smartStatus.statusTone \|\| (readinessBadge?.ready ? 'ready' : 'pending')}`}>{smartStat |
| F0342 | `client/src/pages/admin/AdminInvoicesPage.jsx:1161` | Reverse prepaid invoice has no pending state — double-click fires two money-moving POSTs | P1 → P2 | Client side is as claimed: AdminInvoicesPage.jsx:1161-1180 `handleReversePrepaid` never calls setRowActionBusy, and the button at :1975-1981 has no `disabled`/opacity, unlike handleVoid :1126-1139 and |
| F0353 | `client/src/pages/admin/AdminInvoicesPage.jsx:7126` | Follow-up panel shows 'Loading follow-up…' forever when the fetch fails | P2 → P3 | AdminInvoicesPage.jsx:7126-7131 `const d = await adminFetch(\`/admin/invoices/${invoiceId}/followup\`).catch(() => null); setData(d);` and :7152-7157 `if (!data) return (<div …>Loading follow-up…</div |
| F0359 | `client/src/pages/admin/AgentDecisionsPage.jsx:584` | AgentDecisionsPage form fields have no associated <label>; notice/error banners have no role | P2 → P3 | Facts hold: client/src/pages/admin/AgentDecisionsPage.jsx:584 `<div style={{ fontSize: 11, … }}>Actual Human Reply</div>` and :593 `Final / Rewrite Reply` are plain divs, not `<label htmlFor>`; :585-5 |
| F0371 | `client/src/pages/admin/AutonomousContentReviewPage.jsx:296` | Autopilot tab renders a TruGreen-style forest-green/lime brand hero, cream page background and iPhone mockup i | P1 → P3 | The rendered facts are accurate — client/src/pages/admin/AutonomousContentReviewPage.jsx:296 `bg-[#FAF7EF]` (non-embedded only), :298 `rounded-2xl bg-[#143D2A]`, :299 `bg-[#43B02A]/25 blur-3xl`, Phone |
| F0379 | `client/src/pages/admin/BlogPage.jsx:112` | Module-level `isMobile` constants (Blog, Reviews, ReviewVelocityEngine) never update; RVE imports useIsMobile  | P2 → P3 | Partly right, partly wrong. Right: client/src/pages/admin/BlogPage.jsx:112 `const isMobile = typeof window !== "undefined" && window.innerWidth < 640;` consumed inside components at :141 (Card padding |
| F0405 | `client/src/pages/admin/CompliancePage.jsx:77` | `sTableWrap` uses `overflow: hidden` — Compliance and Discounts tables clip their right-hand columns on narrow | P1 → P2 | CompliancePage.jsx:77-82 `const sTableWrap = { background: D.card, border: \`1px solid ${D.border}\`, borderRadius: 12, overflow: "hidden" };` wraps `<table style={{ width: "100%", borderCollapse: "co |
| F0414 | `client/src/pages/admin/CustomersPageV2.jsx:1015` | CustomersPageV2 carries dead helpers and weight-700 / page font overrides against the Tier 1 contract | P2 → P3 | Dead code is real: :1005 `function serviceInitials(c)` and :1015 `function detectTier(c)` — the only matches for either name in the file are their definitions; :151 `function StageBadgeV2` is referenc |
| F0425 | `client/src/pages/admin/DiscountsTabs.jsx:636` | Money fields as type="number" without step — cents flagged invalid, spinner/scroll-wheel mutates dollar amount | P2 → P3 | client/src/pages/admin/DiscountsTabs.jsx:630-634 (Amount), :638-642 (`Max Discount ($)`), :717-721 (`Min Subtotal ($)`) are `<input type="number" style={sInput} value=… onChange={(e) => upd(…, e.targe |
| F0441 | `client/src/pages/admin/EquipmentMaintenancePage.jsx:1477` | Equipment maintenance record and mileage log saves fail silently (console.error only, no error UI in the file) | P1 → P2 | client/src/pages/admin/EquipmentMaintenancePage.jsx:29-40 `function af(...)` → `.then((r) => { if (!r.ok) throw new Error(\`HTTP ${r.status}\`); return r.json(); })`; :1440-1479 record modal `submit`  |
| F0442 | `client/src/pages/admin/EquipmentPage.jsx:160` | Module-level `isMobile` snapshot never updates — wrong layout after rotate/resize in 6 ops files | P1 → P2 | Module-level snapshots are real as cited: client/src/pages/admin/EquipmentPage.jsx:160 `const isMobile = typeof window !== "undefined" && window.innerWidth < 640;`, EquipmentMaintenancePage.jsx:117 (7 |
| F0447 | `client/src/pages/admin/EstimateToolViewV2.jsx:1062` | Tier/frequency rows are click-only divs — not keyboard operable | P2 → P3 | The div is click-only as claimed — EstimateToolViewV2.jsx:1062-1075 `<div onClick={onSelect} title=… className=…>` with no role/tabIndex/onKeyDown (whole-file grep for onKeyDown\|role="button"\|aria-p |
| F0457 | `client/src/pages/admin/EstimatesPageV2.jsx:832` | WorkQueueRail and FilterSheetV2 are defined but never rendered (~240 dead lines in the live page) | P2 → P3 | Dead as claimed: `function FilterSheetV2(` (598), `const WORK_QUEUE_GROUPS = [` (811), `function WorkQueueRail(` (832) are unexported, and the only other references in client/src are 858 (`WORK_QUEUE_ |
| F0465 | `client/src/pages/admin/EstimatesPageV2.jsx:3038` | CommercialProposalModal.jsx (381L, Tier 1) is unreachable; EstimatesPageV2 comment still names it as the propo | P2 → P3 | Facts hold: `grep -rn CommercialProposalModal client/src` returns only the definition (components/estimates/CommercialProposalModal.jsx:57, 381 lines) and the comment at EstimatesPageV2.jsx:3038 '— pr |
| F0475 | `client/src/pages/admin/InventoryPage.jsx:2535` | Money and count columns in Tier 2 tables are left-aligned with no tabular figures | P2 → P3 | Partly refuted. InventoryPage.jsx:2535-2551 — both cited cells set `fontFamily: "'JetBrains Mono', monospace"`; monospace figures are tabular by construction, so 'no tabular figures' does not hold for |
| F0488 | `client/src/pages/admin/LeadsTabs.jsx:4331` | 'Convert to Customer' asks the operator to paste a raw customer UUID | P1 → P2 | The defect is real: LeadsTabs.jsx:4331-4337 `<Input label="Customer ID (required)" value={formData.customer_id} … placeholder="Existing customer UUID" />`, submit at :1157-1162 `if (!formData.customer |
| F0491 | `client/src/pages/admin/NewsletterPage.jsx:199` | NewsletterPage keeps an unused PageHeader and TabBar (with 10px text and the .tab-pill-scroll class) | P2 → P3 | Dead code confirmed: NewsletterPage.jsx:138 `function PageHeader({ onCompose, subscribersActive, sendsData })` and :199 `function TabBar({ tab, tabs, tabCounts, onSelect })` are the only occurrences o |
| F0497 | `client/src/pages/admin/PPCDashboardPage.jsx:559` | Admin pages inject Google Fonts <link>s (DM Sans) that the Roboto !important rule can never render | P2 → P3 | Link is real and dead: client/src/pages/admin/PPCDashboardPage.jsx:559-562 renders `<link href="https://fonts.googleapis.com/css2?family=DM+Sans…" rel="stylesheet" />` inside the body (React 18.2, cli |
| F0504 | `client/src/pages/admin/PendingDraftsTab.jsx:224` | In-app links rendered as raw <a href> trigger full page reloads (PendingDrafts 'Open in Communications', Queue | P2 → P3 | Real: client/src/pages/admin/PendingDraftsTab.jsx:68-73 `communicationsHref` returns `/admin/communications?draftId=…` and :224-229 renders it as a raw `<a href={communicationsHref(draft)} style={{ …, |
| F0511 | `client/src/pages/admin/ProjectsPage.jsx:3776` | ProjectsPage mixes Tier 1 primitives + Tailwind with 157 inline style blocks and hand-rolled buttons/inputs | P1 → P2 | The mix is real inside single components, not just the file: client/src/pages/admin/ProjectsPage.jsx:5 imports `Badge, Button, Dialog, DialogBody, DialogFooter, Select`; useConfirmDialog (:46-109) ren |
| F0516 | `client/src/pages/admin/RevenuePage.jsx:202` | ~68 admin data effects fetch without an AbortController or mounted/sequence guard (stale-response races) | P2 → P3 | Cited effects are unguarded as claimed: client/src/pages/admin/RevenuePage.jsx:202-206 `useEffect(() => { adminFetch(\`/admin/ads/revenue-attribution?period=${period}\`).then((d) => setAttr(d)).catch( |
| F0524 | `client/src/pages/admin/SEOPage.jsx:920` | Geo-grid scan poller (setInterval every 20s, up to 18 ticks) is never cleared on unmount or tab change | P2 → P3 | Leak is real: client/src/pages/admin/SEOPage.jsx:919-929 inside runScan: `let n = 0; const t = setInterval(async () => { n += 1; const s = await adminFetch("/admin/seo/geo-grid").catch(() => null); if |
| F0542 | `client/src/pages/admin/TaxPage.jsx:171` | TaxPage `isMobile` is a module-level constant evaluated once — layout never responds to resize/rotation | P1 → P2 | Real: client/src/pages/admin/TaxPage.jsx:171 `const isMobile = typeof window !== "undefined" && window.innerWidth < 640;` is module scope, read at 95-97 (StatCard), 262, 2649, 2959, 3663, 3891, 5046,  |
| F0552 | `client/src/pages/admin/TimeTrackingPage.jsx:3059` | Four TimeTracking tables render with no horizontal-scroll wrapper — the page scrolls sideways on phones | P1 → P2 | Real, and larger than reported: client/src/pages/admin/TimeTrackingPage.jsx has nine `<table` (888, 1031, 1354, 1908, 2000, 2072, 2282, 3059, 5226) and only three `overflowX: "auto"` wrappers (886, 13 |
| F0557 | `client/src/pages/admin/TriageInboxTabV2.jsx:656` | Triage card action row squeezes the caller name to nothing on phones | P1 → P2 | Structure is as claimed: TriageInboxTabV2.jsx:623 `<div className="flex items-start justify-between gap-3">` (no wrap), :624 `<div className="min-w-0 flex-1">`, :626 name `text-14 font-medium text-zin |
| F0561 | `client/src/pages/admin/WinLossSlicesCard.jsx:32` | 148 raw <table> in admin vs 10 ui/Table consumers; Tier-1 files drop the overflow-x wrapper ui/Table provides | P2 → P3 | Duplication half holds: ui/Table.jsx:4-9 wraps `<table>` in `<div className="w-full overflow-x-auto">`; WinLossSlicesCard.jsx hand-rolls seven `<table className="w-full text-13">` (:32, :154, :191, :2 |
| F0564 | `client/src/pages/admin/autonomous-content/shared.jsx:70` | Autopilot controls below 44px on mobile and sub-14 tag/label text | P2 → P3 | Touch-size part confirmed: autonomous-content/shared.jsx:76 PillTab `h-9 shrink-0 rounded-full px-4 text-13` (36px), :189 ActionBtn sm `h-9 px-3.5 text-12`, AutonomousContentReviewPage.jsx:310 Refresh |
| F0566 | `client/src/pages/admin/dashboard/ChannelROI.jsx:80` | Low-sample amber pill copied inline four times; SampleBadge.jsx and charts.jsx LowSamplePill both exist but th | P2 → P3 | Byte-identical span in ChannelROI.jsx:80-82, FunnelBySource.jsx:100-102 and MarginByLineBars.jsx:36-38 (`inline-block text-11 px-1.5 py-0.5 rounded-sm border border-amber-300 bg-amber-50 text-amber-70 |
| F0573 | `client/src/pages/admin/dashboard/Verdict.jsx:5` | Triage palette hex codes redefined in six files instead of importing the exported CHART_* constants; AgingBar  | P2 → P3 | charts.jsx:37-47 exports CHART_INK '#18181B', CHART_ALERT '#C8312F', CHART_SUCCESS '#10B981', CHART_WARN '#F59E0B'. Retyped literals: Verdict.jsx:5-10 (with :3-4 comment "same constants as charts.jsx  |
| F0581 | `client/src/pages/admin/pipeline/UnifiedPipelineView.jsx:124` | Entire client pipeline/ directory (Unified Pipeline + second stage model) is unreachable since May but still m | P1 → P2 | Unreachable is confirmed: App.jsx:368 `const AdminPipelinePage = lazyWithRetry(() => import('./pages/admin/EstimatesPageV2'));` mounted at :568 `<Route path="pipeline" …>`; a grep for UnifiedPipelineV |
| F0585 | `client/src/pages/tech/TechHomePage.jsx:511` | Report button stays enabled but silently does nothing on completed / sent / closed visits | P2 → P3 | Core claim holds: client/src/pages/tech/VisitBriefPanel.jsx:486 `<button onClick={() => onProject(service)} style={btn}>` carries no `disabled`, `btn` (467-471) keeps `cursor: 'pointer'`, and the labe |
| F0587 | `client/src/pages/tech/TechHomePage.jsx:968` | Quick Move result toast is fixed at bottom:16 over the bottom nav, without safe-area inset | P2 → P3 | Cited code is exact: TechHomePage.jsx:968-975 `position: 'fixed', bottom: 16, left: 16, right: 16, zIndex: 1100` with no safe-area term, dismissed by `setTimeout(() => setRainOutResult(''), 6000)` (96 |
| F0591 | `client/src/pages/tech/TechHomePage.jsx:1689` | Next Stop action row packs up to six ~34px-tall, 12px-label buttons in one non-wrapping row; the next action i | P1 → P2 | Facts hold: TechHomePage.jsx:725-757 renders Navigate, Protocol, Quick Move, En Route (primary) plus On Site (primary, `ON_SITE_ELIGIBLE = new Set(['en_route'])` :196) and Sync Stop (primary, `stopOut |
| F0594 | `client/src/pages/tech/TechRecapCapture.jsx:140` | Recap-clip tag sheet is painted over by the bottom nav (same z-index, earlier in DOM) and ignores the home-ind | P1 → P2 | Mechanism confirmed: TechRecapCapture.jsx:140 overlay `position: 'fixed', inset: 0 … zIndex: 50, display: 'flex', alignItems: 'flex-end'`, sheet :141 `padding: '16px 14px 22px'` with no inset; the com |
| F0597 | `client/src/pages/tech/VisitBriefPanel.jsx:467` | In-brief service action buttons (Report / Photos / Zone / 🚩) are ~32px tall with 12px labels; the flag button  | P1 → P2 | Measurements hold: VisitBriefPanel.jsx:467-471 `const btn = { padding: '8px 10px', borderRadius: 6, fontSize: 12, fontWeight: 600, border: … }` (≈32px tall at 12px), and :495 `<button onClick={() => o |
| F0605 | `client/src/utils/admin-fetch.js:17` | Admin fetch is fragmented: 2 shared wrappers with different contracts plus 81 inline adminFetch copies | P1 → P2 | Counts reproduce: 80 files define their own `function adminFetch`/`const adminFetch` outside the two helpers, 49 import utils/admin-fetch, 27 import lib/adminFetch, 135 non-test files read `waves_admi |
| F0606 | `client/tailwind.config.js:67` | `text-10` / `text-15` / `text-20` / `text-24` are not Tailwind tokens — the classes emit no CSS and silently i | P1 → P2 | Bug is real: client/tailwind.config.js:67-76 `fontSize` extends only '11','12','13','14','16','18','22','28', `plugins: []` (:98), no safelist/presets, postcss.config.js plain tailwindcss 3.4 (no nume |
| F0613 | `server/services/twilio.js:153` | SMS-guard admin alert links to unmounted /admin/sms-templates (lands on customer login) | P1 → P2 | server/services/twilio.js:153 `link: "/admin/sms-templates"` (inside notifySmsGuardBlocked → triggerNotification('internal_admin_alert')). grep for 'sms-templates' in client/src and server finds only  |
