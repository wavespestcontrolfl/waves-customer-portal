# DECISIONS.md

A running log of design and architectural decisions for the Waves Portal. Each entry explains the *why*, not just the *what*. Keep entries short — a paragraph is plenty.

**Why this file exists:** six months from now, when someone asks "why are all our buttons uppercase?" or "why is there no dark mode?", this file is the answer. Without it, the decision gets re-litigated every time a new pair of eyes looks at the system.

**Format:** append new entries at the bottom. Never edit or delete old entries — if a decision is reversed, add a new entry explaining the reversal. The log is immutable.

**Entry template:**

```
## [Date] — [Short title of decision]

**Decision:** [one-sentence statement of what was decided]

**Context:** [what was the question or situation]

**Reasoning:** [why this choice over alternatives]

**Revisit if:** [what would prompt reconsideration]
```

---

## 2026-04 — UI redesign baseline: monochrome + red, uppercase CTAs, light mode only

**Decision:** The admin portal will use a near-monochrome design language (grayscale + red as the only chromatic color), UPPERCASE letter-spaced CTAs, and will support light mode only.

**Context:** The portal had accumulated inconsistent use of color, weight, and button styling as features shipped over time. The owner wanted a "super clean professional dashboard" with strong visual hierarchy.

**Reasoning:** Employee software optimizes for throughput and error reduction, not engagement. A monochrome palette with red reserved for items needing team action makes alerts impossible to ignore and everything else visually quiet. UPPERCASE CTAs push the aesthetic register toward institutional (Attio, Pylon, Bloomberg) rather than consumer-friendly (Notion, Linear). Dark mode was cut because the primary usage context — Virginia in a sunny Bradenton office and techs on iPhones in Florida sunlight — doesn't benefit from it, and supporting it doubles QA surface for every page.

**Revisit if:** Virginia or techs specifically report the interface feels cold or hostile during extended use; a significant portion of usage shifts to low-light environments; customer-facing surfaces (which use different design rules) bleed into admin contexts.

---

## 2026-04 — Customer-facing surfaces use a separate, warmer design language

**Decision:** Customer-facing surfaces (estimate, invoice, booking, appointment, portal, post-service) will use a distinct, brand-forward design language — not the admin monochrome spec. Wave Teal accent, warmer neutrals, Title Case CTAs allowed, larger type sizes, imagery permitted.

**Context:** The admin spec's monochrome-institutional register would actively hurt conversion on surfaces where a homeowner is deciding whether to trust Waves with ongoing payments.

**Reasoning:** Consumer UI and employee UI solve different problems. Applying a Bloomberg Terminal aesthetic to a $261/month lawn service estimate creates a trust gap at the exact moment trust matters most. Consumer surfaces need warmth, brand presence, and clarity for non-experts — attributes that would be noise in admin.

**Revisit if:** The separation creates component-duplication pain that outweighs the aesthetic benefit; customers report the estimate page feels "off-brand" relative to the marketing site.

---

## 2026-04 — Tier 1 redesign scoped to 5 admin routes

**Decision:** Full-redesign (tier 1) treatment is limited to Dashboard, Dispatch (absorbing Schedule as a view toggle), Customers + Detail, Estimates + /new, and Communications. Every other admin route receives a "tier 2" token pass only: apply tokens, strip colored classes, convert status indicators — no layout restructuring, no archetype enforcement.

**Context:** The portal has 30+ admin routes. A full redesign of all of them would take months and apply design polish to pages Virginia rarely opens.

**Reasoning:** Design polish has diminishing returns beyond the core daily-use surfaces. The 5 tier 1 routes are what Virginia touches every hour. The others work fine functionally and benefit from visual consistency (tokens) without justifying full restructuring. This also keeps the project scoped to something shippable in weeks, not months.

**Revisit if:** A tier 2 page becomes a daily operational burden during the tier 1 migration; a business change elevates a tier 2 page to tier 1 status (e.g. commercial pest vertical requires a new operational view).

---

## 2026-04 — Tech Home (`/tech`) will not be touched in this redesign

**Decision:** The tech portal home page stays exactly as it is. Other tech pages (`/tech/route`, `/tech/estimate`, `/tech/protocols`) receive tier 2 token treatment only.

**Context:** Jose, Jacob, and Adam use the tech app every working day without complaint. The field-facing UI has different ergonomic requirements (big tap targets, sun glare, one-handed use) that risk being degraded by a design system designed primarily for office use.

**Reasoning:** Don't fix what isn't broken. The field team has muscle memory on the current layout. A design refresh without a specific field-reported problem is all downside: no throughput gain for the techs, potential disruption. Revisit when there's specific field feedback.

**Revisit if:** A tech reports specific frustration with the current home page; tech portal metrics (job completion time, photos-per-job) regress.

---

## 2026-04 — Intelligence Bar UX is out of scope for the visual refresh

**Decision:** The Intelligence Bar (104 tools across 13 contexts) will not be restyled piecemeal during the redesign. It needs its own UX design exercise first — covering discovery, organization, context-awareness, favorites/recents — before any visual work touches it.

**Context:** The Intelligence Bar has accumulated tools faster than a UX pattern for organizing them. Restyling each tool within the current structure would lock in an organizational model that may be wrong.

**Reasoning:** Visual refresh without fixing the underlying UX is lipstick on a pig. Better to pause, design the bar's organizational model properly, then apply visual polish. Doing it twice is more work than doing it once correctly.

**Revisit:** After the 5 tier 1 pages ship and stabilize. At that point, dedicate a separate design pass to the Intelligence Bar as its own project.

---

## 2026-04 — Feature flags are mandatory for every tier 1 page ship

**Decision:** Every redesigned tier 1 page ships behind a per-user feature flag. Rollout order: operator → Virginia (1 business day formal UAT) → all admin users → flag removed after 2 weeks stable.

**Context:** Virginia dispatches real jobs on the live portal. A silent regression locks her out of daily work until someone reverts.

**Reasoning:** Instant rollback without a deploy is the only safe way to iterate on a production tool used by the operations team during working hours. Flags are cheap; downtime is expensive.

**Revisit if:** Flag infrastructure becomes itself a maintenance burden that outweighs the safety benefit (unlikely at this team size).

---

## 2026-04 — Storybook is the canonical component reference

**Decision:** Storybook (or a `/admin/_design-system` internal route) ships before any page migration. Every component from the spec renders there in every state.

**Context:** Written specs drift. When Claude Code implements a page three weeks into the project, its reference should be a rendered component, not prose.

**Reasoning:** Visual references beat textual references for design implementation. A rendered component shows exactly what "default" and "hover" and "disabled" look like with no interpretation needed. Prose specs leave room for drift, especially across many PRs and AI sessions.

**Revisit if:** Storybook overhead outweighs its value for this team size — unlikely while there are multiple AI-assisted sessions building against the system.

---

## 2026-04-18 — Pre-redesign performance baselines captured (Tier 1 routes)

**Decision:** Record raw Core Web Vitals and JS transfer sizes for the 5 Tier 1 routes against production, on the current codebase, before any redesign foundation work lands. Freeze these numbers so PR #2+ can prove regression-or-not.

**Context:** Captured via Chrome DevTools MCP driving a local Chrome against `portal.wavespestcontrol.com` on git SHA `dc1db9b5a3286823c4a8a4548fad0ba796f1ae0a` at `2026-04-18T10:30:58Z`. Customer Detail (`Customer360Profile.jsx`) is rendered inline from CustomersPage — there is no `/admin/customers/:id` route — so it is folded into the Customers baseline. All resources served from Cloudflare immutable cache (`transferSize ≈ 0` on repeat visit is realistic for this portal); `decodedBodySize` is the regression-proof number.

Raw numbers:

| Page | URL | LCP | CLS | TTFB | Script decoded | Total decoded | fetch (wire / decoded) | resources |
|------|-----|-----|-----|------|----------------|---------------|------------------------|-----------|
| Dashboard | /admin/dashboard | 712ms | 0.13 | 208ms | 1564 KB | 3285 KB | 3.2 KB / 7.1 KB | — |
| Dispatch (→ Schedule) | /admin/schedule | 1448ms | 0.00 | 216ms | 1564 KB | 2903 KB | 2.1 KB / 172.6 KB | — |
| Customers (incl. Customer360 inline) | /admin/customers | 760ms | 0.00 | 205ms | 1564 KB | 2879 KB | 1.8 KB / 148.9 KB | — |
| Estimates | /admin/estimates | n/a | 0.01 | 136ms | 1732 KB | 2992 KB | 6 KB / 93.6 KB | — |
| Communications | /admin/communications | n/a | 0.00 | 216ms | 1564 KB | 2782 KB | 3 KB / 51 KB | 18 |

Raw traces archived at `/tmp/waves-baseline-traces/0{1..6}-*.json`.

**Reasoning:** Spec §8.5 requires a regression check after each Tier 1 page ship. Without a dated, SHA-locked pre-redesign baseline, "we regressed" becomes a matter of opinion. One shared LCP/CLS number per route, captured the same way every time (MCP-driven Chrome, repeat-visit with Cloudflare cache warm), removes the debate. LCP was unmeasurable on Estimates and Communications under the trace — those pages render through state transitions that never produced a single definitive largest-contentful paint event; redesign will make them paint a single hero region and their LCP will become measurable, which is itself a win.

**Revisit if:** Any Tier 1 page ships a PR that moves LCP by >150ms or CLS by >0.05 vs these numbers — that PR's description must explain the move before merge. If Cloudflare cache rules change (e.g. bundles stop being immutable), replace these baselines with fresh captures.

---

## 2026-04-18 — In-repo `/admin/_design-system` route over standalone Storybook

**Decision:** Ship the canonical component reference as an authenticated in-repo admin route (`/admin/_design-system`) rather than standing up a separate Storybook app. Keep the route behind admin auth, gated to non-production OR an explicit user-id allowlist, and excluded from the sidebar nav + `robots.txt`.

**Context:** Spec §8.2 calls for "Storybook (or a `/admin/_design-system` internal route)" — explicit permission to pick either. This portal is a single React/Vite SPA with inline-styles-migrating-to-Tailwind, one build, one deploy, one auth system.

**Reasoning:** A second app (Storybook) means a second build pipeline, a second deploy target, a second auth story for an internal tool, and a second dependency graph to keep in sync with production React/Vite. An in-repo route reuses the existing bundler, layout primitives, auth middleware, and deploy pipeline — every primitive rendered there is literally the one production uses, not a Storybook facsimile. The tradeoff is losing Storybook's built-in controls/addons, which this team has never used. Leave the door open: if the design system grows to need args tables and snapshot testing, Storybook can be added later without throwing the route away.

**Revisit if:** Multiple external designers/contractors start contributing components and need a sandboxed environment without portal credentials; or interaction testing (Storybook test-runner) becomes a thing this team wants.

---

## 2026-04-18 — Hand-rolled primitives with Radix fallback, not shadcn CLI

**Decision:** Build the 13 UI primitives (Button, Input, Select, Checkbox, Radio, Switch, Textarea, Badge, Card, Table, Dialog, Sheet, Tabs) by hand in `client/src/components/ui/`. Use `@radix-ui/react-dialog` as the underlying primitive for `Modal` + `Sheet` only, if hand-rolling focus-trap logic produces noticeably worse UX. No `shadcn` CLI, no `class-variance-authority`.

**Context:** Spec references shadcn/ui as an aesthetic reference, not a dependency requirement. shadcn's CLI copies vendored source into the repo — after that, it's indistinguishable from hand-rolled. This project already uses inline styles + `D` palette; introducing shadcn's CVA pattern here would create a third styling idiom alongside the two that already exist.

**Reasoning:** The spec's whole point is restraint — a small fixed set of primitives, no prop explosion, weights limited to 400/500, colors limited to the zinc ramp + one alert red. `cva` encourages variant proliferation, which is exactly the drift the spec is pushing against. Hand-rolled components in the same idiom as the rest of the codebase are easier for the next person (human or AI) to understand and extend. Radix's Dialog/Sheet primitives solve one genuinely hard problem (focus-trap + scroll-lock + escape-key + portal) that is not worth re-implementing; reach for them only if the hand-roll gets ugly.

**Revisit if:** Primitive count grows past ~20 (CVA starts paying off at higher variant counts); or a second engineer joins who's more productive with the shadcn mental model.

---

## 2026-04-18 — Admin shell pivots to Square-inspired warm-stone + blue CTA

**Decision:** The admin shell (sidebar, app frame, future top-level chrome) adopts a Square Dashboard-inspired light theme: white `--surface-primary`, warm-stone `--surface-page`/`--surface-hover`, near-black warm-neutral text, and a single `#006AFF` blue reserved for the one primary CTA per screen. Tokens live in `client/src/styles/theme-square.css` scoped to `.admin-shell-v2`. Ships as `AdminLayoutV2` behind the `admin-shell-v2` per-user feature flag. Location switcher from the original pitch removed — work scopes by customer address, not GBP location.

**Context:** The earlier Tier 1 spec (zinc ramp, monochrome) was correct in spirit but clinical in execution; Virginia and the operator both have years of Square muscle memory from Square Appointments / POS. Building in the visual language they already know collapses training cost on redesigned pages to roughly zero. Stone beats zinc because zinc reads cold against the warm tonality Square has settled on; the difference is small in isolation but compounds across a dashboard.

**Reasoning:** Combining the token file and the first shell consumer in one PR instead of splitting them — inline hex in `AdminLayoutV2` followed by a tokens-retrofit PR would bake silent drift into the file that matters most, and leave a multi-day window where both shells coexist with different levels of tokenization. Tokens aren't the expensive part (≈40 lines of CSS vars); the expensive part was picking the palette, which is already decided. Scoping tokens to `.admin-shell-v2` (rather than `:root`) prevents leakage into flag-off V1 pages while both shells coexist, and leaves room for a `[data-theme="tech-dark"]` override for the tech portal later without rewriting consumers. Location switcher dropped because Waves doesn't actually scope work by GBP location — jobs scope by customer address, so the pattern was imported from Square without a real workflow match.

**Revisit if:** Florida-sun glare on tablets forces the office team off light mode (unlikely — Virginia works indoors). Also revisit if the next Tier 1 page migration onto these tokens reveals missing semantic slots (e.g. chart colors, success/warning — intentionally omitted until a real consumer needs them).

---

## 2026-04-18 — Rename `ai_conversations`/`ai_messages` → `agent_sessions`/`agent_messages`

**Decision:** Rename the `ai_conversations` and `ai_messages` tables to `agent_sessions` and `agent_messages`, freeing the `conversations`/`messages` namespace for the new unified customer communications schema.

**Context:** PR 1 of the comms unification (see entry below) introduces a `conversations` + `messages` pair to consolidate SMS, voice, voicemail, email, and Beehiiv touchpoints into a single channel-agnostic thread + message model. The existing `ai_conversations` table — created in migration 039 alongside `call_log` — sounds like the same thing but is actually agent execution-state: AI assistant session tracking with `tool_calls`, `tool_results`, `requires_approval`, `escalated`, `conversation_summary`. It is not a customer-facing thread.

**Reasoning:** Letting both names coexist means every developer (and every future Claude Code session) has to relearn "when we say conversation we mean…" for the lifespan of the codebase. The cost is silent for years and then expensive once. Six file references via grep, ~45 string occurrences total; a one-PR rename is cheaper than the ambiguity tax. `agent_sessions` is more accurate to what the table actually stores (a session per agent run), and `agent_messages` parallels it. Migration is a straight `renameTable` — Postgres tracks FKs by OID, not name, so the existing `ai_messages.conversation_id → ai_conversations.id` constraint continues to work; only the constraint *name* is now stale (cosmetic).

**Revisit if:** A future agent framework requires a different mental model (e.g., persistent agent memory that spans sessions) that doesn't fit `agent_sessions` semantics. In that case, add a new table; don't rename again.

---

## 2026-04-18 — Unified `conversations` + `messages` as the comms schema; PR 1 ships dual-write only

**Decision:** Land a single `conversations` + `messages` schema that all channels (voice, sms, email, newsletter, voicemail, system_note) write into, alongside `blocked_numbers` + `blocked_call_attempts` for spam handling. PR 1 adds the tables and dual-writes from existing webhooks; the inbox keeps reading the legacy `sms_log`/`call_log`/`emails` tables. PR 2 cuts the read path over and backfills history.

**Context:** Today the admin Communications inbox reads only `sms_log` — calls, emails, and recordings live in three other tables that the inbox never joins. Virginia experiences this as "I can't find Aaron's old texts" and will experience it as "I can't find Aaron's old emails" the moment email gets surfaced. Beehiiv newsletter sends are entirely opaque. The pain is the same pain across six channels; solving it once is the lever. The full strategy doc covers seven sequential PRs; this entry covers PR 1 only.

**Reasoning:** The conversation row is keyed on `(customer_id, channel, our_endpoint_id)` — meaning a customer who's texted two of our 25 Twilio numbers gets two SMS conversation rows. That preserves the routing fidelity (reply-from must default to the number the customer most recently used per channel) without forcing Virginia to triage that customer across two threads — the inbox UI groups by `(customer, channel)` via a derived view layered over the raw rows. Dual-write in PR 1 (rather than cut-over) means a botched migration doesn't break the inbox Virginia uses 8 hours a day; old reads keep working until new tables are validated. Spam-block tables ship in the same migration so the data model is complete from day one — defining the full `block_type` enum (`hard_block`, `silent_voicemail`, `ai_screen`, `sms_silent`) now means PR 4's inbox dropdown and the later AI-screener PR don't need schema changes to introduce them.

**Revisit if:** The dual-write phase reveals a column the new `messages` schema is missing (likely candidates: `email`-specific threading headers, Beehiiv campaign IDs, Twilio Voice Intelligence tags). Add as `metadata` keys first; promote to columns only if they become query targets. Also revisit if backfill volume turns out to be larger than expected — current estimate ≈ 50–100k rows total across legacy tables, well within a single in-place migration.

---

## 2026-04-18 — PR 2 lands the read-path cutover; sms_log + call_log become write-through legacy

**Decision:** Cut the Communications inbox, dashboard inbox, Customer 360 timeline, and channel-stats endpoints over to read from `conversations` + `messages`. Preserve the dual-writes to `sms_log` and `call_log` for the queue/processing flows that still depend on them (scheduled-SMS cron, call-recording processor). Backfill historical SMS + voice rows via a standalone idempotent script (`server/scripts/backfill-comms-pr2.js`) rather than a knex migration so Railway's deploy timeout is not a constraint.

**Context:** PR 1 left a dual-write in place but every read still hit the legacy tables. Five surfaces consume those reads: `GET /api/admin/communications/log`, `GET /api/admin/communications/stats`, `GET /api/admin/dashboard/inbox` (+ `/read` + `/reply`), `GET /api/admin/customers/:id` timeline, and the call-recordings list. Switching all five at once is the right unit because the alternative — leaving any one on the legacy table — means Virginia sees ghost messages where the dual-write started (everything older missing) until the next cutover lands. Backfill solves that by populating history before cutover, but the only safe way to ship cutover + backfill together is to do both in this PR.

**Reasoning:** Adds three columns to `messages` (`is_read`, `message_type`, `ai_summary`) for legacy fields the schema didn't model in PR 1 — the alternative was stuffing them into `metadata`, which would have made the inbox unread query a JSONB filter. Adds outbound dual-write in `TwilioService.sendSMS` and `POST /api/admin/communications/call`, since PR 1 only wired inbound webhooks. Reply-from on the dashboard inbox now passes `our_endpoint_id` from the originating thread back into `sendSMS({ fromNumber })`, which closes a long-standing routing bug where a reply could go out from the default Lakewood Ranch line even when the inbound landed on a spoke tracker — Virginia's "why is this customer getting two threads from us" frustration. Also fixes a P0 schema collision discovered en route: `admin-call-recordings.js` was inserting into `blocked_numbers` with the pre-PR-1 column shape (`phone`, string `blocked_by`); the call-disposition spam path would have thrown a column-not-found error the first time anyone tagged a call as spam.

The backfill script is idempotent on `(channel, twilio_sid)` so a partial run that gets killed (Railway, network blip) can be safely resumed. Email is intentionally out of scope — `emails` has no twilio_sid, lives on a separate Gmail-sync pipeline, and the inbox does not surface email today; the schema will accept email rows whenever PR 5 (Beehiiv + email intake) gets to it.

**Revisit if:** A consumer is found that still reads `sms_log` or `call_log` directly and breaks because of the cutover. The known-safe writes-through targets are the scheduled-SMS cron (queries `where status='scheduled'`) and the call-recordings processing pipeline (`processing_status` + `recording_url` workflow). If a third consumer surfaces, decide per-case whether to add a unified read or extend the dual-write further.

---

## 2026-04-18 — PR 3 lands Customer 360 unified Comms tab; SMS + voice share one thread

**Decision:** Replace the Customer 360 Comms tab's SMS-only feed with a unified per-customer thread that interleaves SMS and voice messages in a single chronological view. Ships a new `GET /api/admin/customers/:id/comms` endpoint and rewires the Comms tab in both `Customer360Profile.jsx` (V1) and `Customer360ProfileV2.jsx` (V2) to fetch from it. Channel-aware rendering: SMS stays as chat bubbles; voice renders as a compact call card with duration, ai_summary (or transcript fallback), answered-by, and an inline audio player when a recording is attached.

**Context:** Before this PR, the Customer 360 Comms tab consumed `data.smsLog` — the per-customer SMS-only feed returned by `GET /api/admin/customers/:id`. Voice interactions existed only in the Timeline (a separate zone further down the panel, mixed with service/payment/note rows). From Virginia's and Waves's perspective, a customer's "conversation with Waves" is one stream — remembering whether you last talked to Aaron over SMS or over the phone before you open his profile is the kind of friction the unification is specifically designed to remove. This PR consumes the new `conversations` + `messages` read surface that PR 2 made authoritative; it's the first customer-facing view wired to it beyond the Timeline cutover.

**Reasoning:** Both V1 and V2 had to ship in the same PR because the feature-flag (`customers-v2`) is set per-user — cutting V2 alone would leave flag-off users on the legacy SMS-only Comms tab for the lifespan of the rollout, and the strategy doc is explicit that unification is the load-bearing improvement here, not the visual refresh. Channel-aware rendering (voice as a card, not a bubble) is chosen over flattening both to bubbles because a voice touchpoint has fundamentally different content — duration, who answered, an audio recording — that doesn't compress into a chat-bubble metaphor without losing information Virginia needs for CSR coaching and call review. The endpoint includes `ourEndpointLabel` via `TWILIO_NUMBERS.findByNumber` so a future PR 4 iteration can show "last SMS came in on Lakewood Ranch HQ" without re-querying; for now the field is plumbed but not displayed. Limit defaults to 100 with a 500 ceiling — larger than sms_log's old 20-row cutoff because the unified view is the primary place Virginia reconstructs multi-month customer histories; the old 20-row limit was the main complaint on long-tenure customers.

**Revisit if:** Call transcripts + ai_summary grow long enough that the inline card crowds the thread (current calls average 1–3 minutes with 60–200-word summaries; if average summary length passes ~400 words, collapse-by-default is the right move). Also revisit once PR 5 adds email to the schema — email belongs on this same unified thread, and the current channel-aware dispatcher will need a third branch with its own card shape (subject line, thread depth indicator, attachment chips).

---

## 2026-04-18 — PR 4 lands inbox filter chips, reply-from lock, and unknown + block UX

**Decision:** Bundle four inbox-hardening sub-features into a single PR on the Communications SMS tab: (1) filter chips row (All / Unread / Unanswered / Unknown / Blocked) that stacks with the existing StatCard-driven `smsFilter`, (2) reply-from lock that pins the outgoing number to the thread's `our_endpoint` once a thread reply is active, (3) Unknown-contact badge + styling on thread rows where the phone number doesn't resolve to a customer, (4) Block / Unblock inline action on each thread row backed by three new endpoints (`GET/POST/DELETE /api/admin/communications/blocked-numbers`). Also folds in a P1 bug fix on `server/routes/voice-agent.js` — the inbound-voice spam check was querying `blocked_numbers.phone`, a column that no longer exists after PR 1's `phone → number` rename, and the query's failure was being silently swallowed by a try/catch meant for "table doesn't exist yet."

**Context:** PR 1–3 gave Virginia a unified message schema and a unified Customer 360 thread, but the inbox itself — the list view she spends most of her day in — was still unchanged from the pre-unification UI. Three complaints show up repeatedly in her feedback: (a) no way to filter to just the threads that actually need her (unread, unanswered) without hunting; (b) the reply-from selector quietly resets between thread clicks, so she accidentally replies from the wrong Twilio number; (c) there was no inbox-level action to block a known spam number — every block had to be tagged post-hoc from call-recordings, which only catches voice spam, never SMS. At the same time, the voice-agent spam block was broken since PR 1 and nobody noticed because the try/catch hid the error, so this PR closes the loop on both inbound SMS spam UX and inbound-voice spam enforcement.

**Reasoning:** Bundled over split because the four sub-features share one surface (the inbox list), one state slice (`blockedNumbers` + `statusFilter` + `threadLock`), and one user (Virginia). Splitting would either force the first PR to include UI scaffolding that the second PR duplicates, or ship a half-wired inbox (e.g. chips without Block action) for a week. Filter-chip state chosen as a new `statusFilter` rather than overloading the existing `smsFilter` (which is tied to StatCard clicks filtering by message type — reminder / review / post-service / recurring) because the two concepts stack: "show me unread reviews" is a legitimate query. Phone matching uses a last-10-digit `phoneLast10` normalizer + a `Set` of blocked last-10s — blocked rows store E.164 (`+1…`) while thread `contactPhone` comes from the messages table in varying shapes, and string-equality across stored formats would miss matches. Reply-from lock mirrors how the real conversation works: when Virginia clicks Reply on a thread that landed on Lakewood Ranch HQ, every subsequent outbound in that session should go out from Lakewood Ranch HQ, not silently default to the first number in the select; an explicit Override link is rendered next to the locked banner for the rare case where she genuinely wants to switch. Block action writes synchronously on click (no confirm modal) because the reverse — an Unblock button rendered on the same row — is one click away and the cost of accidental block is low; this matches the standing "minimum-surgical" preference.

On the voice-agent fix: the alternative was to ship it as its own pre-PR but the bug was caught while auditing spam-block code paths for this PR's UX, both mutations touch the same table/shape, and the PR's headline feature is "block this number" — shipping an inbox Block button while leaving inbound voice blocking broken would have been misleading to Virginia. The try/catch that was swallowing the error stays in place (it legitimately guards against the table being absent on very old deploys), but the column reference is now correct and the logic checks `block_type === 'hard_block'` before issuing `<Reject/>` so that soft-block types (`silent_voicemail`, `ai_screen`) can take different branches later without a second column-rename-style regression.

V1 + V2 shipped in the same PR for the same reason PR 3 did: `comms-v2` is set per-user, so cutting only one version would leave half the team on a different inbox behavior for the rollout window. V1 uses inline styles + the `D` palette (teal border for the lock banner, amber for Blocked chip, slate-muted for Unknown); V2 uses Tailwind + the `Badge` primitive with `tone="strong"/"muted"`. Both render identical behavior; only the chrome differs.

**Revisit if:** Virginia starts getting customer complaints that legitimately-unknown new leads are being blocked because the Block action is too close to the tap target on mobile — if that happens, move Block to a thread-detail secondary action rather than a row-level affordance. Also revisit once PR 6 (template effectiveness analytics) lands: the filter chips may need an additional `Template-sent` facet to filter down to threads where a template was the last outbound, which the current `statusFilter` state slot can accommodate without schema changes.

---

## 2026-04-18 — PR 5 lands in-house newsletter sender on SendGrid; Beehiiv earmarked for removal

**Decision:** Ship a newsletter composer, subscriber management, and a send path inside the admin portal, built on top of the PR 1 unified comms schema. SendGrid (Twilio-owned) is the sending provider, swapped in late during the design conversation after the operator pre-configured a SendGrid account with domain authentication on `wavespestcontrol.com` (DKIM at `s1._domainkey` + `s2._domainkey`, return-path subdomain `wavesnewsletter.wavespestcontrol.com`, link-tracking subdomain `click.wavespestcontrol.com`). Scope covers everything admin-side — migration, provider wrapper, admin API routes, public unsubscribe endpoint + RFC 8058 one-click header, V2 Newsletter tab with Compose / History / Subscribers sub-views, and a feature flag (`newsletter-v1`) to gate the surface until DNS records propagate. V1 does not get a Newsletter tab — the feature is net-new and V1 users had no newsletter tool before, so this is not a 1:1 visual refresh. Public signup form + spoke-fleet archive pages are explicitly deferred to PR 6 so they can be designed against the customer-facing brand brief rather than the monochrome admin spec.

**Context:** Waves currently pays Beehiiv and uses their hosted newsletter product. Two problems: (a) cost grows with list size and the revenue model is misaligned with a small-list small business, and (b) Beehiiv sends from their own SPF/DKIM (newsletter.wavespestcontrol.com CNAMEs to cname.beehiiv.com for the web archive only) so a customer's newsletter opens never appear on their Customer 360 thread, defeating the whole point of the PR 1 unification. Every newsletter send to a known customer should dual-write into `messages` with `channel='newsletter'` so Virginia reconstructing a customer's history sees the same stream of touchpoints — SMS, voice, email, newsletter — without jumping tools.

**Reasoning on the provider:** The initial design recommendation in the same session was Resend, on grounds of DKIM simplicity (one CNAME vs three) and modern API. The operator pivoted to SendGrid mid-implementation because (a) the Twilio account already exists and adding a SendGrid sub-account collapses billing onto one invoice, (b) the SendGrid Sender Authentication wizard had already been completed before code review, so the DNS work was effectively done, and (c) at the volume Waves actually sends, the per-1k pricing delta between SendGrid and Resend is roughly $5/mo — not load-bearing against the operational simplicity of one vendor. The code wrapper is small enough (~150 LOC) that switching providers later is a ~1-hour change if SendGrid deliverability disappoints. The interface in `server/services/sendgrid-mail.js` (`sendOne`, `sendBatch`, `unsubscribeUrl`, `injectUnsubscribeFooter`) is intentionally vendor-shaped enough to swap without consumer changes — `admin-newsletter.js` only knows about the higher-level helpers, not SendGrid REST shapes.

**Reasoning on the schema:** Splits into three tables — `newsletter_subscribers` gets patched (adds `unsubscribe_token`, `customer_id` FK, bounce tracking, vendor contact id) so the minimal 20260416 table becomes usable for one-click compliance; `newsletter_sends` is one-row-per-campaign with aggregate counters; `newsletter_send_deliveries` is per-recipient and is what a future SendGrid Event Webhook handler will update on `bounce`/`spamreport`/`open`/`click`. Splitting campaigns from deliveries (rather than stuffing everything into `newsletter_sends`) means a 5k-recipient send doesn't require a JSONB array mutation for every event — the webhook handler upserts a single row by message id. The deliveries column is named `resend_message_id` historically (artifact of the brief Resend-first scaffolding); kept as-is to avoid a follow-up rename migration since it's vendor-portable in semantics — it stores whichever provider's message id is current.

**Reasoning on unsubscribe:** Ships RFC 8058 compliant out of the gate. SendGrid's `personalizations[].headers` array supports per-recipient `List-Unsubscribe` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click`, and a `{{unsubscribe_url}}` substitution inside the body footer expands to the per-recipient URL in the same API call (1000-recipient personalizations cap, chunked at 500 for safety). The POST endpoint always returns 200 (even for missing / malformed tokens) because Gmail treats any non-200 as an unsub failure and may penalize sender reputation. The GET endpoint renders a self-contained HTML confirmation page (no template engine, no client bundle) so it works during any outage that might take down the SPA. SendGrid's own `subscription_tracking` is explicitly disabled — we own the unsub flow so Customer 360 stays the single source of truth.

**Reasoning on the feature flag:** Default is off — the tab literally does not appear in the nav until the flag is flipped, because sending before DNS is configured + verified would produce SPF/DKIM-fail bounces that damage `wavespestcontrol.com`'s sender reputation. The gating rationale is not "we're unsure if this works" (it's tested end-to-end via the public unsubscribe path); it's "we physically cannot send mail until DNS propagates and SendGrid's wizard turns green." The flag is a forcing function: flip it the same day DNS goes live.

The Beehiiv service (`server/services/beehiiv.js`) and its `EmailAutomationsPanel` consumer are intentionally left in place — they handle 7 Zapier-replacement automation flows (lead nurture, review thank-you, treatment-specific emails) that are separate from the broadcast newsletter. A later PR will migrate those to SendGrid (or, if the BI agent needs templated marketing automation, to SendGrid Marketing Campaigns) once the newsletter path has run in production for a few cycles and the DNS + deliverability baseline is proven. Do not remove Beehiiv in this PR.

**Revisit if:** Bounce rate exceeds 2% on the first real send, which would indicate the subscriber list imported from Beehiiv has decayed — in that case, run a re-engagement sequence before any blast. Also revisit if SendGrid's shared-IP deliverability proves insufficient (less likely now with the dedicated authenticated domain) — at Waves's volume a dedicated IP is overkill, but Postmark becomes a serious alternative for transactional reputation if needed; the vendor-coupled surface is small enough (the wrapper + future webhook handler) that switching is a single-PR change.

---

## 2026-04-22 — `--danger` token aligned to marketing brand red (`#C8102E`)

**Decision:** Swap the customer-tier `--danger` token in `waves-customer-surfaces-spec.md` §3 from a generic red (`#B91C1C`) to the marketing site's brand red (`#C8102E`, matching `--color-brand-red` on `wavespestcontrol.com`). `--success` stays at `#047857` unchanged — marketing does not define a brand green and the operational success color is a portal concern, not a marketing inheritance. The swap is spec-only for now; `/apps/portal/src/styles/brand-tokens.css` does not yet exist in the repo, so no live CSS change is required and no render-site regression is possible at this stage. When the customer-tier portal surfaces begin implementation (§7.1 `/login` first), the token file will be published with the new value baked in.

**Context:** Same session as the §3 brand-token swap to marketing's `--waves-navy` (`#1B2C5B`) earlier today. Two token placeholders remained after that pass — `--danger` and `--success`. Marketing has a dedicated brand red (`#C8102E`, visible as an accent on bordered marketing elements and pulled from the site's Tailwind config as `--color-brand-red`) but no corresponding brand green. Leaving `--danger` generic while the rest of the tokens pulled from marketing would introduce a visible inconsistency the first time an overdue invoice, a destructive confirm modal, and the marketing CTA were on screen in the same viewing session — the reds wouldn't match, and on a customer portal where the invoice is a branded artifact, that matters.

**Reasoning:** `#C8102E` passes WCAG AA on white surface by a margin — contrast ratio 5.88:1 against `--surface` (`#FFFFFF`) versus the 4.5:1 normal-text threshold, 3:1 large-text threshold. The corrections prompt called out a fallback (`#B00D26`) if AA failed; contrast is clear so no fallback needed and no deviation to note. `--success` stays put because the portal's operational semantic (payment confirmed, service complete, earned-receipt green moment per §7.10) is a portal-tier design decision, not a marketing inheritance — and marketing has no established green to borrow from. Forcing a marketing-aligned green when marketing doesn't have one would be manufacturing a token drift rather than fixing one.

The four documented render sites — `/pay/:token` overdue header band + Amount Due figure, Invoice PDF "PAST DUE" block (§7.11.1), `PortalPage` Balance Due tile overdue state, and destructive `<ConfirmModal>` buttons (admin surfaces per `waves-admin-email-ui-spec.md`, now marked out-of-scope per same-day corrections) — will all inherit the swap automatically via `var(--danger)` once the token file publishes; none have been implemented against the new spec yet, so there is no existing UI to regression.

**Revisit if:** A future build discovers the brand red on a dark or mid-tone admin surface (e.g., a dark-mode variant or a red-on-colored-pill pattern) where `#C8102E` fails AA. The fallback `#B00D26` from the corrections prompt remains the documented secondary; any adopted deviation should add an entry here, not silently change the token file. Also revisit if marketing rebrands — the `--waves-navy` + `--color-brand-red` pair is the current canonical accent pair; if marketing drops one, re-pull and reconcile.

---

## 2026-04-28 — Termite bait install pricing realigned (1.75× → 1.45×, default Trelona → Advance, labor estimate 15 min/sta → 5 min/sta)

**Decision:** Drop `TERMITE.installMultiplier` from 1.75× to 1.45×, switch default `system` from `'trelona'` to `'advance'` across all entry points (`service-pricing.priceTermiteBait`, `estimate-engine.js`, `property-lookup-v2.js`), correct per-station `stationCost` to verified wholesale (`advance` $14 → $13.16 from $131.60/10-cs RFID; `trelona` $24 → $22.05 from $352.80/16-cs pre-baited annual RFID), and re-calibrate the install-labor cost estimate at `service-pricing.js:474` from 0.25 hr/sta (15 min) to 0.083 hr/sta (5 min) to match observed real-world pace. Trelona stays selectable as the premium upsell. Per-station `laborMaterial` ($5.25) and `misc` ($0.75) are unchanged — they are the only labor recovery in the marked-up base (the install labor figure at line 474 is for margin tracking only and does not flow into the customer price), so removing them would silently under-recover labor and was deferred to a separate formula refactor.

**Context:** All U Need Pest Control invoice from 4/29/2025 (same Manatee market, 2750 89th Street Circle E, Palmetto): 21 Sentricon Recruit HD bait stations installed for $375 total (no tax, ~78 min on-site, single tech). Sentricon Recruit HD is active-bait at the same wholesale tier as Advance ($13–15/sta). Our engine on the same 21-sta footprint quoted $1,103 (Trelona default × 1.75) — roughly 3× the competitor's doorstep number. Even on Advance the engine returned $735 (~2× competitor). The 1.75× multiplier had been bumped from a prior 1.45× during a margin audit on the assumption that the install needed to stand on its own; in reality bait-station economics are a 5–10 year monitoring annuity (Waves charges $35–$65/mo) and the install is a foot-in-the-door for that recurring stream, exactly as the competitor is operating.

**Reasoning on the multiplier:** Reverting to 1.45× brings the canonical 21-sta job (1,764 sf footprint, slab, block, standard landscape) to **$583 on Advance** and **$854 on Trelona**. With the corrected labor estimate (see below), engine-reported `installation.margin` lands at **20.5% on Advance** and **23.9% on Trelona** — under the 35% global floor but acceptable for termite, which is the **only** pricing function that doesn't return `marginFloorOk` (`service-pricing.js:485–502` returns `installation.margin` for display but no floor flag), so `discount-engine.js:134` does not reject the line at sub-floor margin. The field is informational; the real economics for bait stations are the 5–10 year monitoring annuity ($35–$65/mo), and the install is the foot-in-the-door. Stress-cases still clear the floor: Advance + crawlspace + wood frame = $821 at 43.6% margin. Going below 1.45× was modeled (1.35× lands Advance at ~$543, 1.25× at ~$502 with the $6/sta buildup intact) and rejected: a single competitor data point isn't a market average, and 1.45× already puts our doorstep within ~50% of the competitor's loss-leader install. If we see 3+ more invoices at $375–$450 across SWFL bait-station competitors, drop to 1.35× as a follow-up; the change is one line.

**Reasoning on the labor estimate change:** The pre-existing 0.25 hr/sta (15 min) figure at `service-pricing.js:474` predates the wholesale audit and proved empirically wrong against the competitor invoice — 21 Sentricon stations installed in 78 min by one tech = 3.7 min/sta. Bait-station install is fast: walk the perimeter, auger or core a hole, drop the pre-baited cassette, cap, log the RFID. It is not the trenching-and-injecting work the legacy figure seems to assume. Calibrating to 5 min/sta (0.083 hr) is the conservative middle ground — slightly above the observed 3.7 min/sta to absorb travel between stations on irregular lots, irrigation chase, and per-station data entry, but well below the 15 min that effectively double-billed labor on top of the $5.25/sta `laborMaterial` buildup that was already present in the marked-up base. Net effect: reported install margin moves from ~−0.5% / 9.5% (Advance/Trelona at 1.45× with the old labor figure) up to 20.5% / 23.9%, which is what the unit economics actually look like. This change touches only the cost-side margin computation; `installPrice` (the customer-facing number) is unaffected. The figure is hard-coded with no DB override path, so the migration in this PR doesn't need to address it.

**Reasoning on the default switch:** Trelona was the historical default in part because it ships pre-baited (no separate cartridge install), but the price difference is significant ($22.05 vs $13.16 wholesale = $186/sta delta marked up at 1.45× = ~$270 difference on a 21-sta job). For the residential SWFL market where the competitive doorstep is the deciding number, leading with Advance is the right shape — Trelona becomes a "premium active monitoring" upsell with a clear story (RFID + always-active bait + fewer service callbacks for recharging) rather than the silent default. The orchestrator at `estimate-engine.js:181` and the property-lookup pre-fill at `property-lookup-v2.js:1219` both updated to match so admins building a quote without explicitly choosing get Advance.

**Reasoning on wholesale cost correction:** The prior $14/$24 figures were within $1–2 of actual wholesale, so the cost correction itself moves the headline number trivially (~$15–25 on a 21-sta job). The reason to update them anyway is engine integrity — `installMargin` at `service-pricing.js:477` is reported back to the estimate UI and feeds margin-floor checks at `GLOBAL.MARGIN_FLOOR` (35%). Slightly inflated cost basis falsely depresses reported margin; correcting it gives operators (Waves directly) accurate visibility when they're editing prices at the margin.

**Reasoning on the unchanged $6/sta buildup:** `service-pricing.js:473–476` calculates `installMaterialCost = stations × (stationCost + laborMaterial + misc)` and applies the 1.45× markup only to that aggregate; `installLabor` (15 min × $35/hr per station) is computed at line 474 but only flows into `installCost` for margin tracking, not into the marked-up base. The $5.25 `laborMaterial` + $0.75 `misc` per station, marked up 1.45×, contributes ~$8.70/sta to the customer price — close to the actual labor cost ($8.75/sta) it's effectively standing in for. Removing the buildup as "redundant" was tempting on first read but would drop labor recovery to zero, leaving the install priced at material × 1.45 only and putting margin under floor on small jobs. The cleaner long-term fix is to fold actual install labor into the marked-up base and delete the synthetic buildup; that's a formula refactor with regression-test implications and was scoped out.

**DB migration:** The `pricing_config` table has two coexisting termite_install schemas — short-key (from `admin-pricing-config.js`, what `db-bridge.js:127–141` actually reads) and long-key (from migration `20260414000026`, dead-read but populated). Migration `20260428000004_termite_install_pricing_april_2026.js` does a single JSONB merge that updates both shapes so any environment converges regardless of which seeder ran. Without the migration, `db-bridge.js`'s 60s reload would silently overwrite the new `constants.js` defaults with the stale DB row.

**Revisit if:** Three or more SWFL competitor invoices come in at $375–$500 for ~20-sta installs (drop multiplier to 1.35× and consider rebalancing the buildup at the same time). Also revisit if the Corteva or BASF wholesale lines change by >10% — the cost basis is hard-coded and `db-bridge.js` only overrides if the DB row has the matching key. Also revisit if Waves decides Trelona's RFID + always-active bait is a meaningful enough differentiator to lead with again, in which case the default flip is one line back.

---

## 2026-05-03 — Public booking confirms customers from address match and stabilizes curated availability

**Decision:** Ship the address-based public-booking flow in two mainline commits: `c064afa` (`Fix public booking availability loop`) and `db23154` (`Confirm booking customer from address match`). The public booking page now avoids reloading availability when the geocoded coordinates are unchanged, hides redundant address-label copy, and switches the step-3 copy to a confirmation state when an existing customer is uniquely matched by address.

**Context:** The touched surface is `client/src/pages/PublicBookingPage.jsx`; the backend touchpoint is `server/routes/booking.js` (`GET /api/booking/customer-lookup` and `POST /api/booking/confirm`). Customer lookup can now resolve a single customer by normalized address with optional city/ZIP guards, and confirmation re-verifies that address before accepting an existing-customer booking without phone verification.

**Reasoning:** The booking-curation rules remain aimed at presenting the 3-4 best times instead of a raw slot dump: prefer different days, balance AM/PM choices, and weight soonness so customers see useful near-term options without every refresh reshuffling the list. The address-match confirmation path reduces friction for known customers booking from their service address while avoiding ambiguous address collisions by requiring a unique match.

**Revisit if:** Multiple customers legitimately share the same service address (multi-unit, property manager, duplex) and the unique-match rule blocks good bookings. In that case, add unit-aware matching before loosening the existing address verification.

---

## 2026-05-23 — Data Hygiene Agent ships deterministic-first, proposal-based, and sensitive-aware

**Decision:** Build the Data Hygiene Agent as a phased, review-first system: scanners create `data_hygiene_proposals`; admins approve/reject through an allowlisted apply layer; every applied/reverted mutation writes a transactional `audit_log` row. Phase 0 creates the durable foundation only: `data_hygiene_runs`, `data_hygiene_proposals`, `data_hygiene_source_extractions`, `data_hygiene_sensitive_vault`, feature gates, audit helpers, and a no-op orchestrator with a DB-backed run lock and stuck-run reaper.

**Context:** The live data snapshot on 2026-05-22 reframed the work. The visible raw-phone inbox rows are primarily orphaned `conversations.customer_id IS NULL`, not blank customer names. Calls are the richer source for operational details: `call_log.ai_extraction` already has hundreds of structured rows, and call transcripts are more useful for gate/access extraction than inbound SMS. Dedupe must stay advisory and account-level because multiple `customers` rows under one `customer_accounts` row represent real rentals, second homes, property managers, and shared contacts. Shared phone/email alone is not a merge signal.

**Reasoning:** Deterministic rules (phone/email/ZIP/name whitespace, link candidates, backfill candidates, dedupe candidates) stay explainable and reviewable. LLM use is limited to Phase 4 extraction, gated separately, tracked in `data_hygiene_source_extractions` by `(source_type, source_id, extractor_version, source_hash)`, and retry-limited with `attempt_count` plus terminal `failed_max_retries` so bad transcripts do not burn budget forever. `data_hygiene_runs` uses a partial unique index on `status='running'` instead of `pg_try_advisory_lock`, avoiding pooled-connection lock/unlock ambiguity. The reaper marks stale running rows failed after two hours so a missed close cannot permanently block scans.

Sensitive operational fields (gate codes, lockbox codes, garage codes, access notes) are not stored raw in proposal or audit rows. Normal proposal/audit surfaces hold redacted values and keyed HMAC hashes; raw before/after values live in `data_hygiene_sensitive_vault` encrypted with `DATA_HYGIENE_VAULT_KEY`, read only for permissioned reveal/revert paths and themselves audit-logged. This reconciles two requirements that otherwise conflict: masked long-lived audit tables and conflict-guarded safe revert.

**Constraints baked into the design:** `stripe_customer_id` and Square fields are read-only; email-derived names remain low-confidence proposals forever; auto-apply is explicit opt-in in every environment; Phase 1.5 bootstrap can backfill only from already-linked calls, while orphan calls produce link proposals first; actual merge execution is out of scope for v1 and the Possible duplicates tab is read-only.

**Revisit if:** The first dry-run week shows more than 2% rejection on any auto-apply candidate rule, if the sensitive vault needs key rotation tooling, or if property managers/multi-property accounts create too many ambiguous link proposals for manual review. Actual merge handling should remain a separate Stripe-aware Phase 6 plan.

---

## 2026-05-28 — Pest pricing: margin guard, one-time recurring-incentive model, config guardrails

**Decision:** Three coupled changes to pest control pricing in `server/services/pricing-engine/`:
1. **Post-discount margin guard for pest.** Generalized `applyMarginGuard` (was Tree & Shrub-only) to also cover `pest_control`, using pest's single `costs.annualCost` (direct + admin) as the cost basis. The WaveGuard **auto** discount is now capped so displayed margin never drops below the 35% floor; the estimate engine applies the guard to pest in the same branch as T&S. **Manual** owner discounts are intentionally NOT capped — they emit a warn-only entry in `summary.marginWarnings` (`type: manual_discount_below_margin_floor`, distributed proportionally across eligible recurring lines) and set `manualMarginWarning`/`manualFinalMargin` on the line. `validateEstimateDiscounts` stays retired (returns `[]`); the new warnings are computed inline in `estimate-engine.js`.
2. **One-time pest = `(quarterlyPerApp + $99 setupEquivalent) × 1.20 premiumMultiplier`**, floored at $199 (rarely binds). Replaces the old `quarterly × 1.75`. The one-time price is now always **anchored on the quarterly rate** — the estimate engine passes pest line `basePrice` (frequency-independent) instead of the selected-frequency `perApp`, fixing a bug where a customer quoted recurring monthly would get a one-time anchored on the discounted monthly per-app. New `ONE_TIME.pest` shape: `{ setupEquivalent, premiumMultiplier, floor }` (legacy `multiplier` removed).
3. **DB-bridge economic guardrails.** `validatePestPricingConfig` now rejects `frequencyDiscounts` outside `(0, 1]` and validates the new `ONE_TIME.pest.{premiumMultiplier, setupEquivalent}` fields. (`floor > base` is intentionally allowed — it just raises the minimum quote; `max(floor, base + adjustments)` still lets larger homes exceed it.) Sync reads new `onetime_pest` keys (`setup_equivalent`, `premium_multiplier`); the legacy `multiplier` key is ignored (different semantics — would mis-map). Migration `20260528000021_onetime_pest_setup_premium_model.js` rewrites existing `pricing_config.onetime_pest` rows to the new shape (preserving any customized floor); the route seed default is updated to match.

**Context:** Originated from a pest-pricing audit. Findings: pest had no margin-floor enforcement after discounts (only T&S did), so WaveGuard + manual discounts could silently quote pest below cost; `validateEstimateDiscounts` was a no-op; DB validation was type-only (accepted 0/negative/base<floor); and one-time pest (~$205) sat at or below a recurring customer's visit-1 cost ($99 setup + $117 quarterly = $216), giving customers no reason to commit to recurring.

**Reasoning:** Owner chose "cap auto, warn on manual" (loss-leader/goodwill pricing stays possible but is flagged) and "setup + premium markup" for one-time (one-time now strictly exceeds recurring entry: ~$259 vs $216 at 2,000 sf). At default constants the margin cap never binds (pest margins ~47–61% even at Platinum), so existing quotes are unchanged; the guard only engages if base is lowered or discounts deepened. The client-side `client/src/lib/estimateEngine.js` still carries the legacy `× 1.75` one-time math but is deprecated and not the live pricing path (V2 admin tool prices via the server engine); it was left untouched.

**Revisit if:** the margin cap starts binding on normal Platinum bundles (means base/cost drifted and pest base pricing needs a real re-anchor, not a discount cap), or if the one-time premium suppresses one-off conversions enough to hurt top-of-funnel. The legacy client estimateEngine one-time math should be reconciled or removed when that file is next refactored.

---

## 2026-05-29 — One-time pest = pure multiple off the quarterly rate (2.2×)

**Decision:** Replace the one-time pest formula `max($199, (quarterlyPerApp + $99) × 1.20)` (shipped days earlier in #1324) with a straight multiple: **`max($199, quarterlyPerApp × 2.2)`**. Drop the flat `setup_equivalent` ($99) and `premium_multiplier` constants; one knob now: `ONE_TIME.pest.{multiplier: 2.2, floor: 199}`. Migration `20260528000031` rewrites the `pricing_config.onetime_pest` row to `{floor, multiplier}` (supersedes `20260528000021`); seed default + db-bridge sync (`onetime_pest.multiplier`) + the client mirror (`lib/estimateEngine.js`) all updated to match.

**Context:** Owner's insight during review of #1324: one-time pest should **not** carry a separate size component, because the quarterly rate already encodes every property metric — footprint, lot, tree/shrub density, pool/cage, driveway, complexity, property type, age. A flat `+$99` add-on actually *inverted* the scaling: small/simple homes paid a higher effective multiple (~2.5× quarterly) than loaded/complex ones (~1.9×), the opposite of real job cost. A pure multiple makes one-time track job difficulty proportionally.

**Reasoning:** 2.2× keeps a typical home (~$117 quarterly) at ~$257 (≈ the prior $259, smooth transition) while correcting the curve: small/simple homes settle at the $199 floor (keeps the foot-in-the-door lead competitive), loaded homes rise to reflect real effort (e.g. ~$180 quarterly → ~$396). The recurring incentive is preserved and **enforced two ways** (both surfaced and hardened across Codex review rounds): (1) the db-bridge validates `floor` and `multiplier` *together*, scanning reachable quarterly bases with the SAME dollar rounding the pricer uses (`max(otFloor, round(q × multiplier))`) and requiring the result to stay **strictly above** recurring visit-1 (`PEST.floor + PEST.initialFee`) — so neither a lowered floor nor a near-boundary multiplier that only ties after rounding can quietly break it; (2) a runtime post-discount clamp keeps the 15% recurring-customer loyalty perk from ever dropping a one-time visit to/below that same recurring visit-1 cost on small homes (clamped strictly above, +$1 minimal margin since prices are whole dollars). Together these guarantee a one-off is always strictly more expensive than committing — for every property and every customer type.

**Blast radius:** At default constants the change only moves one-time pest (typical ~flat, loaded up, small down to floor); recurring pricing, the margin guard, and all other services are untouched. Migration is pre-deploy + fail-fast; sync reads only `multiplier` (legacy `premium_multiplier`/`setup_equivalent` ignored, incompatible with the `>=2` guard). Client mirror updated in lockstep (drift test enforces parity).

**Revisit if:** one-time conversion data shows 2.2× is suppressing one-off leads (lower it toward 2.0, the floor of the guard), or margin analysis on heavy initial visits says the multiple should be higher for the largest properties (consider a mild progressive multiple rather than reintroducing a flat add-on).

---

## 2026-05-29 — German Roach Cleanout = severity tiers, all-in flat price (replaces footprint + setup charge)

**Decision:** Replace the German Roach Cleanout (`german_roach`) pricing model. Old: footprint-interpolated base (`$450` base, `$400` floor, `±$40…+$85` footprint adjustment) **plus** a separate `$100` setup charge, fixed at 3 visits (`total = price + 100`, e.g. `$574` at 2,800 sf). New: a flat, **all-in** price keyed purely on infestation severity, with the visit count rising by tier:
- Light → `$350`, 2 visits
- Medium (`moderate`) → `$450`, 3 visits
- Heavy → `$550`, 4 visits

The tier price is the full customer total — there is **no separate setup charge** (`setupCharge: 0`) and **footprint is no longer consulted at all**. `severe` collapses into the Heavy tier; a missing/invalid severity defaults to **Light** (`severityWasDefaulted: true`). `pricingModel` is now `german_roach_severity_tier_cleanout` (the old `german_roach_three_visit_cleanout` is retained as `legacyPricingModel` for backward compat). The line stays `noRecurringDiscount` (excluded from WaveGuard/recurring percentage discounts) as before.

**Implementation:** `SPECIALTY.germanRoach` in `constants.js` is now `{ defaultSeverity, tiers: { light, moderate, heavy } }` (the `base`/`floor`/`setupCharge`/`footprintAdj` keys are gone). `priceGermanRoach` (`service-pricing.js`) does a severity → tier lookup instead of footprint interpolation. The client mirror (`client/src/lib/estimateEngine.js`) was updated in lockstep (drift test enforces server/client parity across all three tiers). `severity` now flows through both line-item normalizers (`v1-legacy-mapper.js` + client) so the quoted tier is visible to the UI. `db-bridge.js`'s `validatePestPricingConfig` was updated to validate the new tier shape (`defaultSeverity` references a real tier; each of light/moderate/heavy has a positive `price` and `visits`) instead of the removed `base`/`floor`/`footprintAdj` keys. The V2 server translate path (`property-lookup-v2.js`) already forwarded `germanRoachSeverity || roachSeverity` into `services.germanRoach`, so no engine-routing change was needed there.

**Admin UI:** both estimators (`EstimateToolViewV2.jsx` default, legacy `EstimatePage.jsx`) now show an **Infestation Severity** selector (Light/Medium/Heavy with visit + price hints) when the roach Service Type is "German Roach Cleanout", wired to a new `germanRoachSeverity` form field (defaults `light`). V2 posts it in the server options payload; the legacy page maps it to `roachSeverity` for the client-preview engine. The Service Type option label dropped the hardcoded "— 3 Visit Program" suffix since the visit count is now tier-dependent.

**Context:** Owner request — the old footprint+setup model "made no sense" for German roach: square footage is a poor proxy for the real cost driver (number of return trips to break the breeding cycle), and the separate setup charge obscured the all-in price. Severity tiers are easier for the office/techs to quote and for customers to understand, and tie visit count directly to infestation severity.

**Blast radius:** Only `german_roach` pricing moves; every other service (including the single-visit `pest_initial_roach` initial knockdown fees and the zeroed `roachModifier`) is untouched. The DB seed row `onetime_german_roach` is inert with respect to the engine (db-bridge only validates `SPECIALTY.germanRoach`; it does not override it from DB config), so no migration was needed. Default (no severity selected) now quotes Light/$350/2 visits where the old default was $574 at 2,800 sf.

**Revisit if:** severity self-reporting proves unreliable (techs consistently re-tier on site) — consider an in-field severity confirmation step before the cleanout price locks; or if a very large heavy infestation genuinely costs more than the flat Heavy tier covers (reintroduce a bounded size or visit add-on rather than reverting to footprint interpolation).

---

## 2026-05-29 — WDO inspection → filled FDACS-13645 PDF + combined report-with-invoice send

**Decision:** Built the missing pieces of the WDO inspection workflow so a `wdo_inspection` project can be turned into the genuine, filled state form and delivered to the customer alongside an invoice in a single action.

**What shipped:**
- `server/services/pdf/wdo-report-pdf.js` — fills the **real** FDACS-13645 AcroForm from `project.findings` using `pdf-lib`, then flattens it. Field map covers Section 1 company/inspector header (from `server/constants/business.js`), property/requester, Section 2 finding checkboxes + narrative, inaccessible-area checkboxes (keyword-detected), previous-treatment + treated-at-inspection radios, treatment method, and Section 5 comments. The form is flattened (locked); the licensee **signature line is left blank** by design.
- The official `forms.fdacs.gov/13645.pdf` is owner-password encrypted with no usable AcroForm in the older bundled copy. The current official form **is** a fillable AcroForm (71 fields). We committed the decrypted (qpdf `--decrypt`, empty password) fillable copy to `server/assets/forms/fdacs-13645-fillable.pdf` as the fill template. The pre-existing flat reference PDF at `client/public/forms/fdacs-13645-wdo-inspection-report.pdf` is untouched (techs still open it as a reference).
- `POST /admin/projects/:id/send` now attaches the filled FDACS-13645 PDF to the report email for WDO projects.
- `GET /admin/projects/:id/fdacs-pdf` — admin preview/download of the filled form.
- `POST /admin/projects/:id/send-with-invoice` — combined delivery: **one** email (FDACS PDF + invoice PDF attached, report link + pay link in body) and **one** SMS (report link + pay link). Supports `dry_run` to surface the resolved invoice amount before sending. Auto-creates a draft WDO inspection invoice when none is linked, priced from `SPECIALTY.wdo.brackets` (≤2500 $175 · ≤3500 $200 · >3500 $225). On delivery it transitions the invoice `draft → sent` directly (not via `InvoiceService.sendInvoice`) so the invoice's own SMS path doesn't fire a duplicate text.
- `project-email.js` gained `attachments` pass-through on `sendProjectReportReady` + a new `sendProjectReportWithInvoice` (reuses the seeded `project.report_ready` template — no new template to ship).
- UI: a **"Send report + invoice"** button on the WDO project detail (`ProjectsPage.jsx`), gated to `wdo_inspection`, non-closed projects. Dry-run first to confirm the invoice amount, then send. New `pdf-lib` dependency added to `server/package.json`.

**Signature protocol:** Per Rule 5E-14.142 F.A.C. / Ch. 482.226 F.S., form 13645 must carry the licensee/cardholder signature and date. We do not auto-forge a signature — printed name, ID-card number, and date are filled; the signature line stays blank for the licensee to sign before filing. A stored-signature-image option was deliberately deferred.

**Blast radius:** Additive. New module + two new routes + one extended email helper + one UI button. The existing `/send` flow is unchanged except for the WDO email attachment. To avoid coupling the projects route's boot to the `@waves/lawn-cost-floor` workspace package, the WDO fee is read from `pricing-engine/constants.js` (pure config) rather than importing `service-pricing.js`.

**SMS constraint:** SMS cannot carry attachments, so the customer gets the FDACS + invoice PDFs by email; the SMS carries the report link and the pay link. Both PDFs are also reachable from those links.

**Revisit if:** the owner wants the signature captured digitally (add a stored/e-sign step before flatten); or wants the combined send to skip the dry-run amount confirmation; or FDACS reissues the form with renamed fields (the filler logs and skips unknown fields rather than throwing, so a revision degrades gracefully but should be re-mapped).

**Addendum (same day, PR #1386 review):** WDO auto-invoice pricing changed from the `SPECIALTY.wdo.brackets` lookup to a **flat $250** for now, per owner. The real WDO tiers are **$150 / $200 / $250** by *structure* footprint, but the WDO inspection form doesn't capture structure footprint yet (`customers.property_sqft` is treated *lawn* area, not the structure), so inferring a tier from customer columns mis-bills small houses on large lots. The flat fee is a draft the operator can adjust in the dry-run; switch `resolveWdoInspectionFee` to the $150/$200/$250 tiers once the form captures structure sq ft. Also: the FDACS Section-1 inspector identity (Print Name / ID Card No) is now resolved from the project's technician (`created_by_tech_id` → `technicians.name` + `fl_applicator_license`), since the WDO findings don't collect it and the PDF routes load the project without the `tech_name` join.

**Addendum 2 (same day, PR #1386):** Three WDO additions. (1) **Licensee e-signature** — per owner, a WDO report is an official FDACS-13645 filing and now requires a captured signature before it can be sent. New `projects.wdo_signature` JSONB, a `POST/DELETE /admin/projects/:id/wdo-signature` endpoint, a canvas `WdoSignaturePad` on the WDO project detail, and the signature image is stamped into the form's `signaturelicensee` field before flattening. The report/combined sends now 422 with `code: 'signature_required'` until signed; the detail GET returns signature metadata only (image stripped). (2) **Supplemental Photo Addendum** — the generated PDF now appends addendum pages (2 captioned photos per page, "Page A-N") built from the project's S3 photos, matching the owner-supplied sample. (3) **Combined-send invoice guard** now also rejects `processing`/`sending` invoices (not just paid/void), matching the canonical invoice non-sendable set. Note: AI-written WDO narrative and photo upload already existed (`draftProjectReport` is photo-grounded; `project_photos` + `WdoIntelligenceBar`), so this work focused on the report *output* (signed FDACS + photo addendum) rather than re-building capture.

**Addendum 3 (same day, PR #1386):** WDO auto-invoice pricing moved from the interim flat $250 to **structure-footprint tiers** ($150 ≤2500 sq ft · $200 ≤3500 · $250 >3500), now that the WDO form captures footprint. Added a `structure_sqft` findings field to `wdo_inspection`; `resolveWdoInspectionFee(findings)` tiers on it and defaults to the top $250 tier when it's blank (conservative; operator adjusts in the dry-run). Kept local rather than `SPECIALTY.wdo.brackets` (still $175/$200/$225 and keyed to lawn-area `property_sqft`), which the owner confirmed is stale; the customer-facing estimate engine still reads those constants and is out of scope for this PR.

**Addendum 4 (same day, PR #1386):** WDO "Property specs" — the WDO Intelligence lookup (`lookupPropertyFromAITrio`, Claude web_search over SWFL county appraisers + listing sites) already resolved construction material / foundation / roof / year built / sq ft / stories; now surfaced. `WdoIntelligenceBar` renders a **Property specs** panel (with source + confidence + "verify on site") and an **Apply to report** button that maps the profile onto findings via `client/src/lib/wdoProfileToFindings.js`: sq ft → `structure_sqft` (drives the fee), stories+construction → `structures_inspected`, slab foundation → a Section-3 "Crawlspace: N/A — slab-on-grade" note, and year/construction/roof → Comments. The resolved profile is cached on `projects.property_profile` (new column) so the panel renders on reload without re-running the lookup; the detail GET returns it (image-free, small) and the list strips nothing extra. Suggestions only — everything stays tech-editable for the legal filing. (Phase 1+2 of the property-specs idea; prior-treatment/permit-history lookup deferred as Phase 3.)

**Addendum 5 (same day, PR #1386):** WDO **treatment/permit history** (Phase 3). New `server/services/property-lookup/wdo-history-lookup.js` — a focused Claude web_search call over SWFL county permit portals + listing remarks that returns prior WDO treatment (fumigation date/fumigant/company), most-recent re-roof permit year, and relevant permits, with sources + confidence. Opt-in endpoint `POST /admin/projects/wdo-history` (separate from Analyze so it's not paid on every run), cached on new column `projects.wdo_history`, returned by the detail GET. `WdoIntelligenceBar` gains a **"Treatment history"** button + a **Section 4** panel with **"Apply to Section 4"**, mapping via `applyHistoryToWdoFindings`: previous_treatment → `previous_treatment_evidence`, fumigation/notes → `previous_treatment_notes`, re-roof/permits → Comments. Conservative by design — returns "unknown" without a concrete source, never fabricates, and everything stays tech-editable + "verify on site" for the legal filing. Reliability caveat noted earlier (county permit coverage is uneven) stands; this is best-effort triage, not authoritative.
---

## 2026-05-29 — Estimate slot picker spreads across distinct days (no more single-day window dumps)

**Decision:** The customer-facing estimate slot picker ("Find a date & time that works for you" — `SlotPicker.jsx`, backed by `estimate-slot-availability.js`) now curates its primary + expander slots to **one window per distinct day, soonest first**, instead of filling all six primary cards with every available window on the single soonest date. The very first card is always the genuine soonest/earliest slot (the "book me ASAP" option), surfaced **regardless of route proximity**; each subsequent card steps to the next distinct day, and the per-day lead window is rotated so the list shows a spread of times (9a / 10a / 11a / 1p …) rather than nine identical 9 AM windows.

**Implementation:** New `diversifyByDay(sortedSlots)` helper in `server/services/estimate-slot-availability.js`, wired into `selectCustomerFacingSlots` (used by both the geocoded route-aware path and the no-coords ASAP fallback). It buckets the already-`compareCustomerFacingSlots`-sorted pool by date (Map preserves chronological insertion order), keeps the soonest day's earliest window pinned first, rotates each later day's bucket by its position for time variety, then round-robins one slot per day before doubling up. `splitSlotResults` still slices the first 6 into `primary` and next 3 into `expander`, so the customer now sees up to 9 distinct days. Exported under `_internals.diversifyByDay` for tests.

**Context:** Owner report (mobile screenshot): the standalone book-online estimate flow was showing "Sunday, May 31 — 10:00 AM / Sunday, May 31 — 11:00 AM / Sunday, May 31 — 1:00 PM / Sunday, May 31 — 2:00 PM …" — every card the same day. The old `selectCustomerFacingSlots` sorted purely by (date, time) and sliced, so the soonest date monopolized the list. This brings the estimate flow in line with the already-documented booking-curation philosophy (2026-05-03 entry: "prefer different days, balance AM/PM choices, weight soonness") that `booking.js` `curateSlots` already followed but the estimate-slot path never adopted.

**Blast radius:** Pure ordering/curation change inside `selectCustomerFacingSlots`; slot generation, geocoding, route-optimality classification, collision filtering, window spreading, caching, and the `{ primary, expander }` response shape are all unchanged. The prior test `does not hide earlier same-day windows for date diversity` codified the old single-day-stacking policy and was reversed (renamed to `spreads across distinct days instead of stacking one day`), with new coverage for the round-robin + soonest-first + time-spread behavior.

**Revisit if:** customers ask to see multiple times on the same soonest day up front (e.g. "I can only do tomorrow — show me all of tomorrow's openings"), in which case add a per-day expand affordance rather than reverting the cross-day spread; or if very sparse availability (only 1–2 bookable days in the 14-day window) makes the round-robin wrap feel repetitive, in which case fall back to richer same-day time variety for those days.

## 2026-05-29 — WDO combined send: serialize invoice resolve + stop dry-run from minting drafts (fix/wdo-invoice-dedupe-race)

**Decision:** `resolveOrCreateProjectInvoice` (server/routes/admin-projects.js) now performs its reuse-or-create decision inside a `db.transaction` that takes a `FOR UPDATE` lock on the `projects` row, and the dry-run path no longer creates a real draft invoice.

**Context:** Post-merge audit of PR #1386 (WDO inspection workflow) found two issues in the combined report+invoice send. (1) **Duplicate drafts under concurrency:** the dedupe chain (explicit id → `projects.invoice_id` → service linkage → create) was a read-then-write with no lock and no DB uniqueness backstop, so two overlapping `POST /:id/send-with-invoice` (the dry-run racing the real send, or a double-click on "Send report + invoice") both saw `invoice_id` null and both called `InvoiceService.create`, leaving two WDO drafts for one project. (2) **Dry-run side effect:** `resolveOrCreateProjectInvoice` ran before the `dry_run` early-return, so every "preview the amount" click minted + persisted a real draft (and burned an invoice number); cancelling at the confirm dialog stranded an orphan.

**Implementation:** The create path is wrapped in `db.transaction((trx) => …)`; it locks the project via `trx('projects').where({id}).forUpdate().first()`, re-reads the (possibly just-committed) `invoice_id` inside the lock, and reuses it if present — so the racing POST blocks until the winner commits the link, then reuses the same invoice. `persistProjectInvoiceLink` gained an optional `runner` (the trx) so the in-lock link update runs on the lock-holding connection (avoids self-deadlock). On `dryRun`, step 3 returns a non-persisted preview (`{ id: null, status: 'preview', subtotal, tax_amount, total }`, `created: true`) computed by new `previewWdoInvoiceTotals`, which mirrors invoice.js's single-source tax rule (residential untaxed; commercial via the same `TaxCalculator.calculateTax(customerId, 'WDO Inspection', subtotal)`), so the previewed amount matches what the real send bills. The route now derives `dryRun` from an explicit boolean (`=== true || === 'true'`) instead of truthy-coercing `req.body.dry_run`. Client (ProjectsPage) sends `invoice_id` only when the preview resolved an existing invoice, and omits the empty invoice-number label for brand-new WDOs.

**Blast radius:** Confined to the WDO combined-send path. `InvoiceService.create` still auto-commits on the pooled connection (it never touches the locked `projects` row, so no lock conflict); the explicit-`invoice_id` final-send path, the `'sending'` atomic claim, `markDeliverySent`, signature gate, and PDF build are all unchanged. Residential totals are identical (fee == total). The real send still creates exactly one draft and links it.

**Revisit if:** the project-row lock contends under future high-frequency automated WDO sends (move to a partial unique index on `projects.invoice_id` + 23505-as-reuse instead of a lock); or if commercial WDO previews ever diverge from the billed total (consolidate `previewWdoInvoiceTotals` and invoice.js tax into one shared helper rather than mirroring).

## 2026-05-29 — WDO audit follow-ups: lookup retry budget, blank-signature guard, pre-save history cache (fix/wdo-audit-followups)

**Decision:** Three deferred P2s from the #1407 audit, bundled. (1) The web_search Anthropic clients in `wdo-history-lookup.js` and `ai-property-lookup.js` (3 sites) now pass `maxRetries: 0`. (2) The `/:id/wdo-signature` endpoint rejects a blank/uniform-canvas image even when the PNG/JPEG magic bytes are valid. (3) The `/wdo-history` pre-save (no `project_id`) path gets a process-local TTL cache.

**Context:** Audit-of-the-audit findings explicitly deferred out of #1407. (1) The SDK default `maxRetries: 2` re-runs the entire `web_search` budget (`max_uses`, up to 8) on a transient 429/5xx, so one lookup could fan out to ~24 searches and ~3× wall-clock. (2) A direct API POST could save an all-white or fully-transparent (but structurally valid) PNG; the send gate treats any saved signature as complete, so it would emit an officially "signed" FDACS-13645 with no signature. The UI blocks this via `hasDrawn`, but the server didn't. (3) The project path caches history in the `wdo_history` column and early-returns; the pre-save tech/customer path re-billed the web-search call on every reload.

**Implementation:** (1) one-line `maxRetries: 0` at each client + a comment; single attempt already degrades to null on error. (2) new `signatureHasInk(buffer)` decodes with `sharp` (already a server dep, used in `social-media.js`) and rejects when every channel's `stdev <= 1.5`; empirically a blank white / fully-transparent canvas is `stdev 0` while even a ~0.5%-coverage stroke is `stdev ~25`, so the floor separates cleanly. Decode failure does NOT block (magic bytes already validated; UI `hasDrawn` remains primary). Returns 400 `{ code: 'signature_blank' }`. (3) `wdoHistoryAddressCache` (module-local `Map`, 6h TTL, 500-entry cap, oldest-evicted), keyed by normalized address (property-level public-records data, not customer-specific). Served only on the `!project_id && !wantRefresh` path; `refresh:true` still forces a fresh lookup; the project path is unchanged.

**Blast radius:** No schema, no API shape changes (cache adds a `cached: true` flag already used by the project path). Tax/invoice/PDF/send paths untouched. `sharp` is lazy-required inside the ink check so module load is unaffected in test contexts.

**Revisit if:** Waves moves to multiple server instances (the pre-save cache is per-process — move to Redis/DB if cross-instance consistency matters); or a legitimate ultra-minimal signature ever trips the `stdev > 1.5` floor (lower it, or switch to a non-background-pixel count).

---

## 2026-05-30 — Pest control "Service Recap" — lightweight complete-without-bill for pest_control

**Decision:** `pest_control` services now complete through a new lightweight **ServiceRecapModal** instead of being forced into the heavy `CreateProjectModal` "project report" (or the full `CompletionPanel`). The recap captures a quick internal note, product chips (chemicals applied), an AI-drafted customer message, and an optional "text the customer" send — then completes the visit in **recap-only mode (no billing)**. Available on **both** surfaces: admin dispatch (`DispatchPageV2`, light theme) and the tech portal (`TechHomePage`, dark theme). Scope is `completionProfile.category === 'pest_control'` (the services-table backed signal, not the broad `detectServiceCategory` fallback).

**Implementation:** New shared service `server/services/pest-recap.js` (`resolveEligibility` / `buildRecapContext` / `draftRecapMessage` / `submitRecap`). `submitRecap` transitions `scheduled_services.status -> completed` via the canonical `transitionJobStatus`, upserts the `service_records` row keyed by the direct `scheduled_service_id` FK (same row tech photos + customer-portal history read), replaces its `service_products` rows, flips `track_state -> complete`, and — only when asked — texts the customer via `sendCustomerMessage` (`purpose: 'service_completion'`). It deliberately never invoices, mirroring the existing `oneTimeRecapOnly` recap mode but as a slim pest-only path so the 1700-line completion handler and its `project_required` 409 gate are left untouched. AI copy reuses the existing `completion-recap.js` `generateRecap` generator (no new prompt). Three endpoints added to `admin-dispatch.js` after `/products/catalog` (`GET :id/pest-recap/context`, `POST :id/pest-recap/draft`, `POST :id/pest-recap`); the router's `requireTechOrAdmin` plus the tech portal already calling `/api/admin/*` means one endpoint set serves both surfaces, with a tech-only ownership guard. Client: new `client/src/components/ServiceRecapModal.jsx` (theme-aware, inline-styled like `CreateProjectModal`); `DispatchPageV2.handleComplete` and `TechHomePage`'s per-row project action branch to it for pest control via `isPestControlService`.

**Context:** Owner report — tapping **Complete** on a pest control re-service opened the documentation-heavy project report ("One-Time Pest" `project_required` profile) rather than a quick completion. Owner wanted something lighter than the full Complete flow with an AI text field "about what we did" to send the customer. Confirmed decisions: store in `service_records`/`service_products`; complete but **no billing**; **all** `pest_control` services; both surfaces; draft → confirm → send.

**Blast radius:** Additive. The full `CompletionPanel` / `CreateProjectModal` paths are unchanged for every non-`pest_control` service; the existing completion endpoint and its gate are untouched. Only the `handleComplete` branch (admin) and the per-row project action (tech) gain a pest-control fork. Recap completion writes the same `service_records` shape as the canonical flow, so customer-portal history and tech photo attach work unchanged.

**Revisit if:** owner wants email/portal recap channels (current send is SMS only — `sendCustomerMessage` email/portal is still follow-up); or wants the recap offered as a third explicit choice on non-pest services; or wants recap completions to optionally request a review (intentionally omitted to stay lightweight).

**Addendum (idempotency, Codex P1):** `submitRecap` is now serialized and the recap text is at-most-once. `scheduled_service_id` is only a non-unique index, so a double-tap / browser retry / admin+tech race could pass the check-then-insert and duplicate the `service_records` row **and** text the customer twice. Fix: the transaction opens with a `FOR UPDATE` lock on the parent `scheduled_services` row (re-reading live status under the lock, since the pre-transaction read can go stale), so concurrent submits queue and the loser sees the winner's committed row → UPDATE, not a second INSERT. The double-text half is closed by a new `service_records.recap_sms_sent_at` claim (migration `20260529000010`): the column is set inside the same locked transaction, so only the first submit sends; any submit that finds it already set is suppressed (`smsError: 'duplicate_suppressed'`). If the post-commit send fails/blocks, the claim is released (`recap_sms_sent_at -> NULL`) so a later retry can re-attempt. A record whose claim is still NULL (e.g. completed earlier via the heavy `/complete` path, or a recap saved without texting) still sends — preserving "complete now, text later." (This claim-based gate was chosen over gating purely on `didComplete`/`createdRecord`, which would have suppressed those legitimate later sends.) Client `ServiceRecapModal` also gained a `useRef` re-entrancy guard as defense-in-depth. Covered by `server/tests/pest-recap-idempotency.test.js`.

**Addendum 2 (cancelled/skipped rejection, Codex P1):** the original guard treated `cancelled`/`skipped` as terminal alongside `completed`, so a recap on a cancelled/skipped visit skipped the status transition but still wrote a `completed` `service_records` row, marked the tracker complete, and could text the customer — emitting "completed" customer artifacts for a visit the status machine says never happened. Now only `completed` is the idempotent re-recap case; `cancelled`/`skipped` (checked under the same row lock) abort the transaction having written nothing and return `ok:false` with reason `service_cancelled`/`service_skipped` (HTTP 409 via `recapStatusForReason`).

**Addendum 3 (preserve recorded products on re-recap, Codex P2):** the update path unconditionally `del`eted all `service_products` then inserted only what the modal submitted — but the modal starts with an empty product selection, so reopening a completed recap to re-send/edit (without re-picking every chemical) wiped the recorded applications. Two-part fix: (1) server backstop — product rows are only replaced when the submit specifies a **non-empty** set; an empty submission leaves the existing rows untouched (the modal has no way to express "remove all," so empty = no change). (2) `buildRecapContext` now returns the existing record's `service_products`, and `ServiceRecapModal` pre-selects them (matched to the catalog by name) on open, so editing builds on the recorded set rather than replacing it from empty. Covered by a "no products selected preserves recorded chemicals" test.

---

## 2026-06-11 — Specialty services → Service Report V1 pipeline (PR 0: product/UX contract)

**Decision:** 13 specialty project types (termite_inspection, pest_inspection, flea, cockroach, rodent_exclusion, rodent_trapping, wildlife_trapping, one_time_pest_treatment, one_time_lawn_treatment, mosquito_event, palm_injection, termite_treatment, bed_bug) will complete through the standard appointment lifecycle + CompletionPanel + auto-delivered Service Report V1 instead of the Projects flow. WDO + Pre-Treat Cert stay in Projects. This PR ships the binding product/UX contract only — no code: `docs/design/specialty-service-completion-contract.md` + 10 golden report fixtures in `docs/design/specialty-report-fixtures/`.

**Context:** Owner direction: same en-route → complete flow and the same customer report UI as recurring pest/re-service, with service-specific fields; tech completes in under a minute; customer gets a short plain-English report they actually read. Pest Pressure must not render for these — trend types (rodent, wildlife, bed bug, cockroach, flea, termite) get a relabeled activity gauge backed by a new `service_activity_scores` store. Locked decisions: auto-send (no review gate; staged pilot + per-type delivery_mode kill switch), report-every-trapping-visit with progress framing, tech-set 0–5 activity with derived prefill, appointment-only creation post-cutover, $0 linked follow-up appointments.

**Implementation (contract highlights):** profile row = feature flag (`service_completion_profiles.completion_mode` flip; `project_type` stays as the findings-schema pointer; `serializeProfile` will emit `findingsType` and null out `projectType` for service_report profiles). All implementation code ships dark; cutover = standalone explicit-allowlist migrations in phases (1: pest_inspection/mosquito/palm/one-time lawn+pest → 1b: ONE trend key at delivery_mode=internal_only → 2: trend types → 3: termite, compliance-gated on FS 482.226 / FS 482.2265 / FAC 5E-14). Customer copy is generated deterministically at completion and persisted in `service_data.typedReportSnapshot` (schemaVersion/copyMapVersion/summaryTemplateVersion + generated Today's Result + customer-labeled findings + serviceKey/serviceLabel/reportTypeLabel) — reports render from the snapshot forever. Hard constraints: p90 ≤60s mobile completion (telemetry-enforced), ≤4 required service-specific fields, no required free-text, AI never blocking, zero states render positively, banned-words list ("clear"/"eliminated"/"no infestation"/…), client-read instrumentation gates Phase 2.

**Blast radius:** None — documentation and fixtures only.

**Revisit if:** fixture review changes per-type required fields or summary templates (bump summaryTemplateVersion/copyMapVersion semantics in PR 1); a per-customer property/location entity is added (activity history is keyed customer+indicator for v1).

---

## 2026-06-11 — Specialty services → Service Report V1 pipeline (PR 1: dark backend foundation)

**Decision:** All server-side plumbing for typed specialty completions, shipped DARK. A typed completion activates only when a service's `service_completion_profiles` row has `completion_mode='service_report'` WITH a `project_type` pointer — no row is flipped in this PR, so production behavior is unchanged for every service. The profile row is the feature flag; cutover happens later via standalone explicit-allowlist migrations.

**Implementation:**
- **`service_activity_scores`** (migration `20260611000004`): per-visit 0–5 activity for trend types (rodent/wildlife/bed bug/cockroach/flea/termite). Separate from `pest_pressure_scores` by design — the pressure engine's consumers all assume engine-model rows, and separation guarantees recurring Pest Pressure history can never be polluted. `UNIQUE (service_record_id, indicator_key)` (composite, not bare) is the retry/resume idempotency guarantee and leaves room for multi-indicator visits.
- **`delivery_mode`** on profiles (migration `20260611000005`): `auto_send | internal_only | disabled`. `internal_only` renders + stores the report (token/PDF) with no customer SMS/email — the Phase-1b shadow mode. Plus global kill env `SPECIALTY_REPORT_DELIVERY_DISABLED`.
- **`bed_bug_treatment` services row** (migration `20260611000006`): fixes "bed bug has pricing config but no services row." Ships `customer_visible:false, booking_enabled:false` and `project_required` profile — zero behavior change; exposure gated on owner copy approval.
- **Profile pointer fixes** (migration `20260611000007`): `flea_tick`→`flea`, `rodent_exclusion%`→`rodent_exclusion` (stay project_required; only the correct project form opens).
- **`server/services/service-report/activity-indicators.js`** — single source of truth: indicator config (shared `indicator_key` across rodent trap/exclusion and termite inspect/treat so trends accrue per program), findings-select→score derivations (every gauge select now has a 0/"None observed" option — registry updated), score→customer wording (never a raw number; banned-words safe), technician→customer label/value maps, typed-findings validation, next-step chips, and **`buildTypedReportSnapshot`** — the immutable customer-copy snapshot (`service_data.typedReportSnapshot`, versioned: schemaVersion/copyMapVersion/summaryTemplateVersion) including the generated Today's Result text. Reports will render from this snapshot forever; copy is never recomputed from live templates.
- **`serializeProfile`**: emits `findingsType` (= project_type when mode is service_report) and **nulls `projectType` for service_report profiles** — stale `if (profile.projectType)` client logic can never route a typed completion into the Projects flow. Consumer grep verified: all server consumers key off `projectBacked`.
- **`POST /:serviceId/complete`**: optional `structuredFindings`/`activityScore`/`nextStepChips` (stale clients submit none → identical to today). When submitted: full registry validation (unknown keys, select values, required flags, strict 0–5 score; gauge types require a score, derived prefill recognized server-side), snapshot persisted into `service_data`, activity row inserted **in the durable trx** with `onConflict(...).ignore()`, **pest-pressure orchestrator + clientPestRating skipped** (explicit gate — these types detect to the 'pest' line and would otherwise pollute pressure history), **billing pre-gate** ported from the project flow (`resolveProjectCompletionBilling`, 409 `completion_billing_required` before any customer artifact; $0 resolves not_billable; `followup_included` appointments bypass), delivery-mode comms suppression (`effectiveSendCompletionSms` covers completion SMS, email queue, bundled + delayed review requests; `disabled` also skips PDF), `followupSuggestion` in the response (cockroach gated on canonical `species === 'German'`).
- **Dispatch jobs payload**: embeds `findingsSchema` per appointment (fields + required flags + photo categories + gauge config) so mobile completion never blocks on a registry fetch.

**Blast radius:** Zero behavioral for all current traffic — every new code path conditions on `profile.findingsType`, which no active profile row produces yet. Recurring/pest/lawn completions byte-identical (regression-tested). The two pointer-fix rows change which Projects form opens for flea/rodent-exclusion jobs (the correct one).

**Revisit if:** a per-customer property/location entity is added (extend activity history key); multi-indicator visits become real (composite unique already permits).

---

## 2026-06-11 — Specialty services → Service Report V1 pipeline (PR 2: dark report support)

**Decision:** Report-side rendering for typed specialty completions, still DARK (renders only when a service_record carries `service_data.typedReportSnapshot`, which only typed completions write — and no profile is cut over yet).

**Implementation:** `buildReportV1Data` detects the snapshot and (1) **hard-suppresses Pest Pressure** (`pestPressure=null`, `pressureIndex=null` — explicit gate, since these types detect to the 'pest' line and would otherwise leak the pressure UI onto e.g. a cockroach report), (2) builds `data.activity` via new `activity-scores-store.js` (snapshot's own score/trend words — resolved at completion, never recomputed — plus cross-visit history from `service_activity_scores`, current visit marked, `isBaseline` when no priors), (3) exposes `data.typedReport` (todaysResult / customer-labeled findings / reportTypeLabel / isProgressVisit) straight from the snapshot, (4) remaps the metrics band's `pressure_index` slot to the activity level for gauge types (mirrors the lawn_health remap; non-gauge types drop it via the existing null-pressure filter). Client: gauge primitives (MeterSvg/PressureHistoryChart/TrendChip) extracted unchanged to `report/GaugePrimitives.jsx` (PestPressureCard re-imports; pure move, own commit); new `ActivityCard` (wording-first — level word leads, no numeric score headline, no customer rating picker, baseline visits say "Baseline recorded today" and never claim a trend); `TodaysResultCard` opens every typed report right after the status card; `TypedFindingsCard` renders snapshot items with customer labels (zero states render as results); PestPressureCard slot swaps to ActivityCard when `data.activity` present; QuickNav gains an Activity link. PDF/static modes inherit automatically (same page).

**Tests:** `tests/typed-report-data.test.js` — golden-fixture-driven (all 10 fixtures from PR 0 asserted: Pest Pressure suppressed even with the engine fully enabled, snapshot copy passthrough, banned-words absent, zero states render, gauge/metrics behavior, first-visit no-trend) + history hydration + recurring-pest regression. Existing service-report-v1 suite untouched (70/70).

**Deferred to the cutover PR:** short progress-visit SMS copy (DB template work — owner-reviewable alongside Phase-1 golden-report approval).

**Blast radius:** zero for recurring reports (typedSnapshot absent → all new paths skip; regression-tested).

---

## 2026-06-12 — Specialty services → Service Report V1 pipeline (PR 4.5: Projects creation gating, dark)

**Decision:** Projects creation is gated per type by **live cutover state**, not registry metadata: a project type is "appointment-managed" iff an ACTIVE `service_completion_profiles` row routes it to `completion_mode='service_report'` with that `project_type`. Pre-cutover this PR is a complete no-op (no flipped rows exist), which is exactly the point — creation for a type shuts off at the moment that type actually cuts over, never before.

**Implementation:** `appointmentManagedProjectTypes()` (service-completion-profiles.js, fails open to the empty set). `GET /admin/projects/types` serves per-type `appointmentManaged` flags; `POST /admin/projects` 422s (`project_type_appointment_managed`) so stale clients can't recreate the dual-entry path. CreateProjectModal filters its type picker by the flag (explicit `allowedProjectTypes` prop still wins — the WDO/special-project dispatch path keeps working); ProjectsPage's general-create list filters from the fetched registry; TechHomePage routes typed jobs (profile `findingsType` set) away from both the recap modal (no findings/billing gate) and project creation, pointing techs to the Dispatch completion form. Legacy project records of every type stay fully usable — list/detail/edit/send/close untouched.

**Blast radius:** zero until a cutover migration flips a profile row; verified by tests (empty-set pre-cutover, fail-open on missing table/query error) and client build.

---

## 2026-06-12 — Specialty services → Service Report V1 pipeline (PR 3: typed CompletionPanel)

**Decision:** Completion UX for typed specialty jobs on both DispatchPageV2 (desktop) and the mobile appointment sheet, active only when an appointment's `completionProfile.findingsType` + embedded `findingsSchema` are present (post-cutover). One panel, adaptive content — no separate typed surface.

**Implementation:** New hoisted `TypedFindingsSection` in SchedulePage.jsx (mobile + desktop variants, matching each branch's existing tokens): registry fields via `ProjectFindingFieldInput` with required markers; 6-button word-labeled 0–5 activity gauge with the contract's derive-then-pin semantics (untouched picker recomputes from the derive select; first tap pins technician-set; submits `activityScoreSource`); next-step chips (≤4); optional recommendations textarea with non-blocking AI draft. New `POST /:serviceId/findings-recap/draft` (FLAGSHIP tier via config/models, per-tech ownership guard, opt-in compact comms context modeled on admin-projects ai-write, banned-words instruction, all failures non-blocking 4xx/5xx). Typed mode hides lawn/WaveGuard/tree-shrub/protocol sections and the Pest Pressure tech rating; keeps products, photos, outcome, interaction, SMS toggle, review request. Submit gating mirrors the server (missing required labels + gauge); payload adds `structuredFindings`/`activityScore`/`activityScoreSource`/`nextStepChips`/`completionTelemetry` (persisted to structured_notes for the ≤60s budget analysis). Billing 409 (`completion_billing_required`) closes the panel into the existing checkout flow (`onBillingRequired` → `setCheckoutService`), with typed inputs included in the draft autosave so the payment detour loses nothing; `admin-fetch` now surfaces `err.code`. Success screen suppresses auto-close on `followupSuggestion.required` (the schedule-followup CTA renders once PR 4 supplies `onScheduleFollowup`) and notes internal_only delivery. DispatchPageV2 + mobile sheet route CreateProjectModal only for `specialProject || linkedProject` jobs.

**Judgment calls:** photo caption chips skipped (the panel's photo capture has no caption affordance — would have meant inventing UI); telemetry rides inside the /complete body rather than a new endpoint; checkout 409 closes the panel (z-index stacking) and relies on draft restore + existing post-payment reopen.

**Blast radius:** zero until cutover (no profile emits findingsType); non-typed completion paths byte-identical; `err.code` on admin-fetch errors is additive.

---

## 2026-06-12 — Specialty services → Service Report V1 pipeline (PR 4: follow-up booking)

**Decision:** The completion success screen's "Schedule follow-up" CTA books the suggested visit as a **PENDING appointment** — the existing pending → confirmed dispatch flow is the admin confirmation step, so the heavy scheduling validation stack isn't duplicated and the booking can't silently bypass capacity review (the contract's "admin-confirmed request" option).

**Implementation:** Migration `20260611000011` adds `scheduled_services.followup_included` (the typed billing pre-gate bypass for included program visits — the column PR 1's gate already reads) and `followup_source_service_id` (FK to the completed visit; the idempotency key — a retried CTA tap returns the existing booking, `alreadyScheduled: true`). New `POST /:serviceId/schedule-followup` (per-tech ownership guard via assertRecapOwnership): requires a typed profile (`followup_not_typed` 409 otherwise — this is not a generic booking API), validates date ≥ today ET, clones customer/tech/service/zone/duration/window from the source visit, $0 + `create_invoice_on_complete=false` + `followup_included=true`, status `pending`, graceful 503 if the migration hasn't run. DispatchPageV2 wires `onScheduleFollowup` (CTA shipped in PR 3) → endpoint with the suggested date → schedule refresh.

**Blast radius:** dark — the CTA only renders on typed completions with `followupSuggestion.required` (no profile emits that pre-cutover); the endpoint 409s for non-typed services.

**Revisit if:** trapping cadence needs multi-visit chains booked upfront (today each completion books the next check), or follow-ups should inherit a non-zero price for non-included programs.

---

## 2026-06-11 — Specialty services → Service Report V1 pipeline (PR 5: Phase-1 cutover)

**Decision:** Phase-1 pilot cutover ships as one explicit-allowlist migration (`20260611000012`) flipping 16 profiles to `completion_mode='service_report'` — per-key `WHERE` (key + expected pointer + `project_required` + active) with `count===1` assertion per row inside a transaction, so any prod drift aborts loudly with nothing flipped. `delivery_mode` stays `auto_send` (staged pilot, pure auto-send; per-profile `delivery_mode` and `SPECIALTY_REPORT_DELIVERY_DISABLED` remain the kill switches). Rollback = `knex down` restores `project_required` + exact prior pointers.

**Allowlist:** pest_inspection, new_customer_inspection (pest_inspection); mosquito_event + **mosquito_one_time repointed → mosquito_event** (it fell into the generic pest fallback at seeding, but its work content is the mosquito report family and the generic schema's required activity gauge doesn't fit); palm_injection; lawn_aeration/lawn_care_one_time/lawn_fungicide/lawn_insect_control/lawn_inspection (one_time_lawn_treatment — schema has no required fields, so inspection/aeration degrade gracefully); bee_wasp_removal/fire_ant/mud_dauber_removal/pest_initial_cleanout/pest_re_service/tick_control (one_time_pest_treatment). **Excluded deliberately:** `general_appointment` (generic catch-all; required activity gauge is wrong-frame) and `waveguard_initial_setup` (recurring-program onboarding, not a specialty one-time) stay `project_required`; trend types are Phase 1b/2; termite is Phase 3 after FS 482.226 / FAC 5E-14 signoff.

**Progress SMS (deferred from PR 2):** `service_report_v1_progress` template (`20260611000013`) — short SMS whose lead sentence is the snapshot's generated Today's Result headline (immutable, versioned, banned-words-safe, identical to the linked report). delivery.js selects it via `typedProgressContext(record)` (snapshot `visitSequence > 1` + non-empty headline); invoice-link completions keep `service_report_v1_with_invoice`; no headline → plain template. Inert for Phase 1 (single-visit families) — live before Phase 1b flips the first trend key.

**Verification:** full 641-migration replay on a scratch DB, then up (16 flipped, exclusions held, recurring NULL-pointer `service_report` rows untouched) / down (exact pointers restored incl. the repoint) / re-up.

**Blast radius:** THIS IS THE CUTOVER — from deploy, the 16 keys complete through the typed CompletionPanel and auto-send Service Report V1; project creation for the five pilot project types shuts off (PR 4.5 gating keys off live profile state). Gated on the golden-fixture readability sign-off.

**Revisit if:** pilot telemetry breaches the p90 ≤60s completion budget, or report-quality findings from the first auto-sent reports require copy-map revisions before Phase 1b.

---

## 2026-06-11 — Tree & Shrub: 6-visit Standard mandate; retire 9x/12x; add 4x Light

**Decision:** The 6-visit **Standard** program is now the mandated default Tree & Shrub cadence (matches the "10/10 SWFL Tree & Shrub Protocol" `six_x` in `server/config/protocols.json`). The 9-visit **Enhanced** and 12-visit **Premium** tiers are **retired** — the documented protocol tops out at 6 visits, so pricing 9/12-visit programs charged customers for visits we never schedule. A 4-visit **Light** tier (protocol `four_x`, "best for cleaner properties with low pest history") is added as a manual downsell that is **never auto-recommended**. The `directCostRatioTarget` multiplier is **unchanged at 0.43** — the over-pricing came from the cadence (and the default auto-escalating to Enhanced on any signal, including the unknown-bed-area fallback), not the markup.

**Implementation:** `constants.js` `TREE_SHRUB.tiers` → `{ light: {4x, 0.075, $40}, standard: {6x, 0.110, $50} }`, `recommendedTier: 'standard'`. Material rate is annual $/sqft; Light ≈ 4/6 of Standard since material scales with applications. `service-pricing.js`: `evaluateTreeShrubTierRecommendation` always returns `standard` (reason codes retained as "full-program signals," now advisory only); `normalizeTreeShrubTier` maps both legacy `enhanced` and `premium` → `standard` with structured warning codes (`tree_shrub_enhanced_deprecated_mapped_to_standard`, `tree_shrub_premium_deprecated_mapped_to_standard`). `db-bridge.js` syncs `4x_light` / `6x_standard` rates (ignores legacy `9x_enhanced` / `12x_premium`). `v1-legacy-mapper.js` tier rows → `['light','standard']`. Client mirror `client/src/lib/estimateEngine.js` tiers/rates updated to match (Standard remains the recommended index). Migration `20260611000001_tree_shrub_six_visit_mandate.js` updates the live JSONB `pricing_config` rows and writes a `pricing_changelog` entry (v4.4→v4.5, category `rule`). Legacy estimates keep their stamped `pricing_version` and are not re-priced.

**Effect:** Typical ~2,000 sqft-bed home default drops from ~$109/mo (Enhanced 9x) to ~$66/mo (Standard 6x); displayed margin stays ~50% (target direct-cost ratio 0.43 ⇒ ~57% gross pre-admin; 35% floor unchanged). Light 4x prices ~$45/mo at the same property.

**Tests/baselines:** `pricing-engine-tree-shrub-v44.test.js` rewritten for the new tier set + Light worked example (49 pass). Regression scenarios that requested `treeShrub: enhanced` switched to `standard`; local baselines (`pricing-engine.local-baseline.json`, `pricing-engine-v1-adapter.local-baseline.json`) regenerated via `CAPTURE_BASELINE=1 LOCAL=1`. The prod DB baseline `pricing-engine.baseline.json` was **not** regenerated (no DB access this session; it is already stale from the June mosquito reprice) — it must be recaptured against a DB/prod environment.

**Pre-existing (not from this change):** `waveguard-pricing-exposure`, `pricing-engine-manual-discount`, and `lawn-pricing-v2-db-sync` fail in a DB-less environment (they assert prod-DB-synced Lawn V2 values); they fail identically on a clean tree.

**Review follow-up (consumers of the retired tier):** a code review caught several consumers still speaking the old `enhanced` vocabulary; the rename was propagated to all of them — `customer-pricing-ai.js` (portal AI now offers a Light 4x downsell instead of a phantom "Enhanced 9-visit" option that priced at Standard), `estimate-public.js` `treeShrubTierKey` (surfaces the Light row to customers; keeps the `enhanced` mapping so saved pre-v4.5 estimates still render), `estimate-assistant.js` `normalizeFrequencyKey` (adds `light`; keeps `enhanced`, still a live **Lawn** tier), the `admin-pricing-config.js` bootstrap seed (now seeds `4x_light`/`6x_standard`), and `server/services/pricing-engine/README.md`.

**Revisit if:** the operator wants the engine to auto-recommend Light (4x) for clean/low-pest-history properties per the protocol note (today Light is customer-selectable but not the auto-recommended default), or wants the 0.43 multiplier lowered toward 0.50 (~14% lower list, ~7pp less margin) as a deliberate second step.

---

## 2026-06-11 — Phase-1 cutover migration: self-healing rewrite (deploy unblock)

**What happened:** `20260611000012`'s per-key `count===1` assertions did their job — and revealed that live catalogs have drifted per environment (prod lacks an active `pest_initial_cleanout` profile; staging lacks `mosquito_event`). Because the migration runs in `preDeployCommand`, the throw blocked every deployment after #1617 merged. A Railway bot PR (#1628) proposed downgrading the assertions to warnings — rejected: silently skipping keys converts the loud-drift guard into silent partial cutover.

**Decision:** deterministic per-key resolution instead of a fixed-count assertion (file edited in place — it never ran successfully anywhere, so knex has no record of it): FLIP expected rows; ALREADY for idempotent re-runs; **HEAL** (insert the profile in cut-over shape, seed defaults derived from the services row, marked in `notes`) when the services row exists without a profile; **ABSENT** (loud warn, skip) when the environment genuinely lacks the service or the row is inactive; **THROW** on any unexpected mode/pointer — that's real drift needing eyes, and the transaction still rolls back atomically. Every key's outcome is logged so each environment's deploy log records what it had.

**Verification:** fresh 646-migration replay (flipped=16) + drift simulation: deleted profile w/ service present → healed with correct alert/14 follow-up policy; deleted profile+service → absent; idempotent re-run → already=15; injected wrong pointer → throws and aborts; down() reverts healed rows to valid `project_required`.

**Durable lesson:** environment catalogs are admin-mutable — cutover migrations must resolve per-key against live state, never assert a replay-derived count.

---

## 2026-06-11 — Specialty services → Service Report V1 (Phase 1b: rodent shadow pilot)

**Decision:** Phase 1b flips **one** trend-type key — `rodent_trapping` — to `completion_mode='service_report'` at **`delivery_mode='internal_only'`** (migration `20260611000016`). Typed CompletionPanel, activity scores, trend words, progress framing, and $0 follow-up CTA all go live for that key; reports render + store (token/PDF) but **no customer SMS/email** sends. Rodent over cockroach because the lead tech runs rodent jobs daily — fastest real-visit feedback loop on the trend machinery before Phase 2 flips the broad batch.

**Scope:** the single core key only. CTA-booked trap checks clone the source visit's service, so the visit chain stays typed end-to-end on `rodent_trapping`. Siblings (`rodent_trapping_followup`, inspection/sanitation/exclusion variants) stay `project_required` until Phase 2 — a manually-booked `rodent_trapping_followup` visit still routes to Projects during the shadow.

**Graduation:** owner reviews stored shadow reports → one-line flip to `auto_send` (separate migration). Rollback = `knex down` (restores `project_required`/`auto_send`; policy columns untouched).

**Verification:** scratch-DB replay at current main: flip / idempotent re-run (re-ensures internal_only) / down (exact restore incl. followup alert/3) / heal-from-deleted-profile — all verified by direct up()/down() calls.

**Per-key resolution** follows the self-healed 20260611000012 pattern (flip / already / heal / absent / throw) — live catalogs are admin-mutable per environment, so cutover migrations never assert replay-derived counts.

---

## 2026-06-11 — Appointment-managed gating: full-cutover semantics (Phase-1b round 3)

**Decision:** a project type is appointment-managed (unlinked/ad-hoc creation blocked, hidden from pickers) only when it has **fully** cut over — at least one active `service_report` profile AND no remaining active `project_required` profile of that type. Partially-cutover types (Phase 1's excluded `general_appointment`/`waveguard_initial_setup` keys, the Phase-1b single-key rodent shadow) stay creatable ad hoc, because legitimate project-backed services of that type still exist. The dual-entry risk lives on LINKED creation, which the per-service guard in `POST /admin/projects` already rejects independently (linked profile decides; bypass scoped to the profile's own type).

**Disclosed behavior change:** unlinked `one_time_pest_treatment` project creation re-opens (the type is partially backed by the two excluded keys) — it had been blocked since the Phase-1 deploy. `pest_inspection`/`mosquito_event`/`palm_injection`/`one_time_lawn_treatment` remain fully cut over and blocked.

**Also this round (Codex P1):** the auto-report **share** path (`POST /share/auto_report_<id>` + public `GET /shared/:token`) now 404s for suppressed typed reports (`typedReportDelivery !== 'auto_send'`) — ownership alone could previously mint a public PDF link for a hidden Phase-1b record, bypassing the four-surface suppression.

**Shadow-report PDF controls + #1631 dropped-commit recovery (fix/shadow-report-pdf-controls).** Post-merge Codex P2 on #1631: staff viewing an internal_only report got a dead Download PDF link (plain `<a>` can't carry the staff JWT; no PDF is rendered for suppressed records anyway). The V1 `/data` payload now nulls `pdfUrl` and sets `internalOnly: true` for suppressed records — only staff ever receive that payload (the `router.param('token')` gate 404s everyone else) — and the viewer renders an internal-review notice in place of the download/share bar (Share suppressed too: its public URL 404s for customers). Second commit recovers #1631's final commit (34578434), dropped between push and merge when the branch was recreated from a stale state: frozen-delivery re-derivation on crash-resumed completions, fail-closed 503 on linked-profile resolver errors (`project_link_unverifiable`), and the report-page-metadata test updates (whose absence left that suite red on main).

**Rodent trapping sectioned findings — schema v2 (feat/rodent-sectioned-findings).** Owner spec 2026-06-12: per-service tap-to-fill checklists replace free-text findings. rodent_trapping's 7 textarea-heavy fields become 14 sectioned fields (Evidence observed / Trap activity / Conducive conditions / Work completed / Recommendations / Customer communication) using two new field types: `chips` (always-visible multi-toggle, comma-joined storage per the multi_select convention, every element validated against options before reaching the immutable snapshot) and `count` (integer stepper, 0–9999, empty = not recorded ≠ 0). Required stays `species` + gauge + (new) ≥1 next-step chip — `REQUIRED_NEXT_STEP_TYPES`/`nextStepRequiredForType`, 422 `next_step_required` ("every rodent report ends with a clear next action"). Today's Result composes deterministically from the checklist (`rodentTrapSentence`: "We checked 8 traps, removed 2 captures, refreshed the bait…"); `traps_set` stays in the fallback chain for pre-v2 drafts. Exclusion wording rule: never "rodent-proof" (now in BANNED_CUSTOMER_COPY, all spellings) — copy map renders "Exclusion repairs are recommended to reduce rodent access…". Trapping-specific next-step chips (Continue trapping / Await exclusion approval / Monitor after no activity / Remove traps after inactivity + existing). SCHEMA_VERSION/COPY_MAP_VERSION/SUMMARY_TEMPLATE_VERSION → 2; v1 snapshots immutable and keep rendering (old rodent fixtures retained as backward-render proof; new generated fixture rodent_trap_check_sectioned_v2). AI recommendations draft consumes the richer structured values through the existing prompt unchanged. Other typed services get the same sectioned treatment per-service with owner-supplied content lists (do not guess business content).

**Sectioned findings for all remaining typed services (feat/service-sectioned-findings).** Owner spec 2026-06-12, extending the rodent-trapping framework to mosquito_event / palm_injection / one_time_lawn_treatment / pest_inspection (LIVE Phase-1 auto_send types — these change tech input AND customer findings cards on deploy) + cockroach / wildlife_trapping / bed_bug (dark until Phase 2). Each gets sectioned `chips`/`count`/select fields per the owner's content lists; derive-source fields (cockroach `activity_level`, bed_bug `evidence_level`) and prior requireds preserved. New requireds: mosquito `activity_level`+`standing_water`, palm `palm_condition`, lawn `lawn_condition`, wildlife `target_animal` (now a select). REQUIRED_NEXT_STEP_TYPES = all 8 sectioned types — every typed report ends with a tapped next action. Composed "what we did": `trapActivitySentence` generalized to wildlife; per-type work-chip→phrase maps (mosquito/palm/lawn/cockroach/bed bug); inspections compose from areas inspected. Mosquito headline leads with observed level ("Mosquito activity was light today"; zero = observational absence). Default headline drops the " Summary" suffix (matches approved fixture style). Banned-copy "-proof" generalized (rodent/wildlife/pest/bug/mosquito/critter/animal). Observation-scoped absence copy mapped (palm "No visible pest or disease indicators were observed at today's service" etc.). 7 builder-generated fixtures; registry-wide sweep test (every option/value-label/next-step sentence banned-copy clean; every per-type chip has a sentence). Required-field counts stay ≤4 per type (owner's longer "required" lists implemented as optional quick-checks — ratify in PR).

**AI photo analysis for typed reports (feat/typed-report-photo-ai).** Owner spec 2026-06-12: completion photos get an AI pass whose output lands on the customer report. Flow: tech attaches photos (existing, data-URLs pre-submit) → "Analyze photos with AI" (typed services only) → new `POST /:serviceId/photo-analysis/draft` sends the local data-URLs + findings context to MODELS.VISION (temp 0.2, same trust shape as findings-recap/draft: assigned tech, typed profile authoritative, decodeDataUrlPhoto size caps) → strict-JSON `{photoSummary, captions[]}` parsed/validated by `services/service-report/photo-analysis.js` (banned-copy check incl. "-proof", one retry, 502 on dirty — AI never in critical path). Tech reviews: summary is an editable textarea, captions show under thumbnails (removable with their photo). Submit: captions persist to `service_photos.caption` (+aiTags.captionSource='ai'), summary into `typedReportSnapshot.photoSummary` (≤600 chars) — BOTH join the typed `customerCopySources` banned-copy 422. Report renders the summary atop Field Photos; captions were already rendered (`caption || stateBadge`). Not draft-persisted (photos aren't — a summary without its photos is stale). Thumbnail wrappers lost their fixed 80px height so captions flow.

## 2026-06-12 — Bait station typed flows: termite + rodent station checks (owner spec)

Two new typed findings flows on the sectioned tap-to-fill framework — inspection/compliance style, distinct from trapping (trapping = captures; stations = consumption/exterior pressure + station condition):

- **`rodent_bait_station`** — station counts (total/checked/inaccessible), service chips, consumption select (None→Empty), evidence, station condition, attractants/harborage, customer recommendations. New indicator `rodent_bait_activity` derived from consumption — deliberately NOT shared with `rodent_activity` (exterior pressure must never mix into interior trapping trends). pestNoun 'Bait station' + new `programNoun` config ('Rodent Bait Station Program — Progress Visit') keeps headlines from implying interior infestation.
- **`termite_bait_station`** — station counts incl. stations-with-activity, activity select (None observed / Active termites present / Previous feeding noted) deriving the shared `termite_activity` indicator (station checks extend the same trend line as inspections), activity signs, bait condition + actions, station condition + actions, conducive conditions, customer recommendations.
- **Owner risk wording enforced in code:** zero states scope to "the accessible bait stations today" (termite) / "no bait consumption or visible rodent evidence" (rodent); property-wide "no termites on property" added to BANNED_CUSTOMER_COPY; rodent consumption value labels say "exterior rodent pressure". Shared `bait_consumption` field key keeps DISJOINT value sets per type (termite uses '* feeding') so the global copy map can't leak one program's wording onto the other (regression-tested).
- **Required next step** for both types; required cores stay small (rodent: stations_checked + bait_consumption; termite: + termite_activity) — owner's longer lists are optional quick-checks per the 60-second completion budget.
- **Migration 20260612000001:** inserts the missing recurring catalog service `rodent_bait_quarterly` (quarterly, variable price — blank per manual-pricing convention, internal_notes marker so down() never deletes an admin-created row) and points 8 keys at the new flows at `internal_only`: rodent_bait_setup + rodent_bait_quarterly ride the rodent shadow; termite_bait/active_annual/active_bait_quarterly/monitoring/cartridge_replacement/installation_setup stay internal until the FS 482.226 / FAC 5E-14 signoff. Disclosed trade: the four recurring termite keys previously auto-sent GENERIC reports — that send stops (zero visits in 12 months). Bonds + renewal untouched. Marker-based rollback ([bait_station_action=...]) verified on a scratch DB: up / idempotent re-up / byte-exact down.

## 2026-06-12 — Agents hub: one canonical surface for agent supervision (agents-hub-consolidation)

**Decision:** agent oversight consolidates under `/admin/agents` as a tab wrapper (`AgentsHubPage`, Tier 1 V2 shell), following the `AdminDispatchPage` precedent. Tabs: **Overview** (existing `AgentOpsPage` — fleet cards + task queue) and **Triage & Decisions** (existing `AgentDecisionsPage` — shadow decision review, formerly "Agent Review" buried under Marketing). Both pages stay in place as files (Tier 2 styles intact) and gain an `embedded` prop that suppresses their own `AdminCommandHeader`; AgentOpsPage's Refresh action lifts into the hub header via a handler-ref registration (`setRefreshHandler`), the same pattern as dispatch's `setOpenCreateHandler`. Per-tab URL state via `?tab=`; `/admin/agent-decisions` redirects to `?tab=decisions` so bookmarks and server `actionUrl`s keep working (the one server-side `actionUrl` in `admin-agents.js` now points at the hub directly). Nav: Operations item renamed "Agent Ops" → "Agents"; the Marketing "Agent Review" item and the MorePage Intelligence entry are removed/redirected — one nav entry total.

**Why a hub, not sibling routes:** the SMS brand-voice loop work (shadow draft review, judge scores per intent, graduation/config controls) needs a home; each lands as an additional tab here rather than scattering new top-level nav items. Operator Inbox deliberately stays in Communications — that is Virginia's working queue, not agent supervision.

## 2026-06-15 — No-show behavior: customer text + tracker card (no-show-behavior)

**Decision:** the dispatch "Mark as no-show" action (`MobileAppointmentDetailSheet` → `PUT /admin/dispatch/:id/status`, `status='no_show'`) now (1) texts the customer a reschedule invite and (2) flips the GPS tracking-link page to a "missed visit" card. Previously the action only flipped status + wrote audit/activity rows — no customer-facing effect, and the en_route/on_site/completed/cancelled branches were the only ones wired to track-transitions/SMS.

**Bug found + fixed along the way:** `no_show` was never in `scheduled_services_status_check` (nor the `job_status_history` to/from mirrors from 20260426000004/000006), so the transition actually threw a CHECK violation — the button surfaced "Failed to mark no-show: Internal server error." Migration 20260615000005 widens all three constraints in lockstep (per the rule documented on 20260426000006). Several services already treated `no_show` as terminal (waveguard-existing-services, prepaid-series, customer-pricing-ai, context-aggregator), so the value was always intended — the constraints just never caught up.

**SMS:** new `appointment_no_show` template (migration 20260615000006), seeded like the reschedule/cancel templates. Copy: "Hi {first_name}, it's {tech_name} from Waves Pest Control. We missed you for your service today at {time}. If you'd like to get back on the schedule, just reply to this message or give us a call and we'll find a new time." `{tech_name}` is the assigned tech's first name (falls back to "the team"); `{time}` is the appointment's scheduled start. Sent via `AppointmentReminders.handleNoShow` through the same landline/opt-out-guarded `safeSendAppointment` path as the cancellation notice; best-effort in the route's `no_show` branch (a Twilio/template failure never fails the committed status flip). Respects `notifyCustomer !== false`.

**Tracker:** a no-show is an operational-status flip that does not move the `track_state` ENUM, so `track-public.js` derives `state: 'no_show'` from `status` (no ENUM migration). `TrackPage` renders a warm `NoShowCard` (amber `COLORS.orange`, not alert red) — "We missed you, {name}." + "Call to reschedule" CTA — mirroring `CancelledCard`. Customer-surface warm tone per the customer-facing brief; admin spec deliberately not applied.

## 2026-06-17 — Third-party payer (Bill-To) — Phase 1 (feat/third-party-payer-billing)

**Problem:** the person who receives the service ≠ who pays, for a whole class of relationships (builder/GC pretreatments, property managers, realtors at closing, HOAs). The only existing lever was `notification_prefs.billing_email` — an invisible string you had to remember to set *and clear*, carrying no identity (AP departments reject an invoice addressed to the homeowner with no PO) and not reusable across jobs. Owner directive 2026-06-17: model **Payer** as a first-class reusable Bill-To, separate from the customer of record. Phase 1 scope (owner-chosen): default-payer-on-customer **plus** per-job override; no payer portal yet (AP just receives the PDF); per-payer cadence field present but only `due_on_receipt` wired (NET/consolidated statements are Phase 2).

**Model (migration 20260617000002):** new `payers` table (display/company name, AP email/phone, billing address, `payment_terms`, `requires_po`, `tax_exempt`+cert, nullable `stripe_customer_id` to future-proof a portal, `active`). Links resolve in order `scheduled_services.payer_id ?? customers.payer_id ?? self`; `po_number` is per-job (on the scheduled service). `invoices.payer_id` + `po_number` are **snapshot at creation** so a historical invoice keeps its bill-to even if the link later changes (same discipline as tier/price snapshots). FK `ON DELETE SET NULL`; the payer is a Bill-To entity, **not** a customer — MRR/acceptance/reporting stay keyed on the homeowner, AR rolls to the payer.

**Resolution + safety (`server/services/payer.js`):** `resolveForInvoice()` and `attachToInvoice()` **never throw** — a payer lookup can never block invoicing; both fail soft to self-pay. A **deactivated** payer link also falls back to self-pay rather than mailing a dead AP inbox. `invoice.create()` resolves + snapshots only when a payer is found, so the inserted row is byte-identical for the (dominant) self-pay case.

**Reroute (`invoice-email.js`):** when an invoice carries a payer snapshot, the email auto-routes to the payer's AP inbox (`payerRecipient` → role `payer`). Precedence: an explicit operator one-off override still wins; a payer with no usable AP email falls back to the customer billing contact (never strands the invoice). `invoiceRecipientFor` now honors an override `role`. PDF `billBlock` renders **Bill-To = payer** + **Service address = homeowner**, and the meta block shows the PO. Same attach is wired into the `admin-projects` WDO+invoice bundle. Receipts stay customer-routed in Phase 1.

**Assignment + UI:** `/api/admin/payers` CRUD (admin-only); `payerId` accepted on customer `PUT /:id` (account default) and `payerId`/`poNumber` on `PUT /admin/schedule/:id/update-details` (per-job, `cols`-guarded). New `/admin/payers` management page (Finance nav) + a default-payer selector on the Customer 360 Billing tab + a "Bill to (third-party payer)" + PO control on the Edit-appointment modal. Tests: `payer-service.test.js` (resolution precedence, fail-soft) + payer-reroute cases in `invoice-email-recipient.test.js`.

**Phase 2 (not built):** NET terms → accrue to a consolidated monthly statement per payer instead of an instant invoice; PO **hard** enforcement; tech-completion "Billed to … — don't collect on site" banner (land after the CompletionPanel rebuild to avoid double work).

**Codex round 23 hardening (GitHub bot — 1 P2 + 1 P3 on the round-22 head).** (1) **P2 — payer tax exemption lost on invoice EDIT (`invoice.js`).** `create()` forces `taxRate=0` for a tax-exempt payer, but `calculateUpdateFinancials` (the `update()` recalc) re-taxed any commercial invoice from the customer property type alone — so editing a line item on a tax-exempt payer invoice before sending put tax back on the AP total. The recalc now reads the exemption off the invoice's frozen `payer_id` (direct `payers` lookup, degrades to normal tax if the table doesn't exist yet / payer inactive) and skips tax when exempt. (2) **P3 — estimate-accept advertised a suppressed payer pay link (`estimate-public.js`).** Round 21 nulled `invoicePayUrl` for payer invoices but left `invoiceMode`/`invoiceLinkDelivered` set, so `buildAcceptSuccessPayload` still returned `nextStep:'pay_invoice'` and `buildAcceptNotificationPayload` told the customer to "use the invoice pay link" (falling back to `/?tab=billing`) with no usable link. Added a `payerBilled` flag threaded to both builders: the homeowner gets a "sent to your billing contact — nothing due" message and no pay-invoice next-step, while the **admin** copy still correctly says the invoice was sent (so `invoiceMode`/`invoiceLinkDelivered` are intentionally left intact rather than zeroed, which would have falsely told the admin "invoice not sent"). 59 payer-suite tests green.

**Codex round 22 hardening (GitHub bot pass on the clean head — 4 P2s, all payer-subsystem polish).** The local pre-push hook was P0:0/P1:0; the GitHub `@codex` bot then surfaced four P2 suggestions on `3448cbece`, several siblings of earlier fixes. (1) **`previewInvoiceTotals` per-job payer (`invoice.js`)** — the dry-run tax preview resolved the payer from `scheduledServiceId` only, so a `serviceRecordId`-linked WDO/report preview ignored a tax-exempt per-job payer and showed tax the real (exempt) invoice won't bill; now derives `scheduled_service_id` from the service record first (mirror of the round-21 `create()` fix). (2) **`/api/billing/balance` unbounded scan (`billing-v2.js`)** — finding the latest non-payer attempt loaded the customer's entire paid/failed/refunded ledger; now keeps the `.first()` fast path when the customer has no payer invoices and uses bounded paging (50/page, 500 cap) otherwise. (3) **Project send dry-run routing (`admin-projects.js`)** — the `dry_run` confirm-dialog preview reported homeowner self-pay routing even for payer-billed sends; it now resolves the payer (persisted snapshot, or the customer/per-job default for a brand-new preview) and returns `payer_billed`/`report_only` + the payer AP recipient, matching what the real send does. (4) **Override + unattachable payer PDF (`invoice-email.js`)** — an explicit recipient override skipped the payer guard, so a payer invoice whose payer can't attach (legacy/no-snapshot/deactivated) rendered the homeowner as Bill-To to the AP recipient; now synthesizes the same "Third-party payer" placeholder the public PDF routes use before building the attachment (the no-override branch still fails closed). 59 payer-suite tests green.

**Codex round 21 hardening — a P0 estimate-accept pay-token leak + two payer-creation gaps, all rooted in `InvoiceService.create()` now auto-resolving a default payer.** (1) **P0 — public estimate accept returned the payer's pay token (`estimate-public.js`).** The invoice-mode accept path returned `inv.token` as `invoicePayUrl` to the homeowner; with `create()` auto-setting `payer_id` for a customer with a default payer, that handed the homeowner the payer's bearer `/pay/:token` link while the invoice was emailed to the AP. Added one guard after all invoice-id reassignments (covers the billByInvoice, converter, and annual-prepay paths): resolve the final invoice's `payer_id` and null `invoicePayUrl` when payer-billed, which also suppresses the client `pay_invoice` next-step (gated on the URL). (2) **P1 — `serviceRecordId`-only invoices ignored per-job payer overrides (`invoice.js`).** `create()` resolved the payer from `scheduledServiceId` only, but completion-time callers pass `serviceRecordId`; it now derives the visit's `scheduled_service_id` from the service record before resolving, so a job-level Bill-To override on a completed visit bills the right party (the row's own `scheduled_service_id` linkage is unchanged). (3) **P1 — the customer AI minted a payer draft before redacting it (`tools-expanded.js`).** `send_payment_link` excluded *existing* payer invoices but still called `create()` when none existed; for a customer with a default payer that minted a payer-billed draft just to return `payer_billed`. It now resolves the effective payer first and returns the redacted response **without** minting.

**Codex round 20 hardening — draft payer invoices no longer fall back to a stale snapshot AP email (P1).** The round-16 unfrozen branch in `attachToInvoice` only *overwrote* the snapshot AP email when the live active payer had a valid one — so when the live payer was active but its `ap_email` was cleared, the invoice kept the (possibly stale) creation-snapshot AP email and `sendInvoiceEmail` / the recipient preview / project send could deliver to that old address. For unfrozen (draft/scheduled/sending) invoices the live ACTIVE payer now fully controls AP routing: use its current AP email if valid, otherwise **clear** the snapshot AP email (`snap.ap_email = null`) so `payerRecipient()` returns null and delivery fails closed. Identity stays for display. Test: unfrozen-clears-stale-AP-when-live-has-none in `payer-service.test.js`.

**Codex round 19 hardening — the admin invoice recipient preview is now payer-aware (P1).** `getInvoiceDeliveryRecipients` (`/admin/invoices/:id/recipients`) selected only the customer + `notification_prefs`, so for a payer-billed invoice it reported the homeowner's SMS/email. `AdminInvoicesPage` gates Send Invoice / Send Receipt / Record-Payment-receipt off this endpoint, so a payer invoice with an AP email but no homeowner contact was shown as undeliverable (Send disabled), and a homeowner-phone-only account offered receipt-via-SMS — which the server suppresses as `payer_billed`, leaving the AP receipt unsent. Now, when `invoice.payer_id` is set, the endpoint attaches the payer (`PayerService.attachToInvoice`) and returns the payer AP (`payerRecipient`) as the only email recipient with **no** SMS recipient and a `payerBilled: true` flag; an unattachable/AP-less payer reports no recipient so Send stays disabled until the operator adds an AP email — matching the server's fail-closed send behavior.

**Codex round 18 hardening — payer payment failures no longer rescore the homeowner's health (P1).** `handlePaymentIntentFailed` (and the refund/batch paths) rescore the customer via `customerHealth.scoreCustomer(payment.customer_id)`, and `computePaymentScore` (`customer-health.js`) reads the customer's `invoices`/`payments` and penalizes the score for late/failed rows — but payer-billed invoices (and the payments linked to them via `metadata.invoice_id`) are stored under the homeowner's `customer_id`, so an AP payment failure was lowering the *homeowner's* health/risk score and surfacing payer AR as customer delinquency. Fixed at the **scorer** (not just the webhook trigger) so it also covers the nightly batch rescore and any other caller: `computePaymentScore` resolves the customer's payer-billed invoice id set once and excludes payer rows (invoices by `payer_id`; payments by `metadata.invoice_id` → payer invoice) before counting on-time/late/failed, over-fetching to keep a representative self-pay sample. Degrades to no-exclusion if the `payer_id` column doesn't exist yet (migration not run). The webhook rescore call sites are left as-is since the recompute is now correct regardless of what triggered it.

**Codex round 17 hardening — the last payer-balance leak in the customer AI lookup (P1).** `lookup_customer` (`tools.js`) summed every failed/overdue `payments` row for the homeowner `customer_id` into `outstandingBalance` with no payer exclusion — the sibling of the round-14 `getBillingInfo` fix — so an AP payment failure could still surface as the homeowner's balance through the assistant. Extracted the exclusion into shared helpers `payerInvoiceIdSet()` + `isPayerPaymentRow()` (so the two AI lookups can't drift) and applied it to both `lookup_customer` and `getBillingInfo`; matches the `/api/billing` (billing-v2.js) filter. No payer payment row (linked via `metadata.invoice_id` to a `payer_id` invoice) now reaches any customer-facing assistant balance/history.

**Codex round 16 hardening — a P0 in-person collection leak + a fail-closed gap in the round-15 attach change.** (1) **P0 — charge-now REUSE could hand a payer invoice token to tech checkout (`admin-schedule.js`).** The round-7 guard on `POST /:id/invoice` only checks the *current* service/customer payer resolution (`resolveForInvoice`); if the live payer link was cleared/deactivated after an invoice was minted, that resolution reads self-pay and the guard passes. The reuse branch then applied prepaid credit and returned the *existing* invoice's `token` — the AP's bearer pay link — letting the tech collect the AP's bill from the homeowner in person. Now rejects (400, same message as the fresh-mint guard) when `existing.payer_id` is set, before any credit or token is returned. (2) **P1 — unfrozen invoices attached a stale snapshot for a deactivated payer (`payer.js`).** The round-15 change still always assigned the snapshot to `invoice.payer` and only refreshed the AP email when the live payer was active, so a draft/scheduled/sending invoice whose payer was deactivated after minting would still deliver to the stale snapshot AP inbox. `attachToInvoice` now splits cleanly: an **issued** invoice (`sent_at`/frozen status) keeps its immutable snapshot (recovering a live AP only if the snapshot never had one); an **unfrozen** invoice requires a live **active** payer and is left **unattachable** (`invoice.payer` unset → delivery fails closed for operator correction) when the payer is missing/inactive. Tests: unfrozen-deactivated-is-unattachable + issued-keeps-frozen-when-deactivated in `payer-service.test.js` (the prior "does NOT recover from a deactivated payer" case was rewritten — its old null-AP attach is now a fail-closed non-attach; the JSON-string-parse case was made issued so its no-live-query assertion still holds).

**Codex round 15 hardening (post-merge deep pass) — three structural footguns where payer state could still escape its guards (P1×3).** (1) **Dispatch completion returned the payer pay token (`admin-dispatch.js`).** Round 5 stopped opening the in-person payment sheet for payer completions (`invoicePaymentActionRequired`), but the response still returned `invoiceToken` — the AP's bearer pay link (`/api/pay/:token`). A cached/mobile client or the tech holding the response could open it and collect the AP's bill from the service recipient. Now nulled when `invoice.payer_id` is set (mirrors the round-5 `track-public.js` suppression); id/status/total still returned for display. (2) **Resend could overwrite the frozen AP email (`payer.js` + `invoice.js`).** Round 12 froze the delivered AP email on *issued* invoices by reading `invoice.status`, but `sendViaSMSAndEmail` claims a sendable invoice by flipping its status to `'sending'` (`claimInvoiceForSend`) BEFORE `attachToInvoice` runs — so resending an already-sent/viewed payer invoice looked undelivered and replaced the frozen snapshot AP email with the live payer email. `attachToInvoice` now decides "issued" from the persistent `invoices.sent_at` (COALESCE-set on first delivery, survives the claim), falling back to the status set. (3) **Deleting a payer nulled the invoice guard (`migration 20260617000002`).** `invoices.payer_id` was `ON DELETE SET NULL`, but it's the flag every fail-closed path keys off — a payer delete would strand `payer_snapshot` while making the invoice look self-pay, re-exposing the payer's invoice/pay/receipt to the homeowner. Changed to `ON DELETE RESTRICT`: an issued invoice's bill-to is immutable; payers are deactivated (`active=false`), never hard-deleted (no DELETE route exists), so this only blocks a raw delete of a payer that still carries billing history. `customers.payer_id` / `scheduled_services.payer_id` stay `SET NULL` (live reassignable links, no snapshot).

**Codex round 14 hardening (post-`origin/main` merge) — the customer-facing AI assistant was the last surface that could leak a payer invoice to the homeowner (P1×2).** Merging main (which carried the AI assistant's payment tools unchanged) re-exposed sibling paths the earlier rounds closed on the server routes. (1) **`send_payment_link` (`tools-expanded.js`)** auto-selected the most recent unpaid invoice with no `payer_id` exclusion and returned the pay URL — so a homeowner asking the assistant for a payment link could be handed the *payer's* pay link (and round 10's `sendViaSMSAndEmail` branch would silently bill the AP off a homeowner chat). The auto-select now `whereNull('payer_id')`, and an explicit payer `invoice_id` returns a redacted `{ payer_billed: true }` (no send, no pay URL) — the payer is billed only through the admin/dispatch payer-send path. (2) **`check_payment_status` (`tools-expanded.js`)** selected payer invoices and returned `cardBrand`/`cardLastFour`/status — the payer's card details to the homeowner; now excludes `payer_id` on auto-select and redacts an explicit payer id to `{ payer_billed: true }`. (3) **`getBillingInfo` (`tools.js`)** — the AI mirror of the `/api/billing` balance/history that rounds 6–8 hardened — read raw `payments` by `customer_id`, which includes payer payment rows (they sit under the homeowner but join to a `payer_id` invoice via `metadata.invoice_id`); it now drops those rows from `recentPayments`/`outstandingBalance` using the same invoice-id-set filter as `billing-v2.js`. (4) **`POST /admin/invoices/:id/send` `saveBillingRecipient` (`admin-invoices.js`)** persisted a one-off AP override into the **homeowner's** billing prefs without checking `payer_id`, which would reroute that homeowner's own future self-pay invoices to the third party; the save is now gated on `!invoice.payer_id` (payer AP delivery persistence is handled by `payer.freezeApEmail`). The same `origin/main` merge reconciled the App.jsx/AdminLayoutV2 nav with main's Documents→Contracts consolidation (#1872), keeping the new Payers/Finance entry alongside Contracts.

**Codex round 13 hardening (same PR) — three "looks delivered / stays unsent" bookkeeping gaps (P2).** (1) **Project combined-send surfaces a missing payer AP** — when a payer invoice has no usable AP email, `payerBilled` is false; the route now records an explicit `channels.payer_email` failure AND folds the payer leg into the project's `delivered`/`deliveryStatus` (a payer project isn't "delivered" until BOTH the homeowner report and the payer invoice go out). Previously the homeowner-report success marked the project delivered while the invoice was silently restored to draft and the AP never got it. (2) **Schedule LIST returns `payerId`/`poNumber`** — `/admin/schedule/list` didn't select them, but the Edit-appointment modal opened from the list echoes them on save, so a normal save posted blank `payerId`/`poNumber` and silently cleared an existing per-job Bill-To/PO (and tripped the admin-only actual-change 403 for techs). Added both to the list select + payload (matching the detail endpoint). (3) **Auto receipt queue stamps `receipt_sent_at` for payer invoices** — a payer-billed receipt skips the homeowner SMS path (where `sendReceipt` normally stamps `receipt_sent_at`), so the auto queue left the invoice in `needs_receipt` forever and a batch/manual resend duplicated the AP receipt; the queue now stamps it when the payer AP email delivers.

**Codex round 12 hardening (same PR) — the AP email is frozen ON DELIVERY, not creation (+ fail-closed everywhere).** The clarifying model from this round, consistent with the round-3 intent ("an *issued* invoice keeps its Bill-To"): the snapshot freezes the bill-to **identity** (name/address) at creation, but the **AP email** (delivery address) is only frozen once the invoice is issued/delivered. (1) **`attachToInvoice` prefers the live active payer AP email for UNDELIVERED invoices (P2)** — `AP_FROZEN_INVOICE_STATUSES` (sent/viewed/overdue/paid/prepaid/processing/terminal) gates it: a draft/scheduled/sending invoice tracks the live payer's current AP email so an operator's correction takes effect on a resend; an issued invoice uses the frozen snapshot. This fixes the stale-AP-on-retry case for **both** `sendInvoiceEmail` and the project combined-send (both call `attachToInvoice`), not just the flagged path. (2) **Shared `PayerService.freezeApEmail` (P2)** — the round-11 persist logic moved into `payer.js` and is now also called by the project payer-send (`admin-projects`) after `channels.payer_email` succeeds, so a recovered/corrected AP email is frozen regardless of which path delivered it. (3) **Dispatch already-delivered set includes `overdue` (P2)** — an overdue pre-minted invoice reused on a delayed completion no longer re-sends a duplicate AP email. (4) **Fail-closed placeholder extended to receipt JSON + both PDF routes (P2)** — `receipt-v2` JSON, the `/pay/:token/invoice.pdf`, and `/receipt/:token/pdf` now render a "Third-party payer" placeholder (not the homeowner) when `payer_id` is set but the payer can't attach, matching the round-11 pay-page fix. Tests: issued-vs-undelivered AP resolution in `payer-service.test.js`.

**Codex round 11 hardening (same PR) — persistence/dedup/fail-closed follow-ons (+ a jsonb-mutation bug).** (1) **Persist the delivered AP email, not just one-off overrides (P2)** — `persistPayerApIfNeeded` now freezes the AP email the invoice was actually delivered to (live-recovered, operator one-off, or first send of a legacy invoice) onto `payer_snapshot`, no-op once the snapshot already carries it; previously a *recovered* AP email (round 5) was delivered but never persisted, so a later payer edit/deactivation stranded the receipt/pay-page bill-to. **Bug found doing this:** `attachToInvoice`'s recovery mutated the snapshot object **in place** (`parseSnapshot` returns the jsonb-as-object by reference), so the persist check saw the email "already there" and skipped — `attachToInvoice` now clones the snapshot before filling the recovered email. (2) **Gate dispatch completion payer send to unsent invoices (P2)** — `invoiceCreated` is also true when a completion REUSES an existing unpaid invoice (pre-minted `sent`/`viewed`, or `invoiceAlreadySent`); the payer send now skips already-delivered statuses so it can't duplicate the AP billing email (fresh completion invoices are `draft`). (3) **Recipient-versioned project payer key (P2)** — the round-10 stable key dedup'd forever even after the AP email changed, so a corrected-and-retried `/send-with-invoice` returned the old blocked/bounced result; the key now includes a sha1 of the AP email — dedupes a repeat to the same inbox, re-delivers to a corrected one. (4) **Pay page fails closed for unattachable payers (P2)** — `pay-v2` serialized `payer:null` when `payer_id` was set but the payer couldn't attach (legacy, no snapshot, inactive/deleted payer), and the React page rendered that as self-pay with the homeowner as bill-to; it now serializes a "Third-party payer" placeholder so the page stays non-self-pay (save-card suppressed, billing email blank). Tests: live-recovered-AP snapshot-persist case in `invoice-email-recipient.test.js`.

**Codex round 10 hardening (same PR) — fail-closed keying + two SMS-only/retry stragglers.** (1) **Reroute keyed on `payer_id`, not the attached object (P1, hole in round 4).** `sendInvoiceEmail` / `sendReceiptEmail` keyed the payer reroute/fail-closed branch off `invoice.payer` (the attached object). A payer-billed invoice whose payer can't be attached (no snapshot + live payer inactive/missing) left `invoice.payer` unset, so the branch was skipped and the invoice/pay-link (or receipt with the payer's card last4) fell through to the homeowner — exactly what every prior round forbade. Both now key off `invoice.payer_id` and **fail closed** when no payer recipient resolves. (2) **AI `send_payment_link` tool (P2)** — `tools-expanded.js` called `sendViaSMS` directly, which short-circuits to `payer_billed` for payer invoices (no AP email, no finalize); it now routes payer invoices through `sendViaSMSAndEmail` (shape-agnostic `sent` from `sent||ok`), mirroring the round-8 batch fix. (3) **Duplicate payer invoice on report retry (P2)** — the `admin-projects` combined-send finalizes the invoice off the payer AP delivery even when the homeowner report leg fails, so a `/send-with-invoice` retry re-billed the AP a duplicate invoice/pay-link (the idempotency key carried a per-call timestamp). The payer copy now uses a **stable** `project.report_with_invoice:{project}:{invoice}:payer` key — dedupes after the first success, still retryable after a genuine failure. Tests: payer-can't-attach fail-closed cases (invoice + receipt) in `invoice-email-recipient.test.js`.

**Codex round 9 hardening (same PR) — symmetric ACH guard, a hole in my round-7 fix, and the one-off-AP-recipient end-to-end gap.** (1) **ACH success state reset (P1, symmetric to round 6)** — `payment_intent.succeeded`'s `us_bank_account` branch reset the homeowner's `ach_status='active'`/`ach_failure_count=0` off the payer payment's `customer_id`; now guarded so a payer/AP bank clearing can't reactivate a homeowner's suspended ACH. (2) **Blocked payer-completion send marked delivered (P1, hole in round 7)** — `sendInvoiceEmail` returned `{ok:true}` even when SendGrid resolved `sent:false` (recipient suppressed/unsubscribed), so the round-7 dispatch finalization (which keys solely off `.ok` because the homeowner SMS is suppressed) could mark a never-delivered payer invoice as sent. `sendInvoiceEmail` now returns `{ok:false, blocked}` on `sent===false`; the dispatch gate is unchanged (fixed at the source, benefits every caller). (3) **Payment plans rejected for payer invoices (P1)** — `POST /admin/invoices/:id/payment-plan` 400s for payer-billed invoices before inserting a homeowner-scoped plan / pausing follow-ups (round 6 only suppressed the confirmation email; a payer-facing plan flow is Phase 2). (4) **Payer name PII in logs (P1)** — `admin-payers` create log drops the `display_name`, logging only the payer id. (5) **One-off AP recipient now end-to-end (P2)** — when a payer with no AP email is invoiced via an explicit operator one-off recipient, `sendInvoiceEmail` freezes that AP email onto `invoices.payer_snapshot.ap_email` on a successful send, so the (async) receipt and the pay page route to the same AP contact the invoice was billed to instead of resolving no recipient / prefilling the homeowner. (6) **History limit vs payer filter (P2, finally robust)** — `/api/billing` now pages through history (via a new `getPaymentHistory(customerId, limit, offset)`) excluding payer rows until `limit` customer-visible payments are filled or the table is exhausted (bounded ~10 pages), replacing the `limit*3+10` heuristic — no metadata-cast over the whole table. (7) **Pay-page billing email (P2)** — `PayPageV2` no longer prefills the homeowner's email into Stripe billing details for a payer invoice; it passes the payer email or leaves it blank for the AP to enter. Tests: blocked-send `ok:false` + one-off-AP snapshot-persist cases in `invoice-email-recipient.test.js`.

**Codex round 8 hardening (same PR) — a self-inflicted regression + two completeness gaps.** (1) **`/api/billing` history `ReferenceError` (regression from round 7).** The round-7 over-fetch rewrite dropped the `const { limit } = req.query` destructuring, so `parseInt(limit)` threw on every authenticated billing-history request (no route-level test covers this handler, and `node --check` can't catch a runtime `ReferenceError`). Restored as `parseInt(req.query.limit)`. (2) **Upcoming/next-charge balance leak** — round 6/7 filtered payer-linked `failed` rows from `/api/billing/balance` but left the `upcoming` sum + `nextPayment` reading every `status='upcoming'` row, so a payer-linked upcoming autopay row still showed in the homeowner's `upcomingCharges`/`nextCharge`. Both now apply the same `isPayerPayment` exclusion. (3) **Batch immediate-send stranded payer invoices** — `POST /admin/invoices` with `sendImmediately` called only `sendViaSMS`, which short-circuits to `payer_billed` and never finalizes; payer-billed batch rows now route through `sendViaSMSAndEmail` (payer AP email + finalize) while self-pay keeps the SMS-only immediate send. Note: the round-7 regression shipped because the billing-v2 route handlers have no test coverage (the repo's route tests exercise `_private` helpers, not HTTP handlers); the fixes here were verified by `node --check` + identifier audit rather than a new bespoke harness.

**Codex round 7 hardening (same PR) — stranded payer AR + the last collection/dunning siblings.** (1) **Stranded payer AR on dispatch completion (the one real bug, self-inflicted in round 4).** Suppressing the homeowner completion-SMS pay link for payer invoices left the auto-created invoice delivered to nobody — the homeowner channel can't finalize it and there's no payer send in that handler. The dispatch completion path now routes a payer-billed auto-invoice to the payer AP inbox (`sendInvoiceEmail`) and finalizes on success (`markDeliverySent({ email: true })`); a payer with no usable AP email leaves it unfinalized for operator correction (never mails the homeowner). (2) **Charge-now pre-completion mint** — `POST /admin/schedule/:id/invoice` (the tech-checkout in-person collection mint) now resolves the payer and 400s for payer-billed visits, so the sibling of the round-6 terminal-handoff guard can't collect the AP's invoice from the homeowner either. (3) **Balance-reminder balance math** — `getCustomerBalance` now excludes payer-linked `failed`/`upcoming` payment rows before computing `totalBalance`/`daysOverdue` (round 6 only filtered the oldest-invoice pay link; an AP ACH failure row still inflated the homeowner's balance and could trigger an early reminder). (4) **Bill-to routing is audited** — a `payer_id` change on a customer is now in `sensitiveFields`, so redirecting a customer's invoices to a different AP account writes a `customer.update_sensitive` audit entry. (5) **History limit vs payer filter (P3)** — `/api/billing` over-fetches before excluding payer rows so a homeowner whose newest payments are AP payments still gets up to `limit` visible self-pay rows. Tests: balance-reminder mock updated for the new payer-ids lookup.

**Codex round 6 hardening (same PR) — the adjacent paths behind each round-5 fix.** Round 5 fixed the primary surface of each leak; round 6 closes the sibling path that shares the same data. (1) **Charge endpoints reject payer invoices server-side** — `StripeService.chargeInvoiceWithSavedCard` throws and `POST /api/stripe/terminal/handoff` 400s for a payer-billed invoice, so a stale client or direct admin/tech call can't charge the homeowner's saved card / mint an in-person handoff for the AP's bill (the round-5 dispatch flag only hid the UI). (2) **ACH failure webhook** — `handleAchFailure` early-returns for a payer-billed invoice (looked up by the PI), so an AP bank failure never mutates the homeowner's `ach_status`/card-default or texts them retry/suspension notices. (3) **ACH processing webhook** — the `payment_intent.processing` handler returns before its direct `ach_payment_processing` SMS for payer invoices (the `sendAchProcessing` email already self-guards, but the SMS is sent inline). (4) **Refund / payment-plan / retry lifecycle emails** — `sendRefundIssued`, `sendPaymentPlanConfirmed`, and `sendPaymentRetryNotice` resolve the linked invoice and return `payer_billed` for payer invoices, so refunding the AP's payment or putting a payer invoice on a plan no longer emails the homeowner. (5) **Legacy balance-reminder cron** — `BalanceReminder.getCustomerBalance` + `latePaymentCheck` exclude `payer_id` invoices when picking the "oldest unpaid invoice" pay link (the round-5 fix only covered `LatePaymentService`; the balance-reminders job is a second overdue path). (6) **Customer balance — failed payments** — `/api/billing/balance` now excludes payer-linked `failed` payment rows from `currentBalance` and the `lastPaymentFailed` banner (round 5 only filtered the unpaid-invoice sum; an AP ACH/card failure row sits under the homeowner's `customer_id`). (7) **Manual offline payments** — `POST /admin/invoices/:id/record-payment` stamps `metadata.invoice_id` on the `payments` row for payer-billed invoices so the customer-facing history/balance filters catch them; self-pay rows stay unlinked to preserve the existing receipt-total fallback. Tests: refund + payment-plan `payer_billed` skips in `payment-lifecycle-email.test.js`.

**Codex round 5 hardening (same PR) — close the last homeowner-leak channels + the snapshot strand.** Round 4's "never bill/chase the homeowner" principle, applied to every remaining surface a payer invoice could leak through. (1) **Project combined-send billing copy** — the self-pay billing-contact fallback (`else if (channels.email?.ok)`) is now gated on `!isPayerInvoice`, so a payer invoice with no usable AP email is simply not delivered (operator fixes the AP email) instead of mailing the homeowner the third-party invoice + pay link. (2) **Dispatch mobile collection** — `invoicePaymentActionRequired` now gates on `!invoice.payer_id`, so a payer-billed completion never opens the in-person payment sheet (techs must not collect AP's invoice from the service recipient). (3) **Customer tracker** — `track-public.js` suppresses `invoiceToken` for payer-billed invoices, so a homeowner with the public track link can't open the payer's `/pay/:token` page. (4) **Stripe lifecycle emails** — `sendPaymentFailed` + `sendAchProcessing` (payment-lifecycle-email.js) early-return `payer_billed` for payer invoices; those notices resolve recipients off `invoice.customer_id` (homeowner) with the pay link, so the homeowner is no longer told the payer's card failed / ACH is processing. (5) **Legacy late-payment-checker** — its overdue query now excludes `payer_id` invoices (the new follow-up short-circuit only covers the new sequence; the legacy cron selected unhandled overdue invoices and texted/emailed the homeowner). (6) **Customer billing balance** (`/api/billing/balance`) excludes payer invoices from `currentBalance`, so the homeowner doesn't see the payer's unpaid amount as their own. (7) **Customer payment history** (`/api/billing`) drops rows whose `metadata.invoice_id` joins to a payer-billed invoice, so the homeowner never sees the payer's card brand/last4/Stripe PI id. (8) **Snapshot AP-email strand** — `attachToInvoice` recovers the live AP email from the still-active payer row when the frozen snapshot has none (invoice minted before ops filled it in), without disturbing the frozen name/address identity; otherwise round-4's no-homeowner-fallback would strand that invoice forever even after the AP email is corrected. (9) **Recurring payer/PO-only edits** — `PUT /admin/schedule/:id/update-details` now propagates a parent payer/PO change to pending child visits independently of the date/cadence rewrite gate, so editing only the Bill-To on a series reaches future visits. Tests: snapshot AP-email recovery (+inactive-payer guard) in `payer-service.test.js`; lifecycle-email `payer_billed` skips in `payment-lifecycle-email.test.js`.

**Codex round 4 hardening (same PR) — never bill or chase the homeowner for a payer invoice.** The round-3 principle (a misconfigured payer must not leak onto the homeowner) is now applied to the remaining channels. (1) **Invoice email no longer falls back to the homeowner** — a payer-billed invoice with no usable AP email (and no explicit operator one-off override) now FAILS (`sendInvoiceEmail` returns `ok:false`) instead of routing the PDF + pay link to the customer billing contact; the operator adds the payer AP email (or a one-off recipient) and resends. This covers the normal/batch/scheduled send paths that share `invoiceRecipientFor`. (2) **Receipt email is payer-only** — a payer receipt goes to the payer AP inbox or NOT at all (no homeowner fallback that would expose the payer's payment-method last4); a missing AP email returns the standard "No receipt recipient email" skip. (3) **Receipt SMS suppression is non-retryable** — `actionableSmsFailure` now treats `payer_billed` like `already-sent`/`no-phone`, so the receipt delivery queue stops retrying/failing a job whose homeowner SMS was intentionally suppressed. (4) **Dispatch completion SMS** drops the pay link for payer-billed jobs (`allowCompletionInvoiceLink` gates on `!invoice.payer_id`) — the homeowner still gets the report-only completion text, AR routes to the payer. (5) **Invoice follow-ups never arm for payer invoices** — `scheduleForInvoice` short-circuits on `invoice.payer_id` (centralized, so `markDeliverySent`/project-send and every other caller are covered), so the dunning sequence can't chase the homeowner with the payer's pay link. Phase 1 has no payer dunning sequence (Phase 2). Tests: invoice-email no-fallback (+override-still-sends) and receipt payer-only routing in `invoice-email-recipient.test.js`; `payer_billed` non-retry in `receipt-delivery-queue.test.js`.

**Codex round 3 hardening (same PR).** (1) **Bill-to is now snapshotted, not just FK'd** — `invoices.payer_snapshot` (jsonb) freezes the payer name/company/AP-email/address at creation; `attachToInvoice` prefers the snapshot (no live query, no active dependency), so an issued invoice/receipt keeps its original Bill-To even if the payer is later renamed/edited/deactivated. Legacy pre-snapshot invoices fall back to the live row (active-guarded). (2) **Homeowner gets a report-ONLY email** for payer invoices (`sendProjectReportReady` with the FDACS PDF + report link, no invoice PDF / pay link); the invoice + pay link go to the payer. (3) **Project suppression keys off `invoice.payer_id`** (`isPayerInvoice`), not AP-email availability — a misconfigured payer (active, no `ap_email`) still never bills the homeowner, and the invoice simply isn't finalized (can't deliver to the payer). (4) **Receipt JSON + ReceiptPage** serialize/show the payer Bill-To (matching the PDF). (5) **Receipt SMS suppressed** for payer invoices (`sendReceipt` returns `payer_billed`) — never texts the homeowner a receipt that would expose the payer's card last4. (6) **`previewInvoiceTotals`** resolves the payer and applies `tax_exempt`, so the WDO confirmation total matches the created invoice. (7) **PO field shows for the EFFECTIVE payer** (`form.payerId || customer.payerId`) so a default-payer job can capture a PO. (8) **Pay page** no longer posts `/consent` for payer payments (clears the stale `?saveCard=1` parent state → no spurious `consent_failed` banner).

**Codex round 2 hardening (same PR) — reroute now covers every money channel.** (1) **Save-card** guard extended to `updateInvoicePaymentIntentMethod` + `finalizeInvoicePayment` (not just create) — an AP user toggling "save card" after the Element loads still can't attach to the homeowner. (2) **Receipt email** reroutes to the payer AP inbox (symmetric with the invoice email). (3) **Pay-link SMS** to the homeowner is suppressed for payer invoices — guarded at the source in `sendViaSMS` (restores the send-claim) and short-circuited in `sendViaSMSAndEmail`; the project combined-send SMS drops the pay-link line (keeps the report link). (4) **Recurring children** inherit the (freshly-updated) parent's `payer_id`/`po_number` on both new-child spawn and existing-child rewrite, so a series doesn't silently revert to self-pay. (5) **Pay page** (`GET /api/pay/:token`) attaches + serializes the payer; `PayPageV2` shows Bill-To = payer / Service address = homeowner + PO, hides the save-card consent, and prefills Stripe billing_details with the payer (not the homeowner). (6) **Project combined-send**: payer AP delivery is now INDEPENDENT of the customer/report email and the invoice is finalized on `payer_email` success (`invoiceDelivered`), so a payer invoice can't be marked sent without the AP inbox receiving it, nor stranded when the homeowner has no email. (7) **Tech 403 regression** fixed — the schedule edit modal echoes payer/PO on every save, so the admin-only gate now fires only on an ACTUAL change vs the stored values. (8) **Per-job dropdown** label is truthful: blank = "Use account default — <name>" when the customer has a default payer, else "Customer pays (self)". Known Phase-2 gap: no explicit one-off self-pay override when a default payer exists (would need a sentinel/flag).

**Codex round 1 hardening (same PR):** (1) **Tax-exempt is wired** (owner-ratified) — a tax-exempt payer forces `taxRate=0` in `invoice.create()`, zeroing tax even on commercial jobs; cert stored for records. `previewInvoiceTotals()` preview not yet exemption-aware (cosmetic; the saved invoice is authoritative) — Phase 2. (2) **Saved-payment opt-in suppressed for payer invoices** — `createInvoicePaymentIntent` forces `saveCard=false` when `invoice.payer_id` is set, so the AP contact's card can never attach to the homeowner's Stripe customer. (3) **Per-job lookup scoped by customer** — `resolveForInvoice` constrains the `scheduled_services` lookup by `customer_id` so a stale/mismatched `scheduledServiceId` can't snapshot another customer's payer. (4) **Inactive payer never attaches** — `attachToInvoice` mirrors `resolveForInvoice`'s `active !== false` guard (a payer deactivated after mint falls back to self-pay on send). (5) **Token/printed PDFs attach the payer** — `/api/pay/:token/invoice.pdf` and `/api/receipt/:token/pdf` attach before rendering so downloaded docs show the same Bill-To. (6) **Project WDO+invoice billing copy routes to the payer AP inbox** (fallback to customer billing contact). (7) **Per-job payer/PO edits are admin-only** on `PUT /admin/schedule/:id/update-details` (route is requireTechOrAdmin; payer routing must not be tech-settable). (8) **PO is advisory** (owner-chosen) — relabeled; the appointment editor shows a non-blocking "usually requires a PO" reminder. (9) Partial payer updates accept snake_case `display_name` (UI rename bug). (10) `resolveForInvoice` now returns `{ payerId, poNumber, taxExempt }`.

## 2026-06-17 — Invoice prepaid status + customer account credit (claude/invoice-prepaid-marking-tibmi3)

**Decision (owner spec 2026-06-17):** an invoice can be marked **prepaid** by drawing down a customer's account-credit balance — the quarterly-prepay flow — and that credit is attributed to the customer via a ledger. Four owner decisions drove the build: (1) credit applies **manually** (operator click), never auto-deducted, to avoid racing Stripe autopay; (2) `prepaid` is a **real new invoice status**, distinct from Stripe-collected `paid`; (3) **no recurring prepay discount** — only an optional waive of the WaveGuard initial/setup fee, recorded as a flag (mirrors the annual-prepay `setupFeeWaived` display flag; does NOT recompute line-item totals); (4) operator surfaces are the **Invoice detail page** and **Customer 360**, no Intelligence Bar tool.

**Account credit is now real.** `customers.account_credits` (added but never read/written since 20260401000085) becomes a cached running balance. New append-only `customer_credit_ledger` (migration 20260617120000) records every signed movement (`delta`, `balance_after`, `source`, optional `invoice_id`). `services/customer-credit.js` is the single writer: `postCreditMovement` locks the customer row, recomputes, refuses to go negative, and writes cache + ledger in one transaction (optionally folded into a caller's trx). Sources: `manual` / `adjustment` / `invoice_application` / `invoice_prepaid` / `referral`.

**Endpoints.** `GET|POST /admin/customers/:id/credits` (balance + history; issue/adjust — admin-only, audited as `customer.credit.adjust`). `GET /admin/invoices/:id/credit-context` (balance + amount due for the modal). `POST /admin/invoices/:id/apply-credit` (admin-only): locks the invoice, draws down credit up to the amount due, increments `invoices.credit_applied`, and — when fully covered — flips status to `prepaid` (+`prepaid_at`/`prepaid_by`), stops the follow-up sequence, and writes a `payments` ledger row so revenue dashboards count the coverage. Partial application reduces the balance due and leaves the invoice collectible.

**`prepaid` is terminal/uncollectible.** Added to `INVOICE_UNCOLLECTIBLE_STATUSES` (invoice-helpers.js) — so `assertInvoiceCollectible` blocks re-payment on the public pay page (pay-v2 already routes every collect path through it) and the invoice stats exclude it from outstanding/overdue. Treated like `paid` where "money's in": receipt PDF + receipt send (receipt-v2, invoice.sendReceipt), send-invoice guard, Customer 360 `amount_paid`, list status color/badge, and the new `Prepaid` list filter (`directStatuses`). Receipts for credit-prepaid invoices behave exactly like manual-cash-payment receipts (the `payments` row carries no `invoice_id`, so `loadPaymentForInvoice` returns null and the generator falls back to invoice totals — same as the existing manual path).

**Revenue recognition (called out for owner ratification):** account credit is a holding bucket (issuing credit writes NO `payments` row); revenue is recognized when credit is **applied** to an invoice (the apply-credit `payments` row, amount = applied). This keeps prepaid money as deferred until tied to a serviced invoice and avoids double-counting. The invoice-level `total_collected`/`paid` stats still count only `status='paid'`, so prepaid sits in its own bucket rather than the invoice-based collected figure.

**Referral credit deliberately NOT wired (deferred).** Referral rewards already accrue to a separate promoter wallet (`promoters.available_balance_cents` etc.) with its own `pending`→`earned`→`credited` lifecycle; auto-posting them into `account_credits` would double-credit live payouts. `customer_credit_ledger.source` reserves the `referral` value and a nullable `referral_id` so a later reconciliation PR needs no migration.

**UI.** Invoice detail (Tier 2, `D` palette): new `ApplyCreditModal` + "Apply credit" action — shows available credit vs amount due, optional waive-setup-fee, partial/full preview, "Apply & mark prepaid". Customer 360 (Tier 1 V2, components/ui): new `AccountCreditPanelV2` on the Billing tab — balance, ledger history, admin-only Issue/Deduct credit form.

**Codex review follow-up (same PR).** Hardened the new status against its downstream blast radius: (1) **partial credit application removed** — apply-credit now requires credit to cover the invoice in full (a partial application would leave the invoice collectible while the Stripe/Terminal pay paths still price off `invoice.total`, over-collecting); to prepay part of a bill, lower the invoice first. (2) The prepaid transition now **cancels any open PaymentIntent** first (refusing if money is in flight, mirroring the cancelled-service triage) so a stale session can't charge the card after the credit is consumed. (3) It **stamps `paid_at`** alongside `prepaid_at` so AR/outstanding dashboards (`paid_at IS NULL`) and annual-prepay term activation (keyed on `status='paid' || paid_at`) treat it as closed; `syncTermForInvoicePayment` is now invoked on the transition. (4) `assertInvoiceVoidable` rejects `prepaid` (voiding would strand the consumed credit — reverse the credit instead); the generic invoice edit path is edit-locked when `credit_applied > 0` (same rationale as deposit-credit invoices). (5) Receipts wired for prepaid on the remaining guards (`/send-receipt` route, `/batch/send-receipts`, `sendReceiptEmail`); the credit `payments` row carries `metadata.invoice_id` so receipt rendering + the cancelled-service money guard see it. (6) PayPage redirects `prepaid` to the receipt; Customer 360 `balanceOwed` excludes `prepaid`; the `setup_fee_waived` column now feeds the customer-facing `setupFeeWaived` callout. **Still deferred:** there is no credit-reversal/undo flow for a prepaid invoice (void is blocked) — a mistaken prepaid needs manual correction; a proper reverse-and-restore-credit action is a follow-up.

**Codex re-review follow-up (revenue recognition — owner decision).** Codex flagged that booking a paid `payments` row on every credit application would report goodwill/courtesy credit as collected/taxable income. Owner decision: **recognize cash when it is RECEIVED, not when applied.** Issuing credit now takes a funding kind — `prepayment` (money received: cash/check/Zelle/card) books a paid `payments` row at issuance; `goodwill` and `adjustment` are non-cash and book nothing. `apply-credit` no longer inserts a `payments` row at all (prevents double-counting cash and taxing courtesy credit). Customer 360's Issue-credit form gained the Funding (prepayment vs goodwill) + Method selectors and an inline note of the revenue effect. Also closed the remaining collection guards so a prepaid invoice can't be double-collected: record-payment's race guard uses `INVOICE_UNCOLLECTIBLE_STATUSES`, and the Terminal handoff mint + PI-create re-verify reject `prepaid`; `needs_receipt` list + the customer ReceiptPage now include prepaid.

**Codex round-4 follow-up (prepaid receipts + reversal — owner decisions).** The proactive "prepaid invoices get receipts" wiring kept surfacing payment-received copy across every channel (SendGrid + SMTP email, SMS, receipt PDF, public ReceiptPage), which is misleading for goodwill/account-credit coverage. Owner decision: **do not send payment receipts for prepaid invoices.** Reverted prepaid out of the receipt paths (`sendReceiptEmail`, `/send-receipt` + `/batch/send-receipts`, `InvoiceService.sendReceipt`, `receipt-v2` PDF + state, `needs_receipt` list, admin Download-PDF/Send-receipt buttons, client ReceiptPage). Instead, the customer pay link renders a friendly **"You're all set — nothing due · covered by your account credit"** state in `PayPageV2` (no payment receipt, no charge UI). Second owner decision: **added a reversal path** — `POST /admin/invoices/:id/reverse-prepaid` (admin-only, + a "Reverse prepaid" button on prepaid rows) restores the consumed credit to the customer (a `+adjustment` ledger movement) and reopens the invoice to `sent`; the issuance cash row is intentionally left (the money is held as credit again). Also excluded `prepaid` from the AI assistant's unpaid-invoice payment-link selector (`tools-expanded.js`) so it can't try to bill a covered invoice.

## 2026-06-18 — Lawn Care Re-Service (claude/lawn-care-reservice-waves-di4frf)

**Decision:** add a **Lawn Care Re-Service** to the Service Library, mirroring the existing **Pest Control Re-Service** (`pest_re_service`) one-for-one for the lawn-care line — a free callback visit between regular service intervals for active recurring lawn-care customers experiencing breakthrough weed / disease / insect pressure. Single additive migration `20260618000001_lawn_re_service.js`; no runtime code change.

**Why migration-only:** the re-service concept is already fully generic. The `is_callback` flag and the dispatch callback routing / service-record / document callback copy all key off the `"Re-Service"` (or `"callback"`) name, so naming the row **"Lawn Care Re-Service"** picks up all of that automatically. Admins select it from the new-appointment modal exactly like the pest re-service: it ships `customer_visible: false` / `booking_enabled: false` (internal, not self-bookable) but `is_active: true`, and `GET /admin/services` filters on active only, so it surfaces in the admin service lookup.

**Service row** (`lawn_re_service`): category `lawn_care`, `billing_type: 'one_time'`, `pricing_type: 'variable'` / `base_price: null` (no charge for active WaveGuard / recurring lawn-care customers), `is_taxable: true` / `tax_service_key: 'lawn_care'`, `requires_license: true` / `license_category: 'L&O'`, `icon: '🔁'`, `sort_order: 14` (after the lawn family). Tax key + license category follow the rest of the lawn catalog (`pest_re_service` used `pest_control` / `GHP`).

**Completion profile:** seeded directly in the post-cutover shape — `completion_mode: 'service_report'`, `project_type: 'one_time_lawn_treatment'`, `portal_visibility: 'token_only'`, `portal_attach_policy: 'recurring_customer'` — matching the rest of the lawn family (`lawn_aeration` / `lawn_fungicide` / `lawn_insect_control` / `lawn_inspection`) after the Phase-1 cutover (`20260611000012`). `PROJECT_TYPE_BY_SERVICE_KEY` already routes `/^lawn_/` to `one_time_lawn_treatment`, so no separate cutover migration is needed — lawn re-service completions render through the typed lawn CompletionPanel + Service Report V1, the same end state `pest_re_service` reached.

**Codex review follow-up (owner: "fix both pest + lawn"):** Codex flagged two real gaps in the *shared* re-service design (pre-existing — they affected `pest_re_service` identically; this PR did not introduce them). Owner directed fixing both lines, so the fix is service-key-aware rather than lawn-only. (1) **Auto-callback flag** — the new-appointment modal never sends `isCallback`, and downstream callback reporting (`service_records.is_callback`) + document PDF copy (`documents.js` `is_callback`) read the persisted column, not the service name. New `server/services/re-service.js` (`isReService` / `RE_SERVICE_SERVICE_KEYS = {pest_re_service, lawn_re_service}`); the scheduler (`admin-schedule.js` POST `/`) now derives `is_callback` from the catalog row, so both re-services persist the flag and flow into callback metrics + the "no additional charge" PDF copy. (2) **Free callback could bill monthly dues** — a re-service for a WaveGuard customer zeroes `estimated_price` at scheduling, and completion fell back to `monthly_rate` (`admin-dispatch.js` invoice path, and the one-time completion billing guard `projectCompletionInvoiceAmount` in `project-completion.js`, which would otherwise 409-block or invoice a month's dues). Both now skip the `monthly_rate` fallback when `is_callback` is set — an explicit positive price still bills if the operator set one. Regression coverage added in `tests/project-completion.test.js`.

## 2026-06-19 — Restore the Generate-AI-report button + corner dictation mic in CompletionPanel (claude/pest-control-ai-bart-voice-nef2bf)

**Decision (owner directive 2026-06-19):** partially reverse the Phase-2 CompletionPanel simplification (#1875, b274498). The owner uses the **manual "Generate AI report"** button daily — the tech enters brief structured notes, taps the button, and AI drafts customer-facing visit copy into the Technician Notes box for them to review/edit before completing. #1875 removed that button on the premise that report copy would be generated server-side at completion, but the tech-facing draft-and-edit workflow is still wanted, so the button is restored. The Phase-3 "auto-derive at completion" path is unaffected — the two are complementary (this just gives the tech an editable draft in-box).

**Restored verbatim from b274498's parent:** the module-level `generateAiReport(payload)` fetch helper (`POST /api/admin/schedule/generate-report` — the endpoint was never removed, `admin-schedule.js:4535`), the `buildAiReportPayload()` method (classifies inputs by provenance so the prompt won't promote a recommendation/concern into a confirmed finding), the `generating` state, and the `!quickComplete`-gated Generate button in **both** CompletionPanel render variants (mobile `M` palette + desktop `D` palette). Behavior is 1:1 with the pre-#1875 implementation; button label gains a ✨ prefix.

**Dictation mic relocated to the notes-box corner (owner: "little microphone ... in the bottom right").** The `useSpeechDictation` voice-to-text (added in #1875) is unchanged; only its control moved. The wide "🎙 Dictate notes" pill below the textarea is replaced by a small circular mic button absolutely positioned in the **bottom-right corner of the Technician Notes textarea** (both variants). The textarea reserves right-side padding (`paddingRight` 52 mobile / 50 desktop when `dictation.supported`) so typed text never runs under the mic; idle shows 🎙, listening shows a red ■ stop glyph. Falls back gracefully (no mic) on browsers without the Web Speech API.

## 2026-06-19 — Commercial proposal PDF + multi-building line items (claude/siesta-key-pest-estimate-o18m9e)

**Decision (owner ask):** for commercial leads (the Siesta Key HOA case — two towers + 50 lake houses), turn an estimate into a formal, sendable **line-item proposal PDF** built from a **multi-building** model, and **attach the PDF to the estimate delivery email** (SMS keeps texting the estimate link — carriers can't attach files).

**Multi-building model — JSONB, no migration.** The proposal lives in `estimates.estimate_data.proposal` (the table already carries a `category: COMMERCIAL` enum + JSONB `estimate_data`). Shape: `{ enabled, title, preparedFor, propertyAddress, taxRate, taxLabel, terms, buildings: [{ name, note, lineItems: [{ description, quantity, unitPrice, frequency, taxable }] }] }`. `frequency ∈ monthly|bimonthly|quarterly|annual|one_time`. `server/services/estimate-proposal.js` is the single source of truth: `normalizeProposal` (reads the authored proposal, else **synthesizes** a single-building fallback from engine line items / stored totals so any estimate can still produce a PDF) + `computeProposalTotals` (annualizes by cadence → annual recurring + monthly equivalent + one-time + first-year total).

**Tax is not re-implemented here.** Taxability is CPA-signed and owned by the tax module; this feature only sums the per-line `taxable` flags the operator sets and applies the proposal's `taxRate`. With no rate set (the residential-HOA default — common-area pest non-taxable, lawn never taxable in FL) tax is $0 and every line renders non-taxable. The PDF marks taxable lines with `*` and notes the rule.

**PDF.** `server/services/pdf/estimate-pdf.js` (pdfkit), reusing invoice-pdf.js chrome 1:1 — navy masthead with logo + FDACS license, branded footer, zinc rules. Renders per-building sections with a line-item table (description / cadence / qty / rate / amount), per-building subtotal, a totals block (monthly equiv · annual recurring · one-time · tax · **first-year total**), and a Terms & assurances block (FDACS license, COI-on-request, IPM + callback guarantee). Multi-page paginator (`ensureSpace`) re-prints a slim continuation header + table header across pages (verified at 14 buildings / 7 pages).

**Routes (admin-estimates.js, admin-auth):** `GET /:id/proposal` (normalized proposal + totals), `PUT /:id/proposal` (author/replace — recomputes the three authoritative total columns from the line items, since the operator-entered items ARE the commercial quote, and stamps `category=COMMERCIAL`), `GET /:id/proposal.pdf` (inline branded PDF — same generator as the email attachment, so download and emailed copy are byte-identical). **Email attachment:** `sendEstimateNow` builds the attachment **only when `proposal.enabled`** (an authored proposal) — synthesized fallbacks are NOT attached, so ordinary residential estimate emails are unchanged; PDF failure is non-fatal (link email still sends). Threaded through `sendEstimateEmail` → SendGrid `attachments` (base64) with a nodemailer conversion for the SMTP fallback path. Not an Intelligence Bar tool, so no write-gate registration.

**UI (Tier 1, components/ui).** New self-contained `client/src/components/estimates/CommercialProposalModal.jsx` opened from a **"Commercial proposal"** row action in `EstimatesPageV2` (desktop pipeline). Add/remove buildings + line items, per-line cadence + taxable toggle, tax-rate field, live totals (client mirror of `computeProposalTotals`), **Save proposal** (PUT) + **Download PDF** (adminFetch → blob → new tab; admin auth is a bearer token, so a plain anchor won't carry it). Kept as an isolated component + 4 small edits to the 3.8k-line page (import, state, menu item, modal render) to minimize blast radius.

## 2026-06-19 — Third-party payer Phase 2 — statement close + NET-terms delivery (feat/payer-statements-close-deliver)

**Decision:** build the close + deliver layer on top of the merged accrual core (#1929), completing the NET/consolidated-statement path Phase 1 deferred (see the 2026-06-17 entry — Phase 1 wired only `due_on_receipt`; a payer on `net15`/`net30` accrues each month's billed visits onto one OPEN `payer_statements` row). Phase 2-here turns that open row into a sent, AP-processable document: **finalize (freeze) → render PDF → email the payer's AP inbox**, operator-driven from the admin payer routes, shipped dark behind `GATE_PAYER_STATEMENTS`.

**Finalize (`finalizeStatement`, payer-statements.js):** freezes totals + the bill-to snapshot + the NET due date and flips `open → finalized` inside a transaction. Concurrency mirrors accrual exactly: it takes the **same per-(payer, period) advisory lock** as `getOrCreateOpenStatement` FIRST, then row-locks the statement `FOR UPDATE` before freezing — so a late visit either attaches before the freeze, or finds the statement non-open and opens the next period (never lands after close, never races the total). Idempotent: a non-open statement is returned unchanged. The due date is statement-dated NET in ET (`etDateString(addETDays(date, termDays))`, `net15`→15 / `net30`→30, default 30); `overdue` stays **derived** from `due_date`, never stored (carried from the P1 design). The frozen `payer_snapshot` is refreshed from the live payer at close (mirrors `invoices.payer_snapshot`) but a payer-read failure never blocks the close — it falls back to the statement's existing snapshot.

**Ledger + PDF.** `loadStatementLines` returns the attached non-void invoices as a chronological visit ledger (homeowner name + service address per line). `server/services/pdf/payer-statement-pdf.js` (pdfkit) renders one consolidated NET-terms statement — a line per visit + rolled-up totals — from the **FROZEN snapshot, never the live payer**, so a reprint always matches what was billed (reuses invoice-pdf.js chrome 1:1; multi-page paginator).

**Delivery (`payer-statement-email.js`) carries the P1 invariant end-to-end: a statement bills the PAYER, never the resident.** The AP recipient resolves **only** from the frozen snapshot, with the live payer's AP email as a *recover-missing* fallback — there is no homeowner fallback. **No AP email ⇒ the send fails loudly** (`no_ap_email`) rather than mis-billing a homeowner. Send path: SendGrid template `payer.statement.sent` (seeded in `20260620000002` — a `transactional_required` billing template so it's never marketing-suppressed; the SMTP path remains a dev/safety fallback caught via `template not found`), PDF attached, then stamps `finalized → sent` + `sent_at` atomically (`WHERE status='finalized'` so a viewed/paid statement is never downgraded and a resend never re-stamps). Only billed-deliverable statuses are sendable (`finalized`/`sent`/`viewed`) — `open` returns `statement_not_finalized`, `void`/`paid`/anything else `statement_not_sendable`, so a voided statement can never reach AP rendered as "Due". `dryRun` builds the PDF + resolves the recipient WITHOUT sending or stamping (for the dry-run-first rollout + admin preview).

**Routes (admin-payers.js, admin-auth + requireAdmin).** `GET /:id/statements` (recent), `GET /:id/statements/:sid` (statement + lines), `POST …/close` (txn-wrapped finalize; `{ send, dryRun }` chains the AP send so "close & send" is one click), `POST …/send` ((re)send a closed statement; 409 while still open). Every statement route is **ownership-scoped** (`loadOwnedStatement` 404s if the statement's `payer_id` ≠ the addressed payer, so one payer's URL can't reach another's). Read paths are ungated (return `[]` until the gate flips); the mutating close/send paths are gated on `payerStatements`. The send runs **outside** the close transaction (never hold a DB txn across an email send).

**Codex pre-push follow-up (2 rounds, all P1 — no P0).** Round 1: (1) AP email no longer logged (PII — log statement/payer ids + counts); (2) close-and-send no longer reports a failed AP delivery as success (accurate log + 422 mirroring `/send`, frozen statement still returned since re-close is idempotent); (3) overdue computed in **ET not UTC** — `new Date('YYYY-MM-DD')` compared UTC midnight and could flag a statement overdue the prior evening in America/New_York; new `isStatementOverdue` compares the due calendar day vs today in ET (`dateOnlyString` + `etDateString`). Round 2: (4) **seeded the `payer.statement.sent` template** (above) so a prod send isn't a guaranteed `email_unavailable` dead-end once the gate flips; (5) **status allowlist** so a `void`/`paid` statement can't be emailed as due. Round 3 (1 P0 + 1 P1): (6) **P0 — finalize lock key now matches accrual.** `period_start` comes back from the DATE column as a JS Date; interpolating it raw produced `Fri May 01 2026 …` while accrual locks on `${pid}|YYYY-MM-DD`, so the two never shared the advisory lock and a late invoice could attach after the freeze. Normalized via `dateOnlyString` (regression test asserts the Date case). (7) **P1 — first-delivery idempotency key** (`payer_statement_sent:<id>`) so a double-click / retry / concurrent close-and-send can't email AP twice (`sendTemplate` dedupes only on `idempotencyKey`); an explicit resend stays keyless and intentional. Round 4 (1 P1): (8) the `/close` chained send is now **first-delivery-only** — it sends only when the frozen row is genuinely pre-send (`finalized`, no `sent_at`); an idempotent re-close that returns an already-`sent`/`viewed` statement skips the send (`{ skipped: 'already_delivered' }`, not a 422) so a double-click can't mail AP a duplicate after the first-delivery key has lapsed. Resends still go through `/send`. Round 5 (1 P1): (9) the PDF "Issued" row rendered `finalized_at` (a TIMESTAMP) via `dateOnly` (UTC date-only), so a 10pm-ET close showed the next day — switched to `dateTime`/`formatDisplayDate` (ET calendar date); the DATE columns (period/due/service dates) correctly stay on `dateOnly`. Round 6 (1 P1): (10) a **blocked first delivery was unretryable** — `sendTemplate` writes a terminal `blocked`/`bounced` row under the stable first-delivery key (those are in `DEDUPE_STATUSES`), so reusing it deduped forever. Added a `forceResend` flag for the retry. **Round 7 reconciles rounds 6+bot into a force path** (the GitHub bot flagged that a blanket-keyless `/send` would double-send a *first* delivery on a double-click — the exact inverse of round 6): a normal send (`/close` chained or plain `/send`) keeps the stable first-delivery key (dedupes double-clicks); `POST /send { force: true }` is the ONLY keyless first-delivery path, used to retry a known blocked/suppressed attempt that the terminal key would otherwise dedupe forever. Already-sent re-delivery stays keyless. **GitHub bot P2 follow-ups (2 rounds):** `force` no longer goes keyless at all. `forcedRetryKey` walks past each generation whose attempt terminally blocked (base → `:r1` → `:r2` …, checking `email_messages`) to the first non-blocked generation's key. The result is **fresh** for a new generation (so a genuinely blocked statement can be retried) yet **stable** within it — so a double-click or two concurrent forced requests dedupe on `:rN` (the `email_messages` unique key resolves the race) instead of each inserting a keyless row and double-sending. A stray force with no prior block returns the base key unchanged; fails safe to the base key (never keyless) on a lookup error. **`forceResend` is routed through `forcedRetryKey` for ANY status, not just `finalized`** — so a statement SendGrid accepted (stamped `sent`) then bounced async via webhook is still generation-scoped on a force retry rather than falling to the keyless already-sent branch. Force with nothing to retry resolves to the base key and dedupes against the delivered row (no blind resend). **Close-chain concurrency:** the `/close` chained send passes `firstDelivery: true`, which PINS the base key regardless of the row's status at send time — so two concurrent close-and-sends (one stamps `sent` between the other's freshness check and its re-read) dedupe on the base key instead of the late one going keyless. **PDF property column** now renders homeowner name AND service address (a consolidated statement to a property manager / HOA spans many homes, so AP needs the serviced location, not just the name). **Dedupe-loser handling:** when two first-delivery requests race, `sendTemplate` raises `EMAIL_SEND_IN_PROGRESS` for the loser while the winner's row is still `queued`; the loser now returns `{ ok: true, deduped: true }` (and does NOT stamp `sent` — the winner does) instead of a false 422 AP-delivery failure.

**Deferred to a later Phase 2 slice:** month-end auto-close cron (today close is operator-triggered), payer-portal statement view + online pay, PO enforcement, payer deposit roll-forward/refund, and the tech-completion "Billed to … don't collect on site" banner (lands after the CompletionPanel rebuild). 25 tests green (`payer-statements.test.js`, `payer-statement-email.test.js`, `payer-statement-overdue.test.js`).

## 2026-06-21 — Third-party payer Phase 2 — P3 (pay + settle) (feat/payer-statements-pay-settle)

**Decision:** add statement payment + settlement on top of the merged accrual (#1929) + close/deliver (#1941) lanes — AP settles a finalized statement online (self-serve Stripe on the payer's customer) AND/OR offline (check/ACH/wire reconciled by admin), and settlement CASCADES to every accrued child invoice. Gated behind `GATE_PAYER_STATEMENTS`. Owner decisions (2026-06-19): offer both online + offline; card-save OPTIONAL with NO silent auto-charge on due_date.

**Status state machine (`payer-statement-settle.js`).** `open → finalized → sent → viewed → processing → paid` (or `void`). PAYABLE = `finalized`/`sent`/`viewed` (frozen + not-in-flight) — a PaymentIntent may be created only from these. **`processing` is entered ONLY on a CONFIRMED money-in-flight webhook** (`payment_intent.processing` for ACH / `succeeded` for card), never on PI creation — an unconfirmed PI stays replaceable, so merely opening or abandoning the pay page can't lock the statement. Once `processing`, a second pay confirm AND admin reconcile are both refused. `overdue` stays derived from `due_date`, never stored. `settleStatementPaid` cascades (statement `paid` → child invoices `paid` with `paid_at = statement.paid_at`, a settlement marker not N charges → ONE payer-scoped `payments` row), idempotent on already-paid; the offline reconcile passes a PAYABLE-only allowed-status set so it can't settle a statement whose online payment is already in flight (double collection).

**Payments ledger.** Migration `20260621000001` adds `payments.statement_id` (FK→payer_statements) + `payments.payer_id` (FK→payers) and makes `customer_id` NULLABLE — a statement settlement writes ONE row with `customer_id = NULL`, `payer_id`/`statement_id` set, `amount` = the CHARGED total (surcharged for a card; base/surcharge split on the existing `*_cents` columns). Only **customer-keyed** readers exclude payer-scoped rows (per the design wording): the per-customer revenue breakdown in `dashboard-tools` adds `whereNull('payer_id')` so a `customer_id = NULL` settlement row isn't shown as a bogus "no customer" bucket; per-customer queries elsewhere are already `customer_id = X` filtered (NULL excluded). **Business-wide cash/revenue totals INCLUDE the payer settlement row** — it's real cash, counted once (the child invoices get no separate payment rows, so there's no double-count). See round 8/9 below — an earlier over-application of the exclusion to global totals was reverted.

**Self-serve pay (`pay-statement.js` at `/api/pay/statement`, mounted BEFORE `/api/pay` so its two-segment paths aren't shadowed by the invoice `/:token`).** The statement's own `token` resolves it; `GET` stamps `viewed` (sent→viewed) and returns the visit ledger + bill-to; `setup` → `quote` → `finalize` mirror the invoice surcharge two-step but charge **`payer.stripe_customer_id`** (new `ensureStripePayerCustomer`, kept separate from the homeowner flow). Surcharge runs the same `computeChargeAmount(total, methodType, { funding })` quote→finalize as invoice card pay (statements are NOT deposits → surcharge applies normally; rate derived, never hardcoded); ACH/offline carry no surcharge. `setup` reuses a replaceable `requires_payment_method` PI / cancels other unconfirmed states / refuses real in-flight money — and never moves the statement to `processing`.

**Webhook (`stripe-webhook.js`).** A PI carrying `metadata.waves_statement_id` routes to `handleStatementPaymentIntentEvent` at the top of each PI handler and returns — legacy `invoice_id` intents are unchanged. Idempotent, keyed on the active PI: `processing` → `processing`; `succeeded` → settle + cascade (surcharge cents derived from the PI); `payment_failed`/`canceled` → revert to the derived prior payable status (collectible again). NOT feature-gated — a confirmed money event must settle regardless of the flag.

**Admin reconcile (`admin-payers.js` `POST …/statements/:id/reconcile`).** Offline settle (check/ACH/wire) + cascade; refuses while `processing`/`paid`; **cancels any unconfirmed online PI first** (`cancelStatementPaymentIntentIfUnconfirmed`, 409 if money is truly in flight) so the AP can't also pay online after the operator records the check. 36 payer tests green. **Deferred to P4:** AR/aging view + statement-level dunning (`payer_statement_followups`).

**Codex pre-push round 1 (4 P0 + 1 P1).** (P0) (1) the new public `/api/pay/statement/:token` surface is now in the AGENTS public-by-token allowlist with a contract, plus a 64-hex token-format gate + per-IP rate limit on the route. (2) **Surcharge-bypass quarantine** — setup mints a base-amount, client-confirmable PI, so a credit-card payer could skip `/quote`+`/finalize` and confirm at base; the webhook now runs the SAME `shouldQuarantineUnfinalizedCardPayment` guard as invoices before settling and quarantines+alerts a card that succeeded without surcharge finalization (no base-only settle). (3) **Active-PI binding** — the succeeded webhook now locks the statement and settles only when the succeeded PI is the statement's CURRENT `stripe_payment_intent_id`; a stale/replaced PI that collected money is an orphan/over-collection → alert for manual refund, never settle. (4) **Reconcile fails closed** — if an existing statement PI can't be retrieved/verified, `cancelStatementPaymentIntentIfUnconfirmed` now throws 409 (was: proceed), so an offline reconcile can't double-collect against a still-confirmable client secret. (P1) the dashboard `paidRevenueTotal`/`paidRevenueDaily` ledger sums also got the `whereNull('payer_id')` payer-row exclusion.

**Codex pre-push round 2 (5 P0).** (1) **Stale surcharge metadata on PI reuse** — a reused `requires_payment_method` PI reset to base kept its old `surcharge_policy_version` (Stripe metadata merges), which the webhook reads as "finalized" → would settle a later base-only card confirm without surcharge; the base reset now explicitly clears `surcharge_policy_version`/`surcharge_rate_bps`/`card_funding` (empty string deletes the keys). (2) **Reconcile ↔ setup race** — a concurrent `/setup` could mint a new confirmable PI between the reconcile's cancel and settle; cancel + settle now run UNDER ONE `forUpdate` lock (which `/setup` also takes), so no new PI can appear in the window. (3) **Wallet surcharge exemption** — the reused invoice quarantine helper exempts wallets; a credit Apple/Google Pay confirmed at base would settle undercharged. Added an `exemptWallets` option; the statement caller passes `false` (full card-family surcharge). (4) **Durable alerting** — statement quarantine/orphan alerts hit `customer_health_alerts` (customer_id NOT NULL) and silently failed; they now write a durable `stripe_orphan_charges` row (tolerates null customer; the operator's manual-refund queue), throwing so the webhook retries until persisted. (5) **Public payload PII** — the by-token route returned `loadStatementLines` wholesale (includes `customer_name`); it now whitelists serviced address + visit fields, no homeowner name.

**Codex pre-push round 3 (3 P0).** (1) **Settlement wrote a non-existent column** — the `payments` insert set `payment_method`, but `payments` has only `payment_method_id` (FK), so EVERY settlement insert would have thrown and rolled back the paid cascade after Stripe may have collected; the method now rides `metadata` (and `payer_statements.payment_method`). (2) **Reconcile fail-open when Stripe down** — `cancelStatementPaymentIntentIfUnconfirmed` returned a no-op for an unconfigured Stripe BEFORE checking for an existing PI; it now loads the statement first and throws 409 if a PI exists but Stripe can't verify it (can't double-collect a still-live client secret). (3) **Statement chargebacks left the statement paid** — the invoice-keyed dispute handler no-ops on a statement payment (keyed by `statement_id`, no `invoice_id`), hiding clawed-back money; added `reverseStatementCascadeForDispute` (created/lost → statement back to owed + children reopened) and `restoreStatementCascadeForDispute` (won → re-settle unless re-collected), keyed on `payment.statement_id`.

**Codex pre-push round 4 (1 P0 + 2 P1).** (P0) the dispute reversal now also clears `payer_statements.stripe_payment_intent_id`/`stripe_charge_id` — otherwise `/setup` + reconcile see the old succeeded PI as active and 409, so the reopened AR could never be re-collected (the disputed `payments` row keeps the PI for audit). (P1) the settled method is derived from the actual attached PaymentMethod (`paymentDetailsFromIntent`) not the PI's allowed-types list — a base-amount debit/prepaid card was being recorded as ACH (the PI allows both). (P1) the remaining IB revenue readers got the `whereNull('payer_id')` exclusion too (`dashboard-tools` MTD/last-month/per-customer/monthly-trend, `tax-tools` two YTD sums) — a final sweep confirms every paid-payment aggregate now excludes payer-scoped rows.

**Codex pre-push round 5 (2 P0).** (1) **PI replacement now fails closed on a cancel error** — `createStatementPaymentIntent`'s reuse branch logged a failed cancel and minted a replacement anyway; if the old PI had raced into processing/succeeded its client secret could still collect → double charge. It now throws 409 unless Stripe confirms the old PI is canceled. (2) **Dispute reversal is now PI-scoped** — `reverseStatementCascadeForDispute`/`restoreStatementCascadeForDispute` take the disputed payment's PI and no-op unless the statement is still settled by THAT PI, so a late `dispute.closed(lost)` for PI A can't undo a replacement settlement (PI B / offline) the AP made after `dispute.created` reopened it.

**Codex pre-push round 6 (2 P0).** The statement succeeded webhook was reworked to trust NOTHING from PI metadata. (1) **Active-PI binding is now a non-null exact match** — it settles only when `stmt.stripe_payment_intent_id === piId` (a null/different stored PI ⇒ stale/orphan/replaced ⇒ record a review item, never settle). (2) **Surcharge is recomputed from the ACTUAL confirmed funding** — `computeChargeAmount(statement.total, methodType, { funding })` from `paymentDetailsFromIntent`, and the charged amount must match (±1¢) or it quarantines. This replaces the metadata-trusting `shouldQuarantineUnfinalizedCardPayment` for statements (a failed `/finalize` could leave a stale `surcharge_policy_version` on a reusable PI, letting the same client secret confirm a different tender — credit at debit base, or debit with a stale credit surcharge). Settlement now writes the recomputed cents, bound to the real tender. (The invoice path keeps the original helper unchanged.)

**Codex pre-push round 7 (3 P0 + 1 P1).** (1) **Self-deadlock fix** — the webhook settle passed `database: trx` inside the settlement payload, but `settleStatementPaid` takes it as the THIRD arg, so it ran on the global `db` and blocked forever on the outer txn's `FOR UPDATE`; now `settleStatementPaid(id, settlement, { database: trx })`. (2) **Active-PI check moved BEFORE the idempotent-paid shortcut** — a stale/replacement PI succeeding after the statement was paid another way now records a `stripe_orphan_charges` review item instead of a silent skip. (3) **Dispute-won restore guards replacements** — it no-ops when the statement is `processing` or carries a different active PI (AP started a replacement after `dispute.created`), instead of overwriting it. (P1) reconcile validates the amount against the LOCKED `total` inside the txn (a close/reroll between the pre-lock read and the lock could change it).

**Codex pre-push round 8 (1 P0).** Fail closed on UNVERIFIED card funding — `paymentDetailsFromIntent` swallows a Stripe charge/PM lookup failure and leaves `funding` null, so a card PI (created at base) confirmed without `/finalize` would recompute against `funding: null` (no surcharge) and settle undercharged. The succeeded handler now records a manual-review `stripe_orphan_charges` row and does NOT settle when a card-family PI has unverifiable funding (ACH is unaffected — it carries no surcharge).

**Codex pre-push round 9 (1 P0 + 1 P1).** (P0) **Dispute ↔ settlement ordering** — the statement dispute path keyed on the `payments` row (`stripe_charge_id`), which doesn't exist until `payment_intent.succeeded` settles, and Stripe doesn't guarantee ordering; a `charge.dispute.created` delivered first skipped reversal and a later succeeded settled clawed-back money. `handleDisputeCreated` now resolves the statement by `dispute.payment_intent` (not the payments row); `reverseStatementCascadeForDispute` reverses a settled cascade OR, when the dispute precedes settlement, **clears the statement's active PI** so the racing/late succeeded fails the active-PI binding and records an orphan instead of settling. (P1) **Reverted the global-total reader exclusions** — excluding payer rows from business-wide cash/revenue made paid statements vanish entirely; the exclusion now lives only on customer-keyed readers (above).

**Codex pre-push round 10 (1 P0).** Pre-settlement dispute hardening: clearing the active PI for a not-yet-settled statement (1) stranded a `processing` ACH row (no PI ⇒ setup/reconcile/revert all refuse) and (2) removed the only PI→statement link before a `payments` row existed (so a later won couldn't restore). Now the not-yet-settled branch ALSO resets a `processing` statement to its prior payable status, and `handleDisputeCreated` INSERTS a durable disputed `payments` row (`status='disputed'`, keyed to the statement + PI + charge) when settlement hasn't written one — so `dispute.closed(won)` can find the statement and restore/settle the reinstated funds.

**Codex pre-push round 11 (2 P0 + 1 P1).** (1) **Atomic dispute reversal + durable row** — the pre-settlement PI-clear and the disputed-row insert now run in ONE transaction (`reverseStatementCascadeForDispute` accepts the caller's `trx`), so a row-write failure rolls back the PI-clear and the Stripe retry's PI lookup still finds the statement. (2) **`dispute.closed` reordered ahead of the row** — `handleDisputeClosed` now mirrors the PI-keyed statement lookup: a `lost` with no `payments` row reverses/blocks the statement + persists the durable disputed row; a `won` leaves the active PI for the eventual succeeded. (P1) **Stale `processing` events** — the statement `payment_intent.processing` branch re-reads the CURRENT PI status from Stripe and skips if it's no longer processing (a stale retry after failed/canceled would otherwise re-strand the statement).

**Codex pre-push round 12 (1 P0 + 1 P1).** (P0) **Late `dispute.created` after a won close** — the statement created branch now honors a recorded `dispute_final` marker (statement-scoped) and skips the reversal, so a reordered/retried created can't clear the PI + reopen children + undo reinstated funds after the dispute was won. (P1) the per-customer revenue breakdown's `whereNull('payer_id')` was unqualified and `customers` also has a `payer_id` column → ambiguous-column error; qualified to `payments.payer_id`.

**Codex pre-push round 13 (2 P0).** (1) **Full refunds reverse the statement** — `charge.refunded` only updated the `payments` row, leaving a fully-refunded statement + children `paid` (AR never recollects). Added a `payment.statement_id` + full-refund branch that reverses the statement cascade (PI-scoped, like the dispute reversal). (2) **Won-before-settlement ordering marker** — a `dispute.closed(won)` reordered before any `payments` row now persists a durable dispute-final marker keyed on the PI (so a late `created` skips reversal), and `settleStatementPaid` now UPSERTS its ledger row on the PI so the eventual `succeeded` consumes the marker (merging `dispute_final`) instead of writing a duplicate.

**Codex pre-push round 14 (2 P0).** (1) **Refund reordered before settlement** — `charge.refunded` (full) now resolves the statement by PI when no `payments` row exists yet, reverses the cascade + clears the active PI + writes a durable refunded marker, so a later `succeeded` can't settle refunded money. (2) **Won-restore surcharge validation** — `dispute.closed(won)` now re-applies a settlement directly ONLY when the row was already validated (`base_amount_cents` present); for a pre-settlement dispute MARKER it restores just the active PI and lets the eventual `succeeded` settle through the canonical `computeChargeAmount`/funding validation (so a base-only credit card can't become paid via won-restore).

**Codex pre-push round 15 — ARCHITECTURAL fix (owner-chosen): serialize the statement-money lifecycle.** Rounds 9–14 patched per-event webhook orderings (settle × dispute-created/won/lost × refund); the root cause was per-event mutation with pre-lock existence checks (TOCTOU). Introduced `withStatementMoneyLock(statementId, fn)` — a per-statement `pg_advisory_xact_lock` that `settleStatementPaid`, `reverseStatementCascadeForDispute`, `restoreStatementCascadeForDispute`, the succeeded handler, and every pre-settlement dispute/refund branch now acquire, so all money mutations for one statement are strictly serialized. The pre-settlement branches RE-CHECK the `payments` row INSIDE the lock (if settle won the race, mark the now-paid row refunded/disputed instead of inserting a stale marker). Two P0s closed by this: (1) a full refund racing settlement no longer leaves a paid row counted (in-lock recheck marks it refunded); (2) a `dispute.closed(won)` on a pre-settlement marker (which may have no future `succeeded` after `created→succeeded→won` orphaned it) is left for operator reconcile rather than marked paid (no stranded paid-row-vs-unpaid-statement).

**Codex pre-push round 16 (1 P0 + 1 P1).** (P0) the POST-settlement refund path's row update + cascade reversal also run UNDER `withStatementMoneyLock` now (a racing `succeeded` could otherwise flip the refunded row back to `paid` between the update and the reversal). (P1) the offline reconcile route now allowlists `check`/`ach`/`wire`/`offline` and rejects `card`/unknown — a `method: 'card'` would have recorded a card settlement at base with no surcharge, bypassing `computeChargeAmount`.

**Codex pre-push round 17 (1 P0 + 1 P1).** (P0) the pre-settlement refund guard now handles ANY refund, not just full — a PARTIAL `charge.refunded` before settlement was falling through to the 0-row generic update and being lost, so the later `succeeded` settled the full gross (overstating cash). Partial pre-settlement now persists a durable `stripe_orphan_charges` review item (full still reverses + marks). (P1) reconcile now takes the statement money advisory lock BEFORE the row `FOR UPDATE`, matching the webhook order (advisory → row), so a concurrent settle/refund/dispute can't deadlock with it.

**GitHub bot round 1 (1 P2).** Don't strand an ACH success on a transient lookup failure: `paymentDetailsFromIntent` defaults `paymentMethod` to `payment_method_types[0]` (= `card` for the dual card+ACH statement PI) when the charge/PM lookup fails, so the fail-closed-on-unverified-card-funding guard could orphan a legit ACH `succeeded`. Added a `resolved` flag (true only when method/funding came from actual Stripe data); the statement succeeded handler now THROWS on `!resolved` so Stripe RETRIES the webhook (the lookup almost always succeeds on retry) instead of guessing `card`.

**GitHub bot round 2 (1 P2 + 1 P3).** (P2) a statement `payment_intent.failed` that actually reverted a `processing` statement (a confirmed ACH debit that bounced days later, or a post-confirm card decline) now raises a durable admin notification — the silent revert left AP/operators no signal. (P3) the `/api/pay/statement` gate middleware now runs BEFORE the rate limiter, so a disabled `GATE_PAYER_STATEMENTS` always returns 404 (a 429 after 60 req/min would otherwise reveal the disabled surface exists).

**GitHub bot round 3 (1 P3).** Honor the "404 when off" contract even ahead of the GLOBAL `/api/` limiter: a pre-limiter gate at `index.js` 404s `/api/pay/statement` when `GATE_PAYER_STATEMENTS` is off, before the app-wide limiter can return a 429 to an IP that already exhausted it.

## 2026-06-21 — Third-party payer Phase 2 — P4 (AR + dunning) (feat/payer-statements-ar-dunning)

**Decision:** complete the NET-terms lane (after P1 accrual #1929, P2 close+deliver #1941, P3 pay+settle #1961 all merged dark) with the two pieces the statement model implies but nothing yet surfaces: **AR/aging keyed on the payer→statement**, and **statement-level dunning to the AP inbox**. Shipped dark behind `GATE_PAYER_STATEMENTS`, backend-first (matching P1–P3, which left the dedicated client statement UI to a later UI-consolidation slice). New: `payer_statement_followups` table, `server/services/payer-statement-followups.js` (dunning engine), `server/services/payer-ar.js` (aging), a dunning cron, admin AR + dunning-control routes, and a `get_payer_ar_aging` IB dashboard tool.

**AR / aging (`payer-ar.js`).** A NEW receivable layer — payer → statement → invoices — parallel to and independent of the per-customer/per-invoice self-pay AR (untouched). **Aging is keyed on the STATEMENT's `due_date`, never the child invoices'** (an accrued invoice has no individual due date; the statement is the receivable — hardest-integration-point #3 from the plan). Outstanding = `finalized | sent | viewed | processing` (`open` is still accruing, not yet a receivable; `paid`/`void` are settled). `processing` (payment in flight) stays an outstanding balance for AR even though dunning pauses on it — the money hasn't landed. Buckets by days-past-due (ET): `current (≤0) | b1_15 | b16_30 | b31_45 | b45_plus`. `summarize()` is pure (no DB) → reused by the per-payer read (`payerArForPayer`), the cross-payer "AR by terms" read (`computePayerArAging`, sorted oldest-past-due-first = the collections worklist), and the IB tool, so all three report identical numbers. Gate-dark safe: every read returns zeros until statements exist.

**Dunning (`payer-statement-followups.js` + config).** The statement mirror of `invoice-followups.js`, but **email-only to the payer's AP inbox — never the homeowner** (the P1–P3 invariant; the invoice followup engine already excludes payer invoices, so the two never overlap). Terms-aware cadence anchored to `due_date` (NOT sent_at — AP was already notified at close; dunning begins only once the NET window lapses): due+0 reminder / +15 firmer / +30 final. Anchoring to `due_date` makes it terms-aware for free (a net30 statement duns 15 days later in absolute time than a net15). ONE `payer.statement.followup` SendGrid template renders all three steps; the per-step tone rides a `reminder_line` config variable (no N-template sprawl). Unlike `payer.statement.sent` (no CTA), the reminder carries a pay CTA → the P3 `/pay/statement/:token` surface; the PDF is NOT re-attached (AP has it).

**Eligibility is driven off the STATEMENT, not the followup row** (`runPending` self-heals): a statement is dunnable while `status IN (sent, viewed)` and past due — the sequence row is created on first dun, so a send that never armed a row can't silently skip dunning. `processing`/`paid`/`void` are excluded by the status filter, so **settling a statement stops its dunning even without an explicit stop**. `viewed` does NOT stop dunning (AP opening the pay link without paying is exactly when we keep nudging — mirrors the invoice followups); only paid/void/processing pause it. One touch per statement per run; a hard delivery failure (incl. no AP email) pauses the sequence for operator attention rather than spinning. Per-(statement, step) idempotency key so a step never double-mails across overlapping ticks; the `runExclusive` cron lease is the outer guard. Cron: `15 10 * * 2-5` ET (staggered 15m after the invoice followups to spare the pool), gated — `runPending` no-ops when off.

**Settle → stop wiring.** `stopOnStatementSettled` is called best-effort OUTSIDE the money txn at both settle sites (Stripe webhook + admin reconcile) — it only tidies the row to `completed` (the eligibility status-filter is the real stop), and is wrapped so a dunning side-effect can never poison the settlement transaction or break on a pre-deploy missing table.

**Admin surface (`admin-payers.js`).** `GET /ar-aging` (cross-payer, registered BEFORE `/:id` so the literal path isn't captured as an id) + `GET /:id/ar` (per-payer outstanding + aging) + derived aging fields (`days_past_due`/`overdue`/`aging_bucket`) added non-breakingly to the existing `/:id/statements` rows. Dunning controls mirror the per-invoice ones: `GET .../followups` (sequence state) + `POST .../followups/:action` (pause | resume | stop | send-now), gated. IB: `get_payer_ar_aging` dashboard tool (read-only → not in write-gates) delegating to the same `computePayerArAging`.

**Schema.** `payer_statement_followups` — `statement_id` UNIQUE + ON DELETE RESTRICT (mirrors `invoices.payer_statement_id`; statements are voided, never hard-deleted), denormalized `payer_id`, status/step_index/touches_sent/next_touch_at + admin pause/stop columns. `payer.statement.followup` email template seeded via the same publish machinery as `payer.statement.sent`. Both ship dark; behaviour gated.

**Deferred (lane-consistent with P1–P3):** the dedicated client AR/statements/dunning page (the UI-consolidation slice — PayersPage still has no statement view), month-end auto-close cron, payer-portal statement view, PO enforcement, and the tech "Billed to … don't collect" banner (after the CompletionPanel rebuild). 20 new tests (`payer-ar.test.js`, `payer-statement-followups.test.js`); 81 payer tests green.

**GitHub bot round 1 (1 P2).** A dunning step whose email key was once suppressed came back from `sendTemplate` as `{ deduped: true, sent: false, blocked: true }`; `fireStep` treated any `deduped` as benign and ADVANCED `step_index` without delivering — silently skipping the reminder after a block. Fixed: `sendFollowupEmail` now classifies a dedupe as benign ONLY when it actually `sent` (already-delivered / in-flight-race winner); a blocked dedupe returns `{ ok: false, blocked }` so `fireStep` PAUSES instead of advancing. The explicit operator retry (`send-now`) now passes `forceRetry`, which walks past terminally-blocked generations to a FRESH key via the P2 `forcedRetryKey` (now exported from `payer-statement-email.js`) so a reminder whose key was suppressed can reach AP after the suppression is cleared — instead of deduping against the dead row forever. +2 regression tests (83 payer tests green).

**GitHub bot round 2 (1 P2).** The webhook `stopOnStatementSettled` ran unconditionally after the settlement txn, but that txn has anomaly paths that return WITHOUT paying (orphan/stale active-PI, unverified card funding, surcharge-mismatch quarantine) — so a non-settling success still completed the dunning row, halting collections on a statement that stays `sent`/`viewed` + unpaid. The txn now returns a `settledNow` boolean (true only on a fresh settle or an idempotent already-`paid`); the success log + dunning-stop fire ONLY when `settledNow`. The `!resolved` transient path still THROWS (Stripe retries), so it never reaches the stop either.

**GitHub bot round 3 (1 P2).** The dunning `EMAIL_SEND_IN_PROGRESS` race-loser returned `{ ok: true, deduped: true }`, so `fireStep` advanced `step_index` while the winning send was only `queued` — if that winner then failed (and auto-retries under the same key), the sequence had already moved past the step and the reminder was skipped. The loser now returns `{ inFlight: true }`; `fireStep` neither advances nor pauses on it (returns `reason: 'in_flight'`) so the step is re-evaluated next tick and only advances once it's genuinely delivered (`deduped && sent`) or freshly sent. +1 regression test (84 payer tests green).

**GitHub bot round 4 (1 P1).** The dunning email's "Pay this statement" CTA pointed at `/pay/statement/:token`, but the SPA has no public client route for it (only `/pay/:token`); P3 shipped the BACKEND `/api/pay/statement/:token` while the client statement-pay page remains the deferred payer-portal UI slice — so the CTA would dead-end at the authenticated portal catch-all (broken link). Dropped the online-pay CTA + `pay_url` from the `payer.statement.followup` template + engine payload (mirrors `payer.statement.sent`'s deliberate no-CTA stance); the reminder now asks AP to reply/call to arrange payment. The CTA + `pay_url` get re-added when the client statement-pay page ships (noted in the migration). 84 payer tests green.

## 2026-06-21 — Third-party payer Phase 2 — P5a (admin statements/AR/dunning UI) (feat/payer-statements-client-ui)

**Decision:** start the deferred client UI slice — the P1–P4 backend shipped the whole NET-terms lane but `/admin/payers` rendered only the list + edit form (PayersPage even still said statements were "coming in Phase 2"). This PR makes the lane **operator-usable**: the payer name now opens a detail Sheet with the statement lifecycle, dunning controls, and per-payer AR; a header "AR aging" button opens the cross-payer collections view. Read-only/no Stripe — the **public statement-pay page is split into P5b** so the money surface is reviewed on its own.

**`PayerDetailSheet.jsx`** (new, opened from the list): a Statements tab (each statement → status + aging badge, expandable to its visit lines + actions) and an AR tab (per-payer `GET /:id/ar` — outstanding, past-due, buckets). Statement actions map 1:1 to the P2–P4 endpoints: **Close & send / Close only** (`/close`), **Send / Resend** (`/send`), **Record offline payment** (`/reconcile` — method + amount mini-form), and **dunning controls** (`/followups/{pause,resume,stop,send-now}`) shown only for a dunnable (`sent`/`viewed`) statement or one that already has a sequence. **`PayerArAgingDialog.jsx`** (new): the org-wide `GET /ar-aging` — by-age + by-terms breakdowns and the oldest-first collections worklist; clicking a payer opens that payer's detail Sheet.

**Gate-dark safe:** the read endpoints return empty/zeros until `GATE_PAYER_STATEMENTS` is on (so the UI shows honest empty states today); the mutating actions surface the server's 403 ("not enabled") rather than faking success. Refreshed the stale "coming in Phase 2" copy in the edit form. Tier 1 V2 throughout (components/ui + zinc; `alert-fg` only on genuinely overdue amounts; type tokens limited to the defined scale). No local client build exists in-repo (client `node_modules` is empty; builds run on Railway) — verified by esbuild parse + full import-graph bundle-resolve instead.

**P5b (next):** the public `/pay/statement/:token` Stripe Payment Element page (no client route exists yet — only `/pay/:token`), which also restores the dunning email's online-pay CTA dropped in P4 round 4.

**Pre-push codex (2 P1, same PR).** (1) **Invalid reconcile amount → full-total charge.** A blank/NaN amount in the offline-payment form serialized to JSON `null`, which the server's reconcile treats as "default to the full statement total" — so a mistyped/cleared field would silently record the whole balance. `ReconcileForm` now validates a positive finite number client-side (disables Record + never submits NaN); the server still re-checks against the locked total. (2) **`paid_at` shown in UTC.** The settled timestamp rendered via the date-only (UTC) formatter, off by a day from ET after ~8pm; added a `dateInET` formatter (America/New_York) for TIMESTAMP columns while the DATE columns (period/due/service dates) keep the UTC date-only render.

**GitHub bot round 1 (1 P2).** The admin "Send to AP" button always posted `{}`, but a blocked/suppressed FIRST delivery leaves the statement `finalized` and `sendStatementEmail` only walks to a fresh retry key in `force` mode — so after AP fixes the bounce the button kept deduping against the dead blocked row and could never deliver. The button now posts `{ force: true }` for a `finalized` statement (safe — `forcedRetryKey` resolves to the stable base key when nothing's blocked, escaping only a real block) while a `sent`/`viewed` resend stays keyless (always re-sends).

**GitHub bot round 2 (1 P2).** "Close & send" finalizes the statement and then 422s when the AP delivery is blocked (response carries the frozen statement). The UI only showed the error and never refreshed, so the row stayed rendered `open` — leaving the operator with only "Close & send" (which reuses the first-delivery key and can't escape the block). `act` now refreshes on a partial-success error (a non-OK response that still carries `statement`), so the row flips to `finalized` and exposes the forced "Send to AP" retry.

## 2026-06-22 — Third-party payer Phase 2 — P5b (public statement-pay page + dunning CTA) (feat/payer-statement-pay-page)

**Decision:** ship the public `/pay/statement/:token` page (the money surface split out of P5a) so an AP contact can pay a consolidated NET statement online, and restore the dunning email's online-pay CTA that P4 round 4 dropped (there was no client page to point it at). The charge lands on the PAYER's Stripe customer; the statement settles to `paid` (cascading to its accrued invoices) via the Stripe webhook — the page only confirms the PaymentIntent.

**`StatementPayPage.jsx`** (new) + the `/pay/statement/:token` route in `App.jsx` (registered BEFORE `/pay/:token` so the two-segment path isn't shadowed; mirrors the server mount order). It mirrors PayPageV2's proven two-step surcharge flow — card: `elements.submit()` → `createPaymentMethod` → `/quote` → confirm → `/finalize` → `handleNextAction` on `requires_action`; ACH: `confirmPayment` direct (no surcharge) — trimmed of the invoice-only machinery the statement backend doesn't expose (no live `/update-amount` PI-reminting, no express checkout, no save-card/consent, no `/error` route). Customer-facing warm brand (WavesShell/BrandCard/BrandButton/SerifHeading), reusing the shared `getStripe` loader. On success it shows an inline "Payment received" state (statements have no `/receipt/:token` page). Gate-dark safe: a disabled `GATE_PAYER_STATEMENTS` 404s the backend, which renders the "couldn't find that statement" state.

**Dunning CTA restored.** New migration `20260622000003` re-publishes a new active version of the `payer.statement.followup` template WITH `pay_url` + the "Pay this statement" CTA (P4's `…0002` seeded it CTA-less); the engine (`payer-statement-followups.js`) supplies `pay_url = <portal>/pay/statement/:token` again (import + payload restored; test re-asserts the URL). 84 payer tests green.

**Verification:** no local client build in-repo (client `node_modules` empty; builds on Railway) — verified by esbuild parse + import-graph bundle-resolve (`@capacitor/*`/`@stripe/*` marked external). **The money page must be manually dry-run-tested before `GATE_PAYER_STATEMENTS` is enabled** (a real card + ACH through to a settled statement) — it can't be exercised in prod until the gate flips, so that test is the go-live gate.

**Pre-push codex (1 P0 + 1 P1, same PR).** (P0) **ACH could confirm a stale surcharged PI.** The card `/finalize` updates the shared PaymentIntent to base+surcharge before confirming; on a failed/abandoned card finalize the PI is left at the card-surcharged amount, and the page let the user switch to ACH (no surcharge) and confirm that same PI — charging the wrong amount and failing webhook amount-agreement. Fixed: every `handleFinalize` failure path now calls `onFinalizeFailed`, which resets the statement to a fresh BASE-amount PI by re-running `/setup` (its `requires_payment_method` reuse branch updates `amount` back to base + clears the surcharge metadata) before any further attempt. Quote/ACH failures don't reset (they never mutate the PI). (P1) **ACH billing details.** `us_bank_account` confirmation needs name + email; the statement Payment Element now seeds `defaultValues.billingDetails` from the AP contact (`billTo.company` / `billTo.ap_email`) so a bank-transfer confirm doesn't fail for missing details.

**GitHub bot round 1 (1 P2).** Handle Stripe redirect returns. An ACH bank-redirect (and 3DS) sends the AP contact back to `return_url` (`/pay/statement/:token?redirect_status=…`); the page just re-loaded + re-ran `/setup`, which 409s against the now-processing PI and showed a false "payment already in progress" error after a VALID redirect. Now `redirect_status` is read once on mount: a `succeeded`/`processing` return skips `/setup` and renders the "thank you — payment is in, receipt to follow" terminal state. Consolidated all success paths (inline confirm, redirect return, ACH processing, already-settled return visit) into ONE honest "submitted, receipt by email" state — it never claims "settled" while ACH is still clearing (settlement + receipt are the webhook's job).

**GitHub bot round 2 (2 P2 → one root fix).** The round-1 redirect handling trusted the `redirect_status` URL param unconditionally, so a stale/bookmarked return URL (e.g. reopened after an ACH failure that `revertStatementProcessing` flipped back to payable) would keep the statement pinned on the "submitted" terminal state and suppress the retry form. Replaced param-trust with **PaymentIntent verification**: Stripe appends `payment_intent_client_secret` to the return URL, so the page now `retrievePaymentIntent`s it and trusts the REAL status — `done` only when the PI is actually `succeeded`/`processing`, else `retry` (mint a fresh PI + show the form). A brief "Confirming your payment…" state covers the retrieve; the `/setup` effect waits for the verdict. Falls back to the param only if the retrieve itself fails. This fixes both the lag-window false error and the stale-param-pins-reverted-statement cases.

**GitHub bot round 3 (3 P2 → two refinements of the redirect verdict).** (a) **Verification failure must not trust the param.** If `retrievePaymentIntent` fails (Stripe.js blocked / offline / stale secret), the round-2 fallback still promoted `redirect_status=succeeded` to `done` — a bookmarked/tampered URL could fake success. Now a failed verification yields an explicit `unverified` state: it neither claims success nor re-mints a PI (which would risk a double-pay), and tells the AP "if you just paid you'll get a receipt — don't pay again; otherwise refresh." (b) **Processing terminal gated on the verdict.** `statement.status === 'processing'` was unconditionally terminal, but a failed-ACH return shows a lagging `processing` GET (until the webhook reverts) even though the retrieved PI already produced `retry` — so it falsely read as success. The `processing` terminal now applies only when the redirect verdict isn't `retry` (a genuinely `paid` statement stays terminal regardless). Dropped the now-unused `redirect_status` param read entirely — the retrieved PI is the sole authority.

**GitHub bot round 4 (3 P2 → bind the redirect verdict to this statement's active PI).** (a) **Bind to this statement** — verification trusted ANY retrieved PI that read succeeded/processing, so a stale secret from a since-refunded statement (or a copied secret on another statement's URL) faked success and hid the form for an unpaid statement. The GET now returns `active_payment_intent_id`; the page only honors a redirect as `done` when the return secret's PI is the statement's CURRENT active PI (else `retry` → form). (b) **Retry bypasses the lagging payable flag** — a verified `retry` whose statement GET still shows `processing` (webhook not yet reverted) used to fall into "Nothing to pay"; `/setup` now runs on a verified retry regardless of the stale `payable` flag (the backend is authoritative — it resets/mints a base PI, or 409s while genuinely processing, mapped to a plain "we're updating your statement, refresh shortly" message). The retrieved PI bound to the active PI is the sole authority; the persistent line-135 generic re-flag is resolved by this architecture.

**GitHub bot round 5 (1 real P2 + 2 false repeats).** Real: **strip stale Stripe params from `return_url`.** ACH `confirmPayment` used `window.location.href`, which on a retry from a bookmarked/failed return still carries the prior `payment_intent_client_secret` — so the next redirect comes back with TWO secrets and `URLSearchParams.get` reads the stale first one. The return URL is now `origin + pathname` (no query), so Stripe appends exactly one fresh param set. The other two flags ("handle redirect returns" / "bind to active PI") are re-posts of already-shipped logic — redirect returns are fully handled (verify → checking/done/retry/unverified) and `done` is gated on the return secret's PI being the statement's `active_payment_intent_id` (the binding check runs BEFORE the retrieve). Round-over-round re-flagging of the same shipped lines on this gated-dark page is the heuristic long-tail; the redirect handling is now architecturally complete (server-authoritative active-PI binding + clean return URL).

## 2026-06-24 — Dashboard: Net Momentum section (Core KPIs) (claude/dashboard-metrics-pest-lawn-deueg8)

**Decision:** add a "Momentum" section to the period-aware Core KPIs card on `/admin/dashboard` so the owner can see the period's growth STORY — customers and recurring revenue gained vs. lost — not just standalone levels. This is the "are we winning?" scoreboard number the existing dashboard lacked (it surfaced Active Customers + a Retention % but never net adds or net MRR movement). Content addition, not a visual refresh; strict Tier 1 V2 (components/ui + zinc; `alert-fg` only when a net is negative — a genuinely alert-worthy shrinking book).

**Backend** (`server/routes/admin-dashboard.js`, `/core-kpis`): added a `momentum` block to the response — `customers: {new, lost, net}` and `mrr: {new, churned, net}`. The **lost** side reuses the existing retention cohort (extended to also `SUM(monthly_rate)`, so `lostMRR = cohortMRR − retainedMRR` shares the SAME single "lost" definition as the count and the two can't drift). The **new** side reuses the canonical conversion-date source of truth (`member_since` in window via `CONVERSION_DATE_SQL` + `whereRealCustomer`), matching the headline "new this month" count. Net = new − lost — **no upgrade/expansion movement is inferred** (no clean source for it), so this is an honest new-vs-lost figure. Both queries wrapped in try/catch; a failure yields `momentum: null` and the section just hides.

**Frontend** (`DashboardPageV2.jsx`): a `Momentum` SectionLabel + two KpiTiles ("Net Customers", "Net MRR") at the top of the Core KPIs body, period-aware for free off the existing Today/WTD/MTD/YTD switcher. New `signed()` helper renders +gain / −loss / bare-0; each tile's sub shows the `+N new · M lost` breakdown; `alert` (red) only when net < 0. Renders only when `kpis.momentum` is present.

**Verification:** no runnable local stack in the fresh web container (DB migrations gated `predev`; client `node_modules` empty and a monorepo workspace pkg `@waves/lawn-cost-floor` isn't linked) — verified by `node -c` on the route, Prettier on the page, and a vite build that transformed all 2386 modules (incl. this page) before failing only on the unrelated unlinked workspace import. Live render not exercised.

## 2026-06-25 — Estimates: React redesign (PR B.2) becomes the default for new estimates — rollout Stage 2 (claude/quote-estimate-reversion-bltk3g)

**Context / why this came up.** The customer-facing one-time quote was reported as "looking like an old version." Root cause is the rollout state, not a regression: `/estimate/{token}` has two renderers — the legacy server-rendered HTML pipeline in `routes/estimate-public.js` and the React redesign (`pages/EstimateViewPage.jsx`, internally PR B.2). The gate (`estimate-public.js` `shouldUseReactEstimateView`) routes to React only when `use_v2_view===true || bill_by_invoice===true || cardHoldForcesReactView`. The `use_v2_view` column shipped **default false** (migration `20260423000002`, Stage 1: flip per-estimate via the `toggle_estimate_v2_view` IB tool). Separately, one-time-choice estimates (`show_one_time_option`) used to be force-routed to React because the mode toggle was "only implemented in the React view"; once the recurring/one-time toggle was rebuilt into the server-HTML `renderPage` (the `canChooseOneTime` branch, #2039) that forced route was removed so the rollout is governed solely by `use_v2_view`. Net effect: with the flag default-off, every quote (incl. one-time) served the legacy HTML — the "old version" the owner saw.

**Decision (Stage 2):** make the React redesign the default for **new** estimates. New migration `20260626000001_estimates_v2_default_on.js` flips the column DEFAULT to `true` via raw `ALTER TABLE estimates ALTER COLUMN use_v2_view SET DEFAULT true` (not knex `.alter()`, so it only changes the default for future inserts — no column rewrite, no nullability change, existing row values untouched). All six estimate insert paths rely on the column default (none set `use_v2_view`), so every newly-created estimate is now born `true` and renders in React; existing rows keep their stored value, so anything still at `false` stays on HTML (as the Stage-1 docstring promised). The per-estimate IB override still wins both ways. The gate is unchanged.

**Safe for one-time quotes:** `EstimateViewPage.jsx` has full one-time parity — `OneTimeModeToggle`, `OneTimePriceCard`, `serviceMode` recurring/one_time handling, structural-one-time forcing, and one-time card-hold/invoice — so defaulting one-time estimates to React is the richer experience, not a degradation. Stage 3 (backfill existing rows + remove the HTML path) remains a separate future PR.

**Codex review — React-default parity/exposure gaps.** Defaulting to React routes ordinary customer estimates through the SPA + its `/:token/data` and `/:token/accept` endpoints, surfacing pre-existing gaps vs. the legacy server-HTML page. Two small, verified ones are fixed **in this PR**; the rest are **split into a separate hardening PR** because they're security-sensitive (P1) and/or need a real-browser preview this container can't run.

Fixed here:
1. **noindex** — the legacy HTML set `<meta robots noindex>`, but the SPA fallback's `applySensitiveSpaHeaders` did not cover `/estimate/:token`, so React-served quotes (address + pricing behind a bearer token) became indexable. Added `isEstimatePath` to `server/utils/sensitive-spa-headers.js`. Estimate tokens come in two shapes — 32-hex (`randomBytes(16)`, admin) AND slug-style `${nameSlug}-${shortId}` (SMS/lead intake) — so rather than match a token shape (which would miss formats and leak, per a Codex P1), it noindexes ANY single-segment `/estimate/<x>` EXCEPT the known public marketing service slugs. Those slugs were extracted from `server/index.js` into a shared `server/config/service-estimate-slugs.js` (now `require`d by the index gateway too) so the privacy-header util and the `/estimate/:token` router agree on which URLs are public marketing vs. customer quotes. Over-noindexing a marketing page is a minor SEO cost; under-noindexing a real estimate leaks PII — so default to noindex. Test coverage added/updated.
2. **Expired-link view signals** — the legacy path short-circuits an expired link before any view tracking; the React `/:token/data` path reaches `shouldCountView`, which had no expiration guard, so opening an expired link fired a false view-count + status flip + admin "Estimate viewed" notify. Added the expiration guard centrally in `shouldCountView` (`expires_at < now && status !== 'accepted'`, mirroring `handleEstimateView`) so both the count and the first-view side effects skip expired links in both renderers.
3. **Uncacheable data payload** (Codex P2, post-merge sync) — the legacy `sendEstimatePage` HTML set `Cache-Control: no-cache, no-store, must-revalidate`, but the React `GET /:token/data` JSON (address, phone/email, notes, pricing, bearer `askToken`) sent no cache/privacy headers. With React the default, that tokenized PII payload could be retained by shared browsers/intermediaries. Stamped `Cache-Control: no-cache, no-store, must-revalidate` + `Pragma: no-cache` + `Referrer-Policy: no-referrer` at the top of the handler (every response path, incl. 404s) to match the legacy page.

Split out to the hardening PR (this default-flip PR must NOT merge ahead of it):
- **P1 — expired/draft data exposure.** `/:token/data` returns the full quote + customer phone/email/address/notes/pricing for **expired** estimates (only labels them `expired`), and both `/:token/data` and `/:token/accept` (`isEstimateAcceptActive`) treat a **null `expires_at`** as active and don't require *sent* status — so a leaked bearer URL for an expired estimate or a no-expiry draft (lead-webhook/agent inserts) can fetch the full payload or accept an unreviewed draft. Largely pre-existing on those endpoints; the default-flip widens it to every new estimate. Fix = enforce viewable preconditions (sent + non-expired, null-expiry = inactive) and redact non-viewable responses to a name+address expired shell.
- **P2 — accepted-estimate recap.** Showing the agreed services/pricing on a reopened accepted estimate (legacy parity) needs (a) the accepted *service mode* persisted/read so a mixed estimate's recap doesn't misrepresent recurring vs. one-time, and (b) the read-only recap to actually disable the add-on toggles (not just the frequency slider). Both are customer-surface changes that need a preview to verify, so they ride with the hardening PR rather than shipping blind here.

**Verification:** migration is `require`-clean and exports `up`/`down`; `sensitive-spa-headers` + `shouldCountView` guards unit-checked (9/9 spa-header tests green, expiration predicate verified). Scope of THIS PR: migration + the two server/header fixes above (no client changes). No `DATABASE_URL` in the container so the migration runs on Railway deploy (verify it ran post-merge); merge the hardening PR first so React-by-default never exposes the expired/draft payload. **(Update: the hardening PR #2089 merged first, as planned.)**

**Parity pass (post-#2089 merge) — remaining React-vs-legacy gaps Codex surfaced on the flip.** After #2089 landed and this branch was synced onto it, Codex flagged a cluster of behaviors where the React default diverged from the legacy HTML page; all closed here so the flip is clean:
- **Data payload now `no-store`** — the legacy `sendEstimatePage` set `Cache-Control: no-cache, no-store, must-revalidate`, but `GET /:token/data` (address, phone/email, notes, pricing, bearer `askToken`) sent no cache headers. Stamped `no-store` + `Pragma` + `Referrer-Policy: no-referrer` at the top of the handler (all response paths).
- **Drafts stay on the legacy renderer** — with the flip, new drafts are born `use_v2_view=true`, but admin "Preview" opens the public `/estimate/<token>`, and the React `/:token/data` gate 404s drafts — breaking draft preview. The gate (`shouldUseReactEstimateView`) now `&& !UNPUBLISHED_ESTIMATE_STATUSES.includes(status)`, so `draft`/`scheduled` render server-HTML (preview works); the flip only takes effect once published. Keeps the #2089 security gate intact (a draft that reaches `/:token/data` still 404s).
- **No view-count inflation on refreshes** — `EstimateViewPage` re-fetches `/data` after preference/slot/accept actions; that endpoint counts a view on every 200, so toggling add-ons inflated `view_count` vs. the single legacy page load. The page now sends `?refresh=1` on every fetch after the first (tracked by a per-token ref), and the endpoint skips view-count + first-view side effects when present.
- **Accepted recap (the deferred P2, now built)** — `EstimateViewPage`'s `!canAccept` branch renders a read-only recap of the booked services + pricing below the accepted banner (legacy parity), via a shared `renderQuoteDetailCards(readOnly, modeOverride)` helper (single source of truth with the live configurator). Codex's two sub-issues are fixed: (a) **mode fidelity** — new migration `20260626000002_estimates_accepted_service_mode.js` persists the accepted mode at accept time (`accepted_service_mode`), surfaced as `acceptedServiceMode` and used to pin the recap, so a one-time acceptance on a mixed estimate isn't misrepresented as recurring (null for legacy rows → falls back to derived mode); (b) **read-only toggles** — `AddOnsBlock` gained a `disabled` prop (disables the checkbox + swaps the instructional copy) threaded through `ServiceSection`, so the recap's add-ons can't be clicked (which previously errored on `/preferences`).

**Parity-pass round 2 (Codex follow-ups on the parity pass — 3 P2).** (a) **Accepted frequency fidelity** — the recap seeded the recurring frequency from `section.defaultFrequencyKey`, so a customer who booked bi-monthly/monthly saw the default quarterly card/price. Added `accepted_frequency_key` to migration `…002`, persisted at accept (`acceptedFrequencyKey`), and `loadEstimate` now seeds `selected` from it for accepted rows — `selectedFrequency` + the combined price both derive from `selected`, so the whole recap follows. (b) **Resolved accepted mode** — `accepted_service_mode` wrote the raw request `serviceMode`, but a structurally one-time estimate commits as one-time even when the body omits it (`treatAsOneTime`); now persists `treatAsOneTime ? 'one_time' : serviceMode` so the stored mode matches the transaction. (c) **Refresh can't suppress the first view** — `?refresh=1` is a public param, so it's now honored only once `viewed_at` is already set; a caller hitting `?refresh=1` first can no longer suppress the initial view-count + admin "viewed" notification.

**Parity-pass verification:** server `node --check` clean; new migration (`accepted_service_mode` + `accepted_frequency_key`) + accept writes + payload fields present; `EstimateViewPage.jsx` + `AddOnsBlock.jsx` esbuild-parse clean; 9/9 header tests still green. **Still needs a Railway preview** (client doesn't build in-container): reopen an accepted one-time estimate → recap shows the one-time plan read-only (toggles inert); reopen an accepted **non-default-frequency** recurring estimate → recap shows the booked frequency/price; a draft `/estimate/<token>` still previews via legacy; toggling add-ons before accept registers one view, not several.

## 2026-06-25 — Estimates: harden the React data + accept endpoints for the React-by-default rollout (claude/estimate-react-endpoint-hardening)

**Context.** Prereq for making the React estimate view the default (companion to `claude/quote-estimate-reversion-bltk3g`). Codex flagged two P1 data-exposure gaps, largely pre-existing on these endpoints and widened by the default-flip: (1) `GET /:token/data` returns the full quote + customer phone/email/address/notes/pricing for **expired** estimates (only labels them `expired`); (2) both `/:token/data` and the accept/select-tier/preferences mutations treat a **null `expires_at`** as active and don't exclude unreviewed **drafts**, so a leaked bearer URL for an expired estimate or a no-expiry/lead draft could fetch the full payload or accept an unreviewed draft. The legacy server-HTML page short-circuited all of these (`new Date(null) < now` is in the past) to the expired/not-found shell before building any payload.

**Decision.** Add `isEstimateCustomerViewable(estimate)` and gate `GET /:token/data` on it: non-viewable → **404** (the SPA already renders its "this link may have expired or isn't valid" screen; no payload built). The signal is **status**, not a null expiry: unpublished states (`draft` — all six insert paths create rows as `draft` — and `scheduled`, which carries a *future* expiry until the send claim) are withheld, `expired`/`send_failed`/archived are withheld, `accepted`/`declined` are legitimate terminal views served in full (legacy parity), and `sending`/`sent`/`viewed` are served subject only to a real *past* expiry. **Admin previews bypass** (`hasAdminMarker` signed cookie / `isAdminIp`) so staff can still review drafts. Tightened `isEstimateAcceptActive` (shared by accept / select-tier / preferences) the same way: reject `draft`/`scheduled`. Status-based (not null-expiry) deliberately: a Codex P2 pass showed `sendEstimateNow` texts the link *before* writing `expires_at`, so a freshly-claimed `sending` row momentarily has a null expiry — a null-expiry-rejects rule would 404 the customer mid-send and broke an existing `isEstimateAcceptActive` test; gating on the unpublished `draft`/`scheduled` statuses closes the leaked-draft class without that fallout. Chose a 404 over a redacted 200 shell deliberately — it's leak-proof with zero render risk, vs. a shell whose render through the SPA's data contract I can't verify in this container; the slightly less branded "expired" screen is an acceptable trade for an edge/leaked-link case.

**Codex rounds 2–3 — admin bypass removed + TOCTOU (P1 + P2).** The first cut bypassed the viewability gate for `hasAdminMarker(req) || isAdminIp(ip)` so staff could preview drafts. Codex flagged this as too weak for releasing withheld PII: `isAdminIp` trusts a **spoofable `X-Forwarded-For`**, and the `waves_admin` cookie is a **2-year, logout-persistent** view-count marker — not authorization. An intermediate fix (`markerBelongsToActiveAdmin`: resolve the subject + require a currently *active* technician) revoked offboarded staff but **still** trusted a marker that survives logout on a shared device, and the public `/:token/data` fetch carries **no current-session credential** to check instead (the customer page sends no admin Bearer token). So the bypass was **removed entirely** — non-viewable estimates 404 for everyone; admin/staff preview of a draft or expired estimate must go through an authenticated admin surface, never this public endpoint. **Tradeoff flagged to the owner:** previewing a draft via the raw customer `/estimate/<token>` URL now 404s; a proper authenticated preview path is a follow-up if needed. Separately (P2 TOCTOU): the new draft/scheduled rejection only ran on the pre-read, so a customer with the page open could still win a race if an operator pulled the estimate back to `scheduled`; mirrored the unpublished guard into the four final-write `whereNotIn('status', …)` clauses (accept / select-tier / preferences / decline) so they now also exclude `send_failed`/`draft`/`scheduled`.

**Out of scope (deferred):** the accepted-estimate detail recap (Codex P2 — show agreed services/pricing on a reopened accepted estimate) needs the accepted **service mode** persisted (no column today; the accept path only echoes `serviceMode` in the immediate success payload) plus a read-only add-on-toggle fix — a separate cosmetic PR, since it needs a migration + a real-browser preview.

**Verification:** `isEstimateCustomerViewable` + the tightened `isEstimateAcceptActive` are unit-tested (`server/tests/estimate-public-viewability.test.js`, 11-row truth table verified inline; jest runs in CI where deps exist). `estimate-public.js` is `node --check`-clean. **Needs a Railway preview before merge** to confirm the integration edges this container can't exercise: a draft/expired token now 404s on the public link **for everyone (including staff)** — confirm office staff don't depend on the customer-URL draft preview (vs. the admin estimate page); a real expired customer link lands on the not-found/expired screen (not a blank); and a normal sent estimate still views + accepts. **Merge this BEFORE the default-flip PR** so React-by-default never serves the expired/draft payload.

## 2026-06-25 — Estimator: recurring spot-foam termite program + foam $250 floor removed (claude/recurring-foam-treatment-za7t6n)

**Context / why.** Two commercial clients independently asked for a recurring (~quarterly) spot foam treatment instead of repeated one-time calls. Owner directive: add a recurring foam option to the estimator at a per-visit rate discounted off the one-time price by cadence, AND remove the $250 foam minimum entirely (both one-time and recurring).

**Owner decisions (locked before build):** cadence ladder anchored on the one-time price — quarterly ×0.90 (10% off/visit), bimonthly ×0.85 (15%), monthly ×0.80 (20%); all three cadences offered; **standalone** — recurring foam does NOT count toward the WaveGuard tier and is not bundle-discountable (the cadence multiplier is its only discount); available to all customers; **$250 floor removed for all foam**. Note this differs from the pest-control frequency ladder (where quarterly is the 1.00 baseline and frequency buys the discount); for foam the one-time price is the anchor, so even quarterly is discounted.

**Foam is code-authoritative, NOT DB-bridged.** Unlike pest/lawn/mosquito/pre-slab, `db-bridge.js` (`syncConstantsFromDB`) never reads any foam key — there is no `config.foam*` handler and `validatePricingConstants` doesn't cover `foamDrill`. So foam's floor, margin, tiers, and the new recurring ladder live solely in `constants.js` + engine code and ship directly (the usual "editing constants.js is inert" warning does not apply here). No `pricing_config` migration was added — adding bridge machinery for foam would be a larger architectural change than requested, and the FM_CAN/FM_BITS rows in the legacy display table are unaffected.

**Pricing engine (server).** `constants.js`: `foamDrill.floor` `r(250)` → `0`; new `foamDrillRecurring { cadenceMultipliers, frequencies {quarterly:4,bimonthly:6,monthly:12}, defaultCadence:'quarterly' }`; added `foam_recurring` to `WAVEGUARD.excludedFromPercentDiscount`. `service-pricing.js`: dropped the `Math.max(floor,…)` in `priceFoamDrill`; new `priceRecurringFoam(points,{cadence})` returning a recurring line item (`service:'foam_recurring'`, `.annual`/`.monthly`/`.perVisit`, cost÷0.45 ×cadence, no floor). `estimate-engine.js`: new `services.foamRecurring` branch that pushes the line item but does NOT add to `activeServiceKeys` (so it can't create/raise a tier). Because it's a `.annual` line excluded from the % discount and not in `MANUAL_RECURRING_DISCOUNT_ELIGIBLE`, it sums into recurring totals at full cadence price with 0% discount automatically.

**Route + legacy mapper.** `property-lookup-v2.js` `translateV2CallToV1Input`: `sel.has('FOAM_RECURRING')` → `services.foamRecurring = {cadence, points}`. `v1-legacy-mapper.js`: added `foam_recurring` to `RECURRING_SERVICES` (so it isn't misclassified into one-time/specialty), and a `svcAdd('Recurring Foam …', foamRecLI, {discountable:false, countsTowardWaveGuardTier:false})` so it surfaces in `recurring.services`. Unlike palm/rodent it is NOT subtracted from `summary.recurringAnnual*`, so it correctly flows into `monthlyTotal`/year1/year2.

**Client.** `lib/estimateEngine.js`: removed `Math.max(250,…)` from one-time foam; new `svcFoamRecurring`/`foamRecurringFreq`/`foamRecurringPoints` fields; recurring foam compute block pushes to `wgServices` for display and into `ra`/`lineItems{discountable:false}` (NOT `ac`) so it joins the recurring monthly total without tier/discount; exposes `recurring.foamRecurringMo/Ann`. `EstimateToolViewV2.jsx` (live admin tool) + `EstimatePage.jsx` (retained V1 module; `EstimateToolView` is exported but currently has no importer — updated for parity): form defaults, the rough-preview memo (`separateRecurringKeys` + non-discountable approx), `selectedServices.push('FOAM_RECURRING')`, the `options` payload, and a "Recurring Foam Treatment" checkbox with Cadence + Drill Points selectors.

**Price effects.** One-time foam Spot $250 → ~$182 (floor gone); Moderate ~$308, Extensive ~$434, Full Perimeter ~$598 (now pure cost÷0.45). Recurring Moderate (10 pts): quarterly $277/visit ($1,108/yr), bimonthly $262/visit, monthly $246/visit.

**Verification:** server pricing checked via node (one-time floor removal + recurring ladder + standalone: pest+foam stays Bronze/serviceCount 1, foam line 0% discount); full server pipeline (`generateEstimate` → `mapV1ToLegacyShape`) confirms foam in `recurring.services`, `monthlyTotal`, year1, and never in one-time. 5-case client parity test (temp, since removed) matched server exactly. Jest: 694 pre-existing pricing/estimate tests green across 28 suites (incl. the client↔server drift parity suite); the relative-comparison foam tests still pass post-floor-removal. `vite build` compiles (EstimatesPageV2 incl. the new UI). Live render not exercised in-container (admin auth) — recommend a quick visual pass on `/admin/estimates` before merge.

### Round 2 (Codex review on #2092) — recurring foam lifecycle integration

Codex's review surfaced that the feature's footprint extends past the pricing engine into the estimate *lifecycle*. Owner approved going production-complete + auto-pricing commercial. Five downstream fixes:

- **Commercial auto-pricing** (`estimate-engine.js`, client `estimateEngine.js`): recurring foam now bypasses the `useCommercialManualQuote` safety gate and is priced at the cadence rate for commercial profiles too (the requesters are commercial). Deliberate exception — it's the first auto-priced commercial service; all others still route to manual quote. Verified: a commercial profile returns a `foam_recurring` line ($1,108/yr) with no manual-quote line.
- **Acceptance recurrence** (`estimate-converter.js` `supportsConverterFollowUpSeeding`, `recurring-appointment-seeder.js` `serviceKeyFor`): added a `foam_recurring` service-key rule (`/foam[_\s]*recurring|recurring[_\s]*foam/` — matches the recurring line + parent row but NOT the one-time "Drill-and-Foam Termite"), and allowed converter follow-up seeding for `foam_recurring` on all three cadences (quarterly/bimonthly/monthly). Without this, an accepted foam plan booked the first visit but `seedFollowUpsForParent`/`markParentRecurring` never ran, so it didn't recur. Pattern inference already worked off the "(Quarterly/Bimonthly/Monthly)" name + `visitsPerYear`.
- **Annual-prepay floor** (`estimate-converter.js` `nonDiscountableRecurringAnnualFloor`): the earlier `discountable:false` flag was insufficient — saved estimates persist the mapped `recurring.services` shape (no `lineItems`), and the floor only scanned `lineItems`. Now scans both sources (deduped by service key) and derives annual from `mo`/`monthly` when no annual field is present (new `recurringLineAnnualAmount` helper, also used by `isNonDiscountableRecurringLine`). Verified: foam-only floor = $1,108, so annual prepay can't apply the generic 5% on top of the cadence discount; a discountable pest line still yields $0 floor.
- **service_interest classification** (`estimate-service-lines.js`): added `foam` to the `termite` `SERVICE_LINE_PATTERNS` regex (+ `svcFoamRecurring` to the input-based path) so a foam-only recurring estimate classifies as `Termite` instead of `unknown` for follow-ups and admin lists. Verified.
- **Public customer estimate render** (`estimate-public.js`): added a `foam_recurring` category to `categoryForRecurringServiceKey`, label "Recurring Foam Treatment" to `serviceLabelForCategory` + `recurringServiceDisplayName`, and a warm-tone `SERVICE_COPY.foam_recurring` entry, so a foam-only customer quote renders as a foam section (was falling through to the Pest Control category/copy). `excludeFromPctDiscount` already resolves true via the constants exclusion.

**Verification:** 754 pricing/estimate/converter tests green across 31 suites (incl. commercial-safety-gate + prepay + parity drift); targeted node checks for all five fixes; client `vite build` clean. Customer-facing public render still needs a Railway preview (admin auth / no live stack in-container) before merge — confirm a foam-only quote renders under a "Recurring Foam Treatment" header with foam copy, and an accepted foam plan seeds future visits.

### Round 3 (Codex re-review on 7f1fce8) — public self-serve flow + booking

Codex confirmed the round-2 fixes resolved all prior threads and surfaced four more, all in the public customer estimate / self-booking flow. Three fixed; one deliberately skipped.

- **Engine-backed recurring allowlists** (`estimate-public.js`): added `foam_recurring` to both `RECURRING_LINE_SERVICES` sets (`recurringServicesWithSupplements` ~2168, `shapeFrequencyEntry` ~7885) and `RECURRING_SERVICE_KEYS` (~9585). Estimates saved via the engine-result path (not the v1 mapper) now surface foam as a recurring service for prepay eligibility, first-visit rows, and converter acceptance.
- **Cadence preserved through the public bundle** (`estimate-public.js`): the v1 bundle path fell back to `frequencies.slice(0,1)` (generic quarterly) for non-pest single services, so a bimonthly/monthly foam quote was presented and billed as quarterly. Added `foamFrequenciesFromV1Services()` — builds ONE frequency entry from the foam row's saved `cadence`/`visitsPerYear`/`monthly` (foam is an operator-chosen single cadence, not a customer-selectable ladder), billed monthly (annual/12), serviced at the cadence's visits/yr — and wired it into `buildPricingBundle`'s `finalFreqs` chain. The accept path reads this frequency unchanged, so `customerSelection.frequency`/billing now reflect the real cadence.
- **Booking slot sized from the foam tier** (`service-pricing.js` + `v1-legacy-mapper.js` + `estimate-slot-availability.js`): `priceRecurringFoam` now returns `estimatedDurationMinutes` (tier labor hours × 60 → 60/90/120/180), the mapper forwards it onto the recurring.services row, and `durationForService` has a `foam_recurring` case that uses it (clamped ≥60, ≤180) instead of the generic 45-min fallback — so a 10–20 point foam visit doesn't reserve a too-short slot.
- **SKIPPED — second foam pricer's $125 floor** (`calculateFoamPrice`/`services.termiteFoam`): verified `services.termiteFoam` is **not populated by any route or client**, so that spec-version pricer is dead in the live flow. Left untouched to avoid changing unrelated dead code; the owner's floor-removal directive targets the live drill-and-foam path, which is already floored at 0. Flagged for the owner — can zero it for consistency if desired.

**Verification:** 755 pricing/estimate/converter/slot tests green across 30 suites; full pipeline confirms the foam recurring row carries `cadence`/`visitsPerYear`/`mo`/`estimatedDurationMinutes` for all three cadences (quarterly 90min, bimonthly 120min, monthly 180min). The public self-serve render/accept/booking paths can't be exercised in-container (no DB/auth) — **needs a Railway preview**: send a bimonthly or monthly foam quote, confirm the customer page shows that cadence/price (not quarterly), accept it, and confirm the booking slot length + billing cadence are correct.

### Round 4 (Codex re-review on 7d6555d) — prepay mixed-plan split, engine-path cadence, client copy, sqft guard

Four more edge cases, all fixed:

- **Mixed-plan annual prepay split** (`estimate-converter.js` `resolveAnnualPrepayInvoiceTotal`): the round-2/3 `max(discounted, floor)` clamp only protected foam-heavy mixes. For foam + a discountable service (e.g. $1,108 foam + $600 lawn) the 5% was still computed on the whole base then clamped, bleeding part of the discount onto foam. Now the non-discountable floor is split out FIRST and added back at full price: `amount = floor + (base − floor) × (1 − rate)`. Verified: foam-only $1,108 (0% off), foam+lawn $1,678 (5% off the $600 lawn only), lawn-only $570 (unchanged). Updated `estimate-converter-prepay.test.js` (the prior assertion encoded the old clamp semantics).
- **Engine-invocation path cadence** (`estimate-public.js`): the round-3 foam cadence fix only ran in the v1 saved-estimate branch. Added `foamFrequenciesFromEngineResult()` (reuses `foamFrequenciesFromV1Services` against the engine result's `foam_recurring` line item) and wired it into the no-pest engine-invocation branch, so an `engineInputs`/`engineResult` foam quote at bimonthly/monthly isn't exposed as the default quarterly `FREQUENCY_LADDER[0]` (which also lacked `billingFrequencyKey: 'monthly'`).
- **Client React copy** (`client/src/lib/estimate-copy.js`): added a `foam_recurring` entry mirroring the server `SERVICE_COPY`, so the React estimate page header/AI panel (`estimateCopyFor(serviceCategory)`) no longer falls back to pest-control copy for foam-only quotes (the server already sends `serviceCategory: 'foam_recurring'`).
- **V2 estimator sqft guard** (`EstimateToolViewV2.jsx`): a foam-only quote was blocked by the "Enter home sq ft or lot size" dimension guard (only bed bug / pre-slab / Bora-Care were exempt). Recurring foam is priced from drill points + cadence, not footprint, so added a `foamRecurringOnly` exemption — operators can quote it before a property lookup.

**Verification:** 755 tests green across 30 suites (prepay split unit-verified for foam-only / foam+lawn / lawn-only); client `vite build` clean. Public self-serve render/accept still wants a Railway preview (unchanged from round 3).

### Round 5 (Codex re-review on 833eaa2) — engine-result cadence/duration/label polish

Three more, all in `estimate-public.js`, all on the engine-result ingestion + per-treatment rendering paths:

- **Cadence on engine-result supplements (P2)** (`recurringServicesWithSupplements`): a raw `foam_recurring` line item ingested here built a supplement row that dropped `item.cadence` and used the generic display name (no cadence), so on accept the seeder could fall back to the billing key `monthly` and seed monthly follow-ups for a quarterly/bimonthly plan. Now carries `item.name` (which holds the cadence) ahead of the generic name, plus `cadence`/`frequencyKey`/`estimatedDurationMinutes` on the row.
- **Tier duration in the foam frequency (P2)** (`foamFrequenciesFromV1Services`): the foam frequency object dropped `estimatedDurationMinutes` and emitted no `perServiceTreatments` row, but the slot profile reads `frequencies[].perServiceTreatments` first — so 15–20pt foam fell back to the generic 90-min window. Now carries `estimatedDurationMinutes` on the frequency and a `perServiceTreatments` row with the tier duration; `shapeFrequencyEntry`'s per-treatment rows carry it too.
- **Foam label on per-treatment rows (P3)** (`shapeFrequencyEntry.labelForRecurring`): added a `foam_recurring → 'Recurring Foam Treatment'` case (+ `li.name` fallback) so an engine-backed mixed estimate doesn't render a raw `foam_recurring` row. The client `PriceCard` renders `perServiceTreatments[].label` directly (no category-keyed benefit map needed).

**Verification:** 735 tests green across 29 suites; pipeline confirms the foam recurring row survives the supplement merge with cadence/visits/duration/name intact (bimonthly 20pt → 180min). Only `estimate-public.js` changed; no client rebuild. Public self-serve flow still wants a Railway preview.

### Round 5b (Codex re-review on 7b5f10d) — booking label + legacy V1 engine parity

(7b5f10d added the client foam benefit bullets in PriceCard via a parallel session; this branch was fast-forwarded onto it.) Three more:

- **Foam booking label (P2)** (`slot-reservation.js` `canonicalServiceTypeForProfile`): a foam_recurring profile had no case, so a booked recurring-foam accept took the coarse `service_interest` ("Termite") as `scheduled_services.service_type`, and the seeder copied that to follow-ups → Termite-labeled foam appointments. Added `foam_recurring → 'Recurring Foam Treatment'`. (Kept `service_interest='Termite'` as the high-level family — foam IS termite work — now that the booking label no longer derives from it.)
- **Foam-only legacy quote without sqft (P3)** (`estimateEngine.js`): the retained V1 client engine's size guard only bypassed `preSlabOnly`, so a foam-only quote returned "Enter home sq ft or lot size" before pricing. Added a `foamRecurringOnly` bypass mirroring the V2 estimator.
- **Foam row metadata in legacy engine (P2)** (`estimateEngine.js`): the client-engine `recurring.services` foam row omitted `cadence`/`estimatedDurationMinutes`/`discountable:false`, so a legacy-saved foam quote would lose cadence/duration and could stack the prepay discount. Now mirrors the server mapper's row (cadence, frequencyKey, estimatedDurationMinutes, discountable:false, countsTowardWaveGuardTier:false).

**Verification:** 325 slot/service-line/converter/public tests green across 12 suites; client `vite build` clean; a temp vitest confirmed the V1 engine prices a foam-only no-sqft quote and the row carries cadence/duration/discountable (bimonthly 20pt → 180min, discountable:false).

### Round 6 (06-26) — merge main + close the deferred foam floor

Merged `origin/main` (sole conflict: this append-only log — kept both the foam entry and main's invoice-AI entries; no code overlap, main touched none of the 17 pricing/estimate files this branch changed). Then **zeroed the residual `$125` floor in `calculateFoamPrice()`** (`service-pricing.js`), the spec-version `services.termiteFoam` pricer that round 3 deliberately SKIPPED. Re-confirmed it's dead in the live flow (nothing assigns `services.termiteFoam`; its only consumer `estimate-engine.js:1213` never fires; `PricingLogicPage` SpecServicesPanel only documents it), but zeroing its floor makes EVERY foam path consistent with the owner's "remove the minimum entirely" directive and closes the last open Codex thread. Kept `Math.max(0, …)` as a non-negative guard. 86 foam pricing tests green; no test asserted the old floor. **Still wants the Railway customer-render preview before merge** (unchanged from earlier rounds).

### Round 7 (Codex re-review on 268379bc0) — engine-backed foam accept (P1)

**Persist foam service rows for engine-backed accepts.** Codex found a real P1: quote-wizard (`public-quote.js` ~745) and IB agent-draft (`estimate-tools.js` ~481) estimates persist their priced lines ONLY under `estData.engineResult.lineItems` — there is no v1-mapped `result.recurring.services` row. The display path regenerates the foam frequency live (`foamFrequenciesFromEngineResult`) and exposes a sellable `monthly_total`, but the accept path builds `recurringSvcList` via `acceptanceServiceLists()` / `withSupplementedRecurringServices()`, and **those two functions read `estData.result || estData` — they omitted the `estData.engineResult` fallback that 6 other call sites in the same file already use** (e.g. `oneTimePestChoiceAmountForEstimate`). So a foam-only engine-backed accept locked `monthly_total` while `recurringSvcList` came back empty → `EstimateConverter` got nothing to schedule, seed, or invoice. Fix: both functions now source recurring rows from `estData?.result || estData?.engineResult || estData` (`withSupplementedRecurringServices` adds `recurringServicesWithSupplements(engineResult)` while preserving the `engineInputs`/`engineResult` wrapper so downstream pricing can still replay the engine). `recurringServiceKey('foam_recurring')` already returns `foam_recurring` and it's in `RECURRING_LINE_SERVICES`, so the stored line surfaces with service/name(cadence)/annual/monthly/visitsPerYear — enough to schedule + infer cadence + invoice. General fix (also covers any other engine-backed recurring line) but additive — the `result`-bearing v1-mapped path is unchanged. New `estimate-public-engine-foam-accept.test.js` (3) proves it: the 2 engine-backed assertions fail without the fix, the v1-mapped regression passes either way; +286 adjacent foam/pricing tests green.

**Follow-on (same round, found while verifying the P1 fix) — protect engine-backed foam from the annual-prepay 5%.** Surfacing the foam row as recurring makes a foam-only engine-backed estimate *prepay-eligible* for the first time — which exposed a latent gap: `isNonDiscountableRecurringLine` (the prepay-floor gate in `estimate-converter.js`) keyed only off the row's `discountable:false` flag, but `public-quote.js`/`lead-estimate-automation.js` persist `engineResult.lineItems` as a subset that DROPS that flag (and `estimateLineItemsFromData` reads `engineResult.lineItems`). So a foam line from those paths read as discountable → annual prepay would stack the generic 5% on foam, violating "the cadence multiplier is foam's only discount." Fix: `isNonDiscountableRecurringLine` now returns true for `recurringServiceKey === 'foam_recurring'` by service key (scoped to foam only — deliberately did NOT broaden to the whole `excludedFromPercentDiscount` map to avoid touching palm/rodent prepay treatment in this PR). New `estimate-converter-prepay.test.js` case (flag-less foam under `engineResult.lineItems`): floor = $1,108, foam-only prepay 0% off, foam+lawn discounts only the $600 lawn — fails without the fix, passes with it.

### Round 8 (Codex re-review on 931684a40) — compact-row cadence / annual / duration (3×P2)

All three stem from the same compact line-item shape (`public-quote.js` `engineResult.lineItems` map + `lead-estimate-automation.js` `compactLineItem`): `name`+`frequency` survive but `cadence` and `estimatedDurationMinutes` are dropped, and `annual` is the authoritative sold total alongside a rounded `monthly`. Now that round 7 surfaces these rows on accept, the gaps are live. Fixed at the source AND hardened the central consumer:
- **Cadence** (`foamFrequenciesFromV1Services`, used by the engine-result path too): defaulted any non-quarterly foam to quarterly when `row.cadence` was absent → wrong cadence on `/data` + accept selection. Now derives `row.cadence || row.frequencyKey || {4:q,6:bi,12:mo}[visits] || cadence-in-name` before the quarterly fallback (checks bi-monthly before monthly — "bimonthly" contains "monthly").
- **Annual** (same helper): recomputed annual from `monthly×12`, dropping cents (1108 → 1107.96) even though accept locks `selectedFrequency.annual` onto the estimate → invoiced a few cents under the engine price. Now prefers `row.annual ?? row.ann`, then `monthly×12`, then `perTreatment×visits`.
- **Booking duration** (`estimate-slot-availability.durationForService` foam branch): compact rows carried no `estimatedDurationMinutes`, so 15–20pt foam (120/180 min) fell to the 90-min default and self-booking could reserve a too-short slot. Fixed at the source: both compact mappings now carry `cadence` + `estimatedDurationMinutes` (generic, null for non-foam) — `priceRecurringFoam` already emits both, and `recurringServicesWithSupplements` forwards them onto the row. The 90-min foam default remains only for a genuinely-thin row.

Exported `foamFrequenciesFromEngineResult` for testing. Extended `estimate-public-engine-foam-accept.test.js` (now 6): cadence-from-visits/name (bimonthly 6→`bimonthly`, monthly 12 not misread as quarterly), annual preserved to the cent (1108 not 1107.96; perTreatment 277), slot duration 180/120 from the row + 90 default for a thin row. Cadence/annual assertions fail without the helper fix; +293 foam/pricing/slot tests green.

### Round 9 (Codex re-review on 177a9b7b3) — foam classification / mixed-pricing / annual / cadence-key (2×P1, 3×P2)

- **P1 — classify foam before sizing slots** (`estimate-slot-availability.js` `serviceKeyFor`): the foam duration branch only fired when the key was already `foam_recurring`, but an engine-backed row can reach slot sizing labeled only "Recurring Foam Treatment"/"FoamRecurring" (no mapped service key) → fell to the generic 45/60-min window, under-reserving 15–20pt (120/180-min) jobs. Added `/foam/ → 'foam_recurring'` (after the termite check so the one-time "Drill-and-Foam Termite" still maps to termite).
- **P1 — keep mixed foam estimates on full-summary pricing** (`buildPricingBundle` no-pest branch): `foamFrequenciesFromEngineResult` prices ONLY the foam line, so a foam + lawn/tree/mosquito mix would lock/prepay just the foam total and silently drop the other recurring service from the price (while still scheduling it). Now gated to **foam-only recurring mixes** (`recurringServicesWithSupplements(engineResult)` keys all === `foam_recurring`); any mix falls through to the full-summary `shapeFrequencyEntry`. Verified: foam+tree prices the combined annual ($1,595) with both services in `perServiceTreatments`, not the $1,108 foam-only total.
- **P2 — foam annual on the mapped row** (`v1-legacy-mapper.js` foam `svcAdd`): the admin/V2-mapped row carried `mo`/`perTreatment` but not the engine `annual`, so `foamFrequenciesFromV1Services` fell back to `monthly×12` (1108 → 1107.96 on accept/prepay). Now carries `annual: Number(foamRecLI.annual)`.
- **P2 — normalize the cadence key** (`priceRecurringFoam` + `foamFrequenciesFromV1Services`): both only recognized `bimonthly`, but the billing/estimate stack also uses `bi_monthly`; a `{cadence:'bi_monthly'}` caller/replay silently fell back to quarterly (4 visits at the quarterly multiplier instead of the 6-visit bimonthly). Added a `bi_monthly`/`bi-monthly`/`bimonth → bimonthly` alias normalization before each lookup; in the frequency helper the normalization also stops an alias-form `row.cadence` from short-circuiting the derive chain.

Extended `estimate-public-engine-foam-accept.test.js` (now 12): foam-by-name slot classification + termite-still-termite, `priceRecurringFoam`/`foamFrequenciesFromEngineResult` bi_monthly→6-visit, `buildPricingBundle` foam-only vs foam+tree full-summary, mapped foam row annual=1108. All 5 fail without their fix; +334 foam/pricing/slot/converter tests green.

### Round 10 (Codex re-review on c4170e368) — engineInputs-only accept / mixed cadence / cent-exact annual (1×P1, 3×P2)

- **P1 — persist generated foam rows for engineInputs-only accepts** (`withSupplementedRecurringServices`): round 7 read `result || engineResult`, but an estimate stored with ONLY `engineInputs` (a supported display path — `buildPricingBundle` replays the engine) has neither, so accept got an empty recurring list → locked total, no schedule/seed/invoice. Now when no recurring rows are found and there's no `result`/`engineResult`, it replays `generateEstimate(extractEngineInputs(estData))` and supplements from that (engine wrapper preserved; `generateEstimate` already imported).
- **P2 — keep foam cadence in a mixed plan** (`shapeFrequencyEntry` per-service-treatment rows): the round-9 fix routes a foam + lawn/tree/mosquito mix through the full-summary entry whose top-level key is the generic quarterly ladder row. Added `cadence`/`frequencyKey` to each per-service-treatment row so foam's own cadence (e.g. bimonthly, 6 visits) survives on its row even when the bundle header is nominal — scheduling/seeding read it per-service.
- **P2 (Codex) — foam cent-exact in mixed V1/admin bundles** (`shapeFromV1`): Codex flagged that summing `svc.mo` then `×12` locks a foam-in-mix portion at `92.33×12 = 1107.96` vs the engine's `1108`. **Owner directive 2026-06-26: cent-exactness is NOT required** — so the foam-scoped `shapeFromV1` correction (and its test-only `shapeFromV1` export + test) was **reverted**. A foam-in-mix bundle now totals `mo×12`, consistent with how every other recurring service is bundled; the ~4¢ delta is accepted.
- **P2 — exact annual on the client foam row** (`client/src/lib/estimateEngine.js`): KEPT — the legacy client-saved `recurring.services` foam row now carries the already-computed `ann` (`annual: ann, ann`). This isn't "chasing cents": it just passes through the value the engine computed, so the common **foam-only** path (via `foamFrequenciesFromV1Services`, which prefers `row.annual`) shows the natural round `$1,108` with zero core-function risk. (Mixed bundles still use `mo×12` per the revert above.)

`estimate-public-engine-foam-accept.test.js` now 14: engineInputs-only replay surfaces foam on accept (+ wrapper kept) and foam-in-mix treatment row keeps `cadence:'bimonthly'`. The P1 + the two structural P2 assertions fail without their fix; foam/pricing/slot/converter/drift suites green (client `annual` leaves the client↔server drift parity green).

### Round 11 (Codex re-review on 7ca266c52) — engineInputs-only foam section labeled Pest Control (P2)

The round-10 accept fix (`withSupplementedRecurringServices` engine replay) covered the ACCEPT path, but the DISPLAY path had the same gap: for an engineInputs-only foam quote, `buildPricingBundle` builds the foam-specific frequency but `buildPricingServices` reads the static estData (no recurring rows) and falls to `deriveServiceCategory(estData, [], [])`, whose engineInputs **inference fallback recognized pest/lawn/tree/mosquito/termite/etc. but not `services.foamRecurring`** → defaulted to `'pest_control'`, so `/api/estimates/:token/data` (and its section) labeled a foam quote as Pest Control. Fix: added `services.foamRecurring || inputs.svcFoamRecurring → 'foam_recurring'` to the inference list (one line, mirroring the existing per-service entries). This fixes both the `/data` top-level `serviceCategory` (drives the React copy via `estimateCopyFor`) and the empty-recurringKeys section fallback (`buildServiceSection` key/label) — zero impact on other estimate types (foam was the only service missing from the inference). Chose this over supplementing estData with the regenerated engineResult (which would change `recurringServices` for ALL engineInputs-only estimates). Tests: `deriveServiceCategory(engineInputs-only foam, [], []) === 'foam_recurring'` + the `buildPricingBundle` section labels foam (both fail without the fix); 314 foam/pricing/converter/drift tests green.

## 2026-06-25 — Invoices: one-tap AI invoice summary from the linked visit (feat/invoice-ai-summary)

**Context.** The admin invoice builder already had a "Write with AI" button that drafted the customer-facing invoice note, but it only saw what the operator typed plus the line-item descriptions — it had no idea what actually happened at the visit. Most invoices are created against a completed `service_records` row (the operator picks the service), so the richest context (the tech's job summary, the diagnostic readings captured on the form) was sitting one join away and going unused. This makes the AI note ServiceTitan-style: a one-tap summary assembled from the visit, not just from re-typed line items.

**Decision.** Extract the note generation out of the route into a dedicated service (`server/services/invoice-ai-summary.js`) and feed it the linked visit. `POST /api/admin/invoices/notes/ai` now accepts `serviceRecordId` + a `sources` toggle object (`{ jobSummary, forms, lineItems }`) on top of the existing `input` + `services`; the service loads the `service_records` row and assembles the prompt from three sources mapped to the UI toggles — `jobSummary → technician_notes`, `forms → soil/lawn diagnostic readings`, `lineItems → invoice line descriptions`. Back-compatible: with no `serviceRecordId` the old typed-input + line-items path is unchanged, so the base button keeps working everywhere.

**Model tier.** Customer-facing copy → `MODELS.VOICE` (Sonnet, warm/natural), matching `completion-recap.js`, **not** the old `MODELS.FLAGSHIP` this endpoint used (a stray flagship call site for what is plainly customer-voice copy). The now-unused `MODELS` import was dropped from the route.

**Guardrails** mirror `completion-recap.js`: never names products / chemicals / active ingredients / application rates / prices, never says "eliminated/guaranteed/pest-free/eradicated/solved forever", invents no follow-up dates or pests, and never emits raw field codes or internal QA flags (`field_flags` is deliberately excluded from `formatObservations`; readings are translated to plain language). Degrades gracefully — a missing/failed `service_records` lookup returns an empty context (logged `warn`) so the operator's typed input + line items still drive generation rather than 500-ing.

**Summary reaches the customer in the email body.** `sendInvoiceEmail` previously only put the note on the attached PDF. It now surfaces `invoice.notes` as a rendered, HTML-escaped paragraph in the email body (both the HTML and plain-text branches) and as an `invoice_summary` SendGrid merge variable. **Ops prerequisite / soft prod gate:** prod disables SMTP fallback and sends via the SendGrid `invoice.sent` template, so the summary only appears in the prod email once that template renders the new `invoice_summary` merge field — until then the prod email is unchanged (safe, gradual rollout). Dev/staging SMTP shows it immediately.

**UI.** `AdminInvoicesPage.jsx` `CreateInvoice` (legacy `D` palette — matched the file). Flag-gated on `ff_invoice_ai_summary` (off by default; fails closed). When the flag is on and a completed visit is linked (`selectedService?.id` in create mode, `editInvoice.service_record_id` in edit mode), it renders "Pull from visit" checkboxes (Job summary / Field observations / Line items) and sends `serviceRecordId` + `sources`; the button label flips to "Rewrite with AI" once a draft exists. With the flag off, behavior is identical to before.

**Verification.** `server/tests/invoice-ai-summary.test.js` (7) + `server/tests/invoice-email-summary.test.js` (3) green; existing `AdminInvoicesPage.test.jsx` (6) still green. No-overlap rebase onto `origin/main`. `notes/ai` still hard-requires `ANTHROPIC_API_KEY`.

**Codex round 1 (5 findings, all addressed).**
- **P1 — cross-customer visit leak.** `loadServiceContext` fetched the visit by `id` only; the builder doesn't clear `selectedService` when the operator switches customers, so a stale `serviceRecordId` could draft another customer's technician notes into this invoice. Now scoped: `loadServiceContext(serviceRecordId, customerId)` requires BOTH and queries `where({ id, customer_id })`, **failing closed** if `customerId` is absent (mirrors the ownership join invoice creation already enforces). The `/notes/ai` client now sends `customerId`, and selecting a new customer clears `selectedService`/`serviceRecords` (defense in depth).
- **P2 — summarizing a not-yet-performed visit.** The picker returns records of any status. `loadServiceContext` now only treats `PERFORMED_STATUSES` (`completed`/`paid`) as usable; a `scheduled`/`pending`/`void` record yields empty context.
- **P2 — `invoice_summary` not renderable in prod.** Prod sends via the SendGrid `invoice.sent` template (SMTP fallback disabled), which neither rendered `{{invoice_summary}}` nor whitelisted it (the publisher rejects referenced-but-unallowed vars). New migration `20260626000006_invoice_sent_render_summary.js` re-publishes invoice.sent with a `callout` block for `{{invoice_summary}}` (after the greeting, above the details table) + adds the variable to allowed/optional + seeds the fixture. Loads the CURRENT active version (no hardcoded blocks) and is idempotent. The renderer suppresses empty blocks, so summary-less invoices are unchanged.
- **P2 — free-form note tripping the placeholder guard.** `sendTemplate` runs `productionPlaceholderPayloadValues` over every payload value; a legitimate summary like "Sample collected…" matches the `^sample` placeholder pattern and would fail the send. Added a `FREE_FORM_PAYLOAD_KEYS` exemption (`invoice_summary`) in `email-template-library.js`.
- **P3 — generating from only a visit label.** `hasUsableContext` treated `context.serviceType` as usable even with every source toggle off; removed that branch so a summary needs typed input or an enabled source with data.

**Codex round 2 (1 finding, addressed).** **P1 — template version-number collision.** Migration `…0006` derived the new version number from the *active* row, but an unpublished `invoice.sent` draft can already hold `active.version_number + 1`, and `(template_id, version_number)` is unique — so the migration would collide and abort the deploy. Now numbers from the LATEST version across ALL statuses (`MAX(version_number) + 1`), matching the existing publish path. (Same fix applied to `…0007` on #2109.)

## 2026-06-25 — Invoices: AI thank-you message in the invoice email (feat/invoice-email-message, stacked on feat/invoice-ai-summary)

**Context.** Follow-up to the one-tap invoice summary. The summary recaps *what was done* at the visit (and prints on the PDF). This adds a distinct, optional **personal thank-you message** that lives only in the invoice **email** — a relationship note ("thank you for trusting us…"), not a work recap. Chosen over reframing the auto-intro because the owner wanted a separate, operator-editable note rendered alongside the summary (email layout: auto greeting → service summary → thank-you message → line-item table → pay CTA).

**Schema.** New nullable `invoices.email_message` column (migration `20260626000005_invoices_email_message.js`). Deliberately separate from `notes`: `email_message` is email-only and **never** placed on the PDF. Existing rows stay null; the email renders exactly as before when empty.

**Generation.** New `generateThankYouMessage` / `buildThankYouPrompt` in `invoice-ai-summary.js` (reuses the same VOICE-tier call + guardrails). Distinct prompt: 1–2 sentences, warm/appreciative, **no greeting** (the email already opens with "Hi {first}"), and the same no-products / no-prices / no-overpromising rails as the summary. New endpoint `POST /api/admin/invoices/email-message/ai` (`{ customerName, serviceType, input }`), hard-requires `ANTHROPIC_API_KEY`; returns a friendly error (never a 500) when AI is unavailable.

**Persistence.** `email_message` threads through `InvoiceService.create` (new `emailMessage` arg → `email_message` column) and the PUT edit path via the `INVOICE_UPDATE_ALLOWED_FIELDS` allowlist — `email_message` added alongside `notes`/`title` (it is plain customer-facing text, NOT a money/status column, so it respects the audit invariant; the `stripe-payments.test.js` exact-shape assertion was updated to match).

**Send wiring.** `sendInvoiceEmail` renders `invoice.email_message` as its own paragraph **after** the service summary and **above** the line-item table (HTML + plain-text), and passes it as an `invoice_message` SendGrid merge variable. Rendered as a plain paragraph (no info-card chrome) so it reads as a personal note, visually distinct from the boxed summary above it. Same prod gate as the summary: the in-body message only appears in the prod email once the SendGrid `invoice.sent` template renders the new `invoice_message` field; dev/staging SMTP shows it immediately.

**UI.** `AdminInvoicesPage.jsx` `CreateInvoice` (legacy `D` palette). Flag-gated on a NEW flag `ff_invoice_email_message` (off by default, fails closed — independent of `ff_invoice_ai_summary`). Adds an "Email thank-you (optional)" field with its own Write/Rewrite-with-AI button and a hint clarifying it shows in the email, not the PDF; works in create + edit mode (prefilled from `editInvoice.email_message`, which is present because the list query selects `invoices.*`).

**Verification.** New `invoice-thank-you.test.js` (4) + `invoice-email-thank-you.test.js` (3); updated `stripe-payments.test.js` allowlist assertion; `invoice-ai-summary` (7) + `invoice-email-summary` (3) + `AdminInvoicesPage.test.jsx` (6) still green — 82 server tests across the affected suites pass. All changed server files `node --check`-clean. **Stacked PR:** based on `feat/invoice-ai-summary` (#2107); retarget to `main` (rebase, dropping the merged summary commit) once #2107 lands. **UI not yet rendered in a real browser** — flag is off by default and needs a `user_feature_flags` row to surface; recommend a render check before flipping `ff_invoice_email_message`.

**Codex round 1 (1 finding, addressed).** **P2 — `invoice_message` not rendered in prod.** Like the summary, the thank-you note only existed in the SMTP-fallback HTML (disabled in prod). New migration `20260626000007_invoice_sent_render_message.js` re-publishes `invoice.sent` with a plain paragraph block for `{{invoice_message}}` placed directly AFTER the summary callout (greeting → summary → thank-you → details → CTA) + whitelists the variable + seeds the fixture; loads the CURRENT active version so it composes with `…0006` (the summary block) regardless of run order, and is idempotent. Also added `invoice_message` to `FREE_FORM_PAYLOAD_KEYS` in `email-template-library.js` (same placeholder-guard exemption the summary got in #2107) so a thank-you note that happens to start with "Sample…" can't fail the send. This **closes the soft prod gate** the original entry described — both summary and message now render in the prod template once `…0006`/`…0007` deploy.

## 2026-06-26 — PPC offline conversions: automate the existing Data Manager uploader + make call leads visible in the funnel (feat/ppc-call-attribution-dm-cron)

**Context / course-correction.** The original task was "upload offline conversions back to Google Ads + attribute phone-call leads." A first pass built a NEW queue + funnel hooks + an uploader on the Ads API `ConversionUploadService` — which a live validate-only test proved is **deprecated for new accounts** (`"limited to existing users… use the Data Manager API"`). It also turned out the repo **already had a complete Google Data Manager API uploader** (`server/services/ads/data-manager.js`, PR #1730): pull-based qualified-lead (from `leads`, incl. hashed email/phone = Enhanced Conversions for Leads, which already covers phone-call leads) + completed-job-revenue (from `estimate_actuals` + invoice totals), wired to `routes/admin-analytics.js`, logged to `google_ads_conversion_uploads`, service-account auth, and already configured LIVE in prod (`GOOGLE_DATA_MANAGER_ALLOW_UPLOADS=true`, `VALIDATE_ONLY=false`). The discarded first pass was redundant. This entry is the corrected, minimal scope.

**Decision 1 — automate the existing uploader.** `data-manager.js` was **admin-trigger only** (no scheduler). Added a daily 6:40am ET cron in `scheduler.js` that calls `DataManager.uploadConversions` for both conversion types (`periodDays: 7`, `limit: 500`, `validateOnly: false`). Safety: it no-ops unless `GOOGLE_DATA_MANAGER_CRON_ENABLED === 'true'` (opt-in so it never auto-fires on deploy), it's double-gated under the existing `cronJobs` wrapper, and `uploadConversions` still enforces the module's own `ALLOW_UPLOADS`/`VALIDATE_ONLY` gating (validate-only unless the account is explicitly live) and de-dupes per `transaction_id`, so the 7-day re-scan never double-reports. No change to `data-manager.js` itself. Documented the (previously undocumented) DM env vars in `.env.example`.

**Decision 2 — call leads visible in the PPC funnel.** The DM uploader feeds Google, but the `/admin/ppc` ROI views read `ad_service_attribution`, which only the web `lead-webhook.js` ever wrote — so inbound calls were invisible there. New `services/ads/call-attribution.js` `recordCallPpcAttribution()` inserts an `ad_service_attribution` row (`funnel_stage='lead'`, `lead_source='google_ads'`, `campaign_id` resolved from the campaign Google's call-reporting bridge matched), idempotent per `(customer_id, lead_source, lead_date)`. Wired into `google-call-bridge.applyBridge` (per-campaign, on the main line today) and `call-recording-processor.js` lead creation (dedicated GA tracking number → single-number bucket, `campaign_id` null, per-campaign-ready). Fire-and-forget; never blocks call processing.

**Verification.** New `ads-call-attribution.test.js` (13); existing `google-call-bridge` (12) + `data-manager` suites still green; changed files `node --check`-clean. Created one live conversion action during investigation — `Waves - Booked Job` (`…/7662382463`, BOOK_APPOINTMENT) — currently UNUSED by the 2-type DM uploader (harmless; removable, or wire a third type later). **Owner to go live on automation:** set `GOOGLE_DATA_MANAGER_CRON_ENABLED=true` (the DM account is already configured for live uploads).

**Codex round 1 (5×P2, all addressed).** Campaign backfill on an existing null-campaign attribution row (dedicated-number path then bridge); `findLeadForCall` returns the matched lead's `customer_id` so phone-matched leads attribute; pass extracted `serviceInterest` + the actual call date into `recordCallPpcAttribution` (service-line ROI + stable idempotency key for replays); wrap the upload cron in `runExclusive` (no double-POST across pods/deploys).

**Codex round 2 (3×P2, all addressed).** (1) Attribute in the bridge's already-bridged branch too (shared `writeCallPpcAttribution` helper) so calls bridged before this shipped + lead-retry calls land in the funnel (idempotent). (2) New `DataManager.reconcilePendingRequests()` + the cron reconciles prior runs' still-pending request IDs first, so failed/partial uploads flip out of `pending` and become retryable instead of stuck forever. (3) Wider cron window dated by `COALESCE(converted_at, first_contact_at, created_at)` — qualified_lead 60d / completed_job_revenue 30d — to catch late-qualified leads before Google's ~90d import window closes.

**Codex round 3 (2×P2, all addressed) — "attribute actual leads only".** (1) `writeCallPpcAttribution` now requires a `leadId`, not just a `customerId` — a confirmed Google Ads call from an EXISTING customer that matched no lead (a service/support call) is no longer counted as a new PPC lead, mirroring the lead-creation gate in call-recording-processor. (2) In call-recording-processor the lead-source lookup was hoisted to run for BOTH the new-lead and existing-lead paths, and the attribution write moved after the if/else (gated on `leadId`) — so a paid tracking-number call that matches a REUSED lead by phone is now recorded too, instead of only brand-new-lead inserts.

**Codex round 8 (2×P2, all addressed).** (1) The web-owned guard now also covers UTM-only web rows (no click id) — `utm_campaign`/`utm_term` join gclid/wbraid/gbraid, so a later call can't overwrite a consent/ad-blocker web lead's first-touch campaign (reason renamed `web_attributed`). (2) Widened the qualified-lead cron window 60→90 days (the full Google import window) so a lead first contacted up to 90 days ago but qualified only now (is_qualified set without converted_at → dated by first_contact) is still uploaded; anything older is outside Google's window regardless.

**Codex round 7 (3×P2, all addressed) — keep attribution clean + correct.** (1) The dedicated-number path now gates on `source_type === 'google_ads'` (not any `channel='paid'`), so a Facebook/LSA/other paid tracking number isn't mislabelled as a Google Ads call. (2) Call attribution returns `click_attributed` (no insert/override) when the lead's existing row carries a click id (gclid/wbraid/gbraid) — a web-form Google Ads lead's first-touch campaign is no longer clobbered when the customer later phones a paid number. (3) The bridge's already-bridged backfill + retry now only write PPC attribution when the CRM call's `google_ads_call_resource_name` equals THIS `match.googleCall.resourceName` — a call that scored 'already_bridged' off a different nearby Google call no longer gets that other call's campaign (lead-attribution retry, which is campaign-agnostic, still runs).

**Codex round 6 (1×P2, addressed) — upgrade placeholders, not just nulls.** The backfill only filled NULL fields, but the dedicated-number path always inserts a row with a generic `lead_source_detail` ("inbound call") + default service bucket, so a later bridge run (real campaign + a now-known service on the lead) never replaced them. Now: when the bridge first supplies a campaign (`campaign_id` null → set), the generic detail is replaced with the campaign name; and a service derived from a KNOWN interest upgrades a default-placeholder bucket (an unknown/default inference still only fills missing fields). An already-set `campaign_id` is never overwritten with a different one (first-touch wins).

**Codex round 5 (3×P2, all addressed) — dedupe robustness.** (1) `ad_service_attribution.lead_id` is now a UNIQUE index (migration `…0013`) + the call/web inserts use `ON CONFLICT (lead_id) DO NOTHING`, so two overlapping bridge-apply runs can't both insert a row for the same lead (Postgres NULLs-distinct leaves the many null-lead_id web rows unaffected). (2) The advisory lock moved INSIDE `DataManager.uploadConversions` (per-conversion-type) so the manual `/data-manager/upload` admin endpoint is serialized with the cron too (the cron's redundant outer lock was removed; reconcile stays outside). (3) The web attribution insert now stamps `lead_id`, and call attribution returns `other_source` (no insert, no override) when a lead already has a row under a different source — so a customer who fills the web form and then calls the paid number is counted once under the web source, not double-counted.

**Codex round 4 (4×P2, all addressed) — funnel data quality.** (1) Root fix: new `ad_service_attribution.lead_id` column (migration `20260626000013`) — call attribution now dedupes by **lead_id** (one row per lead) instead of the coarse (customer, source, day) key, so two distinct Google Ads leads for one customer on the same day (web form + phone call, or two calls) get distinct rows. (2) Extracted the web path's `inferServiceLine`/`inferSpecificService`/`inferServiceBucket` into shared `utils/service-line-infer.js` (used by both lead-webhook and call-attribution); call rows now populate `specific_service` + `service_bucket`, not just `service_line`, so `/admin/ads/service-lines` buckets phone leads the same as web leads. (3) Bridge backfill now resolves the lead via `findLeadForCall` when `call_log.lead_id` is null (lead matched by phone/customer in metadata, not by the `twilio_call_sid` join) instead of dropping it. (4) `partial_success` is now terminal in `priorUploadSkipReason` — a PARTIAL_SUCCESS request returns only aggregate counts, so re-uploading the whole batch would duplicate the already-accepted records; it's surfaced via `error_message` for manual review instead.
## 2026-06-26 — Customer Health: consolidate three colliding scorers into one engine (feat/customer-health-consolidation)

**Context.** `/admin/health` was driven by THREE nightly jobs colliding on `customer_health_scores`: (1) `customer-health-v2.js` @ 2:00AM wrote `customers.health_score`/`health_risk` — read by nothing (dead compute); (2) `customer-health.js` @ 2:15AM wrote the full diagnostic row (6 sub-scores + overall/grade/churn) with **Algorithm A** (weighted avg of sub-scores), vocab `low/moderate/high/critical`; (3) `customer-intelligence/health-scorer.js` @ 3:00AM **overwrote** overall/grade/churn on the same row with **Algorithm B** (signal-weighted), vocab `healthy/watch/at_risk/critical`, leaving A's sub-scores intact. Net: every row ended the night a hybrid (radar = A, headline score = B), two engines with no shared math. This also caused a **live UI bug** — the dashboard's `RISK_LABELS`/`RISK_COLORS` only define `low/moderate/high/critical`, so after 3AM every non-`critical` customer rendered a blank/uncolored risk chip and the risk-breakdown donut bucketed them into nothing.

**Decision.** One engine. `customer-health.js` (Algorithm A) is now the **sole owner** of `overall_score`/`score_grade`/sub-scores/`churn_risk`/`churn_probability`/`churn_signals`/history/alerts. Its vocab (`low/moderate/high/critical`) is what the dashboard already renders, so making it canonical fixes the live chip/donut bug with **zero frontend change**. `health-scorer.js` is demoted to an **enrichment-only** layer that reads the canonical row and updates ONLY `upsell_opportunities`/`next_best_action`/`lifetime_value_estimate` (renamed `calculateAllHealthScores`→`enrichAllCustomers`, `calculateHealth`→`enrichCustomer`; `identifyUpsells` unchanged; `determineNextAction` re-mapped to A's vocab). The 2:00AM (v2) and 2:15AM (standalone A) crons are removed; scoring now runs once nightly inside the 3AM Customer Intelligence Pipeline: **detect signals → score (A) → enrich (B) → outreach**.

**Signal-fold (the one behavior change).** To avoid regressing signal-driven churn detection when B stops scoring, A now reads `customer_signals` and folds them into the canonical score: `summarizeBehavioralSignals` (pure, exported, tested) returns a **bounded** net adjustment (`SIGNAL_SCORE_FLOOR -20`..`SIGNAL_SCORE_CEIL +10`, so the 6-factor diagnostic stays dominant and the radar never diverges from the headline by more than the cap) plus the negative drivers normalized into `churn_signals` (`warning`→`moderate`, `critical` stays `critical`, `info` never a driver). Because a `critical`-severity behavioral signal lands in `churn_signals`, `determineChurnRisk` escalates to `critical` automatically — so a signal-only churner (clean billing but "switching to Orkin") is now caught. Signal detection runs **before** scoring in the pipeline so the folded signals are tonight's.

**Other touch points.** Scheduler retention query: `whereIn('churn_risk', ['at_risk','critical'])`→`['high','critical']`, and `where('scored_at', today)`→`whereRaw('scored_at::date = ?', [today])` because A writes `scored_at` as a timestamp (B wrote a date string). `admin-customer-intel.js /scan` now mirrors the pipeline (detect→score→enrich). `admin-health.js` dashboard at-risk band → `['high','critical']`, stale dual-vocab comment refreshed. `retention-agent-tools.js run_overall_scores` updated to the score+enrich flow with the correct require path (the prior `require('./health-scorer')` was a pre-existing broken path; the other broken paths in that file — `detect_signals`/`get_retention_metrics`/`generate_retention_outreach` → `signal-detector`/`retention-engine` — are left for a separate fix, out of scope). New migration `20260626000009_drop_dead_customer_health_columns.js` drops the unread `customers.health_score`/`health_risk` (reverse of `…063`, guarded). `customer-health-v2.js` deleted (explicitly approved).

**Verification.** Rewrote `customer-intelligence-health-scorer.test.js` to assert enrichment writes ONLY the 3 intelligence columns (never score/risk/sub-scores), skips the write when no canonical row exists, and maps `determineNextAction` to the new vocab. New `customer-health-signal-fold.test.js` covers the bounded adjustment + driver normalization. 12 tests green; 24 across all touched suites; all changed files `node --check`-clean; real require path (`customer-health → signal-detector`, acyclic) confirmed. **OWNER after deploy:** migration runs on deploy; after one nightly cycle verify `SELECT churn_risk, count(*) FROM customer_health_scores GROUP BY 1` shows only `low/moderate/high/critical`, `customers.health_score`/`health_risk` are gone, dashboard risk chips are colored/labeled, and radar sub-score avg ≈ headline (within the signal cap). Follow-up PR 2 (separate): event-driven rescoring on hot signals + proactive touch for healthy-but-quiet customers.

## 2026-06-26 — Customer Health: event-driven rescoring on hot inbound signals (feat/customer-health-event-rescore)

**Context.** PR 2a, on top of the consolidated single engine. The nightly Customer Intelligence Pipeline scores everyone once at 3 AM ET, so a hot inbound signal — a customer texting about a competitor, a cancellation, or a price complaint — could sit up to ~24h before it moved the score and the owner heard about it. Existing webhook rescore triggers (Stripe payment-failure/refund, review submit) already call `scoreCustomer` on the relevant event, but **inbound SMS — the highest-value real-time churn signal — was the gap**: nothing detected signals or rescored on an inbound text, and the owner churn-alert only fired inside the nightly retention pipeline.

**Change.** New `server/services/customer-intelligence/event-rescore.js` exporting `rescoreOnInboundMessage(customerId, {source})`: reads the customer's prior `churn_risk`, runs the existing per-customer `SignalDetector.detectSignals(customerId)` (keyword + AI-sentiment on recent inbound SMS) so the rescore reflects the new message, rescores via the canonical `customer-health.scoreCustomer` (which folds the fresh signals), and alerts the owner **only on a transition INTO critical**. Wired into `twilio-webhook.js` right after the inbound `sms_log` insert (`messageType === 'inbound' && customer?.id`), fire-and-forget so it never delays the webhook ack — mirroring the adjacent `estimate-conversion-agent.processInboundSms` call.

**Alert + dedup.** `sendCriticalChurnAlert` sends Adam an internal-alert SMS (`TwilioService.sendSMS(ADAM_PHONE, …, { messageType: 'internal_alert' })`, the canonical internal-alert path) marked "(live)", with the top churn driver and the customer's phone — an immediate heads-up, distinct from the nightly retention alert which carries the outreach draft. Dedup is the **transition guard**: fire only when `churnRisk === 'critical' && priorRisk !== 'critical'`, so it fires once at the crossing, never on every subsequent message while already critical. The nightly pipeline uses `scoreAllCustomers` (not this path), so it can't double-fire the live alert.

**Gating + safety.** Entire feature behind `GATE_EVENT_RESCORE` (fail-closed, `=== 'true'`): when off it is a complete no-op and behavior is exactly the nightly-only path. `rescoreOnInboundMessage` never throws (internal try/catch) since it's called fire-and-forget; silent when `ADAM_PHONE` is unset. Reuses existing dedup downstream — `detectSignals` dedupes against unresolved `customer_signals`, `scoreCustomer`→`health-alerts` dedupes the `critical_risk` alert row.

**Verification.** New `event-rescore.test.js` (6): gate-off no-op; detect-before-score ordering; alert on transition into critical (asserts recipient/body/`internal_alert` opts); no re-alert when already critical; no alert when not critical; critical transition with no `ADAM_PHONE` → no alert, no throw. All 20 blast-radius suites green (event-rescore + twilio-webhook + the health suites); changed files `node --check`-clean; module require path (`event-rescore → customer-health/signal-detector/twilio`) confirmed acyclic. The full-suite pre-existing failures (`admin-customers.js:1038` require-cycle, DB-connection infra) are unrelated and present on `main`. **OWNER:** flip `GATE_EVENT_RESCORE=true` on Railway (and confirm `ADAM_PHONE` is set) to enable; leave off to keep nightly-only. Remaining PR-2 work (separate): proactive touch for healthy-but-quiet customers; optionally route the existing Stripe payment-failure rescore through `rescoreOnInboundMessage`'s transition-alert too.

**Codex round 1 (1 P2, addressed).** **Make the critical-transition alert dedupe atomic.** The original `priorRisk !== 'critical'` guard was check-then-act: two concurrent inbound texts for the same customer could both read the pre-critical row and both alert. Replaced with the codebase's atomic-claim idiom (cf. `arrival_sms_sent_at`): new nullable `customer_health_scores.critical_alert_sent_at` (migration `…0010` + scorer auto-heal), and the alert now fires only when `claimCriticalAlert()` wins a conditional `UPDATE … SET critical_alert_sent_at = now() WHERE churn_risk = 'critical' AND critical_alert_sent_at IS NULL` (Postgres row-locks the single current row → exactly one concurrent caller gets rowcount 1). The canonical scorer (`customer-health.js`) clears the column whenever the customer is not critical, so a later re-entry re-claims and re-alerts — one live alert per critical episode. `priorRisk` read removed (the claim is the dedupe). Tests updated to model claim-won (rowcount 1 → alert) vs claim-lost (0 → no alert); 18 health-suite tests green.

**Codex round 2 (3 P2, all addressed).**
- **Preserve the critical-transition guard.** Round 1's claim-only check treated any not-yet-claimed critical row as a fresh crossing, so a customer already critical (via nightly/Stripe scoring, which don't run this path, or before the gate was enabled) would falsely alert "just dropped to CRITICAL" on their next text. Restored the `priorRisk !== 'critical'` read **alongside** the atomic claim: priorRisk gives the episode-level transition semantics (suppresses already-critical), the claim gives concurrency safety (one winner among simultaneous crossings). Both are needed; neither alone is correct.
- **Rescore handled inbound SMS branches too.** The hook only fired on the fall-through `messageType === 'inbound'` path, but an exact "CANCEL"/STOP is classified as `opt_out` and returns earlier — so the strongest churn signal still waited for nightly. Extracted a local `fireEventRescore(source)` helper (gated, fire-and-forget) and now call it on the **opt-out/cancel branch** (`source: 'sms_opt_out'`, before its early return) as well as the general inbound path. The owner alert is internal, unaffected by the customer's SMS opt-out.
- **Make signal detection safe for concurrent inbound texts.** `signal-detector.detectSignals` was read-unresolved-then-insert with no atomic guard, so two concurrent inbound webhooks for one customer could double-insert a signal type and double-count its folded weight. New migration `…0011` adds a **partial unique index** `customer_signals_unresolved_uniq (customer_id, signal_type) WHERE resolved = false` (dedups any existing unresolved duplicates first), and the insert now swallows `23505` — the losing race is ignored. A resolved signal can still be re-detected later. 21 health-suite tests green.

**Codex round 3 (3 P2, all addressed).**
- **Record an opt-out signal before rescoring.** The opt-out hook only re-ran `detectSignals`, whose keyword rule fires `DOWNGRADE_REQUEST` for `cancel`/`stop service`/`not renew` — so a bare `STOP`/`UNSUBSCRIBE`/`QUIT` produced no signal and the rescore was a no-op. `signal-detector` now also keys on `message_type === 'opt_out'` in the recent-inbound scan (the opt-out branch logs the row before firing the rescore), raising the same `DOWNGRADE_REQUEST` regardless of the exact keyword. Kept in signal-detector (not the webhook) so it benefits the nightly batch too; consistent with the existing "cancellation language → DOWNGRADE_REQUEST" rule (no new business judgment).
- **Rescore matched customers on tracking numbers.** The general-path hook was gated on `messageType === 'inbound'`, but an existing customer texting a churn message to a domain/van tracking number is logged as `*_lead`. Dropped the gate — `fireEventRescore('inbound_sms')` runs for any matched customer (it already no-ops without `customer?.id`, and non-customer leads return earlier).
- **Deduplicate health alerts during concurrent live rescores.** `health-alerts.generateAlerts` deduped with SELECT-then-INSERT, so two concurrent rescores could both create an active `critical_risk` row (the SMS was already atomically deduped, but the admin inbox wasn't). New migration `…0012` adds a partial unique index `customer_health_alerts_active_uniq (customer_id, alert_type) WHERE status IN ('new','acknowledged')` (demotes existing active duplicates to `dismissed` first), and the insert swallows `23505`. 21 health-suite tests green; all changed files `node --check`-clean. (`signal-detector` has no unit harness; the opt-out rule is a 2-line extension of the existing untested keyword rules.)

**Codex round 4 (2 P2, all addressed).**
- **Restrict active-alert uniqueness to rule alerts.** Round 3's index was table-wide, but 11 other paths (`stripe.js`, `billing-cron` ×7, `stripe-webhook`) insert one-off operational alerts that legitimately repeat an `alert_type` per customer (one per incident, keyed in `trigger_data`) — the broad index would 23505 their second incident and drop it. Migration `…0012` rewritten: both the dedup-demote and the index are now scoped to the seven `generateAlerts` RULE types (`score_drop`/`critical_risk`/`service_gap`/`payment_issue`/`low_engagement`/`satisfaction_drop`/`new_customer_risk`); index renamed `customer_health_alerts_active_rule_uniq`. Operational alerts are untouched.
- **Avoid claiming the live alert before delivery is possible.** The claim was taken before the deliverability check, so with the gate on but `ADAM_PHONE` unset (or a Twilio failure) the claim was burned with nothing delivered — that episode would never retry until the customer recovered and re-entered critical. Now: `ADAM_PHONE` is checked **before** claiming (no recipient → no claim), `sendCriticalChurnAlert` returns a delivered boolean (false on throw or `sent === false`), and on a failed send the claim is **released** (`releaseCriticalAlert` sets `critical_alert_sent_at = null`) so the next inbound text retries. New test covers the release-on-failure path. 22 health-suite tests green.

**Codex round 5 (3 P2, all addressed).**
- **Handle failed alert redirects as undelivered.** `internal_alert` sends to owner numbers are redirected to Waves notifications, which return `success: true` with `notificationUndelivered`/`notificationError` (no `sent` field) on failure — so the `sent === false` check treated them as delivered and burned the claim. Added `isAlertDelivered(r)` treating any failure shape (`success === false`, `sent === false`, `blocked`, `notificationUndelivered`, `notificationError`) as undelivered; new test covers the redirect-undelivered case.
- **Distinguish wrong-number opt-outs from churn intent.** A "wrong number" report is also logged as `message_type: 'opt_out'` (with `opt_out_reason: 'wrong_number'` in metadata). The opt-out detector now parses `metadata.opt_out_reason` and excludes `wrong_number`, so a recycled/wrong number isn't scored as cancellation intent.
- **Ensure the claim column independently.** The `critical_alert_sent_at` auto-heal was nested under the `!hasPmtScore` branch, so a table auto-created with `payment_score` but without the claim column would never get it (and `claimCriticalAlert` would hit an undefined column). Moved the column-ensure out of that branch to run regardless. 23 health-suite tests green; all changed files `node --check`-clean.

---

## Small-commercial pest pilot pricer (quarterly GPC by building sqft)

**Context.** Commercial pest/lawn had a safety gate (`commercial-helpers.js`) that routed every commercial property to a manual quote — the `small_commercial_pilot` `commercialPricingMode` existed in the admin UI dropdown and was forwarded by `property-lookup-v2.js`, but the engine ignored it (tests asserted it stayed inert). This PR makes the pilot live for **pest only**: quarterly general pest control auto-priced off building square footage. Lawn intentionally stays manual quote (no pilot pricer).

**Engine.** New `COMMERCIAL_PEST_PILOT` block in `constants.js` (DB-authoritative via new `commercial_pest_pilot` `pricing_config` row + `db-bridge.js` sync + validator). New `priceCommercialPestPilot()` in `service-pricing.js`: interpolates a quarterly per-visit price across building-sqft brackets (PILOT DEFAULTS — owner to confirm: floor $95 ≤2,000 sf, $165 @5,000, $245 @10,000, $325 @15,000; bi-monthly ×0.92, monthly ×0.85), declines (returns null → manual-quote fallback) when disabled, no usable building sqft, or above the 15,000 sf ceiling. Prices off the property profile **footprint** (ground-floor area = entered building sqft for single-story small commercial, the right driver for perimeter/ground GPC). `estimate-engine.js` pest gate calls the pilot only when the pest/commercialPest service carries `commercialPricingMode: 'small_commercial_pilot'`; the priced line carries `.annual` so the existing recurring-total machinery sums it, gets **no** WaveGuard discount (unknown discount key → 0) and does **not** count toward the tier.

**Trust boundary.** Every pilot line is `autoQuoteRequiresAdminApproval: true`, `pricingConfidence: 'MEDIUM'` — no pilot price reaches a customer without operator review. The pilot is opt-in per estimate (the dropdown), so default behavior is unchanged (manual quote).

**Tax + customer-facing estimate.** Pilot line is `taxable: true`, `taxCategory: 'nonresidential_pest_control'` (FL commercial pest is taxable). The existing customer-facing **Commercial Service Proposal** (`estimate-proposal.js` + `estimate-pdf.js`, multi-building bid PDF) now: (a) preserves per-line `taxable` from engine lines in the synthesized fallback proposal, and (b) defaults the FL commercial rate (0.07, `DEFAULT_COMMERCIAL_TAX_RATE`) when a taxable commercial line exists, so the PDF shows sales tax without the operator hand-typing a rate (still overridable). `v1-legacy-mapper.js` routes a **priced** commercial pest line into `recurring.services` (manual-quote commercial pest still flows to spec items).

**Verification.** New `pricing-commercial-pest-pilot.test.js` (unit pricer math/interpolation/multipliers/ceiling/no-sqft + through-engine recurring totals + manual-quote fallback + proposal FL-tax). Updated `pricing-commercial-safety-gate.test.js` (the two PR-1 "inert flag" assertions now assert the pilot prices pest while never falling through to residential, and lawn stays manual). 47 pricing-commercial tests green; estimate-proposal/service-lines green; all changed files `node --check`-clean. Full-suite pre-existing failures (DB-connection suites, the `get_payer_ar_aging` IB write-gate snapshot) are unrelated and identical on baseline (verified via stash). **OWNER:** confirm/tune the pilot brackets in the admin Pricing Logic panel (renders the `commercial_pest_pilot` row generically) before relying on it; the engine ships with documented placeholder defaults.

**Round 2 — stories/units/multi-building + Codex review (1 P1, 2 P2, 2 P3, all addressed).**
- **Owner ask:** account for stories (resident callbacks) + number of units (multi-family callbacks) + mixed complexes (a 2-story building *and* a 1-story building on one property). Pilot now prices per BUILDING — each `{ sqft, stories, units }` — and sums: `buildingQuarterly = max(floor, interpolate(sqft)) × storiesMult + perUnitQuarterly × units`, `storiesMult = 1 + perStoryUplift × (stories − 1)`. New config: `stories.perStoryUplift` (0.12), `perUnitQuarterly` ($5/qtr), `unitManualReviewThreshold` (60). New admin "Number of Units" input on both estimators; route forwards `units` + `commercialBuildings[]`.
- **P1 (block until approved):** the v1 routed the pilot as a normal priced recurring line, which the send/accept plumbing would treat as a committed self-serve plan before approval. Reworked: the pilot is now a **manual-review line carrying a SUGGESTED price** (`quoteRequired:true` / `requiresManualReview:true`, `annual`/`monthly`/`price` null; `suggested*` + `buildings` ride along). It reuses the existing commercial quote-required path — public accept refuses online acceptance, the engine keeps it out of recurring totals — while the operator + proposal PDF still show the computed number. Reverted the recurring-routing mapper change.
- **P2 (membership fee):** `recurringServiceKey()` collapsed any `*pest*` to `pest_control`, so a commercial-pest recurring line could trigger the residential WaveGuard setup fee. Added a distinct `commercial_pest` key (defense-in-depth; the line no longer routes recurring anyway).
- **P2 (gross vs derived footprint):** the sqft resolver preferred the profile’s derived `footprint` (homeSqFt÷stories), under-pricing multi-story sites (a 20,000-sf 2-story read as 10,000 sf, under the ceiling). Reordered to prefer GROSS area; derived footprint is last resort. The stories multiplier — not the divided footprint — now carries the multi-story load.
- **P3 (proposal label):** synthesized proposal falls back through `line.label` so the line renders "Commercial Pest Control", not `commercial_pest`.
- **P3 (migration rollback):** down() now compares the live `commercial_pest_pilot` row to the seeded values (stable-stringify) and leaves admin-tuned config in place.
- **Verification.** 71 commercial-pilot + safety-gate + proposal + service-line tests green; estimate-converter combined-routing green; all changed files `node --check`-clean. **OWNER:** confirm the stories uplift (×0.12/story), per-unit reserve ($5/qtr), unit threshold (60), and the sqft brackets in the Pricing Logic panel.

**Pricing values owner-confirmed (2026-06-26).** Waves signed off on the pilot defaults as-is: sqft brackets ($95 floor ≤2,000 / $165 @5,000 / $245 @10,000 / $325 @15,000), per-story uplift ×0.12, per-unit reserve $5/qtr, 60-unit manual-review threshold, 15,000-sf ceiling, cadence multipliers (bi-monthly ×0.92, monthly ×0.85). Comments/migration relabeled from "owner to confirm" to owner-confirmed; values remain admin-editable via the Pricing Logic panel.

**Round 5 — proposal billing-integrity (4 P2). Resolved by reverting proposal auto-population.** Codex flagged that the synthesized fallback proposal pulled the pilot’s *suggested* price (and surfaced $0 manual-quote rows) into a billable proposal — unsafe because commercial pilot estimates are quote-required and skip the server-authoritative price recompute, so a stale-config/tampered number, an un-replaced $0 row, or a missing tax default could reach a customer/invoice. Rather than wire a server-side recompute + $0 send-block + tax-default into the billing/send path, we **reverted `estimate-proposal.js` to its pre-PR state**: the synthesized fallback no longer touches commercial pilot/manual lines. The pilot price stays **operator-facing/advisory** (engine output + the estimate-card line reason), and the customer-facing commercial proposal is **operator-authored** with verified price + tax — matching how all other commercial proposals work (and the module’s CPA-sets-tax design). This eliminated the three estimate-proposal findings (recompute-trust, $0-row send, tax-default) at once. The fourth finding (stale unit count not cleared on re-lookup/address-change in the V2 estimator) was fixed by clearing `units` alongside `measuredTurfSf`. Engine/pricer/route/mapper changes (gross-area pick, stories/units model, unit-count fallback, distinct commercial_pest key, suggested-price advisory) are unchanged. 75 tests green.

**Round 6–8 (Codex hardening, all addressed).** Bundle visibility — a pilot GPC estimate selected alongside another pest-family service (mosquito/termite/one-time pest) now downgrades the whole commercial pest bundle to a single manual quote rather than suppressing the extra line behind the GPC suggestion. Explicit-0 units — estimators send an explicit `0` (omitting only a blank field) so an operator can clear a bad lookup unit count; blank falls back to the property record's `unitCount`. Stale-units hygiene — `units` is cleared on form reset, property lookup, typed + autocomplete address change, and lead/customer prefill. Cadence — the pilot defaults to quarterly and no longer inherits the hidden residential `pest.frequency`. DB fail-closed — an admin-edited bracket curve is applied only when every entry is numeric AND the top bracket reaches `ceilingSqFt` (matching validator invariant), else the safe default curve is kept. Reverted dead V1 `EstimatePage` units plumbing (no default export, not routed). Pushed back on the explicit-0 Home-Sq-Ft finding as shared route behavior (`property-lookup-v2` `Number(p.homeSqFt || p.squareFootage)`) out of pilot scope. Final Codex review: clean ("no major issues"). Owner marked the PR ready.

## 2026-06-26 — Customer Health: "Quiet" surface — proactive reach-out list (feat/customer-health-quiet-surface)

**Context.** PR 2b, the last of the customer-health lane. The at-risk views only surface customers whose score is bad (`high`/`critical` churn risk). But the common way customers churn isn't a dramatic signal — it's quietly feeling forgotten ("the company stopped caring"). A customer with a perfectly healthy score who simply hasn't had a visit or a reply in months is invisible to the at-risk system. This adds a read-only surface for exactly those people so the owner can make a proactive personal check-in. Deliberately **surface-only — no automated messages** (owner choice: for a one-tech op, a "who to call" list beats auto-texting happy customers and the brand risk that carries).

**Backend.** New `GET /api/admin/health/quiet` on `admin-health.js` (inherits the router-wide `adminAuthenticate`). Finds active real customers (`pipeline_stage IN active_customer/won/at_risk`) in the **healthy band** (`churn_risk IN ('low','moderate')` — i.e. NOT already in the at-risk views) whose **last meaningful touch** is older than `HEALTH_QUIET_DAYS` (default 45, env-tunable, floored at 7). "Meaningful touch" = most recent COMPLETED service OR INBOUND SMS — automated outbound (reminders/receipts) deliberately doesn't count, since it doesn't make a customer feel cared for. Brand-new customers awaiting a first visit are excluded (`COALESCE(member_since, created_at) < cutoff`). The whole query is wrapped in try/catch → degrades to an empty list, never crashes the route. The non-SQL logic lives in a tiny testable helper `services/customer-intelligence/quiet-customers.js` (`LAST_TOUCH_SQL`, `resolveQuietDays`, `mapQuietRow` — `days_since_touch` is null for a never-touched customer, who floors to epoch and sorts first).

**Frontend.** New "Quiet" tab in `CustomerHealthTabs.jsx` → `CustomerHealthSection` (between Health Scores and Alerts), matching the existing inline `COLORS`/`Card`/`Badge`/`mono` style. A one-line explainer card + a table (Customer / Tier / City / Score / Last touch / Phone) sorted by longest silence first; empty state when everyone healthy has been touched recently. Self-contained `QuietTab` component, its own `adminFetch('/admin/health/quiet')`, no new deps. (NB: `/admin/health` redirects to `/admin/customers?view=health`, which renders `CustomerHealthSection` from `CustomerHealthTabs.jsx` — NOT the legacy unmounted `CustomerHealthPage.jsx`.)

**Verification.** New `quiet-customers.test.js` (7) covers `resolveQuietDays` (default/override/floor/invalid) and `mapQuietRow` (days computation, never-touched → null, monthly_rate parse). Changed backend files `node --check`-clean; `CustomerHealthPage.jsx` parses clean via esbuild. **UI not yet rendered in a real browser** — recommend a quick visual check of the Quiet tab before relying on it. **OWNER:** optionally set `HEALTH_QUIET_DAYS` (default 45); no gate — read-only surface, safe to ship live. This completes the customer-health lane (engine consolidation #2121 → event-driven rescore #2128 → quiet surface).

**Codex round 1 (2 P2, all addressed).**
- **Add the Quiet tab to the MOUNTED health view.** The tab was first added to `CustomerHealthPage.jsx`, but `/admin/health` redirects to `/admin/customers?view=health`, which renders `CustomerHealthSection` from `CustomerHealthTabs.jsx` — so the page I edited is never mounted. Reverted `CustomerHealthPage.jsx` to `origin/main` and added `QuietTab` + the tab wiring to `CustomerHealthTabs.jsx`/`CustomerHealthSection` (the live component) instead.
- **Exclude archived customers from the quiet list.** Archiving sets `customers.deleted_at` without clearing `active` or the pipeline stage, so the query (which only checked `active` + `pipeline_stage`) could surface deleted customers' names/phones for outreach. Added `whereNull('c.deleted_at')` alongside the existing filters (matching the canonical live-customer predicate). 7 helper tests still green; live JSX esbuild-clean.

## 2026-06-26 — Fix retention-agent tool require paths (fix/retention-agent-tool-paths)

**Context.** Cleanup tail from the customer-health lane. `retention-agent-tools.js` (the Customer Retention managed-agent tool executor) `require`d four of its dependencies as `./signal-detector` / `./retention-engine` / `./health-scorer` — but those modules live in `server/services/customer-intelligence/`, not `server/services/`. So `detect_signals`, `get_retention_metrics`, `generate_retention_outreach`, and `identify_upsells` threw `MODULE_NOT_FOUND` whenever the agent invoked them. Pre-existing (predates the lane); PR #2121 fixed only the one path it touched (`run_overall_scores`) and explicitly deferred the rest.

## 2026-06-26 — Event-driven churn alert: admin notification instead of owner SMS (feat/churn-alert-as-notification)

**Context.** The event-driven rescore (#2128) fired its live critical-churn heads-up via `TwilioService.sendSMS(ADAM_PHONE, …, { messageType: 'internal_alert' })`. In practice that already *redirected* to the admin notification bell+push (owner-phone + `internal_alert` type → `redirectInternalAdminSmsToNotification` → `triggerNotification('internal_admin_alert', …)`, SMS suppressed), but it relied on the owner-phone heuristic and read as "send an SMS." Owner asked to make it an explicit admin notification.

**Change.** `event-rescore.js` `sendCriticalChurnAlert` now calls `triggerNotification('internal_admin_alert', { title, body, link: '/admin/customers?view=health' })` directly — bell + push to active admins, respecting per-admin notification prefs; no SMS, no `ADAM_PHONE` dependency, and (as always for this concept) **never any message to the customer**. Delivered = `stats.bellWritten || stats.push.sent > 0`; on an undelivered/errored notification the atomic claim is released so a later text retries (unchanged dedup semantics). Dropped the pre-claim `ADAM_PHONE` gate (notifications go to admins, not a phone) and the SMS-shape `isAlertDelivered` helper.

**Verification.** `event-rescore.test.js` reworked to mock `triggerNotification` (9 tests): gate-off no-op; detect-before-score; posts `internal_admin_alert` with the right title/body/link on a won claim; no claim/alert when already-critical / lost-claim / not-critical; releases the claim on a thrown or undelivered notification; counts push-only as delivered. `node --check`-clean; require path (`event-rescore → notification-triggers`) resolves. No SMS path remains in the module.

**Change.** Repointed all four requires to `./customer-intelligence/...`. The target modules exist and export the called methods (`SignalDetector.detectAllSignals`, `RetentionEngine.getMetrics` / `generateRetentionOutreach`, `HealthScorer.identifyUpsells` — all singletons). No behavior change beyond the tools now loading. Verified: no `require('./signal-detector'|'./retention-engine'|'./health-scorer')` remain in `server/services/*.js`; `node --check`-clean; all four tool require paths resolve at runtime. Closes the cleanup deferred in the #2121/#2128 entries; the customer-health lane is now fully closed.
